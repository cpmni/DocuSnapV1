# HANDOVER — 2026-09-01 (rollout-build hardening + M=7 resolved)

**Branch** `feat/teach-side-overnight`. **HEAD = origin = `3690e39` — ALL PUSHED, tree clean.** Nothing running
(0 electron/ScanFinder). Latest installer: **`dist\ScanFinder Setup 2.0.0-r20260901-0830-11c1598.exe`** (signed).
Continues `HANDOVER_2026-08-31_NIGHT2.md` (the DARK corroborated-straighten arc). Owner is installing + testing the
build for a customer rollout.

## TL;DR
A daytime hardening pass on the rollout build, then the M=7 date-class investigation which ended as a **DO NOTHING
on code** (the "bug" was a measurement artifact). Built + pushed: the minimise-pop-up fix, the SFDEV toggle audit
(one gap closed + 25 technical toggles gated), and a new installer. Corrected 3 poisoned ground-truth rows in the
test corpus. The only substantive code arc — DESKEW_CORROB_AUTOFILE (last night) — is still DARK and awaits its
owner-machine census.

## Committed this session (all PUSHED)
1. **`cb29ffb` minimise/docked windows stop popping back up.** `createWindow`'s 12s anti-hang backstop force-showed
   a window on `!win.isVisible()` — but a MINIMISED window (and a docked child = restore()+hide()) also reports
   isVisible()===false, so minimising within 12s re-showed it ("random pop-up"). Guarded on a `_revealed` flag (set
   when ready-to-show actually fires) + `!isMinimized()`. `src/main.js`. Restart to load (main-process).
2. **`cb20072` SFDEV toggle for `deskew_corrob_autofile`** — the 09-01 DARK arc had no UI; now a dev-gated toggle
   (index.html row + wiring + DEV_SWITCH_IDS), ships OFF.
3. **`11c1598` gate 25 technical reading-internal toggles behind SFDEV** — a full toggle audit (a general-purpose
   agent cross-referenced ALL_ON_DEFAULTS_93 vs the wiring array vs DEV_SWITCH_IDS vs index.html). Categories A
   (ungated DARK), B (dead gates), C2 (missing-ON), D, E all CLEAN — no rollout risk. The one actionable = ~25
   note-demote/identity/quiet-reread internals that shipped ON but were customer-VISIBLE (DEV_SWITCH_IDS wasn't kept
   current from ~08-13); now gated (values unchanged, still ON). Excluded the owner-parked-visible + genuine customer
   features. `src/windows/settings/renderer.js`.
4. **`3690e39` NIGHT_RUN: M=7 date subset RESOLVED** (see below).
5. Installer rebuilt on `11c1598` → `…-r20260901-0830-11c1598.exe`.

## The M=7 date-class investigation — DO NOTHING on code (a measurement artifact)
Owner asked for a guard against the "biggest silent-misfile risk" (M=7 wrong auto-files / 605). Traced all 5 date
exemplars; **007 rendered the scans + I verified the #1453 crop myself** (it prints "18-01-2025", clean glyphs).
Finding: **the pipeline reads these dates CORRECTLY; the stored ground truth was poisoned.** #1453/#1649 = poisoned
GT (app reads 18-01-2025 / 24-08-2026 @98; decisive: sibling #1908 prints the identical 18-01-2025 with a CORRECT
GT; 40/42 Silverbeck read==GT). #364 = poisoned GT (year 9687; app reads 16-07-2026, corroborated). #953/#1423 =
already HELD by the year-plausibility note; #1423's correct date was read by keyword and WON. **reggie designed a
page-witness guard on the (mistaken) premise the pages print the GT; 007 falsified it. Oracle: DO NOT build it**
(WRONG-LAYER — it's the date twin of `FILING_SANITY_PAGE_MATCH_V2`/Gate A, which false-flagged ~7× in Chris r7 and
still ships OFF). No genuine silent-wrong-date auto-file remains on the current pipeline (proven for the 605-corpus
@200 DPI; hypothesis for unseen templates, covered by the disagreement rule). Full ruling: the a3a6061693e4213cd
Oracle transcript; ledger entry in `NIGHT_RUN.md`.

**Actions from it — DONE:**
- **The recommended "flip" was already live.** `trust_role_disagreement_refuse` is `true` on a fresh install (in
  ALL_ON_DEFAULTS_93 → mig 93; Oracle's "default OFF" was the code default). `field_corroboration_date_fold` defaults
  ON. So #1423's disagreement class is already caught in the shipped build — no flip/census needed.
- **DATA FIX (not committed — Desktop file, not the repo):** corrected 3 poisoned GT date rows in
  `Desktop\ScanFinder Test Corpus\ground_truth.json` — #1453→18-01-2025, #1649→24-08-2026, #364→16-07-2026. Original
  backed up to `ground_truth.json.poisoned-bak-20260901`.

## Verification state — honest
- Toggle audit: fresh-install migration dump confirmed the customer/DARK/gate config; the wiring pin's only red is
  the KNOWN pre-existing stamp-* orphaned panel (unchanged by my edits); all 25 gated ids exist (no dead gates).
- Icon: `assets/icon.ico` is a valid 7-size ico; packaged shows ScanFinder; the Electron logo is a DEV-ONLY artifact
  (`npm start` runs as electron.exe) — not the shipped app. No fix needed.
- M=7: 007's rendering + the 42-doc census + the #1908 sibling + my own crop read = the premise is settled; the
  disagreement rule + date-fold verified default-ON in a fresh migration run.
- NOT verified (owner-machine): the DESKEW_CORROB_AUTOFILE arc census; a packaged-boot smoke of `…-2118`/`…-0830`.

## FIRST ACTIONS for the fresh session
1. Read this + `HANDOVER_2026-08-31_NIGHT2.md` + `docs/designs/DESKEW_CORROB_AUTOFILE_2026-08-31.md`.
2. `git log --oneline -5` — HEAD=origin=`3690e39`, all pushed. Nothing running.
3. If the owner reports rollout results: the new installer is `…-r20260901-0830-11c1598.exe`.
4. The queued autonomous work (NIGHT_RUN): the DESKEW_CORROB arc census (owner-machine, cold state), the Chris
   round (`/christest`), the 2 red pins (owner UI intent), client+cert-tool Electron 44 (`npm install`).

## Deferred (designed/built-DARK, NOT flipped) — load-bearing conditions
- **DESKEW_CORROB_AUTOFILE** (`aa61350`, DARK, Oracle C1-C7): a corroborated straighten-rescue auto-files. **DO NOT
  FLIP** without the census — realdoc reads byte-identical, human-verified enumerated heals, M=0; reproduce the COLD
  import state (the retry only fires in the warming phase — a reprocess of now-warm docs reads clean).
- The two 08-31 arcs (`template_locate_role_qualifier`, `template_fragment_containment_yield`) — DARK, need the
  realdoc-605 flip gate on the owner's real learned DB.

## Needs the USER
- Rollout smoke of `…-0830-11c1598.exe` (boot, licensing, a real import, Quick-check dropdowns, minimise behaviour).
- The DESKEW_CORROB arc census + flip decision. The 2 red pins' UI intent. Client Electron 44 `npm install`.

## Key facts / paths
- **Live DB** `%APPDATA%\ScanFinder\docusnap.db` — mig 101, ~166 confirmed (owner testing). Fresh-install config is
  the owner's validated production set (customer ON · DARK arcs OFF+gated · dev/telemetry/diag locked · onboarding on).
- **Test corpus + GT** `Desktop\ScanFinder Test Corpus` (605 papers; GT now with the 3 rows fixed + a `.poisoned-bak`).
- **Harnesses:** `TESTING/_measure/reslice_20260830/_run_docs.js` (added `DESKEW_CORROB` to its env whitelist; spaced
  paths via `@listfile`); `TESTING/_measure/import_crash_smoke.js` (headless-electron import smoke).
- **Traps:** never name-kill by substring (electron ≠ ScanFinder, but the packaged app runs); a dev `npm start` locks
  repo node_modules → EBUSY the native rebuild (close it first); `git commit -F <file>` only (backticks in `-m` get
  shell-substituted); render pages via `python_backend/render/pages.py --file <pdf> --page N`.
