# HANDOVER — 2026-08-24 (autonomous overnight: corroboration fix + a Chris round + its fixes)

**Branch** `feat/teach-side-overnight` · **origin is at `034a3ad`** (the 2026-08-23 EVENING batch, pushed) ·
**4 overnight commits AHEAD, LOCAL / NOT pushed** (autonomous run — left for the owner to review then push).
Ran while the owner slept, under his instruction: "run the fixes, then have Chris run the tests; fix any
problems and have Chris run again; for recommendations, fix and one more Chris test; sandbox as usual, on auto;
invoke the agents." Every code fix went through advisor/Oracle; nothing tuned to one doc; sandbox-only for Chris.

## The 4 local commits (oldest→newest)
1. **`9c21ed6` corroboration recorder fix** — the Pelican-565 class (the owner's last live question). The record
   discarded a genuine independent mapping/crop agreement whenever the field's WINNER was bare `anchor` (bare
   anchor is the full-page TEXT-LINE reader — the keyword family — which the shared bucket EXCLUDES). Fix: in the
   LOCAL `_corrob_bucket` fold bare `anchor` → keyword (record-only; shared `_crosscheck_witness_bucket`
   untouched). Anchor+keyword collapse to one family (fraud stays closed by same-family skip), anchor+mapping/crop
   now counts as two recipes → the critical-field floor-relax can fire. **Oracle SIGN-OFF-W/COND**; his r19-unmask
   seam closed conservatively (a bare-anchor line agreement never suppresses a genuine keyword-regex dissent —
   `agree_strong`). Kill `CORROB_ANCHOR_AS_KEYWORD=0`. 26 emit pins green; realdoc A/B (RR_APP_ENV=1, relax +
   role-disagree ON) OFF→ON = **778→780 would-auto-file (+2 CORRECT heals), wrong-list IDENTICAL (14, all
   pre-existing date/ref), supplier 100% both, M_type=0.** Go-forward: held docs must be reprocessed to pick up
   the honest record. NOTE: a standalone identify repro of 565 is inconclusive (harness can't replay the full app
   config — the documented caveat); the fix rests on the pins + the A/B + the mechanism.
2. **`33a9f6b` #6 copy** — the ⊕ teach readout said "Anchor (label…)" / "Draw the anchor". Now "The label…" /
   "Point out the label" + the draw-target help string. Internal anchor system + the hidden admin Template Wizard
   untouched. Chris-verified FIXED.
3. **`670ffc7` #3 orphan nudge + honest presence comments** — the "always 1 left behind" class (a graduated
   sender's on-screen doc orphaned while its twins auto-file). Root cause (Oracle-traced): the sweep excludes any
   doc with a viewer and never re-fires when they leave; and at 63 % only a RE-READ lifts it, not the stored-row
   sweep. **Fix D (mandatory, straight):** the below-floor hold panel, when the sender has GRADUATED, now says
   "Other documents from this sender now file themselves… Press Reprocess…" instead of the wrong "lower the bar"
   advice. Fails safe. Also corrected the two STALE presence comments (review/handler.js) that claimed a ~25s
   heartbeat refresh — the desktop renderer never calls review-heartbeat, so presence is a single 60s beat, a
   leaky proxy, NOT a mid-edit guarantee.
4. **`81719c1` #3 copy fix (from Chris re-verify Card 2)** — the #3 hint pointed "in the tools rail"; the labelled
   Reprocess buttons are in the RIGHT panel under "This document". Repointed.
(+ a docs commit: `docs/CHRIS_FULL_APP_REVIEW_2026-08-23_NIGHT.md` + CLAUDE.md LATEST.)

## Chris rounds (sandbox, fresh install, 135 switches ON bar straighten)
- **Round 1 (full vet):** strong "yes, I'd keep using it." Put-back→File-All, the two-line activity strip, and
  EVERY scary-button warning rated truthful/exemplary. 6 findings — I fixed #6 + #3; flagged the rest (below).
- **Round 2 (focused re-verify):** **#6 FIXED, #3 FIXED** (Reprocess lifted the real 63 % orphan to Ready). Caught
  Card 2 (fixed, `81719c1`) + two new flags (Cards 1/3 below). Full report in
  `docs/CHRIS_FULL_APP_REVIEW_2026-08-23_NIGHT.md`.

## OWNER VET QUEUE (diagnosed, NOT built — need your judgement)
- **#1 Terms say "NOT YET IN FORCE" + carry `[SOLICITOR:]` editor notes** (HIGH, first-impression). Legal content
  — your domain. Removing the banner would misrepresent not-yet-final terms; the real fix is finalising the
  solicitor-reviewed text before release. NOT auto-edited.
- **#2 First batch of any new supplier is ALL-HOLD** (the top friction — 12 perfectly-read invoices each held to
  "confirm the sender"). This is the wrong-company-misfile safety (the issuer-confirm hold). Relaxing it — e.g.
  "after 1–2 confirms on an identical-layout batch, stop re-asking the visibly-identical siblings" — is a real
  arc (gary + Oracle + a misfile census). Too risky to auto-relax overnight; the 08-15 hold-siblings history is
  the starting point.
- **#3-A the ZERO-CLICK version of the orphan fix** (Oracle SIGN-OFF-W/COND, DARK): re-trigger the quiet-reread
  LANE (not the sweep) on doc-CLOSE/navigate ONLY (never window-blur), for a held orphan in a ready scope →
  auto-clears the orphan with no click. Deferred: it has a window-close-mid-edit trade-off (a same-value re-read
  could file a doc whose unsaved correction was discarded on close) you should weigh. Full spec + gate in the
  Oracle log / this session. Switch: `sweep_retrigger_on_view_release`.
- **#4 The hold summary counts one field as two problems** ("1 low confidence AND 1 formatting check" for a single
  issuer field). Cosmetic. A correct fix needs the below-threshold field KEYS surfaced from `getReviewQueue`
  (server-side, where the per-field thresholds live) so the client can dedup low∩flagged — a small plumbing add.
  NOT done overnight (a miscount fix would be worse than the honest-but-verbose count).
- **#5 / Card 1 — OCR misses a plainly-printed value** (a date on a slightly-skewed scan reads "Not found";
  Reprocess can't recover it; an issuer inside a coloured banner reads "Not found"). The OCR read arc (007/oscar)
  — the leading-digit-date / banner-read class, same family as the deferred #413 arc. Its own OCR arc.
- **Card 3 — teaching a date by drawing a box clipped "03/" and committed "11/2026" ("Not a valid date") with no
  warning.** The pad-window/clipped-box class + a "warn on an invalid taught value" gap.

## Verification state — honest
- **Corroboration:** 26 emit pins green (heal fails-without-fix, fraud-stays-closed, r19-unmask routes to review,
  narrowness, cross-language mirror) + realdoc A/B clean (above). `node --check` + `ast.parse` clean.
- **#6/#3:** teach/label tests, `test_hold_reason_truthful`, `test_activity_strip`, `test_queue_badge_copy` all
  green after the edits. Chris round 2 confirmed both live.
- **NOT full-suite:** ran the touched + related suites, not `run_all_suites.py`. Pre-existing reds unchanged.

## FIRST ACTIONS for the owner
1. Review the 4 local commits + `git push` when happy (origin is behind at `034a3ad`).
2. Vet the queue above. The most valuable next builds: **#3-A** (zero-click orphan, spec ready, needs your
   mid-edit-trade-off call) and **#4** (the double-count, needs the field-key plumbing). #2 is the biggest
   friction but the biggest risk (misfile safety) — decide the appetite before that arc.
3. **The Pelican-565 class needs a reprocess to heal on the live DB** (go-forward record) — reprocess held docs
   after pulling these commits, or it heals naturally as they're re-read.

## Key facts / paths
- **Live app DB** = `%APPDATA%\ScanFinder\docusnap.db` (fresh, mig 87, all switches on bar straighten +
  telemetry). The live app may still be running from the evening (`ScanFinder1` is the OLD copy — untouched).
- **Chris sandbox:** `<scratchpad>\chris-sandbox\` (userData + Output + Demo Docs), CDP 9223 — left running
  (fresh admin, ~Copperfield graduated, invoice_01 at 100% Ready, invoice_16 held on a missing date). The next
  /christest kills + rebuilds it. Driver: `<scratchpad>\chris-driver` (playwright-core). Capture:
  `scripts\capture-window.ps1 -OwnerPid <sandbox pid>`.
- Corroboration fix: `python_backend/extraction/engine.py` `_corrob_bucket` (~4133) + the `agree_strong` disagree
  filter (~4160) + docstring (~4104); switch `CORROB_ANCHOR_AS_KEYWORD` (default on). Pins
  `python_backend/tests/test_corroboration_emit.py` (26).
- #3 orphan nudge: `src/windows/review/renderer.js` `renderCleanHoldReason` (the `_senderGraduated` block in the
  `conf < thr` branch). Presence comments: `src/modules/review/handler.js` (~785, ~812).

## Traps this session
- **CHRIS TRANSCRIPTS COME BACK EMPTY** and cannot be resumed via SendMessage ("No transcript found") — write the
  report out of the completion notification immediately, and for a re-run spawn a FRESH Chris (he won't know the
  first admin's password → reset the sandbox to create-admin; you CANNOT delete the users row — an audit_log
  append-only trigger blocks it — so re-seed a fresh DB instead and re-apply switches).
- **PowerShell path-guard:** inline JS with `count(*)` or `/\s+/`, or ANY `Remove-Item` text, can trip
  "Remove-Item on system path … blocked" and silently block the WHOLE command — put DB scripts in a `.js` file
  and run with electron-as-node; kill ports with `Stop-Process`, not inline.
- **CDP on this Electron binds 127.0.0.1, not `localhost`** (localhost → IPv6 ::1 misses it). Poll
  `http://127.0.0.1:9223/json/version`; the full `/json` page-list can hang (use the capture-window.ps1 helper).
- **electron-as-node argv:** `electron script.js ARG` → `process.argv[2]` is ARG (no dummy first arg).
- **Renderer changes need the window/app reopened** to load; the sandbox was restarted for the re-verify.
- `core.autocrlf=true` (benign LF→CRLF warnings on commit); repo root is `c:\GIT Projects` (paths `Docusnap/…`).
