# HANDOVER — 2026-09-08 (cold start for a fresh session)

> Branch `feat/teach-side-overnight`. HEAD = `eeb8d56`. **3 commits UNPUSHED** (the audit docs — see §First
> actions). Newest installer: **`dist/ScanFinder Setup 2.0.0-r20260907-2017-dd9a790.REFUSED.exe`** (hardened NSIS,
> ⚠ RETIRED 2026-09-08 evening — renamed `.REFUSED.exe`: the re-audit found every HARDEN_JS artifact shipped WITHOUT `listCaption.js`/`reviewReadiness.js` (Review's "File all ready" threw); fixed in `5a4bd94`, superseded by the rebuilt artifact named in `HANDOVER_2026-09-08_BUILD.md`. NOT a click-through target.
> current — has the separation fix). Live DB `%APPDATA%\ScanFinder\docusnap.db`, **migrations = 136**.
> No running background processes. No uncommitted SOURCE changes.

## TL;DR
This session did two big things: (1) **shipped the source-protection + MSIX/Store packaging path end-to-end**
and made the deployment decisions around it, and (2) ran a **full pre-deployment security + efficiency audit**.
The audit is the deliverable that drives the next session — **read `docs/PRE_DEPLOYMENT_AUDIT_2026-09-07.md`
FIRST** (Oracle-vetted, SIGN OFF WITH CORRECTIONS). Also fixed a real separation bug, added activity-chip
colour-coding, built the class-G SO-note softener (DARK), and fixed the delete-button + window-icon + uninstall.

## The deliverable the next session works from
**`docs/PRE_DEPLOYMENT_AUDIT_2026-09-07.md`** — prioritized work list, no code changed. 4 auditors
(eric/gary/oscar/main-Claude) + an Oracle adversarial vet (`docs/oracle_log.md` 2026-09-07 night). The code is
in good shape; the risks are release DISCIPLINE + artifact TRUST, not code holes. **The Oracle top-3 blockers
ARE the next session's first work** (see §First actions). The one seam all four auditors missed: **census
contamination** — the reference DB already has the TEST switches ON forever, so future validation censuses are
vacuous until it's reconciled.

## Committed this session (pushed through `3825a66`; `4c880e6`/`af62611`/`eeb8d56` UNPUSHED)
**Source protection (JS + Python) — all built + verified this session:**
- `159fb53` Build 1 — ship `python_backend` as sourceless `.pyc` (KEEP_SOURCE=∅; `main.js pythonArgs()` swaps
  `.py→.pyc` when packaged; smoked on `vendor/python`). Default-ON; kill switch `SHIP_PY_SOURCE=1`.
- `2cfac98` Build 2+3 — `scripts/harden-js.js` esbuild-bundles the whole main graph → `main.jsc` V8 bytecode
  (bytenode), OPT-IN `HARDEN_JS=1`. Default build byte-identical. `665c5d0` added minify + opt-in string-array
  (`HARDEN_JS_STRINGS=1`) — measured `isAutoFileEligible` 32→2 residual property keys.
- `53ab390` **bug caught**: the first hardened build shipped readable source ALONGSIDE the bytecode
  (electron-builder merged package.json `files`); fix = package.json `files`-swap in `build-electron.js`.
- `58f0814` two latent bugs esbuild surfaced (dup export + 5 wrong-depth requires in review/handler.js).
- Plan: `docs/designs/SOURCE_PROTECTION_PLAN_2026-09-07.md`.

**MSIX / Store path — WIRED + SHAKEN OUT:**
- `855027d` `appx` block + `build:store` (placeholder identity). `c959d78` wired `HARDEN_JS` into `build:store`
  (Store MSIX now as protected as NSIS — verified trust.js gone as source in the .appx).
- Shook out a signed test `.appx`: builds clean, minimal name-clean manifest (`runFullTrust` only), boots
  `packaged=true`, DB opens, **full in-container import proven** (watch → packaged Python → OCR → auto-file).
  Data goes to the **REAL `%APPDATA%\ScanFinder`** (not virtualized → NSIS↔MSIX share data, no migration issue).
- `dd9a790` **separation bug fix** — `separateFiles` threw `cleanupTempFiles is not defined` (internal name is
  `cleanupFiles`); fired every separation run. NOT MSIX-specific.
- Docs: `MSIX_SETUP.md` §8 (submission checklist + private-audience testing) + §0.1 decision record (see below).

**UI:** `0b2747d` delete-button alignment/affordance · `6071e95` unpack `assets/icon.ico` (window/taskbar icon
was Electron default — nativeImage can't load .ico from inside the asar) · `8aa17a0` activity-chip colour-coding
(you-filed=accent vs auto=green + a left rail) + doc type in the chip tooltip.

**Extraction:** `767b5ac` class G `inline_disagree_corrob_soften` (mig 135 DARK / 136 TEST force-ON) — REWORDS
(never clears) the `_pick_fuller_code` box-clip note when an independent KEYWORD read corroborates; conf<88,
review-bound. gary+Oracle G1-G7. Pin `test_inline_disagree_corrob_soften.py` 22/22. **OWED: OFF==ON realdoc M=0
on the 44-doc inline_disagree cohort before flipping in a customer build.**

**Audit docs (UNPUSHED):** `4c880e6` the audit report · `af62611` NIGHT_RUN ledger · `eeb8d56` Oracle vet.

## Verification state — honest
- Source protection: **proven** — sourceless `.pyc` runs on `vendor/python`; hardened NSIS + MSIX both verified
  (0 readable .py, trust.js bytecode, main.jsc present). NOT done: the owner's own `@electron/fuses read` on the
  shipped exe (bricking-verify, audit P1-5) + a click-through of every window on a clean install.
- MSIX: **full import proven in-container** (log: `[watch] finished batch of 1 (exit=0)`, auto-filed correctly).
  The live-DB test was done with a backup/restore + temp dirs — reverted, zero trace (verified 0 test rows).
- Class G: pin 22/22; neighbours green (corrob_note_resolve 51/51). realdoc M=0 NOT run (owed before flip).
- Separation fix: reasoned + syntax-clean; NOT runtime-re-tested after the fix (the bug was proven live first).
- The pre-deployment audit made NO code changes — it's analysis. Its claims were Oracle-traced to source.
- **Corrected mid-session:** the "clean uninstall for free under MSIX" claim was WRONG — MSIX data goes to real
  %APPDATA%, so uninstall doesn't auto-wipe it (same as NSIS). Fixed in the audit + MSIX_SETUP.

## FIRST ACTIONS for the fresh session (from the audit's Oracle top-3)
1. **Push the 3 audit commits** (`4c880e6`/`af62611`/`eeb8d56`) if the owner wants the branch current.
2. **P0-1 — the test-mig build gate + reference-DB reconciliation** (the #1 risk). Add
   `scripts/check-release-migrations.js` (grep `database/index.js` for the `TEST-BUILD force-ON` marker; fail
   `build` unless `ALLOW_TEST_MIGS=1`) AND a real down-migration / customer-build reset for
   `108/110/112/114/116/118/123/124/126/128/134/136` — the UPSERTs persist even after a code revert, and the
   reference DB has them ON forever (`deleteAppDataOnUninstall:false`), so **without the reset every future
   OFF==ON / M=0 census is vacuous** (the census-contamination seam). advisor+Oracle gate before building.
3. **P0-3 — a `build:release` script forcing `HARDEN_JS=1 HARDEN_JS_STRINGS=1`** so a release never ships
   plaintext JS by accident.
4. **P2-1 + P2-4 — seed `ocr_dpi=200` (customer default is 300, un-corpus-validated + OOM-risky) + recalibrate
   `PER_WORKER_BUDGET_BYTES` ∝ DPI²**, and flip single-doc import parallelism ON. **Validate as ONE cold-DB
   corpus run at `{DPI 200, parallel ON, 12 switches OFF}` → M=0 + zero accuracy delta** — never off the
   contaminated reference DB.

## Deferred (designed, not built)
- **Cython the crown-jewel Python → native `.pyd`** — the only tier that defeats `.pyc` decompilation (own
  session, MSIX/Cython + MSVC, ABI-locks 3.12, costs traceback line numbers). `pendingfeatures.md` + the plan.
- Class G flip → its OFF==ON realdoc M=0 on the 44-doc cohort. Class-G "slice 2" (full clear + auto-file)
  needs F's strict shape rail restored — separate Oracle round.
- P1/P2/P3 audit items — full list + effort in the audit report.

## Needs the USER (owner)
- **Incorporate Six Mile Software Ltd** — THE gating business step. An IV cert leaks your legal name (rejected);
  an individual Store account publishes under your legal name AND is scoped non-commercial; individual→company
  is NOT convertible. Company account / OV-EV cert both need the Ltd. Interim = unsigned NSIS (name-clean).
  Detail: `MSIX_SETUP.md` §0.1 decision record.
- **DB-at-rest decision** (audit P0-4): encryption default-ON or a loud documented plaintext posture.
- **Backend ops:** confirm production `DocumentRoot = public/` (keys/ safety) + set admin 2FA ON.
- **Smoke** the hardened installer: `@electron/fuses read` on the exe + click through every window on a clean box.
- The MSIX **Store submission** needs the Partner Center identity to replace the placeholders
  (`SixMileSoftware.ScanFinder` / `CN=Six Mile Software`) — and DON'T sign the appx (Microsoft signs).

## Key facts / paths
- **Revert list (TEST force-ON migs):** `108/110/112/114/116/118/123/124/126/128/134/136`. mig 135 = class G
  seed OFF; 136 = its force-ON.
- **Build commands:** hardened NSIS `HARDEN_JS=1 HARDEN_JS_STRINGS=1 CSC_IDENTITY_AUTO_DISCOVERY=false npm run
  build` · hardened Store `HARDEN_JS=1 HARDEN_JS_STRINGS=1 npm run build:store` (unsigned for the Store).
- **Traps confirmed this session:** bash heredocs strip a backslash level (use the Write tool for JS with
  Windows paths) · the shell guard refuses `Remove-Item` + a spaced repo path · MSIX self-signed `.appx`
  double-click fails `0x800B010A` (untrusted cert — expected; Dev-Mode `Add-AppxPackage -Register` sideloads
  with no admin/trust) · `CSC_IDENTITY_AUTO_DISCOVERY=false` avoids the vendor-binary signtool EBUSY.
- **Docs written:** `docs/PRE_DEPLOYMENT_AUDIT_2026-09-07.md`, `docs/designs/SOURCE_PROTECTION_PLAN_2026-09-07.md`,
  `MSIX_SETUP.md` (§8 + §0.1), `docs/oracle_log.md` (3 entries: source-protection, class G, audit vet).
- **Memory:** `project_predeployment_audit_20260907.md`, `project_source_protection_20260907.md`.
- **Backend is a SEPARATE deploy** (owner uploads code, NEVER `keys/`).
