# Handover — 2026-07-08 (end of a very long session)

Written because the session got long and quality was at risk. A fresh session should **start with
the CRITICAL OPEN ISSUE below** before doing anything else.

---

## ⛔ CRITICAL OPEN ISSUE — invoice-ref regressions in the real-doc harness (do this first)

The user ran `stress_test/realdoc_regression.js` on the live DB and it reports (full report at
`stress_test/out/realdoc_regression.md`):

- **57 regressions (42 SILENT — wrong value, no review flag)**. ref accuracy **94.6%** (982/1038);
  type/supplier 100%, date 99.9%.
- **M = 1 (MUST be 0)**: doc **#404** would AUTO-FILE a WRONG value — ref `22162` (want `22163`,
  off-by-one digit) **and** date `03-06-2026` (want `16-03-2026`).
- The user's words: **"most of these docs used to process."** So this is a *real* regression, not
  just the known ~8-doc benign City-Office GT-override baseline (see
  `[[project_ab_gt_override_pollution]]`).

**Regression PATTERNS (all on the synthetic test suppliers):**
- City Office: `want '152567' got '1/2'` (reads the Page # "1/2") — MANY docs.
- Cloud VPS: `want '122786'/'122785' got '6102'` (a fragment of the ZIP "Kansas City, KS 66102").
- Anconia: `want '337729'/'337728' got '102'` (fragment) and `want '179914'/'179915'/'317437' got 'null'`.

**What is NOT yet known (the session was interrupted before isolating it):**
1. **Is this from THIS session's code, or from POLLUTED LEARNED DATA?** The user spent the session
   TEACHING invoice_number anchors/templates on WRONG spots (Cloud VPS→ZIP, City Office→Page#,
   Anconia→fragment) to reproduce bugs. Those mis-taught templates/anchors are now in the live DB
   and the harness APPLIES learned data, so they mis-read every doc of that supplier. This is very
   likely the dominant cause — but it was **not confirmed**.
2. **The reggie precedence guard did NOT fire on the `6102` cases** (they're still `SILENT`, not
   flagged, and the value is `6102` not the keyword `122786`). Either the incumbent on those
   CONFIRMED docs is a *located* `template_mapping` (protected, guard intentionally skips) rather
   than a *blind* `template_registration`, or the guard isn't reaching. **Must verify.**

**FIRST STEPS for the fresh session (in order):**
1. **Isolate my code vs polluted data.** Run the harness on the BASELINE with the SAME DB:
   ```
   git checkout main -- python_backend/extraction/engine.py python_backend/ocr/tesseract.py
   find python_backend -name __pycache__ -type d -exec rm -rf {} +
   ELECTRON_RUN_AS_NODE=1 ./node_modules/.bin/electron stress_test/realdoc_regression.js   # DO NOT pipe through tail — read stress_test/out/realdoc_regression.md
   git checkout fix/ocr-multicol-precedence -- python_backend/extraction/engine.py python_backend/ocr/tesseract.py   # restore
   ```
   - If baseline ALSO ~57 + M=1 → it's **polluted learned data**, not the branch code → the fix is
     to CLEAN the mis-taught templates/anchors (Settings → Learning Recovery / Learning Repair), or
     have the user re-teach them correctly; the branch code is likely fine.
   - If baseline ~8 + M=0 → **this branch regressed** → bisect engine.py vs tesseract.py change.
2. Check whether the harness re-OCRs or uses cached OCR (grep the harness for `cached`/`ocr_text`) —
   if cached, the tesseract grouping fix has NO effect in the harness and only engine.py matters.
3. Diagnose why reggie's guard didn't catch `6102`: pick one Cloud VPS confirmed doc (e.g. #446),
   check the incumbent method (located `template_mapping*` vs blind `template_registration*`).
4. Diagnose #404 (the M=1): off-by-one `22162/22163` + a swapped date — is it a NEW OCR misread from
   the two-pass grouping, or pre-existing? Re-run reconstruct on #404's working copy.

**DO NOT build or merge `fix/ocr-multicol-precedence` until M=0 is restored and the 57 is explained.**

---

## Branch / build state

```
main (a398f21)  ── everything MERGED + shipped this session (see below)
  └─ feat/review-import-activity (859ceaa)   ── UNMERGED, BUILT (r20260708-2024)
       └─ fix/ocr-multicol-precedence (d4ebdc0)  ── UNMERGED, NOT BUILT, CURRENT branch
```
- **main a398f21** is clean and validated (tests + a harness that was M=0 at merge time).
- **feat/review-import-activity** (2 commits, built as `...-2024-3873004.exe`, user was testing):
  - `3873004` import/watch activity bar in Review (explains why reprocess is paused; disables the
    reprocess buttons during a batch — fixes the "reprocess flashes and doesn't process" report).
  - `859ceaa` anchor-teach readout restyle (accent border, sans font, value chip, slide-in) +
    auto-dismiss on click into another field; auto-file toast now advances to the next top doc.
- **fix/ocr-multicol-precedence** (adds `d4ebdc0` on top): oscar's 2-pass grouping + reggie's
  precedence guard. **Unit tests pass** (`python_backend/tests/test_ocr_engine.py` — 3-column
  fixture + bold-label decoy). **But the real-doc harness is RED (above).**
- `npm start` (dev app) was RUNNING at handover — reads Python from source (py -3.12), so Python
  changes take effect on the next process/reprocess after clearing `__pycache__`.

## What shipped to main this session (all validated at the time)
Auto-file 100% gate → opt-in; "Issuer is correct" accept + established-issuer fallback; watch
drain-once-per-pass; uninstall never deletes the documents folder; keyboard-focus fixes (reliable
trigger, no `<select>`-flash regression, auto-focus-modal caret via double-rAF); teach-wizard shared
label logic; Review button headings; **OCR two-column header fix — Tesseract `--dpi` (recognition)
+ first robust grouping (the City Office date bug)**; reggie/eric QA follow-ups; forced-update-lock
hardening. Detail in `[[project_field_fixes_20260708]]`, `[[project_ocr_two_column_dpi_fix]]`,
`[[project_focus_repair_mechanism]]`.

## The two fixes on the current branch (design is sound; corpus result is the question)
- **oscar's grouping (tesseract.py `_group_words_into_lines`)** — TWO passes: Pass 1 discovers the
  anchor rows (same seeding as before → no regression by construction), Pass 2 assigns every word to
  its BEST row over the FULL set (order-independent), tie-break `(significant-overlap, max overlap,
  min centre-dist)`. Fixes Anconia's `179914` re-home (verified on doc 915: `INVOICE NUMBER 179914`
  now pairs) while keeping the bold-label totals case (pinned by a new hostile-decoy test).
- **reggie's precedence guard (engine.py ~line 1237, `_KEYWORD_TRUST_FLOOR=90`, `_CONFLICT_CAP=88`,
  `_cmp_norm`)** — a BLIND `template_registration` read (landmark geometry only, no field-label
  evidence) can lose to a strong (≥90) rx-validated keyword that DISAGREES + outscores it; kept for
  review (capped 88), never silently swapped. Located mappings / ⊕ anchors / overrides untouched.
  **Note: this did NOT flip the harness `6102` cases — see the open issue.**

## Likely real conclusion (unconfirmed hypothesis)
The 57 regressions are probably **polluted learned data** (mis-taught templates/anchors from the
session's testing), not the branch code — the branch's unit tests pass and its fixes are provably
correct on the isolated cases. The real remedy is likely **cleaning the mis-taught learning** (or a
DB reset for the synthetic test corpus), plus possibly extending reggie's guard to cover *located*
mappings that a strong keyword disagrees with (currently only blind registration is guarded).
**Confirm via step 1 before acting.**
