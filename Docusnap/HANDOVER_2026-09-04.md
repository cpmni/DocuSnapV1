# HANDOVER — 2026-09-04

Branch `feat/teach-side-overnight`. **HEAD = `4e9230d`.** Last PUSHED = `13fa3a9` (the parent
FORMAT_VARIANCE_RELAX_REF only). **15 commits ahead of origin, ALL LOCAL / UNPUSHED** (kept local for
revertibility — the owner has not asked to push). Working tree CLEAN of tracked files. No installer built
this session (renderer/JS + Python + tests + docs only). Dev app running (`npm start`, background task
`bo0iggwx0`) with **all reference-flag arcs force-ON**.

## TL;DR
Built the entire **REFERENCE-FLAG FAMILY** — six arcs that quieten/soften/heal OCR-confusion noise on
reference numbers (printer serials, invoice/PO), plus a watch-import bug fix. **Every arc is DARK by
default, force-ON in this TEST build (migs 108–118), and REVIEW-BOUND — nothing auto-files a
machine-corrected reference.** Then designed (reggie+gary → Oracle **SIGN-OFF-W/COND**) the flagship
**CONFUSION-PRECEDENCE** feature (mine the `corrections` table into per-scope OCR-confusion facts), and
**the next session's job is to BUILD its 2a tier** per Oracle's A1–A4 conditions. The auto-file HIGH tier
is designed but **gated behind a census** (Oracle H1–H5, NOT a build approval).

## The reference-flag family — all built this session (newest first)
All in the Stage-4.5 ref branch / Gate C, all with their own kill switch, all pinned, all proven
end-to-end on live docs. Migs are seed-OFF (DARK) + a TEST force-ON pair.

| arc / switch | what it does | commits | mig | pin |
|---|---|---|---|---|
| **FILING_SANITY_REF_HISTORY_SOFTEN** | Gate-C "doesn't appear on this page" → truthful soft note when the ref is a confirmed literal whose only page form is a **backed** one-glyph confusable (O↔0, S↔5) with NO live agreement (a `+corrected` adopt). C1 unambiguity + C5 settled-value. Note-text-only, auto-file-neutral. | `a655bb8`/`4d01520` | 117/118 | `test_filing_sanity_ref_history_soften.py` 17/17 |
| **RESOLVE_REF_POSITIONAL** (leg-a) | binarisation re-slice witness (Otsu/adaptive re-read of the taught box — engine has NO higher-DPI render, so binarisation is the feasible independence lever) + per-position majority across ≥3 distinct pixel sources; pre-fill the consensus **review-bound** (≤70 cap + note). Witnesses in `self._code_witnesses`, NOT the corrob record. | `530172a`(core)/`adef73e`(wiring)/`c1fc69f` | 115/116 | `test_ref_positional_consensus.py` 20/20 · `test_resolve_ref_positional_wiring.py` 13/13 |
| **RESOLVE_REF_NEAR_MISS** (leg-b) | pre-fill a confirmed literal when a read is one **backed** OCR slip off EXACTLY ONE confirmed value (singleton edit-1 ball, len≥10); co-located at the `near_miss_confirmed` site, inherits ≤70 cap; hardens `near_miss_confirmed`'s `best_n` count-trap. Review-bound. | `aff08df`/`2831bff` | 113/114 | `test_resolve_ref_near_miss.py` 19/19 |
| **FILING_SANITY_REF_CORROB_SOFTEN** | Gate-C truthful note when ≥2 **live** independent read families agree + confirmed literal + same-length one-glyph slip + no clip. (doc196 752/782.) | `661cd2a`/`5398b05` | 111/112 | `test_filing_sanity_ref_corrob_soften.py` 14/14 |
| **FORMAT_VARIANCE_RELAX_REF_INLINE** | suppress the box-drift disagreement flag (`_pick_fuller_code`) on an exact confirmed literal + Oracle **R2 rigid-credibility guard** (never overturn a credible rigid `<70`). (doc121 heals.) | `238e13a`/`cd1121f` | 109/110 | `test_pick_fuller_code_literal.py` 16/16 |
| **FORMAT_VARIANCE_RELAX_REF** (parent, PUSHED) | suppress the shape "format differs" warn on an exact confirmed literal (`_gate_value`). | `dd4c855`/`13fa3a9` | 107/108 | `test_template_mapper_variance_ref.py` 14/14 |
| **watch db_id fix** | watch-split results rows now open the RIGHT doc + show "Filed" (was: `setImmediate` wrapped the whole `handleFileMessage` → mirror had no `db_id` → every row opened doc #1). | `205143a` | — | `test_watch_row_dbid_sync.js` 5/5 |

Proven end-to-end on the live DB: **doc121** `1984800049` heals clean (INLINE); **doc196** `752923124N3M2`
softens (live soften) / leg-a inert (consensus==committed, held); **doc238** `RFH0738865` softens
(history path). Chris audit 2026-09-03 confirmed the watch fix works.

## ⏭ FIRST ACTIONS (next session) — BUILD confusion-precedence 2a
The design is signed and ready. **Read `docs/oracle_log.md` (the 2026-09-04 CORRECTIONS→CONFUSION entry)
+ this handover's "Deferred" block first.** Then build **2a MEDIUM tier only, review-bound, DARK, mig 119**:
1. **JS reducer `getFieldConfusions(db)`** in `database/modules/learning.js` (beside `getFieldFormats`, ~1484):
   mine `corrections` per (supplier,doctype,field) — same JOIN spine + `MAX(c.id)` per (doc,field) dedupe +
   `status='confirmed'` + `learningExcludedSql` + machine-confirm exclusion. Emit **raw** facts
   `{pos, from, to, support(distinct docs), counter}` (leave the `backed` check to Python — the confusable
   table stays Python-only). **A4: DROP the method-based B7 exclusion** (Oracle SEAM 1: `corrections` has NO
   method column; the `extractions.extraction_method` join SURVIVES a human override, so it would delete the
   self-heal counter-edit; and B7 is ALREADY closed by no-row-on-accept). **A2 support floor: ≥3 distinct
   docs AND ≥2 distinct corrected-values, counter==0 — NOT N=1.** Counter = (1) an opposite in-scope
   correction; (2) a confirmed literal the substitution would BREAK into a different confirmed literal
   (the 752/782 defuse).
2. **`buildTrainingArgs`** (`src/modules/processing/handler.js` ~1786/1830): merge the confusions onto
   `allFormats` by `supplier|doctype|field` AFTER `getFieldFormats`, ride the existing `formatsFile`.
3. **Python** (`format_anomaly_checker.build_format_class_index` ~1033): thread `confusions` onto `fmt_entry`.
   New apply predicate (reggie's): given a fresh read + the facts, correct a position when the fact is
   **backed** (`_is_letter_digit_confusable`), the singleton-ball is unambiguous (reuse `value_is_confirmed_literal`
   + `_within_edit1` — never touch a confirmed pre-value; refuse a confirmed rival within one edit), and
   **A1: SUPPLIER-SCOPED facts ONLY** (refuse when `_sup_fmt is None` / the `''` fallback — reuse `_xsupplier`
   at engine.py:10134).
4. **Engine wiring** at the leg-b site (`engine.py` ~10339): AFTER `unambiguous_near_miss` abstains, apply
   the MEDIUM correction → value + **A3 both-forms honest note** ("We read '…X…'; other documents from this
   supplier had X corrected to Y here — confirm which is right") + `corrected_to` + **conf≤70** + method
   `+confusion_resolved`. Held by the existing `flagged`/`weak-critical-field` gates — **no trust.js change,
   no new column, no auto-file.**
5. **A4 pin the no-row-on-accept invariant** (RED-first): accept an arc pre-fill UNEDITED → zero
   `corrections` rows (renderer dirty-check `renderer.js:4388/4407` + reviewService) — this, not the dropped
   exclusion, is what keeps support N free of self-votes.
6. **Gate:** RED-first pins (fact-applied heal / a genuine serial carrying the `to`-glyph is NOT
   corrupted-then-filed / cross-supplier refused / N-below-floor refused / one counter-example demotes /
   OFF==ON byte-identical) + realdoc **M=0**. Kill switch `confusion_precedence`, DARK, mig 119 (max applied
   = **118**, verified).

## Deferred (designed, NOT built) — the HIGH auto-file tier
**Oracle: CONDITIONS LOCKED, NOT A BUILD APPROVAL — treat as SEND BACK until the census passes.** Ships
ONLY after 2a exists + the census is green. Binding conditions:
- **H1** the `+confusion_resolved` hold is a **top-level unconditional** `isAutoFileEligible` leg (NOT
  nested in the weak-critical-field block, else `_critFieldCorrobRelax` short-circuits it → doubly-weakened
  path auto-files).
- **H2** trust.js **re-derives** support/counter/singleton-ball from LIVE `corrections`+`value_counts` at
  gate time (a JS↔Python parity surface — shared pin); `resolution_meta` supplies only `{from,to,pos}`;
  never trust note-absence or the engine's self-reported tier. **If you won't own the parity port, KEEP the
  ≤70 cap (don't ship HIGH).**
- **H3** `S_AUTO ≥ 10` **distinct docs AND distinct corrected-values** + a session/time spread (not one
  import batch) + ≥2 distinct pixel sources on the current doc as a **separate** counter from `corroboration`.
- **H4** supplier-scoped facts ONLY.
- **H5 census** vs INDEPENDENT GT (never circular "edit==a confirmed value"): **0 misfiles** on 752/782 +
  cross-supplier + doubly-weakened + stale-meta-after-reprocess + genuine-real-serial-carrying-the-to-glyph +
  rubber-stamp-no-row + reducer-keeps-counter-edit; **M=0 AND zero per-field accuracy regression**, prod
  provenance flags on. HIGH also adds a nullable `extractions.resolution_meta` column + an autofile sub-flag.

barry's product wrapper (ships WITH HIGH, not before): a **one-time-ever** discovery line ("Scan Finder
now fixes obvious scan misreads based on your past corrections — you can undo any"), an honest per-doc note,
and a one-click **undo that records a counter-example** (self-heals). NOT a recurring digest. Safety net
barry surfaced: the raw OCR text (`documents.ocr_text`) is already searchable, so even a wrong
auto-correction files under a near-name AND stays findable by the raw misread.

## Verification state — honest
- **Pins:** every arc's pin GREEN (counts above). The `test_format_anomaly_variance.py` source-window pin
  was widened twice (900→2600→4200) to accommodate the leg-b + leg-a blocks inserted before
  `near_miss_confirmed` — a mechanical widen, the structure is unchanged.
- **End-to-end:** doc121/doc196/doc238 proven via the electron-as-node repro harness (scratchpad
  `repro_121.js` — takes a docId, forces the flags, runs the real `process_docs`).
- **NOT run:** no full 605-corpus **realdoc M=0**, no **WARM-DB census** for ANY arc. All are OWED before a
  customer build. Auto-file behaviour is byte-identical by construction for every arc shipped (all
  review-bound), so M=0 is expected-trivial — but has not been executed.
- **Pre-existing (not mine):** `test_settings_wiring.js` fails ONE check (missing `stamp-*`/`dbenc-*` element
  ids from the stamping/db-encryption features) — unrelated to this session.
- **Corrected mid-session claim:** the 09-03 NIGHT handover said doc121 was a "stale pre-code row" that a
  reprocess would heal — that was WRONG; the real cause was the INLINE box-drift choke point (fixed by
  `238e13a`). See the resolved note in `HANDOVER_2026-09-03.md`.

## Needs the USER
- **Flip gates before ANY customer build:** revert the TEST force-ON migs (108/110/112/114/116/118) or gate
  them, and run the WARM-DB census + realdoc M=0 for each arc (see each arc's mig comment).
- **Push decision:** 15 commits sit LOCAL on `feat/teach-side-overnight` (only the parent `13fa3a9` is
  pushed). Kept local for revertibility — say the word to push.
- **Chris audit findings (2026-09-03):** `docs/CHRIS_FULL_APP_REVIEW_2026-09-03.md` — top open item: the
  main-window **import list doesn't flip to "Filed" after a MANUAL confirm** (distinct from the auto-file
  path the watch fix handled; possible duplicate-file trap). Owner's vet before building.

## Key facts / paths
- **Live DB:** `%APPDATA%\ScanFinder\docusnap.db` (backup-API copy to read — WAL file-copy trap). **Max
  migration applied = 118.**
- **Run a pin:** `PYTHONIOENCODING=utf-8 py -3.12 python_backend/tests/<name>.py`. JS pins:
  `ELECTRON_RUN_AS_NODE=1 node_modules/electron/dist/electron.exe <path>`.
- **Repro harness** (real engine on a live doc, forced flags): scratchpad `repro_121.js` (edit the env +
  `<docId>` arg). `dump_formats.js` dumps `getFieldFormats` via electron-as-node.
- **git root = `C:/GIT Projects`** (app under `Docusnap/`); pathspecs need the `Docusnap/` prefix.
- **Dev app** running (`bo0iggwx0`) with all arcs on; the Chris sandbox (port 9223) keeps exiting clean on
  its own — rebuild via `/christest` when needed.
- **Advisors this session:** reggie (predicates), gary (Python + storage/integration), 007+oscar (OCR
  re-slice independence — the engine has ONLY 200-DPI bitmaps, a higher-DPI re-render needs plumbing from
  process_docs), barry (the confusion-precedence product reframe), Oracle (every gate). All verdicts in
  `docs/oracle_log.md`.
- **Memory:** `memory/project_ref_variance_relax_20260903.md` (the whole family + the confusion-precedence
  design) + MEMORY.md index.
