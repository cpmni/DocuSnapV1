# HANDOVER — 2026-09-06 (autonomous afternoon: the log-review fix plan BUILT)

**Owner order (verbatim):** "commit the docs and start 5a then the others in the recommended order - use the agents.
you are on auto - i am away for a few hours."
**Branch** `feat/teach-side-overnight` · HEAD = `cc41c9f` · **13 local commits unpushed** (2 from 09-04 + 11 today).
Dev app NOT running (it was not running when the session began; nothing restarted it). No installer.
Design followed: `docs/designs/LOG_REVIEW_FIX_PLAN_2026-09-05.md` (Oracle per-item verdicts + build order, 2026-09-05).

## What was built — in the Oracle's order, every slice pinned + sibling pins green

| # | Commit | Slice | Switch (all seeded OFF by **mig 122**, UPSERT) | Pin |
|---|---|---|---|---|
| 0 | `8f168b9` | docs: the 09-05 design + oracle_log + pendingfeatures (were uncommitted) | — | — |
| 5a | `dfc3cc4` | `review/handler.js` register() binds `templatesDir` — the learn-on-commit template FILE sync threw ReferenceError on EVERY confirm (×35 in processing.log) | plain fix | `test_template_file_sync.js` §4 EXECUTES the dep (warn spy + file on disk + poisoned-dir sensitivity); §5 lint: every ctx helper called bare inside register() is destructured. Old source fails 7. |
| 3 | `70bfe3a` | both numeric DATE patterns open `(?<![A-Za-z0-9])` (was `(?<!\d)`) — a date can no longer be read out of a code's tail (`PI/26/6000` → `26-01-6000`); `_crop_is_credible`'s date branch now census-hooked; harness gains `RR_CFG`/`RR_PY_ROOT` | non-DARK (strictly tighter) | `test_date_alpha_prefix_gate.py` + JS twin `database/modules/test_date_alpha_prefix_gate.js` over ONE shared vector file |
| 2A | `c912c09` | `_is_bare_label` fuzzy arm: a read ONE OCR slip from a caption TOKEN ("USTOMER"/CUSTOMER) is a bare label at every rung | `anchor_bare_label_fuzzy` → `ANCHOR_BARE_LABEL_FUZZY` | `test_bare_label_fuzzy.py` (OFF documents the bug; negatives; the registration RUNG) |
| 2B | `03be597` | caption clamp computed on the UN-widened box then widened (both relocate rungs); clamp threaded to the RIGID + REGISTRATION rungs (authoritative+labelled+below); caption-band reject MIRRORED at the registration commit | rides existing `RELOCATE_CAPTION_EXCLUDE` + `CAPTION_BAND_REJECT` (default ON) | `test_caption_clamp_threading.py` (the reorder band; rigid/drift/registration rungs; kill switches) |
| 1 read | `ba454bf` | label-less AUTHORITATIVE currency anchor: rigid crop + registration map REFUSED; engine `_apply_labelless_withheld_notes` lands a re-teach note ONLY when the field would end EMPTY (runs LAST) | `anchor_labelless_currency_refuse` → `ANCHOR_LABELLESS_CURRENCY_REFUSE` | `test_anchor_labelless_currency.py` (the design's 1–7) |
| 1 teach | `c09c16c` | ⊕ readout: a CURRENCY field taught with NO caption gets the WARN variant + typed-caption input (refuses nothing) | renderer, keyed on field TYPE | `test_teach_currency_nolabel_warn.js` (source pin) |
| sign | `799c212` | `MONEY_SIGN_CAPTURE` re-attach ported to the ANCHOR mint (`_clean_text_fallback`) | mig-72 `money_sign_capture` (still OFF — **flip is the owner's**) | `test_money_sign_anchor_twin.py` (mints agree on every signed vector) |
| 5b | `8a26f6f` | angle heal spawned `py -3.12 undefined detect_angle.py` — `pythonArgs()` called with no script; fixed + stderr captured | plain fix (revives a dead arm) | `test_angle_heal_argv.js` (main's pythonArgs semantics + spawn spy; old source fails 3) |
| 4b | `25a0cfe` | a shipped-but-UNINSTALLED type's bare name competes as a STRICT top-band standalone heading only (never a mention) | `type_uninstalled_heading_fold` → `TYPE_UNINSTALLED_HEADING_FOLD` | `test_type_uninstalled_heading_fold.py` + 15 type sibling pins |
| 4i | `cc41c9f` | withheld-issuer note names the right actor when the match is the install's own customer_name (COPY only) | — | `test_withheld_issuer_note.py` |

Skipped by design: **4(a)** = owner action (both presets ALREADY exist in `PRESET_CATALOG` with the right aliases —
Statement ['Statement of Account'], Service Worksheet ['Worksheet','Job Sheet'] — tick them). **4(ii)/(iii)** =
Oracle SEND BACK (own census). **Item 1 teach-side page-words caption sourcing** = NOT built (owed; see below).

## Measurements (the Oracle's MEASURE FIRST list)
1. **`field_anchors` census (agent, read-only on a db.backup() copy):** label-less AUTHORITATIVE currency anchors
   live today = **0** (only currency anchor = Nordwind `Total (inc VAT)`, labelled). The Meadowvale one EXISTED
   13:45–13:47 on 09-05 and was **deleted by the app itself**: `learning.saveCorrections → clearAnchors`
   (`learning.js:452-468`, predicate `:673-686` is **document_type-blind**) fired on the owner's two TYPED sign
   corrections (docs 411, 409). Its product is still on disk: 17/20 Meadowvale docs hold `anchor_crop@50`
   totals, 13 in review, 4 `corrections` rows. Geometry recovered from `Debug/diagnostic_2026-09-05T13-36-42-000Z.jsonl`
   (x 0.89174 y 0.38814 w 0.10156 h 0.01739) → re-planted on a COPY for gate (b) (`replant_meadowvale.py`).
2. **Item 3 alnum-prefix census** (`TESTING/_measure/date_alnum_20260906_live/census_new/date_alnum_census.md`):
   2,570 date-site records (crop 2,091 · keyword 479) → **A (old accepted, new rejects) = 2, both `P1/26/…`
   Pelican code windows at the keyword site; B (window moved) = 0**; C same 378; D neither 2,190. The affected
   population is exactly the exhibit class.
3. **Item 4(b) re-detect diff** (`TESTING/_measure/log_review_20260906/type_fold_census.md`): live copy's 541
   stored OCR texts OFF vs ON → **changed = 20, ALL Invoice@80 → Statement@95 heading=True (the Ironclad 20,
   all needs_review); 521 unchanged.** Gate MET on the live corpus. (605 Desktop corpus + Hard Set are PDFs —
   no stored text — their re-detect is owed with a realdoc arm.)
4. **Item 2B reorder band count + value diffs** — instrumented (`caption_clamp_reorder_band` /
   `caption_clamp_reorder_diff` census records); numbers come from the `b_only` arm (running).
5. **Meadowvale eligibility delta** — the `mv_off`/`mv_on` arms (running).

## Realdoc gates — the LIVE DB is a NEW corpus (⚠ read this)
The live DB (`%APPDATA%\ScanFinder\docusnap.db`, max mig 121) was RESET since 08-30: 541 docs, 453 confirmed
(Print Tracker 308 + the Hard-Set suppliers ×20 + Pelican 18 + Meadowvale 7). **The 605-paper `rr_ids.txt` now
selects 15 docs** — the DB-based harness with RR_IDS is VACUOUS on this install (first arm proved it); the
Desktop corpus is a FOLDER (`customer_corpus_score.js` = the COLD model). Every arm below = ALL 453 confirmed
docs on the live DB (~15 min/arm, 8 shards), `RR_APP_ENV=1 OCR_RENDER_DPI=200`, a FROZEN `python_backend`
snapshot per chain (`RR_PY_ROOT`) and a frozen config (`RR_CFG`) — the harness overrides added today.
- **Item 3 chain** `TESTING/_measure/date_alnum_20260906_live/` — arm `new` (alnum config) vs arm `old`
  (`dfc3cc4` config), same snapshot. **`new` DONE:** type 453/453 · supplier 453/453 · ref 448/453 · date 452/453
  · total 19/24; regressions 6 / 4 SILENT — ALL refs (Print Tracker #92 `782…`→`752…` 8↔5, #141/#243
  `1G25802868`→`1625802868` the doc176 class, #288 `HS73429209`→`H573429209`) + **#471 Pelican 0023**: ref
  `P1/26/6000`→`PI/26/6000` [flagged] and date `26-01-6000`→`05-01-2026` [flagged] — **the GT date `26-01-6000`
  is a POISONED confirm** (an impossible year the owner rubber-stamped; the new read is the real date, held).
  `old` arm running → then `diff_arms.py` on the two consensus files; the 4 SILENT refs are expected
  pre-existing (unrelated to the date pattern) — CONFIRM against `old` before calling M=0.
- **Log-review chain** `TESTING/_measure/log_review_20260906/` (waits for the Item 3 chain; snapshot `03be597`
  + working tree = 2A/2B/Item 1/4b/sign twin): arm `b_only` (2B default-on, DARK arcs explicitly `0`) · arm
  `arcs_on` (`ANCHOR_BARE_LABEL_FUZZY=1 ANCHOR_LABELLESS_CURRENCY_REFUSE=1 TYPE_UNINSTALLED_HEADING_FOLD=1`) ·
  `mv_off`/`mv_on` (RR_DB = the copy with the re-planted Meadowvale anchor, all 20 Meadowvale credit notes marked
  confirmed on the COPY, `ANCHOR_LABELLESS_CURRENCY_REFUSE` 0/1). Compare with `diff_arms.py`: `new`→`b_only` =
  2B alone; `b_only`→`arcs_on` = the three DARK arcs; `mv_off`→`mv_on` = Item 1's values + wouldFile delta.
  Chain log: `TESTING/_measure/log_review_20260906/chain.log` ("ALL LOG-REVIEW ARMS DONE").

## NEEDS YOUR APPROVAL (morning) — nothing below was done
- **Push** (13 local commits). · **Flip `money_sign_capture`** (mig 72) — the sign lever; now the anchor twin
  exists, both mints re-attach. · **Flip any of mig 122's three DARK arcs** only after the arms above read
  M=0 + zero fill drop + the eligibility delta you accept.
- **Owner actions (no code):** tick Statement + Service Worksheet from the catalog · re-teach the Meadowvale
  total WITH its caption ("Total to Pay") · Learning Repair: the three wrong-row/sign `corrections` rows
  (`139.36→-139.36`, `120.02→-120.02`, `162.00→-1,178.89`) poison 2a's substrate; **doc 471's confirmed date
  `26-01-6000`** is poisoned GT (send back / correct) · the standing revert of TEST migs 108–118 before any
  customer build.
- **5b seam gate:** OFF-vs-ON sibling run for templates 6/7 (the heal now works → their siblings' Stage-0.5
  compose changes next batch). Do NOT re-darken.

## Findings for the design (new)
- **`clearAnchors` is document_type-blind** (`learning.js:673-686`): a typed correction on a credit-note total
  wipes the same supplier's invoice total anchor too; and the label-less class is self-limiting ONLY when the
  operator TYPES a correction — a re-teach re-plants the same label-less row (id 17 did). Logged.
- **`documents.insert` ignores `working_path`** (test fixtures must UPDATE it) · from a scratchpad script
  `require('better-sqlite3')` must be by ABSOLUTE repo path · a bash heredoc with apostrophes in a Python
  string breaks the tool — write the patch file with the Write tool · `test_stage2_hardening.js`'s
  `pythonArgs: (...a) => a` stub is the masking class (use main's `(s, ...a) => ['-3.12', s, ...a]`).

## Owed / next
1. Read the arms (see above) → record numbers in `docs/oracle_log.md` 2026-09-06 entry + here (ADDENDUM).
2. Item 1 teach-side **page-words caption sourcing** (`region.py --page-words` → `anchorLabel.js` pickers) — the
   actual fix for the `''` label (the 108-DPI left strip reads nothing); draw-tool latency trade-off → owner.
3. 4(b): re-detect over the Desktop corpus + Hard Set (OCR needed) — a night-run item.
4. Hard Set 3-arm re-score (NIGHT_RUN "repeat if any extraction change lands" — several did today).
5. The 4 SILENT Print Tracker ref regressions are the leg-b/2a class (`1G`↔`16`, `HS`↔`H5`, `8`↔`5`) — not
   today's scope; confirm pre-existing via the `old` arm.

## Key paths
Design `docs/designs/LOG_REVIEW_FIX_PLAN_2026-09-05.md` · Oracle `docs/oracle_log.md` (2026-09-05 verdicts;
2026-09-06 BUILT entry) · pendingfeatures 2026-09-06 · measures `TESTING/_measure/date_alnum_20260906_live/`,
`TESTING/_measure/log_review_20260906/` (`census_type_fold.py`, `census_date_alnum.py`, `diff_arms.py`) ·
scratchpad (session-local): `run_item3_arms.sh`, `run_log_review_arms.sh`, `replant_meadowvale.py`,
`stage_hunks.py` (hunk-filtered staging), `census_anchors/REPORT.md` + `census_copy.db` (the db.backup() copy).
Run pins: `PYTHONIOENCODING=utf-8 py -3.12 python_backend/tests/<name>.py`; JS `ELECTRON_RUN_AS_NODE=1
node_modules/electron/dist/electron.exe <path>` (Git Bash).

---
## ADDENDUM 1 (14:45) — the log-review arms LANDED (`TESTING/_measure/log_review_20260906/`, `diff_arms.py` reports)
**Item 3 (`old` → `new`, 453 docs):** IDENTICAL per-field accuracy (type/supplier 100%, ref 448/453, date 452/453,
total 19/24), identical 6 regressions (pre-existing), **wouldFile delta 0** (361 both / 92 neither / 0 either-only).
The only 4 record diffs are NOTE TEXT on docs 471/480: the deskew-retry note reads "was '26-01-6000' → now
'05-01-2026'" under `old` and "was '(empty)' → now '05-01-2026'" under `new` — i.e. **the pattern change heals the
FIRST read (no wrong date is ever minted from the `P1/26/…` tail); the retry had been masking it.** GATE MET.

**2B alone (`new` → `b_only`):** M unchanged (same 6), every accuracy row identical, `customer_name` fill 20/20 and
60/60 unchanged. **18 Oakhaven `customer_name` records changed: 9 docs that committed `USTOMER` / `JSTOMER` /
`USTOMER i` via `anchor_registration` (@57–82) now read `Bramblewood Joinery Ltd`** (the real buyer) via
relocate/keyword. **Eligibility delta −4** (docs 453, 455, 456, 461 were `wouldFile` and are now held): 455/456/461
would have AUTO-FILED with `USTOMER` as the customer (@82!) — held now with the right name = the safety working;
**453 is the price**: right value before (@78 keyword_override, filed), now @69 + the note "Two different names were
read here — the clean value beside the label and a garbled one from the taught box. Kept the label read; please
verify." — the registration rung's caption-band-REJECTED read is still counted as a name DISSENT by the
two-names demoter. 1 of 20 Oakhaven docs = the review-load price; seam logged in pendingfeatures (a
`caption_band_read`-rejected read should be a DISQUALIFIED dissent, not a dissent). Reorder-band census: **25 band
records, 17 value diffs, 17/17 CHANGED by the clamp** — 12 `CUSTOMER` → `Bramblewood Joinery Ltd` (the heal), 5
`CUSTOMER` → address-line garble (`Unt Sept Lane` etc.) that the credibility/name gates then REFUSED (none committed).

**The three DARK arcs (`b_only` → `arcs_on`, `ANCHOR_BARE_LABEL_FUZZY=1 ANCHOR_LABELLESS_CURRENCY_REFUSE=1
TYPE_UNINSTALLED_HEADING_FOLD=1`):** **0 value diffs, wouldFile identical** — INERT on the live confirmed corpus (2B
already removed every `USTOMER`; no label-less currency anchor exists live; the Ironclad statements are needs_review,
not confirmed, so the DB harness never sees them — their gate is the text re-detect census, MET).

**Item 1 on the Meadowvale copy (`mv_off` → `mv_on`, the re-planted label-less anchor, all 20 credit notes):**
OFF = 18 docs commit `anchor_crop@50` wrong-row totals (`-48.64`, `107.2`, `01.7`, … every one carrying a validator
note "less than the subtotal / looks like the subtotal"), 4 of 18 happen to be right; ON = **all 20 →
`anchor_labelless_withheld`, value None + the re-teach note** ("This amount was taught by position only, and totals
move with the number of lines… Re-teach it with its caption"). **Eligibility delta −2**: docs 426 (the teach doc
itself) and 433 (a lucky fixed-row `-129.06`@54 with no note) were `wouldFile` OFF and are held ON = exactly the
pinned trade-off (6); **no doc becomes eligible with a value ≠ the page (the NO-GO condition) — GATE MET.** The
other 18 were already held (wrong value + validator note) and now hold with the TRUTHFUL note instead of a wrong
number. Flip = the owner's call: 2/20 review-load price vs 18/20 wrong values gone.

**Follow-on arms running** (`followon_chain.log`): 5b sibling run `angle_off`/`angle_on` (templates 6/7 NULL vs
healed angle, 32 sibling docs) + the Hard Set 3-arm re-score (baseline files preserved in `hardset_baseline_0831/`).
