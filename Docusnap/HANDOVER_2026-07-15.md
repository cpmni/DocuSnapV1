# HANDOVER 2026-07-15 — poison cleanup · disambiguation guards · icons · type-heading design

**Branch:** `feat/reprocess-throughput-autostraighten` · **3 commits this session, UNPUSHED** (ahead of
origin by 3) · working tree CLEAN (only untracked scratch `stress_test/_vv2.js`, `_vv2.js` ignorable) ·
installer NOT rebuilt · dev `npm start` was running live all session (renderer changes need a Ctrl+R /
restart to show).

## TL;DR
Windows auto-updated/rebooted the PC; prior session's identity work was already committed+clean. This
session: (1) **cleaned the Cascade↔Northgate template poison** on the live DB (backed up); (2) **built +
committed** the Slice D 256-bit detail-hash PRIMARY supplier matcher (`40994fe`, **default OFF**), two
disambiguation-picker guards (`1f02557`), and the Resolve/Straighten icon restyle (`b806fa1`); (3)
**diagnosed + designed** a system-wide document-TYPE fix (PO→Invoice, Worksheet→Sales Order) — advisors +
Oracle SIGN-OFF-WITH-CONDITIONS, **NOT built** (full spec: `docs/designs/TYPE_HEADING_AUTHORITY_2026-07-15.md`).
Owner is about to **reimport a fresh corpus**; the reimport is the right moment to enable Slice D and to
build+gate the type fix (both need a clean corpus baseline).

## COMMITTED this session
- **`40994fe` feat(identity): Slice D — 256-bit detail hash promoted to PRIMARY supplier matcher (DEFAULT
  OFF).** Breaks the Cascade↔Northgate / Northgate↔Copperfield logo collisions at the root. `logo_detail.
  classify_supplier` (nearest-supplier by min-over-set, accept 80 / confident 48 / margin 24, env-tunable);
  Seam A (`anchor.try_logo_supplier_match`) OVERRIDE review-bound @69+note when it disagrees with the
  coarse pick, method stays 'logo'; both-branch enrolment cross-plant guard (`learning.js`
  `_detailCrossPlantCloser` on INSERT + the collide-at-8 COALESCE backfill). Kill switch
  `LOGO_DETAIL_PRIMARY` (default '0'). Tests green (test_logo_detail.py, test_logo_detail_primary.py,
  test_logo_crossplant_guard.js). Full advisor+Oracle round; Oracle C1-C6 met. **Flipping it ON is gated
  on the C7 field-scan measurement — but see "Northgate/Copperfield" below: the measured pair PASSES.**
- **`1f02557` fix(review): two disambiguation-picker guards.** Guard A (`engine._build_candidate_emit`,
  kill `CANDIDATE_OCR_VALIDATE`): drop an un-boxed candidate not present in the page OCR text (kills the
  off-page "Sandpiper Hotels" hint bleed). Guard B (`anchor._is_fuzzy_caption_bleed` + rigid-path flag;
  `engine._name_relocate_should_hold` admits flagged anchor_crop; kill `ANCHOR_CAPTION_BLEED_GUARD`): a
  RIGID taught crop that garbles the field's own caption ("Veliver 10°" from "Deliver To") is held so the
  clean keyword wins — fuzzy leading-token match (Levenshtein ≤0.35) AND name_quality<0.6 (real names
  survive). Oracle C1(note+cap≤70)/C2/C3(name_quality pin) met. Tests green. Committed docs AUDITED clean
  (19/20; 1 garble correctly in review) — this was suggestion-time only.
- **`b806fa1` style(review): Resolve button + Straighten icon.** Resolve = filled-accent inline pill +
  a stroke "route into two arrows" icon. Straighten (both buttons) = a line raised on the right + a
  down-arrow just touching it (replaces the ∞ glyph; geometry chosen by rendering candidates via Playwright).

## DB cleanup (done on the live DB — NOT a code change)
Cascade↔Northgate template poison: 5 templates were cross-linked (Northgate docs under Cascade-named
templates via the logo collision). Re-homed each doc to its own supplier's canonical template, renamed the
2 misnamed templates, deleted the emptied fragment. All 11 templates now name==dominant, integrity ok,
412=412 docs / 272=272 confirmed preserved. **Backup:** `%APPDATA%\ScanFinder\docusnap.db.poison-backup-
20260715-072322` (+ `-wal`/`-shm`). NOTE: the live corpus later CHANGED (owner reimported Copperfield/
Northgate) — that poison cleanup was on the earlier Cascade/Northgate corpus.

## Northgate → Copperfield mis-file (owner-reported; Slice D fixes it — MEASURED)
A Northgate Textiles docket resolved as **Copperfield Electrical** (no "Copperfield" text on the page —
pure logo collision). The shipped **branding cross-check CAUGHT it** (issuer capped 69 + note, review-
bound — correct fail-safe). Measured (`scratchpad/nt_cop_collision.py`): COARSE dist NT↔Copperfield = **10**
(collision, ≤ match band) but DETAIL dist = **126** (cleanly separate); NT↔NT detail = **62** (same
supplier, < accept 80). **So Slice D resolves it** — the field-scan gap holds on this real pair.

## DEFERRED — designed, Oracle-signed, NOT built
**Document-TYPE heading authority (system-wide, built-in + custom types).** Spec + all 6 Oracle
conditions: **`docs/designs/TYPE_HEADING_AUTHORITY_2026-07-15.md`**. 3 roots: (1) type decided by a fragile
same-letterhead logo-sibling template pick that overrides the heading; (2) the heading-authority refuse
doesn't fire because the enhancement pass erodes the RED heading (oscar) AND a column-merged heading scores
low (reggie); (3) confirm-path `_upsertTemplate` reinforces the wrong-type template instead of borning the
correct-type one (gary/owner — "there should be a worksheet template"; all 16 worksheet confirms are
mislinked to sales_order/PO templates). 4-part fix (A oscar raw page-0 detect read · B reggie column-aware
scoring for name/alias banners · C gary refuse + optional override gate · D gary `_upsertTemplate` type-link
guard). **C1 is a SHIP-BLOCKER** (a falsely-trusted heading must fail-toward-review, not auto-file wrong
type at 100). The 16 mislinked worksheet docs are a DATA cleanup (separate, like the Cascade poison).

## FIRST ACTIONS for the fresh session
1. **Reimport the fresh corpus** (owner). Then **enable Slice D from the reimport**: flip
   `LOGO_DETAIL_PRIMARY` default `'0'→'1'` in BOTH sites (`python_backend/logo_detail.py` gate is env-read;
   the switch is checked in `anchor.py:try_logo_supplier_match` and `template_matcher._logo_detail_veto`
   — grep `LOGO_DETAIL_PRIMARY`). It's REVIEW-BOUND (@69+note) so it can't silently mis-file; the very
   first doc of each supplier still needs review (no reference yet — inherent), the rest self-heal after
   one confirm. Run the C7 quick check on the reimport: field-scan histogram (intra < inter with margin —
   the NT 62 vs 126 pair already passes) + `realdoc_regression` M=0.
2. **Build the type-heading fix** to `docs/designs/TYPE_HEADING_AUTHORITY_2026-07-15.md` (C1 first). Corpus
   type-accuracy gate; CONFIRM/extend the harness M to count a wrong-TYPE auto-file (Oracle C5 — the
   standing M is value-oriented and may be blind to right-value/wrong-folder). Live E2E on the real
   Copperfield PO + a worksheet. D (confirm path) is not corpus-reachable → unit pin + live confirm E2E.
3. Optional DATA cleanup: re-link the 16 mislinked worksheet docs to a worksheet template (after D exists),
   like the Cascade poison cleanup.

## Verification state — honest
- Slice D + guards + icons: unit/JS tests GREEN; guard live E2E was byte-identical ON vs OFF on the fresh
  reprocess (inert — the fresh reads didn't hit the trigger conditions; correctness rests on the unit
  tests, which pass). Type fix: DESIGNED only, nothing built, no corpus run.
- The `_type_ambiguity` HOLD is load-bearing for the type fix (Oracle) — the mistyped PO/Worksheet are
  currently HELD for review, not silently mis-filed, so there is no live silent-wrong-type auto-file today.

## Needs the USER
- Reimport the corpus; then the Slice-D enable + type-fix build run against it.
- Reload the Review window (Ctrl+R) to see the new Resolve/Straighten icons (renderer changes).

## Key facts / paths
- DB: `%APPDATA%\Roaming\ScanFinder\docusnap.db` (read-only via Python sqlite3 `?mode=ro` for diagnostics).
- Run JS gate tests via Electron-as-Node: `ELECTRON_RUN_AS_NODE=1 "$(node -e "process.stdout.write(require('electron'))")" <file>`.
  Python tests: `cd python_backend && PYTHONUTF8=1 py -3.12 tests/<file>.py`.
- Scratchpad probes (session-local, may not persist): `nt_cop_collision.py`, `po_type_probe.py`,
  `po_e2e.js`, `guard_e2e.js`, `ocr_audit.py`, `poison_*.py`; design briefs `TYPE_ORACLE_BRIEF.md`,
  `TYPE_HEADING_BRIEF.md` (the durable copy is `docs/designs/TYPE_HEADING_AUTHORITY_2026-07-15.md`).
- A localhost preview server (python http.server :8919) + the owner's `npm start` were left running.
