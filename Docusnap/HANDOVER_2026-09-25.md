# HANDOVER 2026-09-25 — Chris's 09-24 cards VERIFIED LIVE · card 4 ROOT CAUSE FIXED (fast-road env parity, Oracle C1-C7) · card 5 + Ironclad answered at source

**Owner asks this session:** "push it" (done — origin was already at `3807a96`), then "continue" (the open queue in
`HANDOVER_2026-09-25_CHRIS_ISSUES.md`). **Branch** `feat/teach-side-overnight`. Commits this session: `1efae11` (toast
names the types + receipt pin + contract note) · `490ba68` (fast-road display-gate pin) · `b46d1a7` (the card-4
root-cause fix + gate + docs). **Pins:** `node scripts/run-pins.js` 431 files — 430 green after the `test_quiet_lane.js`
window widen; `src/services/test_ref_class_fix.js` ARM B went red ONCE under the suite's load and passes alone (87/87) —
flaky, not this session's code. Read with `HANDOVER_2026-09-25_CHRIS_ISSUES.md` (the per-card
ledger) and `HANDOVER_2026-09-24_QUIET_REDETECT.md`.

## 1. Verify-first list — DONE on the owner's sandbox (CDP 9223), all five pass
Driven over CDP (a throwaway admin `verify` was seeded to sign in, then signed out and DELETED; the sandbox's Review
window was closed; the users table is back to `Chris` only). What the sandbox now holds: Credit Note / Delivery Note /
Statement ADDED from the catalog; doc 3 → Delivery Note, doc 4 → Credit Note (re-read by the quiet redetect in ~10 s),
doc 6 → Statement (via the new "Read it as a Statement" button), doc 5 still untyped (Service Worksheet is not a catalog
type — correct). ⚠ The sandbox app still runs the PRE-fix main process (this session's handler.js change needs a restart).
1. Not-recognised intro = one full-width sentence about the SENDER — verified at source (`renderer.js:2538`).
2. Catalog add → toast "Re-read 3 documents after you added … — the list has been refreshed. The document you have open
   was left as it was … press Reprocess" — LIVE. Miss found + fixed: it named SLUGS (`credit_note`), now type NAMES
   (`1efae11`, pin `test_review_redetect_receipt.js`).
3. Re-selecting the open leftover (detected Statement, now installed) → "Read it as a Statement" → pressed → typed + re-read
   (`document_type_id` 19, stamp cleared) — LIVE.
4. Letters-only never offered for a ref — verified at source + pinned (`test_reextract_fast_gate.js`); no skewed exhibit
   in the sandbox. Root cause below.
5. Catalog dialog copy "so it knows what to look for" — at source + the client copy in sync.

## 2. Card 4 ROOT CAUSE — the fast on-open road spawned Python WITHOUT the switch env — FIXED (Oracle SIGN OFF W/COND C1-C7)
gary (read-only trace) → verified by a direct run → Oracle (`docs/oracle_log.md` 2026-09-25). FACTS:
- `_reextractFastCore` (handler.js ~4484) spawned with `{...process.env, ..._ocrDpiEnv(db)}` only; every STORING road
  composes all four bridges. `_reconcileEnv` carries ~160 switches incl. `REF_ROLE_DIGIT_GATE` (keyword.py
  `_post_label_value`, Stage-1 only — NOT `_gate_value`; the 08-31 note's "ONLY guard" is this same path).
- Reproduced (`%USERPROFILE%\.claude\jobs\68b38f39\tmp\q1_repro.py`, now pinned in `test_ref_role_digit_gate.py`):
  Chris's text + the six catalog labels → gate unset: `credit_note_number` = "Meadowvale" @80 via label "Credit No"
  prefix-hitting the heading "CREDIT NOTE" (multi-word labels have no trailing boundary; the below-walk takes the next
  non-caption line); gate armed: refused → empty → review. Owner-sandbox doc 4 on the Quick road stored no ref — agrees.
- Premise upgraded by the Oracle and VERIFIED: the pill writes into the EMPTY input + `validateConfirm()` → Confirm
  unblocked → a plain Confirm files it as the reference (filename) and `persistConfirmedValues` (mig-93 ON) MINTS it as a
  learned row. So "display-only" was true for the machine, false in effect. No commit gap on any storing road.
**BUILT:** `_pipelineSpawnEnv(db)` (one composition, shard order) used at BOTH the Quick shard and the fast spawn; A/B
kill `REEXTRACT_FAST_PIPELINE_ENV=0` (never the default); the display twin kept as a BELT with its comment amended (C3);
`test_reextract_fast_gate.js` extended (C2 — fails on the pre-fix source); `test_ref_role_digit_gate.py` mechanism case
(C4). **GATE C5 MET** — `TESTING/_measure/fast_road_env_gate_20260925/RESULT.md`: owner copy 12 docs OFF 1 / ON 0; Chris
copy 177 docs OFF 31 / ON 8; GAINED 0, CHANGED 0, timeout deltas 0; every one of the 24 drops is WRONG (19 × `7719.06`
= the footer `VAT Reg No GB 442 7719 06` offered as `vat_tax` — `vat_reg_not_amount` never reached this road; 5 ×
the caption `Date` as `statement_number` on UNTYPED docs — the twin can't resolve a ref role without a type, the armed
Python gate can). C7 = the ON arm itself (a fresh instance of the fixed code). **Owner: restart the sandbox app to get it.**
Logged for later (Oracle C6, `NIGHT_RUN.md` queue): the Stage-1 label tail bound as its own DARK arc + the preset label
reorder ("Delivery No" is the FIRST Delivery Note label and prefix-hits the type's own heading).

## 3. Card 5 (one sender split three ways) — answered at source, design still the owner's go/no-go
Chris's DB: 18 docs "Meadowvale Dairy Wholesale", doc 69 "Meadowyale …", doc 62 "Dairy Wholesale" — ALL
`letterhead_prefill` @69, none confirmed, no learned identity rows. `reviewService.confirm`'s near-match guard
(`issuer_near_match_confirm_guard` ON) calls `learning.findNearMatchIdentity` with **minConfirms = 3 HUMAN confirms**
(machine vias excluded) or a frozen template identity. With 0 confirms today, confirming doc 69 or 62 would mint a
second/third sender; after 3 confirms of the right name both would be HELD (`nearMatchIdentity` 1-edit; `tokenSubrunIdentity`
for the truncated head). Gap = the first-3-confirms window + the Review GROUPING (three headers). Path unchanged: gary
(which arm owns it — the read-time letterhead prefill vs a grouping nudge with `apply-issuer-ripple`) → Oracle; never an
automatic merge. Related DARK: `name_dominant_snap` (2026-08-25).

## 4. Two Ironclad statements typed INVOICE — herald diagnosed (system weakness; design needed)
`TYPE_TITLE_OWNER_PRECEDENCE` (keyword.py ~1214-1236, ON since mig 60) promotes a lone `Invoice` TABLE CELL that lands
alone on reading line 15 (`_HEADING_TOP_BAND_LINES`, `<=`) on the two SKEWED pages (+1.5°/+1.4° shear splits the rows);
the folded UNINSTALLED `STATEMENT` heading (line 0, read perfectly, sum 7.3 vs 4.3) can never be an owner (`known_types`
only) → a CONFIDENT wrong type (`heading=True`). A straight statement with the type column FIRST is stolen the same way
(measured synthetically). Census: Chris-200 OFF→ON flips exactly {46, 52}. Fix direction Option B (a folded heading-only
name that stands alone as a strict heading is a BLOCKING owner → two owners → the sum wins → untyped + nudge; never
promotes an uninstalled name). Gate + the missing Hard Set statement class: `NIGHT_RUN.md` queue (2026-09-25). A plain
Reprocess of 46/52 would now self-correct (Statement installed → machine-authority OVERRIDE).

## 5a. TEST BUILD for the owner's fresh-DB test on another PC (owner: "build it exactly as it should be for testing")
**`dist\ScanFinder Setup 2.0.0-r20260925-0926-c7e0cc0-TEST.exe` — 371 MB.** Recipe (verified at source, `scripts/build-electron.js`
header): `HARDEN_JS=1 HARDEN_JS_STRINGS=1 TEST_BUILD=1 npm run build` after `node scripts/gen-third-party-notices.js`
(re-stamp `c7e0cc0`; the PP-OCR model credit intact). **First attempt without `HARDEN_JS` was REFUSED by
`scripts/verify-release-artifact.js`** (no `main.jsc`, 134 readable modules) and renamed `*.REFUSED.exe` — deleted, rebuilt.
Verifier on the hardened tree (`TEST_BUILD=1 node scripts/verify-release-artifact.js dist/win-unpacked` — the identity
gate needs the env or it refuses a test build as "not a release"): **OK** — bytecode present, no plaintext modules, 9 fuses
as declared, boot smoke exit 0, 15 windows 0 failed; manifest `dist/release-manifest-20260925-0926-c7e0cc0-TEST.json`.
Packaged Python smoke under `resources\vendor\python\python.exe -P`: model present (8,967,018 B), onnxruntime 1.27.0 CPU,
`glyph_reader.available()` True in 96 ms, `extraction.engine`/`keyword` import from the sourceless backend.
**What a TEST build does on FIRST launch** (`database/build_arming.js`, marker `test_build_armed_rev`): arms all 14
`TEST_SWITCH_KEYS` ON — `deskew_corrob_autofile`, `optional_soft_flag_autofile`, `corrob_autofile_band88`,
`filing_sanity_confusable_prefix_autofile`, `confusion_precedence`, `buyer_issued_convention_one_confirm`, `format_class_join`,
`departments_enabled`, `segment_pair_hold`, `issuer_undetected_blank`, `corrob_date_fold_wide`, `recon_singlechar_misread_flag`,
`glyph_confusable_release` (mig 211, "keep DARK" for customers — armed here by the TEST set's design), and
**`quiet_redetect_on_type_change` (mig 216)** — the redetect is therefore LIVE in this build without any SFDEV toggle. The
About box reads `2.0.0 (20260925-0926-c7e0cc0-TEST)`. A later RELEASE build on the same DB disarms them once (C3).
**Fresh PC notes:** licensing enforcement is ON in every packaged build → the new machine needs a backend trial/seat for
ITS fingerprint (`/v1/trial/start` from the licence window; the owner-email-on-trial fires); then the Legal gate → first-run
wizard (output folder) → welcome tour. The installer is unsigned → SmartScreen "Run anyway". The build carries every fix in
`81d33dd` (Chris's 09-24 round, the receipt names, the fast-road env parity) — the code the sandboxes were NOT running.

## 6. "Carry on" (owner, late morning) — two small UI items BUILT, two designs Oracle-SIGNED, awaiting the owner's go
**Built + pushed `99297fd`:** (a) "Set aside all" asks ONCE with the count and the way back (native `confirm()`, the delete's
idiom; pin `test_review_not_recognised_ui.js`); (b) the quiet redetect leaves a DURABLE activity-strip receipt — new kind
`recognised` ("3 given their type" / "3 waiting documents were given their type after you set up Credit Note, Delivery
Note — check and confirm them as usual"), recorded by the lane at job_done via a `recordEvent` dep → `recordReviewEvent`,
fix-family tone, never undoable (pins in `test_review_events.js`, `test_quiet_lane_redetect.js`,
`test_review_redetect_receipt.js`). **Trap fixed on the way:** `src/lib` was never a `run-pins.js` location —
`test_review_events.js` had been red since the 09-07 same-key merge rule (`49a76e4`) unseen; the stale check now pins that
rule and the runner gains `lib` (22 files). Suite 454 files green.
**DESIGN 1 — card 5 sender split → `docs/designs/SIBLING_DOMINANT_HOLD_2026-09-25.md`** (gary → Oracle SIGN-OFF-W/COND
C1-C8). Tier C "converging siblings" hold at the confirm gate + the Review grouping under ONE DARK switch
`issuer_sibling_dominant_hold` (mig 217): the batch's own converging pages (layout witness `convergesByBranding@0.80` or
phash ≤ 13 — verified 18/18 on the exhibit) + HUMAN-confirmed converging siblings (C2, blocking) tally the dominant
spelling; a 1-2-edit / sub-run odd read is ASKED (the existing Use/Keep), never adopted. **C1 blocking:** the teach ask must
run the same check in the same slice (else the wizard freezes the garble as a template identity and loops). C3 fold-first +
budget (perf), C4 `/v1` payload, C5 hand-adjudicated census (FALSE = 0 on Chris's copy / 727 / 605), C6-C8 pins/audit.
**DESIGN 2 — Ironclad statements typed Invoice → `docs/designs/TYPE_OWNER_UNINSTALLED_BLOCK_2026-09-25.md`** (herald →
gary → Oracle SIGN-OFF-W/COND C1-C10; build DARK now, Hard Set `statement_layout` class = the FLIP gate). An uninstalled
SHIPPED type name standing alone as a strict top-band heading becomes a BLOCKING owner in the `TYPE_TITLE_OWNER_PRECEDENCE`
re-rank → two owners → the sum → untyped + "Add Statement" nudge (into the mig-216 redetect's scope). **C1 blocking:**
exclude installed names ∪ their ALIASES from blockers (a "Sales Invoice" alias "Invoice" must never self-block). C3 the
fold's shipped-combination twin proven RED first; C4 separation arms OFF/ON identical (a stolen page's `title_trusted` was
a last-resort split witness); C5 census counts the third-type arm; C9 Hard Set class + scorer rule for uninstalled GT.
**Neither is built** — both are the owner's go/no-go. Both ship DARK with their gates when built.

## 7. Dev-inspector fix — the trace console now shows the STRAIGHTENED re-read (`7057b87`)
Owner's screenshot from the other PC: worksheet date on the panel at 98% ("Found '20-01-2025' after straightening — confirm
once") while SFDEV's trace said "no candidate" on every stage and "—" as the winner. **Cause (at source):** the whole-page
straighten retry (`DESKEW_REVIEW_RETRY`, `process_docs.py` ~1596-1650) runs a SECOND `engine.extract` on the straightened
page with `trace=None` and adopts it whole when its overall is higher; the adoption emitted no event → the console only
ever saw the raw pass. **Fix:** the second extract is traced under `--trace`, tagged `pass: "straightened"` (no slice_dir);
`deskew_adopt` per changed field + a `deskew_pass` receipt (adopted / kept raw); the console routes tagged events into a
per-field `s2` bucket, the winner line reads "… · after straightening (1.6°)", ↻ rows + a doc-level banner. Byte-identical
without `--trace`. Pin `test_trace_console_straightened.js`; deskew pins 36/36; windows 77/77. **Renderer + Python change →
needs the Review window reopened / a rebuilt installer — NOT in `…-c7e0cc0-TEST.exe`.**

## 8. DESIGN 2 BUILT DARK — `type_owner_uninstalled_block` (mig 217; owner "go" on the recommendation: design 2 first) — commit `9abc3a2`
**Suites at the commit:** JS `run-pins.js` 456 files green (five count pins patched to 15: 137 / 163 / 205 / 215 / 216 —
the Oracle's list named three; 137 and 163 also assert the count); pytest 448 passed + **1 PRE-EXISTING red**
`tests/test_identity_fusion.py::test_verdict_conflict_agree_abstain` (fails identically on the committed engine; the
test never touches the type detector — rapidfuzz/threshold drift to look at separately). C4 = MET (see RESULT.md: stacks +
five control sets diff 0; `real_34` re-stitched from its split pages → 34/34 identical).
**Engine** `keyword.py` TYPE_TITLE_OWNER_PRECEDENCE block: ONE `_top_band_owner(phrases_lc)` predicate (the original loop
verbatim) used by the owner loop AND a new blocker loop — nested under `TYPE_OWNER_UNINSTALLED_BLOCK` + the fold + a type
list — over every shipped bucket name not in `_installed_claims` (installed names ∪ their aliases, Oracle C1) that scored
and stands alone in the top band; promotion needs `len(_owners) == 1 and not _blockers`. C7 stale comments fixed.
**Bridge** inside the fold's `if` in `_reconcileEnv` (child; env wins). **Plumbing** mig 217 INSERT OR IGNORE 'false';
`TEST_SWITCH_KEYS` → 15 (count pins 205/215/216 patched; HELD list); ledger entry; `test_migration217_type_owner_
uninstalled_block.js` (seed + nesting regex + engine shape). **Pins** `test_teach_side_gates.py` 36/36 (ten new incl. the
C1 alias self-block negative, the C2 symmetry, the pinned sum-fallback trade-off) on fixtures `tests/fixtures/
ironclad_statement_{46_skewed,44_straight}.txt` (Chris's stored OCR text, synthetic issuer); `test_type_uninstalled_
heading_fold.py` §9 = the C3 red-first twin (the shipped fold+owner combination on the REAL skewed page → Invoice heading
True; block ON → Statement@95). `test_segmentation.py` green.
**Gate** `TESTING/_measure/type_owner_block_20260925/RESULT.md` (+ `census.py`, arm files): Chris copy with the
import-time type set → changed EXACTLY {46, 52} → Statement@95 heading (untyped + nudge downstream, C8); the owner's live
DB copy (758 texts) → 0 changes with its installed set AND with Statement/Credit Note/Delivery Note/Receipt/Remittance/PO
ablated (the C5 third-type arm is EMPTY on real data → trade-off pinned, no review route); pre-change vs new engine
IDENTICAL on all 958 texts with the block unset (the refactor is byte-identical; realdoc OFF is vacuous by construction).
**C4** separation arms (the 09-16 soak census re-run, product env, block OFF vs ON; templates regenerated from the live copy
into job scratch — real data, not committed): see RESULT.md §C4. **Owed before FLIP:** realdoc ON arm (M=0) + the OFF md5
record; Oracle C9 Hard Set `statement_layout` class + controls + the scorer rule for an uninstalled GT type (queued in
`NIGHT_RUN.md`). Nothing flipped. `warm_700.db` stores no `ocr_text` — not a text-census source.

## 9. DESIGN 1 BUILT DARK — `issuer_sibling_dominant_hold` (mig 218; owner "go" after design 2) — commit `49710a7`
Suite at the commit: 458 pin files green (five shape/version pins widened for the new prelude + contract 1.10.0;
the heredoc-strips-a-backslash trap bit once more — regex-heavy pin edits go through the Edit tool, never a Python heredoc).
**Core** `learning.findDominantSiblingIdentity(db, docId, candidate, opts)` + `siblingDominantEnabled(db)` (explicit
'1'/'0' env, else the setting): fold-first aggregate over queued + HUMAN-confirmed rows (machine vias excluded,
`learningExcludedSql`, no `intake='direct'`) → near-test the DISTINCT folds (`nearMatchIdentity` then
`tokenSubrunIdentity`) → fingerprints + the `issuer_sibling_fill` convergence pair (branding 0.80 OR phash ≤ 13) ONLY for
rows in a near fold + the own fold, pair budget 5,000 (fail open) → dominant = `count ≥ 2 AND > own` → `{near, existing,
kind, distance, siblings, confirmedSiblings, own, pairTests, source:'siblings'}`. **Consumers:** `reviewService.confirm`
after a Tier A/B miss, before the letterhead hold (same `ISSUER_NEAR_MATCH` code / ack / bulk-held semantics; audit
`confirm_held_sibling_dominant` with the C8 tally) · `check-identity-near-match` IPC with `docId` + `/v1/teach/commit`
pre-tx (Oracle C1) · `teach.js` passes `docId` at both ask sites (client copy synced) · `/v1` review-confirm 400 carries
`nearMatch` (C4) → contract **1.10.0** (lockstep in `client/apiClient.js` + `test_v1_preview_reads.js`; note in
`docs/detached-client.md`) · `getReviewQueue` post-pass `_stampSiblingDominant` (once per read, size-prefiltered, fold cap
400) → `reviewGroupKey` prefers `issuer_sibling_dominant`, chip "check sender" · `showIssuerNearMatchHold` siblings lead
(count + both spellings, "same sender?", never "already use"). Mig 218 seed 'false'; `TEST_SWITCH_KEYS` 16 (six count
pins patched); ledger; `pendingfeatures.md` card for the client Use/Keep affordance.
**Pins:** `test_near_match_siblings.js` (switch semantics · exhibit a-d · negative control · text budget · tie-break incl.
the pinned 1-vs-1 abstain + majority garble byte-unchanged · C2 confirmed siblings / machine vias / 10-10 · C3 perf 13 ms
confirm, 25 ms queue stamp on 2,000 rows — **the display slice's first draft took 36 s there** (every row of every
near-named fold evaluated); fixed with the size pre-filter) · `test_reviewservice.js` Tier C section · `test_migration218_
issuer_sibling_dominant_hold.js` (seed + every consumer's shape). **Census** `TESTING/_measure/sibling_dominant_census_
20260925/RESULT.md`: Chris's copy → queued holds EXACTLY {62, 69}, replay 0; the owner's live copy → 0 / 0 (256 human-
confirmed docs), ≤ 14 ms. **Owed before FLIP:** one live client confirm of doc 69 (C4 toast) + a Chris round with the
switch armed. Live smoke on Chris's DB copy: 69 → edit d=1 (18 siblings), 62 → sub-run, a correct sibling → no hold,
an unrelated sender → cheap exit.

## 5. Owed / open
- **Decisions (unchanged):** flip mig 216 for everyone or keep DARK · card-5 design go/no-go · Ironclad Option B go
  (gary → Oracle) · the sandbox `output_folder` is still the REAL `Documents\Scan Finder` · durable activity-strip receipt
  · card 8 tab rename / "no type yet" pill · "Set aside all" confirm · natural-language search date parse.
- Owed docs done: `docs/detached-client.md` 1.7.0 → 1.9.0 note. `stress_test/out/c0_live_copy/` was already gone.
- Still owed: the Hard Set digital supplier-EMPTY question · `charsetAcceptService.recomputeOverall` rounding (≤1 pt).
- Traps met: `gate_drive.py` needs the video venv python (websocket-client); the sandbox login page is a separate window
  titled "ScanFinder — Sign in"; after `authLogout` the main window stays (title `ScanFinder`) — restart to be sure; a
  Python heredoc that rewrites a JS pin mangles `\\d` — use the Edit tool for regex lines; renderer source pins must
  normalise CRLF and locate a NESTED function by its indented closing brace.
