# HANDOVER — 2026-07-24 (full day: overnight + live-testing)

**Branch `feat/reprocess-throughput-autostraighten` — PUSHED through `7d11f86` (origin in sync `0 0`).**
Live-testing day with the owner. Readable pipeline overview for non-technical review:
**`docs/DETECTION_OVERVIEW_2026-07-24.pdf`** (+ `.html` source). Per-fix detail in the memory files
(`project_*` — see the slugs below).

## Commits today (7 ahead of the 07-23 NIGHT baseline `21e9369`)
| sha | fix | kill switch | default |
|---|---|---|---|
| `5377e24` | Slice-1d verdict = DO-NOTHING-IN-CODE (Stage-0 veto already live via Store B) | — | — |
| `8e2211c` | Deskew raw-witness — straighten+reprocess no longer corrupts a taught crop | `DESKEW_RAW_WITNESS` | ON |
| `4af4bba` | **Issue 2** own-label exemption — precise labelled reads not over-flagged | `TAUGHT_OWNERSHIP_OWN_LABEL` | ON |
| `5c94db8` | **Located-recovery** — Stage-2.6b re-runs a taught anchor when the supplier resolved late | `LATE_RESCUE_LOCATED_CORROB` | ON |
| `7229cdd` | **Name-presence veto** — kills a cross-supplier logo false-match on the template suggestion | `TEMPLATE_NAME_PRESENCE_VETO` | ON |
| `7d11f86` | **Name-guard clear** — clears a phantom "caption disagreed" flag on a corroborated name | `NAME_GUARD_KEYWORD_CLEAR` | **OFF (dark)** |

Also measured + REVERTED (do-nothing): the naive **cross-tier auto-file lift** (illusory shared-OCR-text
"independence"; the safe form already ships via KEYWORD_ANCHOR_CORROB) — infra kept
(`stress_test/consensus_analysis.js`, realdoc `RR_CONSENSUS`).

## Gate posture
Each fix corpus-gated (`realdoc_regression.js`): M-safe (no new wrong auto-file) + own kill switch +
OFF byte-identical + advisor→Oracle SIGN-OFF. The name-guard clear is the exception — its M-gate rose
10→11 on **#259** (a CORRECT name-flag-clear un-masked a pre-existing REAL ref misread
`DN-28472`→`DN-38472`), so per Oracle + owner it ships DARK (fail-toward-review).

## Installer
Current: **`dist\ScanFinder Setup 2.0.0-r20260724-1432-7229cdd.exe`** — carries the 3 LIVE fixes
(issue-2 + located-recovery + name-veto). The name-guard clear is dark → no behaviour change → NO
rebuild needed for it.

## OPEN — the one tracked follow-up (the name-guard precondition)
**Ref-hold guard:** make the authoritative-crop cross-check (`anchor.py:638-659`) flag a valid-shaped
**single-digit** crop-vs-full-page ref disagreement EVEN when the crop read is sub-credible — the net
that catches #259's `DN-28472`/`DN-38472`. Feeds every ref/date field → its own corpus gate. When it
lands: pin (#259's ref flags; enabling the name-clear doesn't raise silentAutoFile), then flip
`NAME_GUARD_KEYWORD_CLEAR=1` (or ship both together). #259 = the named canary. Valuable independently
(the old City-Office silent-misread class). Full ledger: [[project_name_guard_keyword_clear]].

## OPEN — owner live-checks (on the current installer)
- Thornbury **invoice** → number/date file clean (issue-2). Thornbury **PO_05** → PO Date clean, no 69%
  (located-recovery). Saltmarsh **sales order** → no "Template available: Larkspur" (name-veto).

## Memory (durable per-fix ledgers)
`project_taught_ownership_own_label` · `project_late_located_corrob` · `project_name_presence_veto` ·
`project_name_guard_keyword_clear` · `project_deskew_raw_witness` (+ cross-tier do-nothing) ·
`project_slice1d_donothing`.
