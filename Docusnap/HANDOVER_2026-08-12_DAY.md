# HANDOVER — 2026-08-12 DAY (owner present, heaviest feature day on record)

**Branch:** `feat/teach-side-overnight` · **HEAD `3a751d0`, PUSHED, tree clean** (4 feature commits
today: `5623102` → `e94f2f5` → `6bd591f` → `3a751d0`, over the overnight `b901e66`).

**POST-WRAP ADDENDUM (`3a751d0`) — RAW-CROP WITNESS built:** the 08-11 Oracle-signed recipe-ladder
fix (serif I→1 / ACC-229] class), built cold per its spec, C1-C6 applied: witness compares the
PRE-repair string at BOTH ladder exits (C1 pinned with a separator-stripping repair installed);
FLAG tier (note+corrected_to+cap 84) at the absolute-rung surface; ADOPT tier census-gated per pair
(`RAW_WITNESS_ADOPT_PAIRS`, default empty). Flags `raw_crop_witness_flag`/`_adopt` OFF + toggles +
wiring pins; 18 pins ALL PASS driving the REAL ladder; realdoc dark arm md5-identical. **OWED before
flip: the spec's heal gate** (ref lane 27/1→28/0 witness+sepguard TOGETHER + witness-only arm must
also move + bidirectional per-pair census over corpus AND born-digital arms + C5 Gate-C amendment).
**C4 standing: never flip `CODE_SEPARATOR_STRUCTURE_GUARD` alone — together, sep-guard AFTER.**
**Also post-wrap:** backfill APPLY ran (owner, `!`) and VERIFIED — tpl 14/15 issuer frozen, backup
`docusnap_pre_issuerfreeze_20260812.db`. And a NEW next-arc entry sits at pendingfeatures TOP:
**"why was the Lid→Ltd repair silent?"** — post-reprocess Meadowvale reads customer 'Bramblewood
Joinery Lid' @95 UNFLAGGED (one confusable char from the 38× literal); the shipped Stage-4.5
token repair's own textbook example didn't fire — ROOT-CAUSE FIRST, the confusable-snap tier only
if 4.5 structurally can't own taught reads. No uncommitted batch.
**No installer built.** Owner's app was restarted several times and was CLOSED at wrap.
Migration still 63. **Six Oracle passes today — zero SEND BACKs. NOTHING FLIPPED BY THE SESSION;
the owner flipped several toggles themselves (list below).**

## TL;DR
Owner drove all day; the session shipped SEVEN gated dark slices + two live tools, repaired live
data twice, and corrected two of its own wrong claims. The arc: (1) per-sender field editor
replacing "Save as template"/"Edit type" (Chris+eric+Oracle, all conditions applied); (2) an
afternoon batch — reconcile shadow-attribution, VAT @-rate skip, graduation issuer freeze,
self-discharging supplier pins, graduation-window dial, Learning-Repair currency fix, inert
containment predicate; (3) CONFIRMED_DOMINANT_ADOPT — a junk-flagged name yields to the scope's
single ≥5×-confirmed literal (owner: "minimise interaction where positive confirmation exists";
STRICT variability owner-ruled). Live repairs: all 18 supplier pins cleared (backup taken);
graduation-freeze backfill CENSUS run (tpl 14+15 eligible, APPLY still owed). Every flag DEFAULT
OFF with a Settings toggle + wiring pin; ~90 new pins green; four realdoc arms md5-identical.

## Committed (all pushed)

### `5623102` — Review per-sender field editor (Oracle W/COND ×5, all applied)
Root cause class: "Save as template" + "✏ Edit type" were mis-scoped doors; field editing is a
per-SENDER intent. Resolution-first (display resolver = write authority, `resolveVisibilityTemplateIds`
extracted verbatim — Oracle C3); first-time sender gets an EDITABLE name prompt then an
IDENTITY-ONLY mint via promote-to-template (C1 — `_buildTemplateFields` sees `{supplier_name}` only);
un-hide clears resolver-set ∪ `doc.template_id`, audited (C4; the previously-unaudited
`set-template-hidden-field` widened admin→admin+edit); toggles repaint the open doc from the
server-returned union (C5). "Never on these documents?" link on empty non-structural fields.
Headline Oracle catch: eric's two seam-3 mitigations were ONE gate (young-identity is NESTED inside
`TEMPLATE_IDENTITY_ON_PAGE`, `template_matcher.py:611`). Chris's approved queue-copy was FALSE
(hides reach waiting docs at doc-open — `review/handler.js:547-573`); eric's corrected copy shipped.
Pins: `test_editor_mint_identity_only.js` · `test_sender_field_hidden.js` (containment residual
pinned) + existing suites. Gate: realdoc A/B feature-vs-stashed-base **md5-identical** (142 docs).
**OWED: the owner UI smoke list** (cancel=zero rows · mint→toggle→visible · Show-again on a
garble-named sibling · Settings-side live-update · untyped disabled state · generic refusal).

### `e94f2f5` — afternoon batch (3 Oracle passes; full detail in `docs/oracle_log.md` 2026-08-12)
- **`reconcile_shadow_attribution`** (W/COND C1-C5): corroborated total + ALL-shadow operands ⇒
  note reworded to neutral evidence + 50-cap skipped; note NEVER cleared (pinned at conf 90 AND
  100). Census `RECONCILE_ATTRIB_CENSUS_DIR` = flip evidence. 12 py pins + 2 JS pins.
- **`vat_rate_at_skip`** (reggie, SIGN OFF): reggie REFUTED the brief — bare "VAT" label matches;
  the columned "@ 20%" SEGMENT fails the rate-annotation skip and dies at validation ⇒
  MISSING(tax). One char-class (`[@(]?` + `@` in the residue class), last-segment-safe, 9 pins
  end-to-end (`test_vat_rate_at_skip.py`).
- **`graduation_freeze_issuer`** (W/COND; Oracle ACCEPTED narrowing his own C6 to issuer-only):
  graduation-born templates were IDENTITY-MUTE (all-variable rows) so hint/logo-identity scopes
  could never clear the 95 floor (Oakhaven tpl 14: 17 confirms, docs stuck at hint-cap 85). Birth
  now freezes `supplier_name` to the scopeTrust-graduated string; everything else variable
  (two-way pin). **FLIP CHECKLIST (in the toggle copy): flip WITH `template_identity_on_page`.**
  Backfill `scripts/backfill-graduation-issuer-freeze.js` (SIX census clauses incl.
  ≥DISTINCTIVE_MIN + no-issuer-mapping); **census RUN read-only: exactly tpl 14+15 eligible.**
  Born-mature seam verified PARTIAL (collation: trust.js LOWER(TRIM) vs namePresence binary `=`) —
  direction SAFE, documented, not "fixed".
- **`supplier_pin_self_discharge`** (W/COND): at the final re-assert, a natural read
  (page/layout-evidence allowlist; `keyword_override` + doctype-`fixed` EXCLUDED, pinned)
  normalise-equal to the pin ⇒ natural row kept + `_supplier_pin_discharged` signal → JS clears
  the pin after the merge, audited, race-guarded (`supplier_pin_discharge.js`). First assert
  deliberately untouched (Oracle UPHELD; all 7 `accepted_issuers` consumers verified
  suppression-only). 11 new py pins + 6 JS pins.
- **`graduation_window` slider** (Settings→Processing): trust.js W → setting, clamp 3..50,
  default 10 unchanged; 5 pins. **Owner set it to 5.**
- **Learning Repair currency fix**: money NEVER magnitude/shape-compared (both detectors);
  magnitude-invariant `MONEY_VALID` format check; '2.205.60' garble class still flags; 4 pins.
- **`substantial_containment`** predicate ships INERT in engine.py (reggie spec; 10 pins) —
  consumer slice is a future arc with its own flag.
Gates: dark realdoc arm **md5-identical** to the day's baseline; armed arm (slices 1+2)
md5-identical at report level (zero value changes; fired-subset visibility = the census
instruments, by design).

### `6bd591f` — CONFIRMED_DOMINANT_ADOPT (Oracle B1-B5, all applied)
Exhibits: Ironclad 'Sramblewood Joinery Ltg' (taught-box garble @70) vs 'Bramblewood Joinery Ltd'
20×; Meadowvale 'MDW.-315' (account number in customer) vs 38×. gary: the 2.5d snap is
QUADRUPLE-SEALED on names — wrong layer; terminal step `_adopt_confirmed_dominant` placed AFTER
the corrob build, BEFORE the final trace (B5 — the SFDEV inspector must never show dead junk as
final). B1 adoptee itself passes the junk predicates; B2 corrob record rewritten in-step (memory
family, `independent_agree:false`, dead value retained) so `_corrobLicensed` refuses structurally;
B3 `+confirmed_adopt` rows UNCONDITIONALLY excluded from `getFieldFormats` (dominance can't vote
for itself; `confirmed_via` can't split graduated machine files from humans — both stamp NULL);
B4 owner-ruled STRICT variability (a second distinct confirmed key at ANY count refuses — pinned);
picker suppression free via the `:2612` note-gate. SECOND named `_override_eligible` carve-out —
docstring amended to name both. 22 py pins + 3 JS pins (falsifiable control); realdoc dark arm
md5-identical. **Own-bug catch worth remembering: `"pin" in m` matched 'template_maPINg' — caught
by this slice's own pins before it ran anywhere.**
**Honesty (Oracle correction): armed, the exhibits open in Review with the right value pre-filled,
no picker, no note — they do NOT file silently (`format_anomaly_flagged` already counted). That IS
the owner's asked-for interaction reduction; fully-silent filing was explicitly kept out of v1.**

## Live-DB actions (data, no code)
- **All 18 supplier pins CLEARED** (owner-run via `!` after the permission classifier blocked me
  twice — correct behaviour, noted). Backup: `Desktop\TESTING\_measure\live_backup_20260812_pinclear.db`.
- Backfill census (read-only) run: tpl 14 Oakhaven + tpl 15 Harrowgate eligible, 2 of 2, zero
  surprises. **APPLY NOT RUN** — the owner has the one-liner (below).
- DB snapshot for all realdoc arms: `Desktop\TESTING\_measure\rr_sfe_snap_20260812.db`.

## Owner flips TODAY (their own hand — verify in DB, not here, before relying):
`graduation_window=5` · `graduation_freeze_issuer=true` · `template_identity_on_page=true` ·
`supplier_pin_self_discharge=true` · `corroboration_autofile=true` (was already on).
**`scope_sweep_enabled` still UNSET = OFF** (the 08-02 "flipped ON" record is stale — verified
default-false at `processing/handler.js:2785`). Recommended flip, not yet done.

## Session claims CORRECTED (do not re-derive wrongly)
1. **"Auto-file runs on reprocess" (told to the owner mid-morning) is FALSE** — `_maybeAutoFile`
   has ONE call site, the import path (`processing/handler.js` ~:4173). Reprocess always lands in
   Review. Queue docs are never re-asked; the exits are File All Ready + the scope sweep.
   At wrap, THREE queue docs pass `isAutoFileEligible` TODAY (Oakhaven 98, Castellan 95,
   Silverbeck 100) and sit unfiled for exactly this reason.
2. **The vat_tax brief's "@ defeats the label match" was REFUTED by reggie** — value-selection
   defect, not label matching.
3. **eric's "two mitigations" for mint-from-unconfirmed were ONE gate** (Oracle catch).
4. Chris's approved save-moment copy was factually wrong; corrected version shipped.
5. PowerShell `&` does NOT wait for electron.exe (GUI subsystem) unless the call is PIPED —
   `$LASTEXITCODE` is stale; gate on printed output (bit the first A/B run).

## FIRST ACTIONS (fresh session)
1. Ask whether the owner ran the **backfill APPLY** (`$env:APPLY="1"` +
   `scripts/backfill-graduation-issuer-freeze.js`, app closed). If not → it's the highest-payoff
   single step (Oakhaven/Harrowgate auto-file chain). Verify `template_fields` rows for tpl 14/15
   afterward, then reprocess both groups and CHECK the replay expectation
   (hint@85/logo@72 → template_fixed@95, zero cross-scope movement).
2. Owner smoke of the sender-field editor (list in the `5623102` section) — still unsmoked.
3. The queue: three eligible docs → File All Ready; recommend `scope_sweep_enabled` flip.
4. Owed gates before any flip: pin-discharge census arm (COPY of the 18-pin backup, AFTER the
   backfill — gary/Oracle build order); CONFADOPT census (flip bar: would-adopt-vs-GT 100%);
   shadow-attribution census.

## Deferred (designed/argued, NOT built) — load-bearing conditions attached
- **Label-above keyword extension** (Silverbeck stacked cells; direction (a) recommended —
  caption-located witness; precision risk needs anchor-path guards). Own advisor+Oracle gate.
- **Containment-witness CONSUMER slice** (predicate is inert; consumer must never clear notes;
  interaction with NAME_UNCLIP standing C6).
- **Un-pin UI control + honest ripple copy** (pin arc companions).
- **tpl 11 "Reg No GB 903" cleanup** — owner route pick still open (reassign 4 docs → tpl 4 then
  delete, vs irreversible merge). Its poison is live (frozen quote-number-as-account @95).
- **Ironclad taught box** — C7 stored-box repair arm (owner decision, backlog) or a 30-second
  re-teach; adoption only masks the garble when the variability clause allows.
- **Queue re-evaluation gap** (eligible docs nobody re-asks) — owner said record; in backlog.
- **Graduated/threshold auto-files stamp confirmed_via NULL and count toward the HUMAN graduation
  window** (`trust.js:538` excludes only scope_sweep/auto_corroborated) — pre-existing, filed by
  Oracle, OWN ARC, do not fix casually.
- **Template Manager right column doesn't span full height** (owner-reported cosmetic, unfixed).

## Needs the USER
Backfill APPLY · editor smoke list · File All Ready / sweep flip · tpl 11 route ·
C7-vs-re-teach · flip decisions once censuses run.

## Key facts / paths
- Live DB `%APPDATA%\ScanFinder\docusnap.db` (migration 63). Backups today:
  `live_backup_20260812_pinclear.db` + `rr_sfe_snap_20260812.db` in `Desktop\TESTING\_measure\`.
- New test entry points: `py -3.12 python_backend/tests/test_{reconcile_shadow_attribution,
  vat_rate_at_skip,substantial_containment,confirmed_dominant_adopt}.py` (+ extended
  `test_supplier_pin.py`); Electron-as-Node: `database/modules/test_{sender_field_hidden,
  editor_mint_identity_only,supplier_pin_discharge,dominant_adopt_learning,graduation_template,
  scope_trust,trust_shadow_row_skip}.js`, `src/windows/settings/test_settings_wiring.js`,
  `src/services/test_repair_suspects.js`.
- Realdoc A/B pattern + the GUI-exe wait gotcha: scratchpad `rr_*.ps1` scripts (session-mortal) —
  re-derive from this handover's correction #5.
- Oracle log: `docs/oracle_log.md` — THREE 2026-08-12 entries (sender editor · afternoon batch ·
  CONFIRMED_DOMINANT_ADOPT). All conditions + flip bars live there.
