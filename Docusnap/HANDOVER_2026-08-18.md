# HANDOVER — 2026-08-18 — the overnight fix loop (rounds 7+8) + website/ops triage

**Branch `feat/teach-side-overnight`, 15 commits `b477872`…`2408145`, ALL PUSHED to origin.**
Owner is on holiday (South Ireland, hotspot); authorized the autonomous fix→Chris-rerun loop
2026-08-15 night. Two full cycles ran; both acceptance gates passed.

---

## ⭐ OWNER OPS CHECKLIST (the website "overloaded" thread — 2 panel toggles, no code)
Diagnosed 2026-08-18: intermittent bare "overloaded" on scanfinder.co.uk = IONOS front proxy
serving its saturation page when a bot burst (IONOS stats: **ChatGPT/GPTBot is the majority of
~250 visitors**) overlaps the slow shared origin (static homepage TTFB measured 0.7–4.2 s). The
app's own traffic is negligible (validate on launch + every 6 h — verified in `src/main.js:407`).
Package has NO logs folder (webspace root checked — only `licensing/` + `public/`), so IONOS
support chat is the only route to their side of the numbers. **Both hosts share ONE webspace —
a burst takes the main site AND licensing down together.**

1. **◐ IONOS CDN on `scanfinder.co.uk` — TOGGLED ON by the owner 2026-08-18, propagation
   pending** (main still answered `server: Apache` at 16:0x; flips to `cloudflare` within ~an
   hour — re-verify with `curl -sI https://scanfinder.co.uk | grep -i server`). PROOF IT WORKS ON
   THIS PLAN: `licensing.scanfinder.co.uk` already serves via Cloudflare (`Server: cloudflare`,
   `cf-cache-status: DYNAMIC` — proxied, never cached: correct for an API) while the zone's
   nameservers are IONOS's own (`ui-dns.com`) — the IONOS-CDN-powered-by-Cloudflare feature.
   The subdomain needs NOTHING added. Once the main site flips, the static homepage caches at
   the edge → prospective buyers never see "overloaded" again.
2. **☐ Add an hourly IONOS Cron Job** (panel → Cron Jobs, included in Webhosting Plus):
   `php /<webspace>/licensing/lib/polar_reconcile.php --apply`
   This is the designed missed-webhook net (heals grants/extends from Polar's active set; never
   auto-revokes; dry-run without `--apply`). Needs `keys/polar_api_token` present on the host
   (code reads `keys/` first, env `POLAR_API_TOKEN` second — `lib/polar_reconcile.php:20`).
   WHY IT MATTERS: Polar retries any non-2xx and our receiver is idempotent (webhook-id PK), so
   blips only DELAY a buyer's key email — but a PROLONGED outage makes Polar auto-disable the
   endpoint (documented in `public/v1/polar_webhook.php`), and the cron is what heals that.
   Today it exists only as a Windows wrapper (`scripts/Reconcile-Polar.ps1`) that runs while the
   owner's PC is on.
3. **✅ UptimeRobot LIVE** (set up 2026-08-18, account cmccully@gmail.com): two monitors @5-min —
   `scanfinder.co.uk` (id 803768617) + `licensing.scanfinder.co.uk` (id 803768644), both
   emailing cmccully@gmail.com on down. Use its data to decide if anything more is ever needed;
   do NOT upgrade the IONOS plan pre-emptively (Webhosting Plus is adequate — the issue is
   burst concurrency, not capacity).
4. Standing facts: the licensing gate is FAIL-OPEN offline (an unreachable backend never locks a
   customer's app). Polar checkout is Polar-hosted (payment succeeds even with the site down).
   The deployed site is STALE (owner hasn't uploaded recently) — the local deploy tree builder is
   `licensing-backend/scripts/Build-IonosDeploy.ps1` → `output/licensing-backend-ionos`.

---

## THE FIX LOOP — what shipped (all Oracle-vetted, every extraction change kill-switched)

### Round-1 fixes (commits `b477872`…`96d0875`) — validated by Chris round 7
- **`b477872` hold-siblings identical-rewrite** — THE regression root cause: `nearMatchIdentity(X,X)`
  returns `near:false reason:'identical'` BY DESIGN, and `_identityOverwriteGuard` treated every
  `!near` as a genuine identity change, so the teach's second identical write marked templates
  pending → 200/200 sender-confirm notes, File-All dead. Fix: trim+lower literal equality (tidied
  both sides) early-return = the release comparator; Oracle BLOCKING condition applied — a TAUGHT
  confirm skips `noteIdentitySupported` (else a genuine-change teach would self-release the
  round-4 defence). + identity EDGE-JUNK tidy at the freeze seam ("Pelican Office Interiors -"
  class; dirty incumbents KEPT for scope continuity). Flow-level pin through real
  reviewService.confirm.
- **`3a913ce` P prefix-confusable adopt** (`ref_prefix_confusable_adopt`, OFF) — owner-directed
  PI/P1 class: adopt the ≥0.90-dominant ref prefix over a 1-confusable head, PAGE WITNESS
  REQUIRED (W1 corrected_to byte-equal / W2 keyword candidate), both-forms refusal (the live
  Pelican 3× confirmed P1* rows stand it down — remedy Learning Repair), suffix untouched, no
  confidence change. Explicit pinned class table (L in the 1-class; B↔E excluded; separators
  excluded).
- **`9965003` raw-witness vacuous flag** (`raw_witness_vacuous_suppress`, OFF) — the
  "compare-a-value-with-itself" question: `_witness_note` suppresses the vacuous pair when armed,
  reworded copy keeps the load-bearing "one character differs" marker verbatim; renderer
  isApplied/badge coherence.
- **`da81981` migration 71** — fresh-install `auto_file_threshold=90` seeded ONLY when documents
  count==0 (owner's live DB untouched — Oracle predicate); the two switches seeded OFF.
- **`4a89b30` bin-changed broadcast** — purge/'/v1' previously broadcast NOTHING; notifyAllWindows
  pull-model signal + Search debounce; bin self-repaints.
- **`96d0875`** — the "B-arm inert on Reprocess-All" claim retired as a code-vintage NON-BUG
  (batch predated commit `7141316` by 3 h; `mergeReprocessRows` innocent).

### Round-2 fixes (commits `04c110f`…`119d7f7`) — validated by Chris round 8
- **`04c110f` round-7 small cards** — approved-vs-automatic banner split (`recent_auto_filed`
  carries an `approved` subset), "differs from the value shown here" note copy, "Teaching saved."
  done-screen; round-7 report archived.
- **`119d7f7` the round-2 extraction arc** (all OFF, migration 72):
  - `filing_sanity_page_match_v2` — Gate C's false "'X' doesn't appear on this page as written":
    the PAGE PASS itself misreads confusables (`P1/26/9910`, `VX$22033`) or splits tokens
    (`SB-ORD7 4238`). v2 = same-LINE sepless joins + prefix-region-only backed-confusable
    tolerance (suffix byte-equal PINNED; Oracle A1: the compare projection KEEPS class symbols).
  - `vat_reg_symbol_confusable` — the `$540` phantom VAT: the letterhead reg's own misread `5`→`$`
    WAS the money witness, disarming the vat-reg guard; armed, a `$`-only all-mid-run witness
    folds to `5` and the existing leg ladder judges (`+symfold`); everything else stays a hard veto.
  - `money_sign_capture` — `£-428.58` minted unsigned (no currency pattern admits leading `-`) →
    false "disagrees" on a CORRECT credit total; armed, a minus hard against the digits is kept at
    `_clean_value`; `parse_amount` stays magnitude-only BY DESIGN (pinned).
- **`47b247d` delete-surface disambiguation** (round-8 card 1) — resolved at source as NOT a
  stale-selection bug (toolbar name + currentDoc written from the same object synchronously);
  the driver had hit a ROW-× sharing the action-bar's title. Row ×s retitled "Delete this row's
  document"; row dialogs carry a mismatch clause naming BOTH docs when row ≠ open doc.
- **`2408145`** round-8 report archived with triage.

## CHRIS VALIDATION (both reports verbatim in `docs/CHRIS_FULL_APP_REVIEW_2026-08-16.md`)
- **Round 7** (fresh install, mig-71 defaults): sender notes 200/200→**0/400**, File-All 0→**184**,
  wave-2 auto-file ~55%→**94%**, all filed Pelicans PI-correct, **zero wrong value/folder** (29
  eyeballed).
- **Round 8** (round-2 arc, five switches ON between waves): wave-2 **190/200 (95%)**, false
  not-on-page 7→**3** (all three explained: one wave-1 leftover needing a reprocess; one
  kept_existing merge carry-over after an empty re-read; one SUFFIX-region page garble = the
  Oracle-pinned designed residual), **Meadowvale credit note A/B-PROVEN fixed by Chris himself**
  (OFF resurrects $540/unsigned/false-note on the same file; ON → -85.72/-428.58/no note, filed
  at 100), zero wrong value/folder, 0 new empty-field class, per-switch fired-path evidence
  recorded (S1–S5 all FIRED; S3 partial with the three explained).

## OWNER-VET QUEUE (nothing implemented)
1. Round-8 card 3: 4 silent empty-required-field holds — wants a one-line on-screen reason
   ("the reading spot for this field found nothing on this page"). Display-only; do NOT mint a
   validation_note (would block auto-file).
2. Round-8 card 4: Home screen "while you were away" line for the startup catch-up sweep
   (the split banner lives only in Review).
3. Round-8 card 5: the twin switch names ("Fix an obvious one-character misread…" vs "Fix a
   one-character misread at the start…") — rename the new one to lead with its distinctive part.
4. Round-7 leftovers: the 2/400 Nordwind unrecognisable-quote residue; the Processed-folder
   asymmetry question (auto-filed originals stay in Processed; manually-filed ones move).

## OWED BEFORE ANY LIVE DEFAULT-ON FLIP (the five new switches)
`ref_prefix_confusable_adopt` · `raw_witness_vacuous_suppress` · `filing_sanity_page_match_v2` ·
`vat_reg_symbol_confusable` · `money_sign_capture` — all seeded 'false' (migs 71/72; pinned that
a full migration run leaves them false). Owner's standing gate: the **OFF==ON md5 corpus arm**
(`RR_APP_ENV=1 OCR_RENDER_DPI=200 realdoc_regression.js`) + **Oracle ratify**. Round-8 sandbox
proof exists WITH fired-path evidence — the corpus arm is the remaining leg.

## STATE / GOTCHAS
- Suite: 484→489 files, baseline 11 documented pre-existing reds, ZERO new (three whole-suite
  runs). ~100 new pins across the two arcs.
- Round-8 sandbox lives at the session scratchpad `chris-sandbox` (CDP 9223); owner's live app +
  DB untouched; live backup `TESTING/_measure/live_backup_20260815_night_start.db`; git tag
  `snapshot-20260815-night-start` pushed.
- An in-session uptime probe (5-min, both hosts) logs to the scratchpad `uptime_probe.csv` and
  dies with the session — UptimeRobot is the durable monitor.
- Trap for the next session: `test_template_code_edge_clean.py` slices template_mapper source at
  the FIRST def — a new module-level def above the flag block breaks it (put helpers below).
- Memory: `memory/project_overnight_fix_loop_20260816.md` carries the root-cause index.

---

# PART 2 — the working session (2026-08-18 afternoon/evening, owner present)

Commits `47b247d`…`4bd4a2c`, all PUSHED. Owner drove a fresh sandboxed install
(`Desktop\ScanFinder Fresh Test`, its own userData; live app untouched) and reported defects live.

## Shipped
- **`06171af` the issuer near-match hold was a DEAD END** — the confirm-review IPC hand-back
  whitelisted `code`/`confirmedBy`/`prefixOutlier` and DROPPED `nearMatch`, so the renderer's
  inline note (carrying the only two escapes — "Use <known>" / "Keep <typed>") never rendered and
  a toast printed its own placeholder as if it were the company name. A correct document could not
  be filed by any route. Also: the free-text type-ahead ATE TYPED CHARACTERS (datalist re-attached
  every keystroke + `closeSuggest` blurring the field being typed in) — long mis-filed as the
  2026-07-11 "third focus failure mode" OS desync. It is neither OS nor unexplained.
- **`3c92b1b` the tour stopped teaching the reprocess loop** (it literally sold "Reprocess all — it
  gets smarter" as the workflow) + corrected trust.js's stale "graduation lowers to 98" comment
  (TRUSTED_FLOOR is 95).
- **`47b247d` delete-surface disambiguation** — row ×s shared the action-bar's title and identical
  dialog copy; row dialogs now name BOTH docs when the row ≠ the open doc.
- **`9be2b40` the cold-start countdown** — `unverifiable-value` now says the real reason and what
  clears it, with `learning.FORMAT_SOLID_MIN` named + exported so the copy can't drift from the rule.
- **`47cfddf` the invisible flag** — a stale layout note was hidden by the renderer, still counted
  by the summary, and still holding the doc in the gate. Reported as its own class with the
  one-document re-read that clears it (never Reprocess All).
- **`4bd4a2c` the per-sender finish line** (owner's design) — group headers read "✓ files by itself"
  or "1 more to file by itself", refreshed after every confirm. Derived from the learned FORMAT
  group, NOT a confirmed-document count: the naive version LIED on the owner's install.
- **`5f23c34`** the taught-autofile counterfactual harness (`stress_test/`).

## THE VALIDATED MODEL (full detail: `memory/project_first_batch_filing_model_20260818.md`)
Why a first batch doesn't auto-file: a scope needs **3 documents CARRYING A VALUE** before its
learned format leaves the provisional channel the gate cannot see; a per-doc-unique REFERENCE
classifies as `'constant'` until 3 DISTINCT values exist; and **a taught document contributes
NOTHING** (confirm only UPDATEs/DELETEs rows, never INSERTs, and `getFieldFormats` reads
`extractions` — 9/10 taught docs had no issuer row, several had zero rows).
**PROVED by prediction**: one more Meadowvale confirm would drop both walls at once — the owner
confirmed one and **all 17 released**, queue 53 → 2, Meadowvale gaining solid groups on all four
fields. No page was re-read to make that decision; only a reprocess happened to re-ASK.

## OPEN (nothing built)
1. **Confirm-persists-values** (the teach counting) — gary designing, Oracle to vet. Owner's
   instruction: implement IF recommended, else report back.
2. **Queue-wide Tier-1 free re-check** — Oracle SIGN-OFF-W/COND, blocking C7 (the accept path
   silently falls through to Tier 2) and C8 (accept takes an unvetted docId list; must be a
   server-remembered offer), plus C9-C11.
3. **The `constant`-on-reference asymmetry** — the at100 arm deliberately refuses to trust a
   `constant` verdict; the sub-100 arm enforces it, on the fields where it is most often wrong.
4. Chris round-8 leftovers: silent empty-field holds, the Home "while you were away" line, the twin
   switch names, the 2/400 unrecognised-quote residue.
5. Still owed before any of the five 2026-08-16 switches go default-ON: the OFF==ON corpus arm.
