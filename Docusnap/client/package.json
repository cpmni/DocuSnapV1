{
  "name": "scanfinder-client",
  "version": "1.0.0",
  "private": true,
  "description": "Detached read-only search client for ScanFinder (talks to the core app's /v1 API).",
  "main": "main.js",
  "scripts": {
    "start": "electron ."
  }
}
