# HANDOVER — 2026-08-22 (the overnight "teach 1 → import N → it files itself" arc)

> Resume here. Previous: `HANDOVER_2026-08-20_EVENING.md` (Chris round 10 + the class fix), then the
> 08-21 daytime session (prefill / post-teach card / ripple fix / mig 76–78; Chris round 12 =
> `docs/CHRIS_FULL_APP_REVIEW_2026-08-21.md`). The owner went to bed at ~23:00 on 08-21 with:
> *"continue on auto with the recommended settings and complete all of the slices. When done, brief
> Chris … have him vet the system again under the same sandbox conditions … If Chris comes back with
> any issues, you are allowed 1 free run with the agents to address those issues and have Chris run
> his tests again and report the progress."*

## TL;DR
- **The Oracle verdict landed** (third attempt; two lost to an API error + an accidental session close).
  Per-slice: Slice 2 recompute SIGN-OFF-W/COND build-first · Slice 1 auto-accept SIGN-OFF-W/COND
  **scope-local** · Slice 0 fragment abstain SIGN-OFF-W/COND · Slice 3 quiet lane design signed, build
  gated on a number. Full text + gate results: `docs/oracle_log.md` (2026-08-21 entries).
- **Slice 2 is DEAD by its own gate**: measured on two sandboxes with the real predicate
  (`TESTING/_measure/s2_histogram.js`, `s2_ceiling.js`): 0/22 and 0/378 lifted, ceiling 0.5%. Taught
  siblings already read base ≥95; what holds them is `unverifiable-value` until 3 confirms (the
  08-18 wall) — and 354/378 of the night pile were docs read BEFORE their sender was taught: a
  RE-READ class, not a recompute class.
- **Built, all DARK, mig 79 seeds every switch OFF** — flipping any one back to `false` is the revert:
  - `72f9c7f` **Slice 1** `scope_sweep_auto_accept` — after a HUMAN confirm on (sender, type) the server
    files THAT sender's ready documents itself (the existing sweep offer, auto-accepted, scope-local);
    receipt bar "N from X filed by itself after your confirms · Put back". Preconditions re-checked at
    every pass: `scope_sweep_enabled` + `learning_exclude_machine_confirms` + `autofile_gate_unify`.
  - `fbae2e9` **Slice 0** `letterhead_fragment_abstain` — the geometry letterhead pick abstains on a
    lone word beside a letterhead-sized name-shaped segment ("Cleaning"/"Security"). Census over the
    200 IMPORT fixtures (real OCR): 34 abstained, every one a wrong fragment, 0 correct lost, 0 new wrong.
  - `8de69d5` teach DONE card: real count + "Check them in Review" always when siblings are queued;
    promise wording follows the install (Chris r12 #3/#4).
  - `7819858` **Slice 3** `quiet_reread_enabled` — `src/modules/processing/quietLane.js`: after a TAUGHT
    confirm, the sender's held template-less docs (by name ∪ by page text via `supplierSiblings` seeded
    from the taught doc) are re-read on a below-normal background lane, invisible to
    `_anyProcessingBusy`, killed at every foreground door, merge-gated (`applyReprocessResult(opts.expect)`
    inside one transaction), changed reads held "was X, now Y", filing only via the sweep. Plus Chris
    r12 #1 (typed-issuer ripple doorway) and #5 (honest "File N ready" dialog).
- **Suites:** JS 181 green / 1 pre-existing red (`test_authoritative_anchor`); Python 307 pytest green /
  1 pre-existing (`test_identity_fusion`) + 6 pre-existing script-style reds — all verified red at
  `6c24ab6` in a worktree. New pins: `test_scope_auto_accept.js` (31), `test_quiet_lane.js` (50),
  `test_letterhead_fragment_abstain.py` (15), teach_followup F/G/H, ripple/teach_speaks contract pins.
- **Chris round 13** — see below (sandbox: fresh install, CDP 9223, all four arc switches + preconditions
  ON via `TESTING/_measure/flip_arc.js`; `Demo Docs` copied into the sandbox).

## Chris round 13 (verbatim: `docs/CHRIS_FULL_APP_REVIEW_2026-08-22.md`, commit `fac4446`)
**Verdict: yes, narrowly.** 43 filed, zero wrong folders, undo always worked. Quiet re-read FIXED (line at
~11 s, 19 docs in ~45-70 s, Review usable, foreground Reprocess paused/resumed it, honest "was X, now Y"),
DONE card FIXED, File-All count FIXED, "Cleaning"/"Security" gone on arrival, Put back works. **BUT the
promise "confirm 2 more and the rest file themselves" came true for 1 sender in 4** while the badge said
"✓ files by itself". Measured at source (`TESTING/_measure/r13_probe.js`, `r13_veltrix.js`):
- **Harrowgate** (4 confirms, never taught): 13 docs at 95, no notes → `no-template` (a sub-100 doc needs a
  TEMPLATE; the graduation template mints at the 5th confirm and nothing re-read the siblings after it);
  the badge used `ready = graduated || hasFormat` and ignored the template.
- **Veltrix** (12 confirms + template): 7 held `unverifiable-value` because ONE confirmed value was
  `VX$22033` — `classifyLearnedShape` is all-or-nothing and the dominant-class rescue applied only to
  NON-role fields. One hurried confirm bricked the sender for good.
- Copperfield: genuine empty required field (skewed scans). Pelican: recent-correction + the I/1 note.
- Other cards: Enter didn't offer the ripple (only click-away); Review kept showing a wizard-filed doc
  with a live Confirm; the first-teach dialog spoke of "Template Manager"; the Reprocess warning never
  says "doesn't file"; "lower the bar" hint on a doc with an empty required field; one Pelican P1
  self-filed unflagged (card 2, I/1 class — NOT fixed); "ITH-0093"/"Dairy Wholesale" as senders (card 8
  — NOT fixed).

## The one fix-run (commit `bff8f5b`, Oracle per-fix SIGN-OFF-W/COND in `docs/oracle_log.md`)
- **F1 `role_field_dominant_class`** (DARK, env `ROLE_FIELD_DOMINANT_CLASS`, seeded OFF in mig 79): ONE
  helper `_effectiveClass` at all three sites (scopeTrust loop, its corroboration probe, docTrustGate's
  role branch) — a role field whose strict class collapsed to freetext under one outlier is VERIFIED
  against the dominant class (≥5 distinct, ≥75%); the outlier shape stays refused; dangling-role docs keep
  strict refusal. The 07-20 "do not widen graduation" comment rewritten to record the paired rule.
  Pins `database/modules/test_role_dominant_class.js` (20).
- **F2a**: a graduation MINT (create/link) schedules the quiet lane for the scope (after enrichment).
- **F2b**: with auto-accept ON, `consume-reprocess-completion` runs the scope-local pass BEFORE building
  the consent offer (offer = remainder) — one door, never "File 14" → filed 0.
- **F3**: `ready = (graduated || hasFormat) && hasTemplate`; "learned · needs a layout" badge state.
- UX: Enter settles the issuer box (ripple doorway); Review moves on when the open doc was filed
  elsewhere; teach dialog only for a sender with a layout, plain words; Reprocess warning says it doesn't
  file; no "lower the bar" hint when a required field is empty.
- **Recorded, NOT fixed (Oracle C2a.3):** a 5th confirm delivered by File All Ready graduates the scope
  but `onScopeGraduated` runs only for non-bulk confirms — no template, no lane; the badge now stays
  honest ("needs a layout") but the sender is stuck until a single confirm or a teach. NEXT: fire
  `onScopeGraduated` once per human scope at the end of a File-All batch (never for machine vias).
- Realdoc A/B for Slice 0 (S0-C2): OFF == ON, M delta 0, supplier 100% both arms.

## Chris round 13b (the re-run on the fix build — verbatim in `docs/CHRIS_FULL_APP_REVIEW_2026-08-22.md`)
**Verdict: "Yes, more firmly than round 13"** — 44 filed this sitting (87 total), zero wrong folders,
zero wrong numbers/dates (he page-checked every new file), undo worked every time. Re-verify: (1)
Veltrix unbricked — ONE confirm → 5 filed by itself in 2 s: **FIXED**; (2) Harrowgate 5th single confirm
→ badge honest before/after, layout at 1 s, quiet line at 10 s, **14 filed by itself at 14 s**, all
correct: **FIXED**; (3) Reprocess-as-single-door: Harrowgate/Nordwind exactly as promised, BETTER-BUT
(stale bars, put-back reprocess silent); (4) Enter settles the issuer: **FIXED**; (5) Review moves on after
the wizard files: **FIXED**; (6) copy: fixed except the "You've taught X before…" dialog NOT-SEEN (a
re-teach through the main-window wizard raised no dialog — that path isn't the Review CTA).
**Owner's question:** "two of the three paths are now genuinely hands-off … the first rounds where I
pressed nothing I'd have to remember." Still to press: Reprocess-sender after the confirm that makes a
TAUGHT sender ready (card 1), Reprocess after a re-teach (card 4), the on-screen doc never files itself,
stale offer bars, a put-back sender wants a Confirm not a Reprocess.

### 13b cards → the vet queue (NOTHING implemented — the free fix-run is spent)
1. **Taught sender turns "✓ files by itself" at its 3rd confirm but its waiting docs keep a stale hold**
   (`unverifiable-value:invoice_date` on a new custom type "Quotation"; "Reprocess 17" then filed 16).
   Hypotheses to probe on `TESTING/_measure/sandbox_r13b_end.db` (the end-state snapshot): the badge's
   `hasFormat` is supplier-only while the gate needs the DATE group solid (3 distinct — the teach's minted
   date + 2 confirms may have shared a value), or the lane's pre-confirm rows carry a date SHAPE the learned
   group rejects until a re-read. The Harrowgate path works because graduation fires the lane (F2a);
   a taught sender crossing the 3-confirm line fires nothing. Candidate fix: fire the lane (or a stored-row
   sweep) on the confirm that makes a scope READY, not only on teach/graduation.
2. **Stale consent bars** — the renderer's queue-wide sweep bar ("File up to 14") survives the auto-accept
   that just filed those docs; pressing it files 0 with "Put back in Review" under "Filed 0"; bars stack.
   Fix: on `scope-auto-filed` drop `_sweepState` candidates no longer queued / retire the bar; one bar
   per sender; never a Put-back link under a zero-file result.
3. **"Reprocess N from sender" on PUT-BACK docs: "Completed 0 of 5", silence** — likely
   `_stageReprocessDocs` skips them (working copy consumed on filing; `stored_path` not used as the
   staging source) and reports nothing. Then File All counts them "flagged". Confirming one re-filed the
   rest. Fix: stage from `stored_path` when `working_path` is gone, or say why.
4. **A RE-TEACH of a known sender regressed its siblings** (wizard located the footer "Please quote" line,
   label = a sentence fragment; siblings went 84–93% → "Not found… doesn't match the expected format"
   56–70%) and the lane did NOT re-read them (candidates require `template_id IS NULL`; a re-taught
   sender's docs already carry the template). Fix: for reason 'teach' include docs carrying the re-taught
   template; warn when the located label is a sentence fragment; the "You've taught X before" dialog
   belongs on the wizard path too.
5. Cosmetics: creating a type from Review wiped the read date + left the "no type yet" note; `VX$12345`
   typed into a number box shows "High · 95%"; group headers lose badge/countdown after a re-read/put-back;
   "Reprocess 7" with 2 left; a 29-receipt put back 25; "now that you've taught its layout" on a graduation
   (he confirmed five, taught nothing); Veltrix 0013 skipped by the pass with no visible reason (unsolved —
   records in the snapshot).

## Owner decisions queued
1. **DECIDED 08-22 morning: migration 80 UPSERT-forces the five arc switches ON** (`scope_sweep_enabled`,
   `scope_sweep_auto_accept`, `letterhead_fragment_abstain`, `quiet_reread_enabled`,
   `role_field_dominant_class`) — so a fresh DB and the next `npm start` run the round-13b configuration
   (verified with `TESTING/_measure/fresh_check.js`). Revert = the dev-gated toggle or the settings row.
   Note it also flips them on the LIVE DB at its next start (the mig-76 stance).
   **Also migration 81** (owner: "should these not be on?"): the five round-7 switches + the paired
   `net_misread_total_flag` default ON after their owed realdoc OFF==ON arm ran green (identical tables).
   `TESTING/_measure/fresh_vs_live.js` shows a fresh DB now equals the live DB on every reading switch.
   Dev DB path is `%APPDATA%\ScanFinder\docusnap.db` (`ScanFinder1` = the owner's parked 2,656-doc DB).
2. Slice 2 stays dead unless a corpus shows a base-in-[81,89] population (the scripts are in
   `TESTING/_measure/` to re-measure any DB).
3. Whether "empty → typed issuer" should count as a `recent-correction` for graduation at all (the
   Oracle named it, not bundled — affects every cold-start scope).

## Owner's own fresh-DB run (Document Solutions worksheets, 08-22 late morning) — VET QUEUE
22 real scans, one type, teach 1 → lane re-read 13 → confirm 3 → nothing filed → two Reprocess presses filed
8; 9 held. Probed (`TESTING/_measure/owner_probe.js`, `owner_hist.js`, DB parked as
`%APPDATA%\ScanFinder_docsol_run1_*`): every held doc already matched template 1 with the CORRECT issuer;
the hold is the `identity_variant_adopt` note ("looks like a clipped fragment … please confirm", conf ≤70,
−12) because the STACKED wordmark OCRs as "DOCUMENT" / "SOLUTIONS" on two lines, so template_fixed can't
claim it on-page; the note survives 3 human confirms AND re-reads (regenerated from the pixels).
First pass reproduced cold (`s0_probe.py` on the inbox PDFs): "Patrick" (an address tail) prefilled as the
issuer on doc 1; blanks/garbage on others — the vertical-stack class is outside Slice 0 (same-row only).
**Owner's suggestions vetted:** (A) a "held until more confirmed" section — already what the Check group
is after the lane; the missing piece is the RELEASE. (B) re-check logo hashes after the 2nd/3rd confirm —
the logo already matched; the release is: shed the variant-adopt note when the adopted name equals the
matched template's fixed identity AND the scope has ≥3 HUMAN confirms of it (conf 85, method
`identity_variant_adopt+confirmed`), and fire the quiet lane on the confirm that makes a scope READY (the
3rd), not only teach/graduation (= Chris 13b card 1). Plus two letterhead guards: vertical-stack abstain
(single-word pick with a single-word letterhead-sized name line above/below) and no single-word candidate
directly under an address-ish line. Each a dark switch, Oracle-vetted first. NOT built (no go yet).
`npm start` now runs `scripts/dev-start.js` which sets `DOCUSNAP_DIAGNOSTIC_LOG=on` (dev only; packaged
builds read the setting, default OFF); the log is `<repo>\Debug\diagnostic_<stamp>.jsonl`.

## The two-line wordmark slice (afternoon 08-22, commit `2814925`, mig 82 ON) — built on the owner's go
gary design → Oracle per-part verdicts + a re-rule on three measurements (`docs/oracle_log.md` 2026-08-22):
- **Root cause found by tracing a held doc** (`TESTING/_measure/trace_reprocess.js` on the parked run-1
  copy): `Template matched … (60% via keywords+slug_rescue)` → `Stage 0: 1/3 fields from template` →
  `WARNING: supplier identity changed during extraction — pipeline='DOCUMENT SOLUTIONS' field='DOCUMENT'`.
  The owner's TAUGHT ISSUER BOX reads ONE line of the stacked logotype and that Stage-0.5 read displaced
  the `template_fixed` seed; identity_fusion repaired it with the un-sheddable note. Neither existing keep
  (exact-agreement, region-presence) fires on a partial read.
- **P4 `template_fixed_seed_fragment_keep`** (engine.py `_fragment_agreement_keeps_seed`, branch
  `fragment_agreement`): the read is a whole-token contiguous sub-run of the fixed value AND the issuer band
  prints the fixed value as a contiguous run of issuer-column lines → keep the seed (same method/conf, no
  note). Fired-path: held docs → `template_fixed`@95, overall 100, eligible. Pins
  `test_fixed_seed_fragment_keep.py`.
- **P1 `letterhead_stack_abstain` + `letterhead_depth_guard`** (letterhead.py): the Oracle's mechanism fix
  (a bare GENERIC company word on its own letterhead-sized line is the stack signature — the name-shaped
  gate alone rejects "DOCUMENT" via GENERIC_SINGLES); depth = the text arm's `_MAX_BAND_INDEX` on
  single-token geometry winners, BAND frame. Census: 0 correct lost, 0 new wrong, 2 wrong→None
  (`ITH-0093`), 3 None→correct. Pins `test_letterhead_stack_abstain.py`.
- **P2**: `database/modules/scopeReadiness.js` = THE readiness predicate (badge + lane consume it);
  `quiet_reread_kw_select` (keyword-fingerprint selection at the exported `KEYWORD_THRESHOLD` — the hash
  arm measured DEAD: same-sender phash distance 14–28, 0 of the 8 missed docs, 4% selectivity) and
  `quiet_reread_on_ready` (the crossing fires one lane job; `readyProbe` memoised 10 s). Pins in
  `test_quiet_lane.js`.
- **P3 (note release) DEFERRED, (d) dropped, hash DO NOTHING** — as ruled; the design stays in the log.
- Residual (noted): doc 10's cold read is the two-token garble `ROCUMENT OLUTIONS` — held for review by
  design, outside every rule; a typed correction fixes it.
- Chris round 14 launched on a fresh sandbox (mig 82, the owner's 22 Doc sol scans + Demo Docs) — report
  appended to `docs/CHRIS_FULL_APP_REVIEW_2026-08-22.md` when it lands.

## Chris round 14 (verbatim in `docs/CHRIS_FULL_APP_REVIEW_2026-08-22.md`) — fresh install, mig 82, the owner's 22 real scans
**Verdict: Yes** — 27 filed (17 DOCUMENT SOLUTIONS real scans incl. five bad ones + 10 Pelican), zero wrong
values/folders by the app's hand; "the real-scans happy path happened once, and it was the best minute I've
had with this app": 3rd Confirm → quiet line at 9 s → 17 swept (Print-Tracker left out) → 12 filed by
themselves at 80 s, duplicates honestly suffixed, the app's dates right where the owner's filenames were wrong.
Re-verify: (1) cold pass BETTER-BUT (no "Patrick"/"TIONS"; still "DOCUMENT OLUTIONS" + "Ticket Type" prefilled
on 2 docs); (2) no "clipped fragment" note anywhere — FIXED as far as driven (his issuer box landed on the
FOOTER, so the one-line-of-the-logo path was only partly exercised); (3) the 3rd-confirm re-read reached all
17 — but the TEACH-time re-read selected 0; (4) ready-trigger FIXED for DS, nothing for Pelican; (5) FIXED.
**Diagnosed from the sandbox audit (`r14_copy.db`):**
- DS teach job (12:45:53) ran with `done_ids: ""`: the template's fingerprint existed (12:45:48) but Chris
  taught from the WORST scan (head `Lol O02) | DOCUMENT OLUTIONS`), so neither the text-similarity arm nor
  the keyword arm (both keyed on the taught doc's text) matched any sibling; the 17 at the 'ready' job came
  by NAME after his typed-name ripple. Direction (own Oracle decision, not built): when a teach-time job
  selects 0 on a fresh scope, widen to the import batch's untyped/unnamed held docs (bounded, chunked,
  killable — "re-read every template-less held doc" = the reframe the Oracle asked to see as its own
  decision); and/or a teach-wizard hint "teach from your clearest copy".
- Pelican 'ready' job fired correctly (13:23:27) with NO candidates — all 19 siblings already carried the
  template; they were held by the drawn invoice-number box reading nothing/84%. The ⊕ teach on a sibling
  (13:21) added an anchor; the manual "Reprocess 17" (13:34) applied it and 7 filed. Direction: a ⊕/anchor
  teach on a scope should re-read the scope's TEMPLATE-CARRYING held siblings (what the manual Reprocess
  does) — crosses the 08-21 "template-less only" boundary → Oracle vet first (S3-C5 note-loss seam).
- **Card 1 (safety, top of the queue): filed → Put back → Delete → Empty bin destroys the ONLY copy** (filing
  MOVES the original out of `Processed`; the wizard promises originals are never deleted). Decide: copy on
  filing (disk ×2) or reword the wizard + refuse to purge an only-copy.
- Card 5: the detected-type nudge offered the ISSUER read ("Document Olutions") as a document TYPE; card 4:
  Home's "N ready to file" uses a different predicate from File All Ready; card 6: stale offer bars (13b card 2
  again); card 7: a sent-back document still counts a confirm toward the buyer's graduation; card 8 cosmetics.
- Audit metadata shows `"passes":"[redacted]"` — the audit redactor masks the numeric field name; cosmetic.

## The round-14 vet queue — BUILT (evening 08-22; gary design → Oracle per-item verdicts in `docs/oracle_log.md` "Chris round 14 vet queue" + "Q2 RE-RULE")
The owner said *"go ahead and build it once the oracle signs off"* then *"when finished this, get chris to do a sandbox test"*. Order followed the Oracle's: Q1 → Q4b → Q4c+Q4d → Q4a → Q2 measurement (→ the fingerprint slice) → Q3 (own branch, own Chris round).
- **`00e0cc3` Q1 — KEEP THE PROCESSED ORIGINAL (mig 83, `keep_processed_originals` UPSERT ON for every install).** Root cause of card 1: import drains the original into `<source>/Processed`; the confirm-time `removeSourceFile` (which predates the drain) then unlinked THAT file, so the Output copy was the only copy. Now: `documents.drained_at` (stamped by both drain paths + a one-time backfill where `folder_path` is a Processed dir / the `processed_folder` setting); THE ONE gate in `reviewService.confirm` — OFF = today's removal, ON + drained → untouched, ON + not drained → drained NOW (never unlinked); env `KEEP_PROCESSED_ORIGINALS=0/1`. Seam C1.6: `process-folder` refuses a folder that is the `folder_path` of ≥1 CONFIRMED doc (path-normalised) with an "Import it anyway?" override (env `IMPORT_FILED_FOLDER_GUARD=0`). Copy fixed in four places (wizard :151/:153, help/settings, Settings "Processed Scans" — blank never kept originals in place); Files & filing toggle "Keep original scans after filing" (disk cost stated); the three purge dialogs append "Your original scans in the Processed folder are not touched." when ON. Pins `test_keep_processed_originals.js` (40). **Run-1 "lost scans" reconciled: nothing lost** — all 11 filed PDFs are in `Documents\Scan Finder\Document-Solutions\2026\…` (birth 12:08–12:12; Windows CopyFile keeps the June mtime so `find -newer` misses them), 11 held originals in `Processed\Processed`.
- **`98ef004` Q4b/Q4c/Q4d.** `src/windows/shared/reviewReadiness.js` = THE classifier (flagged › noType › missing › ready, acknowledged flags exempt) behind BOTH `documents.getReviewSplit` (Home) and File All's partition — the "20 ready to file" over untyped docs is gone by construction. Home headline "N senders file by themselves" = `scopeReadiness.isReady` over confirmed scopes (one `getFieldFormats`, memo 10 s), graduation as the sub-line. `shared/offerPrune.js`: offer bars pruned to the live queue on every broadcast refresh, retired when empty; a "nothing"/"auto-accept-running" sweep answer retires a stale OFFER bar; reprocess-offer bar hides at 0; "Reprocess N from X" follows the live count; no "Put back" under "Filed 0"; the post-lane bar says "were re-read just now". Q4d: the "4 more" badge after a send-back was a STALE RENDER (every counter is `status='confirmed'`-gated; verified on `r14_copy.db`) — `_refreshQueueFromBroadcast` now refreshes readiness first. Pins `test_review_readiness.js` (27), `test_offer_prune.js` (19).
- **`07a6632` Q4a — the type nudge.** `_harvest_top_band_heading` skipped line 0 by POSITION; on 17/22 DS scans the title "SERVICE WORKSHEET" IS line 0 (set above the wordmark). Two arms: `TYPE_NUDGE_ISSUER_EXCLUDE` (default ON) — a candidate whose tokens are a subset/superset of the issuer READ (garble-tolerant ≥5-char tokens, ratio ≥0.8) or all-generic is skipped, scan continues (the raw band lines are NOT an exclude set — Oracle C4a.2 dead-guard class, pinned); `TYPE_NUDGE_L0` (default ON after its census; `=0` reverts) — line 0 admissible only with a non-empty issuer read that line 0 is not. CENSUS (`q4a_census.py`, 221 docs, no installed types): OFF 48 offered (3 correct/45 wrong) → subtractive 8 (3/5: 0 correct lost, 40 wrong removed) → +L0 23 (19/4: +16 correct, 0 new wrong). The Review card now follows a type choice. Pins `tests/test_type_heading_nudge.py`, `test_review_untyped_reason.js`.
- **`0d879f8` Q2 — ROOT-CAUSED + the fingerprint slice (DARK).** MEASURED on `r14_copy.db`: the taught doc's own fingerprint (the seed the promote froze) = `["SERVICE","WORKSHEET","Lol","DOCUMENT","OLUTIONS","ILa","Ticket","Location","Work","Address"]` — three OCR-garble tokens on NO other document → every sibling scored EXACTLY 0.70 < 0.75 → teach job `done=0`; the 5-token intersection after three confirms → 21/22. Oracle: the folder-key widening SENT BACK (WRONG LAYER); SUPPORT prune at BIRTH signed. `templates.pruneSeedFingerprint` drops df=0 tokens, guarded by G1 issuer-protect, G2 reward licence (≥2 recovered held same-type-or-untyped docs, no name-disjoint non-prefill claim, AND each recovered doc carries ≥0.6 of the pruned tokens in its OWN top-band fingerprint — the same-layout leg the final census proved necessary: without it three BUYER-issued seeds were licensed by other suppliers' cold docs, cross hits 20→179 each), FLOOR, all-or-nothing half-cap; both birth paths (`_upsertTemplate` CREATE, `graduationTemplate.apply`). CENSUS (`q2_seed_prune_final_census.js`, 220 docs cold-reset): OFF recall 98.9% / cross 6.98% → ON recall 100% / cross 6.98% unchanged; 6 pruned (the DS garble seeds), 78 unlicensed (incl. the buyer-issued class). Switch `fingerprint_seed_support_prune` / env `FINGERPRINT_SEED_SUPPORT`, DEFAULT OFF — **armed ON in the Chris-15 sandbox**; flip for NEW installs (INSERT OR IGNORE) after Chris 15 (Oracle C9). Pins `test_seed_support_prune.js` (i–ix + the buyer-issued refusal).
- **`4be9330` (MERGED `92b60b7`; built in worktree `C:\GIT Projects\Docusnap-q3`, branch `feat/q3-layout-reread`) — Q3 the LAYOUT arm (DARK).** An authoritative anchor write / template mapping write that CHANGED something schedules the lane with reason 'layout': held docs carrying one of the scope's OWNED templates AND the scope's name, minus S3-C5-noted docs; preconditions `quiet_reread_on_layout` + `template_identity_on_page` + a judgeable scope name (≥2 name-arm tokens; the JS mirror is pinned equal to `template_matcher._GENERIC_NAME_TOKENS`) — each skip audited; a REQUIRED role field first-filled is held "Read from your new box — confirm once." unless page-corroborated; valued→empty merges as empty (old value never restored); reasons union on a running job. SEAM-1 engine comment rewritten. Pins `test_quiet_lane_layout.js` (38). Merged after Chris 15; its OWN Chris round (Oracle C3.7) with `quiet_reread_on_layout` + `template_identity_on_page` ON and a planted drifted-box sibling (the wrong-column first-fill must be HELD).
- **Suites at `0d879f8`:** JS 230 green / 4 red — `test_authoritative_anchor` (documented), `test_v1_contract` (3 BAD) + `test_doctype_surface_parity` (1) **verified red at `2d25e87` BEFORE this session's edits** (not ours; add to the documented list or fix), `test_ref_class_fix` transient under load. Python 307 + the documented 1 + 6 pre-existing script reds, zero new. Run the script-style Python tests with `PYTHONIOENCODING=utf-8` or 52 of them "fail" on a cp1252 `→` (the trap that made the first suite run look red).
- **Deferred / not built:** Q2's folder-key widening (refused); the "teach from your clearest copy" wizard hint (UX half — small, unbuilt); the dashboard split's acknowledged-flag divergence is now folded in (C4b.1).

## Chris round 15 (verbatim in `docs/CHRIS_FULL_APP_REVIEW_2026-08-22.md`) — fresh install, the round-14 queue built, Q2 armed
**Verdict: Yes** — 57 filed (35 DS incl. 16 honest duplicates, 18 Saltmarsh, 4 Ridgeway), zero wrong folders/values; *"for the first time nothing went missing and nothing needed a trick."* Round-14 cards: **1 FIXED** (original survives Put back → Delete → Empty bin; the bin dialog says so; the re-import refusal counted 18 exactly and "Import anyway" even restored the destroyed file), **2 FIXED** (the teach-time re-read did 19/19 at 12 s — audit `fingerprint_seed_pruned kept 7 recovered 20` → `quiet_reprocess_job teach done=19`; 18 right values, the app right where the owner's filenames were wrong; drawn-box control on Ridgeway 19/19), **4 FIXED** (Home == File All at six moments), **6 FIXED**, **7 FIXED**, 5 FIXED for the taught scan, 3 BETTER-BUT.
**New cards (owner vet queue):**
1. **"✓ files by itself" over an already-read pile that won't move** (DS 14 ready after confirms 2–4, Ridgeway 9 — nothing for 100–120 s; File All files them; new arrivals DO self-file: 16 on re-import, 13 at Saltmarsh's 5th confirm). **DIAGNOSED (`r15_copy.db`):** no `scope_sweep_offered` row at all for DS after confirms 2–4 — the sweep's `isAutoFileEligible` refused every doc: the teach-time re-read (now that Q2 makes it work) bound the siblings BEFORE any confirm, so they read at **overall 91–93** (no learned formats yet), and the scope floor is **100 until graduation** (`scopeTrust` DS `recent-correction`, Ridgeway `volume needed 2`); the 'ready' crossing at confirm #2 re-read only the one template-LESS doc (the on-screen copy) because the lane's 'ready' arm is template-less-only. Saltmarsh filed because its 5th confirm GRADUATED (floor 95) and the graduation re-read produced oc100. Round 14 had the opposite order (0 at teach → the 3rd-confirm 'ready' re-read bound them WITH solid formats → oc100 → filed at 80 s). **Direction (Oracle vet needed — it is the Q3 boundary again): when a scope crosses READY, re-read its TEMPLATE-CARRYING held docs whose `overall_confidence` < the scope floor (their read predates the learned formats) — the same honour path + on-page guard as Q3; or recompute the format-consistency delta in place.** Alternatively the badge copy: "new documents will file by themselves — File All Ready for these N".
2. **Header-cut copies read perfectly at teach time, unmatched on re-import — ROOT-CAUSED + FIXED `8c0f26b`:** `learnTemplateOnCommit` intersected the DB fingerprint to 5 tokens (File All's 14 human confirms) but the template FILE the Python matcher reads still carried the promote-time 7 → 5/7 = 0.71. Both callers now rewrite the file (`TEMPLATE_FILE_SYNC_ON_COMMIT=0` disables). **Built without a vet as a consistency bug — owner may veto.** Also found: in dev, `templatesDir` was the repo `templates/` folder SHARED by the owner's app and every sandbox (the matcher saw the owner's June DS templates) — fixed for `DOCUSNAP_USERDATA` sandboxes; rounds ≤15 carry the caveat.
3. "This looks like a Poo" (credit note), "Ment", "Print" — the residual junk-caps harvest (census: pre-existing; 'Poo'×2/'Sss'/'Ment'/'Print'). Direction: require a catalog/installed-type match or ≥2 words / a real dictionary word; "Dairy Wholesale" as a sender = the letterhead fragment class on a Demo fixture.
4. Seven false "manually mapped value differs from the usual format" flags at 70% on Ridgeway worksheets after a drawn teach (6 of 7 correct values; skewed fixtures) — the format-anomaly check on a young scope; one genuine catch ("VS.72672" → wider reading WS-73673 right). Direction: the wider reading that matches the learned shape should be the value.
5. The quiet-lane offer bar that answers itself (bar at 39.7 s, auto-accept at 41.2 s) — show the receipt only when auto-accept is on (renderer: skip the offer render when `scope_sweep_auto_accept` is on and the scope is ready).
6. The teach card hidden on a doc with no read fields; the DONE card still says "As you confirm a few more…" with 19 waiting (teachFollowup should count the lane's candidate set — Q2's UX half, unbuilt).
7. The import refusal's frame ("📄 22 documents ready to import" above it; "COULDN'T START" on cancel; `Processed\Processed` nesting on override).
8. Cosmetics: senders (3) vs graduated suppliers (2→1) both shown; an orphan `.metadata` xml after a purge; supplier folders with subfolders can't be imported whole.
Sandbox 15 was replaced by sandbox 16; `r15_copy.db` in the scratchpad.

## Chris round 16 (verbatim in `docs/CHRIS_FULL_APP_REVIEW_2026-08-22.md`) — Q3's own round (Oracle C3.7): `quiet_reread_on_layout` + `template_identity_on_page` + seed prune ON
**Verdict: Yes — "the best ⊕ I've had".** Fix one box on a Pelican sibling → Confirm → the quiet line starts BY ITSELF at 9.2 s (`layout_arm: selected:18`, 17 re-read in 80 s, no "Reprocess 17"); 17 numbers filled, 14 right; **all three wrong first-fills HELD** ("PO-29444" from the neighbouring "Your PO" box — the exact misfile path the Oracle named — and two P1-for-PI) behind "Read from your new box — confirm once."; the doc already carrying an S3-C5 note was left alone (rule c); DS (generic name) got no layout re-read (rule a); Ridgeway ⊕ → 16 re-read, six of seven false "differs from the usual format" flags withdrawn; **34 filed, 0 wrong folders**. Audit rows: `quiet_reprocess_job` #37 (teach, reasons layout+teach, arm selected:0 at a first teach — correct), #79 (⊕ fix, reasons teach+layout, arm selected:18, done 17).
**Cards:**
1. **The wizard accepted a number box drawn ON the label** ("Value: Invoice Number · Label: none found — I'll remember the spot instead · Looks right →") and filed the taught doc as `Invoice.05-01-2026.Invoice-Number.pdf`; that spot read nothing on 19 siblings. Owner vet: refuse a read that equals the field's label / any printed caption, no "Looks right".
2. **FIXED same night (`5709a15`): the C3.3 "confirm once" hold fired on the TEACH-time re-read too** — the wizard's mapping saves put 'layout' into the coalesced job's reasons and the hold keyed on the job, so every first-filled DS date was held (19 docs, 40 notes, 0 ready vs round 15's 17 ready) and a generic-named sender could never shed it (no layout arm). The hold is now scoped to docs the layout ARM selected (`nd.via === 'layout'`); pinned. The other half of Chris's ask ("a confirm should clear that box's confirm-once on siblings") is an owner item.
3. "High · 90%" beside a held wrong first-fill — show "Check" while the note stands; name the other caption ("printed under 'Your PO'"). Owner vet.
4. **Header-cut DS copies still "Couldn't match" (22%) on re-import** — NOT the round-15 file/DB divergence this time (the file sync is in): the pruned seed legitimately keeps SERVICE/WORKSHEET (on 16/21 siblings), so a header-cut copy scores 5/7 = 0.71 on the keyword arm; at TEACH time the lane still read them correctly (bound by another arm — untraced). Measure with `TESTING/_measure/trace_reprocess.js` on `r16_copy.db` + the sandbox's `userData/templates` before designing. Also the import footer said "9 ready" while Home/File All said 0 (the footer uses the pipeline's needs_review flag, not the classifier).
5. Round-15 card 1 unchanged (the pile under a green badge waits for File All) — the READY-crossing re-read of template-carrying docs with oc < floor is the open Oracle item.
6. ⊕ bar jargon ("Anchor", "Invoice Numb" truncated, a blank label); the young-scope false alarms fire on the first read and clear on the second.
**Sandbox 16 left running** (CDP 9223, PID 22824, admin chris / plumbing2026!).

## Traps recorded this session
- `applyReprocessResult` returns the result object on success; `{ dropped }` only with `opts.expect`.
- `_stageReprocessDocs` / `_runReprocessShard` are the ONE staging + spawn for the foreground batch and
  the lane; the thread-cap identity rule lives there. Don't re-inline either.
- The census scripts must pass the engine's own `type_phrases` (`_letterhead_type_phrases`) or
  'ORDER CONFIRMATION' gets picked as a company and the "wrong" bucket lies.
- Run JS tests with `ELECTRON_RUN_AS_NODE=1 ./node_modules/.bin/electron <file>` (node 26 ABI mismatch).
- bash heredocs with mixed quotes fail in this shell — write patch scripts to a file and run them.
- Run the script-style Python tests with `PYTHONIOENCODING=utf-8` (cp1252 stdout turns a `→` in a label into an exit-1 "red").
- `src/modules/templates/` is matched by the `templates/` gitignore line — the tracked handler needs `git add -f`.
- `git -C Docusnap worktree add` puts the worktree at `<path>/Docusnap/...` (the repo root is `C:\GIT Projects`); junction `node_modules` into it for the native module.
