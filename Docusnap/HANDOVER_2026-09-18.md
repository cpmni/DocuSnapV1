# HANDOVER 2026-09-18 — Departments feature COMPLETE (D4 → D7), all Oracle gates green + 2 live bug fixes + Template Manager video

**Branch** `feat/teach-side-overnight` · **HEAD `96f356d`** · **origin CURRENT (0 ahead — everything pushed)** ·
migration **184** · **no running processes** (sandbox killed, builds done). Only uncommitted tree changes = the
owner's own 8 `tools/video_tutorials/*` edits (untouched, theirs). `TEST_SWITCH_KEYS` unchanged (D7 adds no switch;
it's a schema migration).

**Installers built this session (D7, all toggles armed):**
- core `dist\ScanFinder Setup 2.0.0-r20260918-1346-976f42f-TEST.exe`
- client `client\dist\ScanFinder Search Client Setup 1.0.2-r20260918-1509-976f42f.exe`
(earlier 0905/1152/1250 cores + client are superseded.)

## TL;DR
The whole **Departments** feature (admin-only "who may see which documents") was built end-to-end this session and
is **COMPLETE + past every Oracle before-flip gate**, all DARK behind the opt-in `departments_enabled` (seeded
OFF). Shipped in one commit `325a014` (D4→D7) + the perf gate `96f356d`. Also fixed 2 real bugs the owner hit while
testing (a Users-layout CSS break, a `sweep-inview-recheck` ReferenceError). The Template Manager tutorial video was
produced. A dark-switch flip-census night run is queued but NOT yet run.

## Committed this session (all PUSHED)
- **`325a014` feat(departments): multi-department visibility (D4 → D7)** — the whole feature:
  - **D4** Settings UI + IPC (create/rename/retire/delete, per-user membership, master toggle + D-C7 sentence,
    backup D-C3, audit adapter, flip-gated `department-set-enabled`).
  - **D2b** intake bypass CLOSED — `directIntakeService.submit` + `/v1` intake run the D-C9 create rule
    (`validateCreateDepartments`).
  - **denial matrix** (Oracle #7) — outsider sees zero restricted docs across every reader×transport, mutation
    oracle, route-party carve-out; extended to a multi-tag doc + a `MIN()` scalar-emulation oracle.
  - **D3** per-doc tagger (Review) + confirm-time type-default precedence (`applyTypeDefaultAtConfirm`).
  - **D7** the model: a doc / type-default belongs to a SET of departments (`document_departments` /
    `document_type_departments` join, **mig 184**, transactional, byte-identical, **go-forward-only**). Gate =
    "shared (no rows) OR member of ANY". Widening = Oracle own-slice / immutable-foreign (edit user changes only
    their own departments; no self-lockout). Scalars retired-in-place (nulled, columns kept, pinned unread).
  - **`INTAKE_GUARDED` flipped true** (D2b closed the bypass). `departments_enabled` stays seeded OFF — a per-install
    admin opt-in, never a customer default.
  - UI: type "Who can see documents of this type?" multi-select; per-doc "Visible to" (checkboxes + locked foreign
    chips + Everyone); **Chris Card B fixed** (admin rows show "Admins see every department", no ticks).
- **`aefd42a` fix(review): sweep-inview-recheck missing `documents` require** — threw `ReferenceError` every time it
  was armed (found in the owner's live `processing.log`). One-line per-function require.
- **`96f356d` test(departments): D7 perf gate — PASS** (Oracle #6). `TESTING/_measure/dept_perf_20260918/`.
- **`1375e42` docs(night-run): queue the dark-switch flip-census batch** (`docs/designs/NIGHT_RUN_2026-09-17.md`).
- The Users-layout CSS break (`.user-row` needed `flex-wrap:wrap` for the D4 membership row) is in `325a014`.

## Verification state (all READ, honest)
- **5 `test_department_*.js` green**: `visibility`, `denial_matrix` (multi-tag + MIN oracle), `settings`, `backup_remap`,
  and the NEW load-bearing **`multi_migration`** (upgrade simulator: seeds the OLD scalar, fires the real mig 184,
  asserts backfill exact + NULL→0 rows + decision/list byte-identical to the legacy rule + no-reader-on-scalar grep +
  atomic + idempotent).
- **Zero regressions across 306 pins** (`run-pins.js` database 168/168, modules 96/96, services 42/43 — the 1 red is
  the PRE-EXISTING `test_ref_class_fix` timing flake, not D7).
- **Perf gate PASS** (`RESULT.md`): 50k docs / 30k tagged / 8 depts — worst list reader 58ms, join filter ≤ x1.48 on
  non-trivial readers, per-doc access 204µs. `document_departments(department_id)` index makes the EXISTS near-free.
- Build note: the core build was OOM-`killed` at teardown but the installer finished + signed and `package.json` is
  clean (git diff empty) — the "left modified" warning was a false alarm.

## FIRST ACTIONS (fresh session)
1. **Owner live-test** the D7 installers: Settings → Users & Departments → turn it on → create 2-3 departments → set a
   type's "Who can see" to several → tag docs in Review "Visible to" → a colleague in one department sees only
   theirs + shared. (Watch the multi-select + the locked foreign chips + the admin-row line.)
2. **Re-run `/christest`** on the CLEAN `…-1346` build (re-seeded) — his 2026-09-18 vet was cut short (setup screens
   only, no enforcement test); cards A/B/C logged in `docs/CHRIS_FULL_APP_REVIEW_2026-09-18.md` (B already fixed).
3. **The queued night run** (`docs/designs/NIGHT_RUN_2026-09-17.md`) — the dark-switch flip-census batch — not yet run.

## Deferred / owed (designed or queued, NOT built/blocking)
- **C12** (re-import / rejoin recovery) — the mig-180 pair-belt flip gate, from `HANDOVER_2026-09-17_C12_AND_SWITCHES.md`.
- The remaining dark-switch flips (the night-run census output → owner's call, per-switch plan in the C12 handover).
- The 09-15 EVENING queue (VM live-test both installers; Departments D5 folder token / D6 bulk-retag are "later only if asked").
- Template Manager video: owner to voice-check + upload (`Desktop\Tutorials\the-template-manager\the-template-manager-final.mp4`).

## Needs the USER
- Live-test the installers (above). · Voice-check + upload the video. · Decide: run the night run? re-run Chris?

## Key facts / paths
- **Live DB** `%APPDATA%\ScanFinder\docusnap.db` (BLOCKED to Claude script tools; the live `processing.log` IS readable —
  used it this session to diagnose the freeze + the ReferenceError). Diagnostic Logging is OFF by default (no rich log).
- **Departments model:** `document_departments(document_id, department_id)` (doc CASCADE / dept RESTRICT) +
  `document_type_departments` (both CASCADE). Gate = `database/modules/departmentVisibility.js` (`decision`/`visibleDocSql`,
  join EXISTS). Service = `src/services/departmentService.js` (own-slice `setDocumentDepartments`, `validateCreateDepartments`,
  `applyTypeDefaultAtConfirm`, `setTypeDefaultDepartments`, `taggedDocCount`). Design: `docs/designs/DEPARTMENTS_D7_MULTI_2026-09-18.md`.
- **Run pins:** `node scripts/run-pins.js [services|database|modules]`; a single pin: `ELECTRON_RUN_AS_NODE=1
  ./node_modules/electron/dist/electron.exe src/services/test_department_*.js`. **Perf:** `…/dept_perf_20260918/perf.js [nDocs]`.
- **Oracle log:** `docs/oracle_log.md` (D4 2026-09-17 + D7 2026-09-18 verdicts). **Live freeze diagnosis:** an OCR
  timeout drained `LarkspurInteriors_worksheet_10.pdf` to Errors + orphaned a tesseract (killed) — a real "timeout
  orphans the child tesseract" gap worth a fix later (logged, not built).
- Traps: nested git repo (root `C:\GIT Projects`, project under `Docusnap/`); commit via the Bash tool (PowerShell has no
  heredoc); builds need every dev/sandbox electron closed (EBUSY) + are memory-hungry (an OOM killed one at teardown).
