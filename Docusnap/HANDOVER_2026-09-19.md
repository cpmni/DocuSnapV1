# HANDOVER 2026-09-19 — departments audit-debt watertight · C12 Rejoin · licensing dev tool + admin features (create-account, API-activity) · live-server firefight

**Branch** `feat/teach-side-overnight`. **HEAD `dcb11b1`.** **9 commits UNPUSHED** (all local — push is the owner's call).
Migration unchanged (**185**). `node scripts/run-pins.js`: services 45/45, modules 97/97, database 169/169 at the
last full run (0 regressions). No running processes except maybe the Chris sandbox on CDP 9223 (harmless).

## Installers (owner's VM test build)
- **Core (TEST, all switches armed):** `dist\ScanFinder Setup 2.0.0-r20260919-1258-e83bd0a-TEST.exe` (HEAD `e83bd0a` — includes the flicker fix).
- **Client:** `client\dist\ScanFinder Search Client Setup 1.0.2-r20260919-1240-71e6705.exe` (unchanged; flicker fix was core-only).

## Commits this session (newest first, all LOCAL/UNPUSHED)
1. **`dcb11b1` Part C — licensing API-activity + audit hygiene** (Oracle SIGN-OFF-W/COND). New admin page
   `public/admin/api_activity.php` (calls per endpoint 24h/7d, by-hour bars, top IPs, growth line) + sidebar entry
   (`lib/admin_auth.php:510`). `audit_events` indexes `idx_action_created (action,created_at)` + `idx_created` (schema.sql
   + a live-DB online ALTER). `scripts/prune_audit_events.php` — dry-run default, `--apply`, exact-action DELETE of only
   old `license.validated`, batched, refuses w/o the index. **DEPLOYED PARTIALLY:** owner ran the ALTER on the live DB
   (both indexes confirmed) + is uploading the 2 files. The prune script is NOT web-served (above docroot; browser=404 by design).
2. **`dd2d7aa` "New account + issue licence"** (Oracle SIGN-OFF-W/COND) — a `create_account` admin action
   (`lib/admin_actions.php`) + a "+ New account" form on `accounts.php`. Onboards a DIRECT (non-Polar) customer:
   creates account + core entitlement + one-time key (hashed at rest, never logged), atomic, audited. Pin
   `lib/test_create_account.php` (host-run). **NOT DEPLOYED YET** — deploy = upload `lib/admin_actions.php` +
   `public/admin/accounts.php` (no DB change).
3. **`0866ddd`** gitignore `licensing-backend/set-env.php` (never commit live DB creds).
4. **`e83bd0a` practice-run flicker fix** — `applyWindowState` called `win.maximize()` on the tutorial window (the one
   resizable + `maximizable:false` child) → Windows flicker loop (title bar flashing, cursor stuck). Fix: never maximize a
   non-maximizable window — center it. `src/main.js`.
5. **`a52a8a2` licensing call monitor (DEV tool)** — `src/lib/license/client.js` ring-buffers every /v1 call (endpoint,
   source, ms, status); two dev-only IPCs (inert on packaged); a "Licensing calls" card in the **dev inspector**
   (Ctrl+Shift+D then M, SFDEV). Source tags: `startup-validate`/`license-ui`/`activation-test`. **Dev-app only** —
   won't show in the packaged VM build.
6. **`71e6705` C12 "Join with page N"** (barry+eric+gary → Oracle SIGN-OFF-W/COND) — one-click undo of a bad auto-split.
   `python_backend/pdf_join.py` (join working copies, /Rotate preserved) + 2 IPCs (`get-split-undo-info`,
   `undo-document-split`) + the Review button (confirm previews both pages). Shape B only; never auto-files; original
   kept. Module-scope `_resolvePairPartner`/`_rejoinPairSurgery` (exported). Pins `test_pdf_join.py` + `test_c12_rejoin.js`.
   **Unblocks the mig-180 `segment_pair_hold` flip** (still owner-gated; owes the C9 census cells + a manual e2e re-run WITH C12).
7. **`178d93a` departments audit-debt WATERTIGHT** (eric+gary → Oracle SEND-BACK → C-A…C-F) — gated every remaining by-id
   serve/mutate handler + the ON-by-default scope sweep + the classFix/charset/supplierSiblings fan-out leaks. Two
   "off by default" premises were FALSE (ref_class_fix mig 76 / accept_field_chars mig 103 are ON). Byte-identical when
   departments off; coverage + 13-section red-team pins; 0 regressions. **The departments flip-blocking ENGINEERING is
   now DONE**; remaining flip gates are owner tasks (Chris Card 1 switch-vs-copy, D7 live-test).

## ⏭ NEXT — in order
1. **Cloudflare real-client-IP fix (owner said YES, build first).** The live licensing site is behind **Cloudflare** — the
   API-activity page showed all "top IPs" as Cloudflare edges (172.68.x/162.158.x/141.101.x…). So `REMOTE_ADDR` = the CF
   edge, not the real customer, in BOTH `audit_events.ip` AND the rate limiter (`ratelimit.php client_ip()` = REMOTE_ADDR,
   comment already flags this). Consequence: the rate limiter buckets ALL customers behind one CF edge into one 120/hr
   bucket → spurious 429s if traffic grows (harmless at today's tiny volume). Fix: read **`CF-Connecting-IP`** as the client
   IP (validate it's a real IP; fall back to REMOTE_ADDR) for the audit + the limiter. Small; gate via Oracle; live-server change.
2. **Quick Reprocess fix.** Quick Reprocess is IMAGELESS (reuses cached OCR text) so it CANNOT re-run image-based reads
   (taught boxes / anchor crops) — it BLANKS those fields instead of keeping the good value. Live exhibit: a Castellan
   Service Worksheet whose ref (CJB-1834) is read by a taught box (template_mapping "JOB SHEET NO" @90, beats the keyword
   "Job Ref"→JB-8887 @85) → quick reprocess empties it. Fix: quick reprocess must NOT blank a field it can't re-derive
   (keep the existing value, or route docs with taught-box/anchor fields to a full reprocess). `quick_reprocess_enabled`
   mig 104 is DARK for customers (armed only in the TEST build). gary → Oracle → build.
3. **The older queue** (unchanged): the R1 flip-census overnight batch (27 reading switches; fixtures verified —
   `Desktop\Flip Corpus 700\warm_700.db` + `TESTING/_measure/flip_corpus_20260912/rerun_20260916/`; start with 154
   `trust_ref_role_shape` + 157 `template_drift_override_guard`); the mig-180 flip C9 cells; Chris Card 1; owner D7 live-test.
   See `HANDOVER_2026-09-17_C12_AND_SWITCHES.md` PART B + `NIGHT_RUN.md`.

## KEY FINDINGS / product notes
- **The licensing site is barely used** (~26 calls/24h, ~2,200 audit rows since June) → the "goes down sometimes" is NOT
  overload / rate-limiting / audit growth. Today's outages were SELF-INFLICTED deploy errors (see gotchas) or CF/IONOS blips.
  So don't scale; the CF-IP fix is correctness/future-proofing.
- **Import-screen missing values (owner report, NOT diagnosed to root):** a taught doc's values show in Review but the Home
  import table shows '—'. The table reads `msg.supplier_name` + `msg.extractions[docTypeKeys[type].ref]` (main renderer
  ~1212). Emit + forward carry the fields (verified). Owed 2 questions: is Company shown or all blank? Invoice or a custom
  type? Likely a type→field-key mapping miss; a resilient fallback (scan extractions for known ref/date keys) is the fix.
  NOT a regression from this session.
- **Licensing security answer given to owner:** token forgery/extension is cryptographically infeasible (Ed25519, offline
  verify vs pinned pubkey, private key server-only, high-water-mark clock defeats rollback). Only real weakness = patching
  the binary (inherent to desktop). No interval polling recommended (adds load). Optional later: a daily fail-open re-validate.

## GOTCHAS (this session's live-server firefight — READ before touching the licensing deploy)
- **The licensing backend is a SEPARATE deploy** — owner uploads PHP to IONOS, NEVER `keys/`. Source = `licensing-backend/`;
  `Build-IonosDeploy.ps1` packages a copy to `output/licensing-backend-ionos/` (gitignored). Re-running the ps1 REWRITES
  `output/set-env.php` to a blank REPLACE_ template — **do NOT upload that**; the server's real `set-env.php` (per-host,
  gitignored, holds DB creds) must stay put.
- **Two self-inflicted 500s fixed today:** (1) a bad `public/.user.ini` with `auto_prepend_file = "/home/www/licensing/set-env.php"`
  (a path that doesn't exist on IONOS) → 500 EVERY page; fix = comment the line out (db.php loads set-env.php itself via a
  relative require, so no prepend is needed). (2) confusion over the docroot: it's `licensing/public/`; the config that runs
  is INSIDE `public/`, not at the `licensing/` root.
- IONOS sets PHP version PER-DOMAIN/subdomain. `/v1/` returns 403 by design (dir listing off, no index). Health = the site root.
- The dev license-call monitor is inert on packaged builds — dev app (`npm start`) only.
- Dev MySQL (WAMP) was OFFLINE this session → the host-run PHP tests (`test_polar_reconcile.php`, `test_create_account.php`)
  self-skip. Owner runs them on WAMP/the server.

## Memory to save
`project_session_20260919.md` — the session summary + the CF-IP fix + Quick Reprocess fix as the immediate next tasks.
