# HANDOVER 2026-07-26 (Fable 5 · max effort)

Branch `feat/reprocess-throughput-autostraighten` · **ALL PUSHED through `fff6cde`** (origin `0 0`) ·
tree clean · no background processes. **Installer BUILT + hardened:
`dist\ScanFinder Setup 2.0.0-r20260726-1018-58533ea.exe`** (not yet installed on the pilot PCs).
Recovery anchors: tag `milestone-20260726-logo-identity`=`3c37500` (pushed) · DB snapshot
`%APPDATA%\ScanFinder\docusnap.backup-20260726-prebuild.db` · prior installer `r20260724-1432` in dist.

## TL;DR
The logo-identity fix went end-to-end today: iris (new forensics agent) proved the 64-bit "logo hash"
hashes LAYOUT not the mark → slices A (global-rival veto) + C (identity-veto fall-through) built →
corpus BLOCKED naked C (new M #472 + date drop #456) → gary+Oracle designed the G1/G2 corroboration
guards → revised-C8 gate PASSED (M==2 exactly, 377 would-auto-file (+42), no silent wrong values) →
**FLIPPED ON → owner live-validated ("the docs filed fine")** → installer rebuilt with NEW bytecode
hardening (extraction ships sourceless .pyc). Then a NEW class landed: 13 Copperfield POs held @69 by
the taught-ownership cap — DIAGNOSED (blind label-less anchor), PLAN formulated (below), NOT built.

## Committed today (all pushed; every switch state noted)
- `9dfa011` caption-prefix strip (**still DARK** — `ANCHOR_CAPTION_PREFIX_STRIP` default OFF; its own
  flip gate never run) · `2053729`/`6c13ec3` audit docs + template-diagnosis · `522cc3b` **Slice A**
  global-rival detail veto · `c1f9a3f` **Slice C** identity-veto fall-through (C2 supplier exclusion +
  C3 winner bar) · `a89ecb3` docs · `ba8bcea` **G1+G2 corroboration guards** (G1 final-assembly
  page/rail corroboration hold; G2 inverted-confidence displacement keep+note + C6 agree-retention;
  naked C UNREACHABLE — guards key on the `veto_fallthrough` tag) · `0c41bdc` docs · `eeb257d`
  **FLIP: `TEMPLATE_VETO_FALLTHROUGH` + `LOGO_DETAIL_GLOBAL_RIVALS` default '1'** (=0 restores; test
  OFF-cases now set '0' explicitly) · `3e0812c` `docs/ARCHITECTURE_SNAPSHOT_2026-07-26.md` (C++-port
  planning report; MODULES.md verified badly stale) · `3c37500` live-validation note · `58533ea`
  **bytecode hardening** (`scripts/compile-python-bytecode.js` stages `build_python/`: 38 modules
  sourceless .pyc under vendor/python, 15 JS-spawned entries stay .py; kill `SHIP_PY_SOURCE=1`;
  extraResources ships build_python AS python_backend) · `fff6cde` docs.

## Verification state — honest
- **Logo-identity chain:** unit 20+8+6 pins green · 7 sibling suites green · corpus guarded cell:
  M==2 exactly (#472 eliminated), M_type 0, 377/570 would-auto-file, ref/supplier=baseline, dates 98.2
  with {#456,#472} both `[flagged]` note-held (revised-C8 "no silent wrong value" — #456 is the PINNED
  accepted cost (c): an authoritative misreading displacer stores wrong-but-held) · live re-traces
  (#472 held, #456 held, Saltmarsh clean 100) · **owner live-validated: docs filed in the app.**
- **Hardening:** staged tree ran under the SHIPPED embeddable python + vendor tesseract (usage banner
  + one real doc end-to-end); packaged win-unpacked verified ZERO .py in extraction/. ⚠ The HARDENED
  INSTALLER itself has NOT had an install-and-run smoke on a real machine yet — that's first-actions.
- ⚠ Mid-session corrections: my first build "succeeded" claim was WRONG (tail masked npm exit; the
  real failure = Git-bash mangles `taskkill /IM` silently → the dev app never closed → EBUSY on
  better_sqlite3.node). Fixed via PowerShell `Stop-Process -Name electron`. Recorded in CLAUDE.md.

## ⭐ NEXT SESSION'S JOBS (in order)
1. **Owner smoke of the hardened installer** (install over on this PC → import/reprocess/teach once —
   proves the sourceless .pyc tree in anger; rollback = reinstall `r20260724-1432`, DB untouched).
2. **The Copperfield taught-ownership plan (formulated TODAY, not built)** — see below. Slice B is the
   build (gary→Oracle first).
3. **#183 anchor-binding** (the last real corpus M besides #583; Oracle-named backlog: "G1 verifies
   EXISTENCE not BINDING"; project_183_harvest_synthesis fix-A; 007→Oracle→build).

## THE COPPERFIELD PLAN (diagnosed + owner-extended; NOT built)
**Symptom:** 13 Copperfield POs held; po_date correct via keyword but capped @69 + noted by
`_flag_taught_field_ownership` ("taught position couldn't be confirmed").
**Root (DB-verified):** the taught po_date anchor is **BLIND — `label=''`, `offset=(None,None)`**
(taught 2026-07-26 10:27; the ⊕ label auto-detect silently found nothing). Blind anchor ⇒ no
label-lock/relocate/located witness ⇒ rigid crop nicks a fragment (`e 26/10/2026` → format-reject,
whole-string parse_date) ⇒ registration maps into EMPTY SPACE (`a ee` rx0) ⇒ ownership unconfirmable.
po_number (labelled 'Order No.' + offset) works @97 on the SAME page. Also: Copperfield has TWO PO
templates (T27 5-landmarks / T28 0-landmarks, both confirmed_count=0 — fragmentation).
**Why the own-label exemption didn't fire:** "Order Date" is SHARED with sales_order.order_date —
shared labels held by design (4af4bba).
**Slices (all gary→Oracle before build):**
- **Step 0 (OWNER, no code):** re-teach po_date and CHECK THE LABEL CHIP shows "Order Date" before
  saving → reprocess 13 → caps clear. Immediate unblock.
- **A — teach must never silently plant a blind anchor:** warn/ask when label auto-detect is empty for
  a right/below anchor + investigate why the picker missed "Order Date" left of the box (eric + teach).
- **B — generalized located witness:** extend the RECIPIENT_CAPTION_CORROB producer design (gary's,
  for names) to taught REF/DATE anchors — locate a field-specific config label on the page, read
  beside it, emit a located candidate; `_anchor_corroborates` clears the ownership cap on agreement.
  Fixes the class even with a blind anchor. Kill-switched, fail-toward-review.
- **B′ — TYPE-SCOPED label ownership (owner design decision 2026-07-26):** label sharedness must be
  judged against the RESOLVED doc type's field set, not globally — "Order Date" on a PURCHASE ORDER
  is own-label (only po_date claims it there) → exemption fires. Guards: type must be authoritative
  (assigned/confirmed/trusted heading); bare "Date" stays held where the type has >1 date field.
  (Deeper root, deferred: extraction runs against the UNION of all types' fields — the P2 class; the
  eventual cure is type-scoped extraction, the deferred Phase-2.)
- **B″ — single-date crop salvage (owner design decision 2026-07-26):** a format-rejected DATE crop
  gets a salvage pass — if EXACTLY ONE date-shaped token exists and calendar-validates, use it:
  first as a WITNESS (normalise-equal to keyword ⇒ ownership confirmed ⇒ cap clears), and as a
  recover-and-flag commit (capped+noted) only when it's the lone read. Uniqueness guard is mandatory
  (two dates in a drifted crop = ambiguous = reject as today; the #456 wrong-but-valid-date class).
- **C — registration hygiene:** a bad affine fit should ABSTAIN, not emit rx-0% candidates; T27/T28
  dupes feed the existing defrag/merge thread.

## Deferred (unchanged, with conditions)
- **Slice B (mark as PRIMARY matcher)** — blocked on mark normalisation (centroid+pad+binarise)
  shrinking the 256-bit pairwise intra tail; then demote-to-suggest shape, ≥3-member sets, re-gate
  BOTH consumers (logo_detail.py:326-328 pin).
- **Caption-strip flip** — corpus A/B + flip-set page-verify never run; stays OFF.
- **Full hardening pass** (Cython/bytenode/license-into-compiled + code signing) — PRE-LAUNCH
  checklist, not now. Interim .pyc shipping is LIVE as of r20260726. C++ port: engine-first if ever
  (`docs/ARCHITECTURE_SNAPSHOT_2026-07-26.md`; vendor/ bundles python AND tesseract — port Q1 done).
- Template merges (owner click) · Slice-2 IDF hardening · workflow slices 5+6 · H2 pairing — as before.

## Needs the USER
- Install `r20260726-1018` on this PC + pilot PCs (over-install; DB safe) + a one-batch smoke.
- Copperfield Step 0 re-teach (label chip!) + reprocess 13.
- Decide when to run the template merges (Settings → Templates → Suggested cleanups).

## Key facts / paths
- Live DB `%APPDATA%\ScanFinder\docusnap.db` (ro via `?mode=ro`) · snapshot
  `docusnap.backup-20260726-prebuild.db` alongside · migration 54.
- ⚠ Python change ⇒ clear `python_backend/**/__pycache__` · ⚠ Git-bash mangles `taskkill /IM`
  (SILENT fail) — close the app via PowerShell `Stop-Process -Name electron` before `npm run build`.
- Corpus: `ELECTRON_RUN_AS_NODE=1 ./node_modules/electron/dist/electron.exe stress_test/realdoc_regression.js`
  (cells OFF/ON via the env flags; guarded-cell log in the 07-26 scratchpad `rr_GUARDED.txt`).
- New agent: **iris** (`.claude/agents/iris.md`, REGISTERED) — perceptual-matching forensics.
- Memory: [[project_logo_identity_slices_20260726]] · [[project_taught_anchor_ownership_20260726]] ·
  [[project_autofile_blockers_20260725]] · [[project_183_harvest_synthesis]].
