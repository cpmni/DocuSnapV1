# HANDOVER 2026-09-25 EVENING — mig 219 (label tail-bound, census-gated) · deskew invalid-date note · Review banner reconciliation BUILT (Idea B) · suggested-teach census (Idea A) → picker confirmed

**Branch** `feat/teach-side-overnight` · **HEAD `2bb483f` = origin (all pushed)** · migrations → **219** · `TEST_SWITCH_KEYS` **17** · run-pins **459/459** green (last full run before Idea-B cut 2; cut-2's own pins green, see below) · **tree clean** (only untracked pre-existing `TESTING/_measure/*` census artifacts + a stray `../Docusnap - 20.09.26.zip`). No uncommitted code. Predecessor: `HANDOVER_2026-09-25.md` (morning; migs 217/218).

## TL;DR
Owner "read the handover and continue" → four threads, all committed + pushed:
1. **mig 219 `keyword_label_tail_bound`** (DARK, Oracle C6) — a multi-word label ("Credit No") stops prefix-hitting its own printed heading ("CREDIT NOTE"); the card-4 class. Census-gated (label-hit census on the owner's 727 + Chris's 200): one real loss found + fixed (currency labels excluded).
2. **deskew "confirm once" note fix** (owner screenshot) — an invalid raw date ('41-02-2025') is no longer named in the note; reuses the existing put-back plausibility gate.
3. **Idea B — Review status-banner reconciliation BUILT** (owner screenshot: four clashing counts). Oracle SIGN-OFF-W/COND. De-numbered the check bar + a pure `deriveReviewStatus` reducer so "checking" and "ready to file" never co-render.
4. **Idea A — suggested-teach** (owner idea) — advisor round (007+barry+eric) → Oracle **SEND BACK** → the Oracle-mandated **reconstruction census DONE**: blind pre-fill fails (~20% silent mis-locate), the **picker** is the right build. NOT built — awaits owner/Oracle go.

## Committed work (all pushed, oldest→newest)

### 1. mig 219 `keyword_label_tail_bound` — DARK, Oracle C6 (`8b17704`, `955e461`, `2ea3911`)
- **Root cause** (Chris 09-24 card 4): `_label_pattern` (keyword.py) gives a single-word ALPHABETIC label a trailing word bound but leaves a MULTI-WORD label ("Credit No", "Delivery No") without one → it prefix-hits its own heading ("CREDIT NOTE") and the below-walk adopts the next line ("Meadowvale") as the value.
- **Fix:** a scalar twin of `LIST_CAPTION_TAIL_BOUND` — a letter-only `(?![a-z])` on the multi-word-alpha-tail scalar label, gated on `_label_tail_boundable(label)`, env `KEYWORD_LABEL_TAIL_BOUND`, default OFF (byte-identical). **CURRENCY EXCLUDED** (`val_type != 'currency'`): the label-hit census found "Total DueGBP 21,778.54" (money label OCR-glued to a currency code) would be wrongly refused — a real balance lost; card-4 is reference labels, never money.
- **Files:** `python_backend/extraction/keyword.py` (`_label_tail_boundable` helper + the scalar elif). Bridge `src/modules/processing/handler.js` `_reconcileEnv` (one guarded line, composed by `_pipelineSpawnEnv`). Plumbing: mig 219 in `database/index.js`; `database/dark_switches.js` (TEST_SWITCH_KEYS 16→17); five per-mig count pins (137/163/216/217/218) + two flip pins (205 batch/215) bumped to 17; `docs/DARK_SWITCH_LEDGER.md`. Data-only: `database/modules/document_types.js` — Delivery Note preset labels reordered (specific forms first; new catalog ticks only).
- **Tests:** `python_backend/tests/test_keyword_label_tail_bound.py` (OFF preserves the bug → 'Meadowvale', ON reads the real ref → 'CN-88213', digit-glued still matches, single-word untouched, currency excluded); `database/modules/test_migration219_keyword_label_tail_bound.js`.
- **Gate item (i) MET:** `TESTING/_measure/label_tail_bound_census_20260925/RESULT.md` — on the owner's 758 texts + Chris's 200, every refused hit is a heading/address/boilerplate garble, NO clean value lost (a real delivery note fills via "Delivery Note No" identically OFF/ON — precedence-replay verified). **Owed before FLIP:** (ii) realdoc M=0 (needs a confirmed-doc harness) + (iii) Hard Set P==K==N → Oracle → owner flip. **Still DARK.** Next mig = **220**.

### 2. deskew "confirm once" note — invalid raw value not named (`b5c2d5c`)
- **Owner screenshot:** the straightened-retry note read "Read differently after straightening — was '41-02-2025', now '11-02-2025' — confirm once." '41-02-2025' is an invalid date — naming it is pointless.
- **Fix:** `process_docs.py _deskew_changed_note` now uses the existing "Found 'X'" framing (already used for empty-before) whenever the raw read is a garble the put-back would not offer (`_put_back_offerable` False — an invalid date, a broken-money misread). A plausible alternative (a real ref A→B, a name typo) keeps the "was A, now B" comparison. No switch; fail-safe (still a lane-hold, still held for one confirm, `corrected_to` unchanged).
- **Tests:** `test_deskew_review_retry.py` + `test_deskew_retry_field_adopt.py` — the two that asserted the old "was '42-04-2025'" note for an invalid raw date now assert the "Found" framing + that the garble is not named. Both green.

### 3. Idea B — Review status-banner reconciliation BUILT — Oracle SIGN-OFF-W/COND (`89d7145` B3, `7af6d63` cut 2, `9afe00c`/`babb138` docs)
- **Owner screenshot:** four counts on one screen — queue "9" · reprocess "9 read clean" · autofile "Checking … 7 of 18" · activity "19 more offered" — that don't match the queue and clash ("file now" vs "checking if fileable").
- **Diagnosed (all traced at source, in `docs/oracle_log.md` 2026-09-25):** four subsystems, four populations, only the "9" is the live queue. The "18" is the quiet job's `total`, **frozen at job start** (`quietLane.js:510`), population wider than the view. The "19" is a cumulative `issuer_fill` activity event. `renderer.js:2261` already had a comment about a past banner collision.
- **BUILT (cut 1, B3):** `_renderAutofileCheckBar` (renderer.js) de-numbered — now "Checking which documents from <sender> can file on their own…", never a `done of total`. (Oracle: ship the no-number cut first.)
- **BUILT (cut 2):** pure reducer `src/windows/shared/reviewStatus.js` `deriveReviewStatus({inviewActive, offerActive, checkActive})`, fixed priority `countdown > offer > checking > idle`. The background check YIELDS to the offer/countdown, which are NEVER suppressed (**Oracle B1 ship-blocker**: never hide a cancellable auto-file). **Chose the offer-priority variant** over the vetted offer-defers-to-check design — meets every condition MORE strictly (B2 satisfied by construction, nothing deferred) and never mutates the fragile countdown bar. **⚠ Flagged for a re-vet if "checking-first" is preferred.** Loaded via `<script>` in `review/index.html`; `_renderAutofileCheckBar` consults it (reads the live offer bar + `_inviewCd`); the offer teardown (dismiss/review/accept-or-expire) re-renders the check bar so it reappears.
- **Tests:** `src/windows/shared/test_review_status.js` (node unit: priority, the clash reproduced+resolved, the 8-state B1 invariant that offer/countdown are never suppressed, an event sequence) + `src/windows/review/test_autofile_check_bar.js` extended (no-denominator + yield wiring + teardown re-render). Both green. The two countdown pins (`test_reprocess_inview_countdown`, `test_sweep_inview_recheck`) still pass.
- **Oracle conditions status:** B1 met (reducer never writes the countdown bar / never suppresses it — enforced by the offer-priority design + pinned). B2 met by construction. B3 met (no-number first). **B4 recorded N/A** — its "three auto-hide timers" premise does NOT map to the status strip (the bars are event-driven; the in-view countdown is already cancelled on doc-change at `renderer.js:2631`). **GATE not fully met:** the Oracle wanted a runtime event-interleaving harness; **no jsdom in this repo**, so the DECISION was factored into a pure function tested at the logic level instead — the runtime harness is still theoretically owed. The "19 more offered" stays a past-tense activity-strip receipt (barry/Oracle: copy, not a number) — unchanged.
- **⚠ NEEDS THE USER:** a live drive on a REOPENED Review window (renderer change → needs the window reloaded / app restart). Not driven live this session.

### 4. Idea A — suggested-teach: advisor round → Oracle SEND BACK → census DONE (`ddc9987`, `c3409b0`, `6d29aac`, `dc3e061`, `2bb483f`)
- **Owner idea:** auto-draw the value+label boxes from the machine read and ask "is this right?" instead of drawing them; especially disambiguate when 2+ labels match (Job Ref No vs Job Sheet No).
- **CORRECTED PREMISE (all three advisors, verified):** the read-geom the idea assumed the engine "already exposes" is dev-TRACE-only (`engine.py:5703 if not self._trace: return`) AND empty for the untaught keyword case — so the box must be RECONSTRUCTED (words-only OCR + string-match), not wired.
- **Oracle SEND BACK:** the blind single pre-fill can't be both safe (007's located-only gate is inert on the untaught case) and useful (reconstruction 007 won't bless); it removes placement attention while keeping full authority → a rubber-stamped mis-placed box becomes a high-authority all-supplier anchor that poisons the scope. The **picker** is the right layer.
- **CENSUS A2 DONE:** `TESTING/_measure/teach_reconstruction_census_20260925/` (`census.py`, `RESULT.md`, `run_n6.log`). 48 real-corpus docs, `region.py --page-words` (the teach path's own word geometry), ref+date matched against GT: **ref 77% UNIQUE / 23% MULTIPLE / 0 NONE; date 87% / 13% / 0.** Findings: (1) NONE=0 — the value is always findable, so a box can always be made; the risk is picking the WRONG instance. (2) The ambiguous ~20% is entirely two predictable layouts — Ironclad STATEMENTS (list many invoices) + Pelican invoices (ref printed twice). **Verdict:** blind pre-fill FAILS the bar (stays dropped); the census SUPPORTS "UNIQUE → one suggestion, MULTIPLE → the picker, never auto-pick, always re-read-verify". Caveat: clean renders (NONE=0) — real SCANS add a NOT-FOUND fall-back-to-draw tail a scan re-run would quantify.

## Verification state — honest
- **Full pin suite ran 459/459 green** BEFORE Idea-B cut 2. Cut 2's own pins (`test_review_status`, `test_autofile_check_bar`) + the two countdown pins ran green individually AFTER. A full-suite re-run was NOT done post-cut-2 (a memory-pressure warning fired mid-session; the electron pin suite is heavy). **FIRST ACTION: re-run `node scripts/run-pins.js` to confirm 460/460** (the two new pins add to the count).
- mig 219: the label-hit census (gate i) is MET + read; the realdoc M=0 (gate ii) + Hard Set (gate iii) are NOT done (need a confirmed-doc harness — `rr_ids.txt` is EMPTY, so `realdoc_regression.js` has no confirmed docs to reprocess).
- Idea B: NOT driven live (renderer change; needs a Review-window reload). The Oracle's runtime interleaving harness is not feasible here (no jsdom) — replaced by a pure-function logic test.
- Nothing was mis-claimed and later corrected this session except the original Idea-A "the engine already exposes the box" premise, which the advisor round corrected (now recorded everywhere).

## FIRST ACTIONS for the fresh session
1. `node scripts/run-pins.js` → confirm green (expect ~460 files; the two new review/shared pins).
2. **Idea B live drive** (owner-owed): reopen the Review window on the owner's sandbox, trigger a graduated-sender auto-file check + a reprocess offer, confirm "checking" and "ready to file" never co-render and the auto-file countdown is never hidden.
3. **Idea A decision:** if the owner wants the picker, spin the design (reggie = candidate retention in `keyword.extract_fields` — a DARK-gated change to the extraction hot path, keeps only the winner today; eric = the picker UI on the existing readout; 007 = the re-read-verify) → Oracle, THEN build. Do NOT touch `extract_fields` without that go.
4. mig 219 flip: only after (ii) realdoc M=0 + (iii) Hard Set — both need a rebuilt confirmed-doc harness first.

## Deferred (designed / measured, NOT built)
- **Idea A picker** — census SUPPORTS it; rule = UNIQUE → one suggestion, MULTIPLE → picker (never auto-pick), always re-read-verify on the live frame; host surface decided FIRST (⊕ Review = Stage-2 anchor via `saveFieldAnchor` vs Teach wizard = Stage-0.5 mapping); authority never exceeds a hand-draw. Full conditions: `pendingfeatures.md` Idea-A entry + `docs/oracle_log.md`.
- **mig 219 flip** — gates ii+iii owed.
- **Idea B checking-first variant** — if the owner prefers the vetted offer-defers-to-check sequence over the shipped offer-priority, it's a re-vet (would touch the countdown bar → the B1 harness matters more there).

## Key facts / paths
- Live DB `%APPDATA%\ScanFinder\docusnap.db` (BLOCKED to Claude's script tools — copy to scratch). Job-scratch DB copies used this session: `%USERPROFILE%\.claude\jobs\68b38f39\tmp\dbcopy\{live727,chris-sandbox-20260924}.db` (have `ocr_text`) — JOB-MORTAL.
- 605 corpus: `%USERPROFILE%\Desktop\ScanFinder Test Corpus` (PDFs + `ground_truth.json`; **`rr_ids.txt` is EMPTY** → `realdoc_regression.js` is vacuous until a confirmed-doc DB is rebuilt).
- Run pins: `node scripts/run-pins.js` (JS, Electron-as-Node); python pins `py -3.12 tests/test_X.py` from `python_backend/`.
- Census tooling: `TESTING/_measure/{label_tail_bound_census_20260925,teach_reconstruction_census_20260925}/` (both have `census.py` + `RESULT.md`).
- Oracle log: `docs/oracle_log.md` (bottom = the 2026-09-25 two-design entry). Switch state authoritative in `database/dark_switches.js` + `docs/DARK_SWITCH_LEDGER.md`.
- No running background processes left (census finished; pin waiters reaped).
