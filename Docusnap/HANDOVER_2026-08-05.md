# HANDOVER — 2026-08-05 (jitter-crater arc REFRAMED + CLOSED: A/B/C/D slices built dark, all gates green)

**Branch** `feat/reprocess-throughput-autostraighten` · commits this session: `b63bd86` (Slice A) ·
`8f631b8` (B+D) · `2ddd5fa` (C) · `fafd8b4` (C-v2 contract fixes) — check PUSHED state.
**Installer:** unchanged. **Running processes:** none. **Context:** autonomous session continuing
HANDOVER_2026-08-04.md's "FIRST ACTIONS".

---

## TL;DR — the 08-04 charter was overturned, then the crater was closed
1. **Premise overturn (Oracle UPHELD, docs/oracle_log.md 2026-08-05):** "born-digital word-box
   synthesis" was NOT the crater's cause. Rung-level probe + armed-env rerun (j120armed ==
   j120s byte-identical) proved the mechanism: a cut taught box reads a CLEAN PARTIAL
   ('VXC153' of VXC1536) that passes `_gate_value` shape_mode='ignore' and commits silently at
   78-90 — every shipped heal keys on page-vs-taught DISAGREEMENT, and this class is stored-box
   damage on an UNDAMAGED page. Digital-worse-than-scanned = crisp partials PASS the gate;
   scan garble FAILS it and falls through to heals (j120s digital wrong refs 33/49 =
   clean-prefix-of-GT). template_mapper has NO born-digital branch — word geometry exists on
   renders for both renditions. Born-digital synthesis DEMOTED to follow-up (`_page0_geom`
   letterhead only).
2. **Harness infidelity found + fixed (Slice A, harness-only):** teach_from_gt.find_label stored
   NEIGHBOURING VALUES as anchor labels (48/310 mappings; 34% anchorless-or-poisoned) — the live
   wizard's anchorLabel.js was never sick. Fixed with the wizard's sanitizer parity; audit 48→0.
   Re-baselined arms; old crater numbers (70→22 etc.) RETIRED.
3. **Four Oracle-signed slices BUILT DARK + GATED GREEN** (all kill-switched, default OFF):
   - **B** `TEMPLATE_DATE_CLIP_GATE`: rejects date-clip fragments ('07-01-20-', '03-06-202') on
     the RAW read pre-salvage; 4-digit-year+debris exempt; '07-01-20' clean 2-digit PINNED
     accepted. + UNSWITCHED `parse_date` year<1000 floor (ships live — rode the realdoc gate).
   - **C** `TEMPLATE_ABS_EDGE_GUARD` (the centrepiece): word-edge predicate on the ABS rung
     (gate-outcome-independent) → word-bounded GROW → full-res re-read → edge-directional
     comparator (right=prefix, left=suffix, ≤1 cut-glyph slack) → independent-WITNESS check
     (cut word's locate-pass text ⊂ grown value) → consent (codes: _shape_consents ladder;
     dates: complete-un-suspect parse) → clean '_edgegrow' / flagged ≤70 / defer-cap
     fall-through floor ('_edgecut' at final commit only if nothing else healed). Stored
     mapping NEVER mutated (pinned). Names EXCLUDED v1 (NAME_UNCLIP owns that class).
   - **D** `TEMPLATE_LABEL_DIGIT_EXACT`: digit-dominant locate needles require their digit
     sequence in the haystack ('03-06-2026' can no longer lock '07-01-2026').
4. **GATES (SEED=7, shipped flips armed both sides):** clean t300s→t300c3 **ZERO True→False,
   21 pure heals** (ref 70.1→74.7%, date 91.3→93.4% overall) · right-jitter j120n3 **ref
   85.7/66.1 · date 91.1/83.9 · po_ref 100/78.6 · job_ref 100/100** (dark baselines 12.5/19.6 ·
   3.6/26.8 · 14.3 · 0/57) · left-jitter j120L3 ref 69.6/62.5 (was 3.6/17.9) · **realdoc 543:
   baseline==armed** on every axis (ref 95.6, date 99.3, silent 14==14, wrong-auto-file 11==11
   standing corpus residuals, M_type 0, +1 doc GAINED auto-file).
5. **Oracle earned his keep twice:** found the WYSIWYG pin at
   test_template_target_word_snap.py:108 was a DEAD GUARD (empty source slice, vacuous pass —
   rebuilt behaviourally, C-C0) + two seams the specialists missed (B-starves-C composition;
   B over-rejecting complete dates with trailing debris).
6. **C's first contract regressed the clean arm** (18 T→F) — caught by my own t300s control +
   per-doc diffs, three root causes probed and fixed in v2 (fafd8b4): floor pre-empting the
   inline reconcile · prefix-only comparator refusing left-cut heals ('5S-1108'→'CSS-1108') ·
   grow-region garble self-consenting on scans ('2026'→'2096' — the witness check kills it).

## FIRST ACTIONS next session
1. **OWNER FLIP** (settings-bridge BUILT in the continuation: `_reconcileEnv` carries all three
   switches at all 4 spawn sites; Settings→Processing toggles `edge-guard-toggle` /
   `date-clip-toggle` / `label-digit-toggle` wired + div-balance/pairing-checked): RESTART the
   app (main-JS changed — the stale-main gotcha), then tick the three toggles or set
   `template_abs_edge_guard` / `template_date_clip_gate` / `template_label_digit_exact` = true.
   All gate evidence is in stress_test/out/ (t300c3/j120n3/j120L3 +
   realdoc_base_0805/realdoc_armed_0805) and docs/oracle_log.md.
2. Owner live checks still pending from 08-04: teach-snap feel · docket_10 `clip_decline`
   trace · C2b copy vet.
3. Then the 08-04 list's survivors: mapper-heal census (scorer `methods` now records per-lane
   methods — the engine-side HEAL_RE census remains) · customer-name GT · vat_no teach-locator.

## Known residuals (documented, not hidden)
- Left-cut DATE lane digital 46.4% — day-digit fragments; date suffix discipline weaker than
  codes'. Follow-up candidate.
- Issuer 0% under jitter BY DESIGN — names excluded v1; the NAME_UNCLIP_RECONCILE flip decision
  owns the class (still dark/HELD).
- `test_template_rescue.py` 1 failure = the CATALOGUED pre-existing failure (HANDOVER
  2026-07-21) — not this session's.
- The scorer md header "Heal/verify fires captured (0)" is stale wording — fires are now counted
  from the jsonl `methods` field.

## Key facts / paths
- Scorer arms: `stress_test/out/customer_score_<TAG>.{md,jsonl}` — this session's TAGs:
  j120armed (parity check) · t300f/j120f (re-baseline) · j120fa (honest+armed) · t300s
  (shipped-only control) · t300c/j120n/j120L (contract v1 — superseded) · t300c2/j120n2/j120L2
  (v1.5) · **t300c3/j120n3/j120L3 (FINAL)**. LEFT-cut env: `TEACH_JITTER_LEFT=0.18`.
- New pins: test_teach_from_gt_labels.py (12) · test_date_clip_gate.py (19) ·
  test_label_digit_exact.py (11) · test_template_abs_edge_guard.py (26) + the rebuilt WYSIWYG
  pin in test_template_target_word_snap.py.
- Gotchas (standing): electron.exe never .cmd · never edit mapper py mid-arm · `git add -A`
  from repo ROOT stages Backup/ · py -c inline forbidden (use .py files) · the
  test_template_code_edge_clean wiring pin inspects the module prefix BEFORE the first `def` —
  keep new mapper kill-switch getenv lines in the top flag zone, functions below.

---

# LATE-EVENING ADDENDUM — the Straighten arc (owner-directed)

**Owner directive (verbatim intent):** teach happens on STRAIGHTENED pages; customers run
Straighten ON; the template/teach system must be ~100% on slightly-rotated (≤2°) pages. Chris's
scope NARROWED by the owner: sandbox-reprocess the owner-trained docs only (no full app sweep),
Claude reads slices+logs; same safety rules.

**What ran:** 007+gary (convergent: raw-frame crop election) → Oracle SIGN-OFF-W/COND C1-C7 →
BUILT dark `7d88dc4` (`DESKEW_RAW_CROPS`, 18 pins, six sites, per-page 2.0° cap, witness kept
live) → owner hint "look at how Review/target teach does it — no pipeline sharing" → reverted the
region.py rotation-helper coupling `d34da5b` (owner rule pinned) → **GATE RED** `b67d79a`
(dsk_off/dsk_on: refs +7 scanned, but customer −24 / issuer −5 / date −5 — caption-grabs).

**THE TRUTH THE GATE BOUGHT:** stored teach coords carry the TEACH doc's own tilt (θ_teach) —
they match neither frame of a differently-tilted sibling. Deskew's placement normalisation is
load-bearing. Correctly-PLACED deskewed crops read fine (band probe). Review works because
box+pixels share ONE canonical LEVEL frame end-to-end.

**NEXT SESSION #1: the CANONICAL LEVEL FRAME pivot** (pendingfeatures 2026-08-05-late entry:
options a/b/c + constraints). Then Chris sandbox loop (owner-approved scope). Everything dark:
DESKEW_RAW_CROPS, DESKEW_SS_ROTATE — keep the code; the election infra + angles threading are
the pivot's building blocks.
