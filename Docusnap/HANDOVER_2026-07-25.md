# HANDOVER 2026-07-25 (Opus 4.8) — template de-fragmentation

Branch `feat/reprocess-throughput-autostraighten`. **2 commits, UNPUSHED** on top of `431068e`:
`5501be1` (Slice 1) · `aba2f46` (Slice 2). Tree clean except parent-level `../Backup`, `../Docusnap - Copy*`
(pre-existing junk, not mine). Design + gate ledger: `docs/designs/TEMPLATE_DEFRAG_2026-07-25.md`. Memory:
`project_template_defrag_20260725`.

## 1. Field-visibility live-update (the opening ask) — NO CODE NEEDED
Owner: "when I tick which fields show, the on-screen Review doc doesn't update until I click another doc."
Already built (`431068e`, 09:00 today): the broadcast lives in the MAIN process (`set-template-hidden-field`),
which doesn't reload on window reopen — the running app predated it. **Fix = restart the app.** Owner confirmed
"this works" after restart. Nothing to build.

## 2. Template de-fragmentation (the main work) — owner said "finish all slices safely, choose recommended"
Owner spotted duplicate templates (same supplier+type minted 2-4× on different days) in the Layout dropdown.
Verified all 254 linked docs are CORRECTLY assigned (no misfiles — pure fragmentation). Root cause: reuse keyed
off the unstable logo phash (drifts past hamming-13 same-supplier), and branding-reuse was DEFAULT OFF.

**Slice 1 — merge tool surfaces genuine dupes** (`5501be1`, kill `TEMPLATE_MERGE_REVIEW` default ON):
- `templateMerge.js`: split `insufficient` (layout unverifiable) from `divergent` (judged different); new
  `merge_review` action (owner-confirmed, backup-first) for non-divergent clusters at branding jaccard ≥0.85.
- `fieldZoneVerdict`+`layoutVerdict`: field-mapping target zones DEMOTE to divergent when shared fields sit
  in different places (the different-layout catch), PROMOTE when they agree. Inert when no drawn mappings.
- Richness-first canonical (keeps the landmark-rich row; fixes Thornbury id12→id16).
- `settings/renderer.js`: honest copy, clickable member names (open sample), scary note only for divergent.
- Read-only detector + admin UI only. Live scan now: all 4 groups → `merge_review`.

**Slice 2 — stop duplicate births** (`aba2f46`, kill `TEMPLATE_REUSE_BY_BRANDING` flipped to DEFAULT ON):
- A confirm/teach reuses its existing (branding, slug) template instead of minting. Guards already present.
- Replay: 534 confirmed docs → 482 reuse, **0 cross-supplier false matches**. Safety = reversible link + Part-E.

**Gate:** Phillip endorsed; Oracle SIGN OFF WITH CONDITIONS on both — all met. Tests green:
`test_template_merge_plan.js`, `test_template_reuse.js`, + 8 siblings (`test_confirm_supplier_link_guard`,
`test_fingerprint_hygiene`, `test_promote_custom_doctype`, `test_upsert_type_link`, `test_templates`,
`test_graduation_wiring`, `test_reviewservice`, `test_upsert_generic_skip`).

## OWNER TO-DO / OPEN
1. **Review + push** the 2 commits (I did not push).
2. ⚠ **Slice-2 live gate:** on your next app RESTART the flip is live. Confirm a fresh batch of an EXISTING
   supplier and verify it REUSES (no new duplicate template) with the right supplier bind. `TEMPLATE_REUSE_BY_BRANDING=0`
   disables if anything looks wrong.
3. **Run the merges** (optional, when ready): Settings → Templates → Suggested cleanups → "Scan for duplicates"
   → the 4 groups now show a Merge button. Backup-first, owner-confirmed. I did NOT run any merge on the live DB.
   For merge_review, click each member name to view its sample and confirm the field positions match first.
4. **DEFERRED, required before wide rollout** (Phillip): a per-DB IDF/rarity guard in `findByBrandingFingerprint`
   so two suppliers can't collide on 3 generic tokens ("Services Ltd" on one estate). Kill switch is the backstop.
5. Deferred (cheap, optional): page-count divergent signal in `layoutVerdict`.

## RESUME PROBES (read-only, Electron-as-node) in scratchpad
`dupe_probe.py`, `merge_scan.js` (live detector verdicts), `verify_links.js` (doc-assignment audit),
`measure_reuse.js` (Slice-2 cross-supplier replay).
