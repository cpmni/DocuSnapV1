# HANDOVER — 2026-08-26 (resumed the mid-task C2 revision → Oracle re-vet → gate met via census + 2 Chris rounds)

**Branch** `feat/teach-side-overnight` · **HEAD `b08ea49`** · commits LOCAL / NOT pushed (owner reviews then
pushes — standing rule) · **UNCOMMITTED batch: 7 files** (this whole session is uncommitted, nothing flipped).
Context: the previous session's last work was a half-finished, un-vetted revision of the `issuer_sibling_fill`
**C2** safety condition (2 files dirty on disk). This session finished it correctly, ran the Oracle gate, and
verified it on real data + two live Chris rounds.

## TL;DR
- Resumed a mid-task C2 WIDENING of the first-batch letterhead sibling-fill (`issuerSiblingFillService.js`).
  It was buggy (dead keyword arm, un-threaded fingerprint) and changed an **Oracle-signed** safety with no
  logged re-vet.
- Fixed the bugs, ran an **Oracle re-vet → SEND BACK → corrected in place** (replaced the raw keyword arm with
  the app's ONE same-template comparator `convergesByBranding@0.80`; added a mature-sibling identity re-check;
  landed the oracle_log entry).
- **Gate MET**, three ways: real-data census **0 / 1,250,932 cross-supplier convergences** (owner's 1668-doc
  backup); unit PIN h2 with a measured RED-on-old / GREEN-on-new proof; two live Chris rounds (Saltmarsh
  fired-path 0→17, and a full 452-doc CHRISBOT run with **zero wrong-company misfiles across 215 filed**).
- **Also fixed Chris Card 6** (a Put-back tooltip that wrongly said "copies already written to your filing
  folder" on an in-Review sibling-fill/class-fix undo, where nothing was filed yet).
- **Nothing committed or flipped.** `issuer_sibling_fill` stays DARK; the flip is the owner's call and the
  gate is now green for it.

## UNCOMMITTED batch (7 files — all this session)
```
 M src/services/issuerSiblingFillService.js   # the corrected C2
 M src/services/reviewService.js              # thread keyword_fingerprint into the pre-claim src capture
 M src/lib/reviewEvents.js                    # add 'issuer_fill' to the ledger KINDS set
 M src/windows/review/renderer.js             # Card 6 — kind-aware Put-back copy
 M src/services/test_issuer_sibling_fill.js   # PIN h (branding admit) + h2 (collision gate) + h3 (mature id)
 M docs/oracle_log.md                         # the SEND-BACK-then-corrected re-vet entry
 M docs/CHRIS_FULL_APP_REVIEW_2026-08-25.md   # Chris Round 4 (Saltmarsh) + Round 5 (CHRISBOT)
```
(The `??` untracked files in `git status` — SECURITY_HARDENING_REPORT, TEACH_POISONING_ARC, 3 `test_*.js` —
are PRE-EXISTING from earlier sessions, not this session's work.)

### The C2 correction — root cause + fix
- **What it is:** `issuer_sibling_fill` (DARK, env `ISSUER_SIBLING_FILL` / setting `issuer_sibling_fill`) fills
  the QUEUED same-layout siblings when a human confirms ONE `letterhead_prefill` hold, so File-All-Ready
  offers a new supplier's whole first batch after one confirm (Chris's #1 friction). Original C2 (committed
  `4aa5075`, Oracle-signed) proved "same layout" by a TIGHT logo phash ≤4.
- **The mid-task widening (was on disk, un-vetted):** logo ≤13 **OR** `keywordOverlap ≥0.60` — because real
  same-supplier scans spread logo-phash 0–28 (`project_logo_hash_unreliable`), so ≤4 barely fires. Two bugs:
  `_KW_MIN = templates.KEYWORD_THRESHOLD` = **75** (a 0-100 percent) vs `keywordOverlap`'s 0..1 fraction →
  keyword arm DEAD; and `reviewService` never threaded `src.keyword_fingerprint`.
- **Oracle re-vet = SEND BACK** (docs/oracle_log.md, 2026-08-25 retry entry). Catches: (1) the "mirrors
  handler.js:1635" premise was FALSE — that 0.60 is an AND-guard on top of logo≤13, never a standalone OR;
  the app's real logo-independent same-template arm is `findByBrandingFingerprint`@0.80 over DISTINCTIVE
  tokens (`branding_fingerprint.js`, the "ONE source of truth"). (2) raw `keywordOverlap` re-opens the
  two-senders-same-garble collision in the weakest form (asymmetric → a short subset sibling scores 1.0; no
  distinctiveness filter; garbled NAME tokens shared by construction). (3) admit CLEARS the note → a wrong
  admit is a human File-All rubber-stamp.
- **Corrected in place (all 3 conditions):** keyword arm → `brandingFp.convergesByBranding(src, sib, 0.80)`
  (distinctive tokens, ≥3 shared floor, SYMMETRIC ratio); mature-sibling identity re-check
  (`supplierNamesDisjoint(C, establishedIdentity(db, template_id, r.id))` → refuse a foreign mature
  template); oracle_log entry landed + the false "Oracle REVISED" comment removed. Logo arm stays ≤13.

## Verification state — honest
- **Whole `test_issuer_sibling_fill.js` suite GREEN** (Electron-as-Node): original PINs + new PIN h (branding
  admits a logo-drifted genuine sibling), **PIN h2 (the mandated collision gate: genuine admitted, both a
  subset-collision and a name+boilerplate-collision refused)**, PIN h3 (mature disagreeing identity refused).
- **RED→GREEN proven on the REAL modules** (not asserted): old raw `keywordOverlap≥0.60` ADMITTED both
  collisions (subset 1.000, boiler 0.833 → misfile); `convergesByBranding@0.80` REFUSES both (ratio 0.375,
  0.625) while keeping the genuine sibling (1.000).
- **Real-data collision census** (`scratchpad/branding_collision_census.js` on
  `TESTING/_measure/live_backup_20260815_night_start.db`, 1668 confirmed docs / 11 suppliers): CROSS-supplier
  pairs **0 / 1,250,932 converge@0.80 (0.000%)**; SAME-supplier **87.1%** converge. i.e. a garble-collision
  cannot slip the branding arm on real data, and genuine siblings fire strongly.
- **`test_reviewservice.js` + `test_reviewservice_refile.js` GREEN** (edit-2 safety). `renderer.js`
  `node --check` OK.
- **Chris Round 4 (Saltmarsh, no-teach):** confirm 1 → File-All-Ready **0 → 17 filed**, correct folders,
  sender saved (not the Bill-To); cross-supplier (Saltmarsh+Thornbury) never crossed, incl. 3 look-alike-badge
  Thornbury docs the sweep refused; Undo clean; all warnings true.
- **Chris Round 5 (CHRISBOT, 452 scans, ~67 min):** **215 filed, 0 wrong-company misfiles** (206/206
  machine-checked), every bad/degraded read HELD not filed. IMPORT cold **0/200** → IMPORT2 warm **98/200
  (49%)**. LEAK CHECK (main session, independent): owner's real `Documents\Scan Finder` = 0 files written in
  the last 8h — nothing left the sandbox.
- **Sibling-fill nuance:** in Round 5 Chris TAUGHT suppliers first, so IMPORT docs were KNOWN (template read,
  not `letterhead_prefill` holds) → the sibling-fill's trigger was bypassed, NOT a regression. Round 4
  (no-teach) fired it. The feature is scoped to a NEW, untaught supplier's first batch.
- **NOT run:** `run_all_suites.py` / the realdoc corpus — the change is confirm/file-time + a UI-copy edit,
  none of it on the extraction/`isAutoFileEligible` READ path the corpus measures (byte-identical by
  construction; the predicate is pinned by h2/h3 + the census).
- **Corrected mid-session claim:** I twice told the owner the Chris sandbox was "still up" — it auto-exited
  after each run (both harmless). Do not assume a launched Electron stays up across turns; re-check the port.

## FIRST ACTIONS for the fresh session
1. **Review the 7-file uncommitted diff** and, if happy, commit (owner then pushes — origin is behind).
2. **Flip decision on `issuer_sibling_fill`:** the gate is MET (census 0/1.25M + PIN h2 + RED/GREEN + Chris
   R4 fired-path + Chris R5 zero-misfile). Owner decides DARK→on. To turn on: the setting `issuer_sibling_fill`
   = `true` (or env `ISSUER_SIBLING_FILL=1`).
3. **Chris vet queue (NONE a safety issue; nothing implemented — owner vet):**
   - **Card 1 (MOD-HIGH)** — typing a field fix files that doc but doesn't teach WHERE the field sits (Pelican
     piled up 41 blank). Wants a "draw a box to fix the next ones too" nudge. Biggest friction.
   - **Card 2 (MOD)** — buyer-issued PO on the user's own letterhead → no steer (the known buyer-issued class).
   - **Card 3 (MOD)** — first cold batch self-files 0/200 → expectation gap (batch 2 hit 49%); wants a
     one-liner, NOT more-aggressive filing.
   - **Card 4 (MOD)** — Confirm & File with no sender scatters into "Unknown-Company" (Chris's own bulk-
     confirm); offer "these look like Castellan — file there?".
   - **Cards 5/6/7 (LOW)** — "Add 'Quotation'" opens a blank name form (pre-fill it) · **Card 6 FIXED this
     session** (Put-back copy) · practice-run stale hint · draft Terms (legal, owner's domain).

## Deferred / owner-vet queue carried from 2026-08-25 (NOT built)
- The **two DARK detection arcs** `name_dominant_snap` + `branding_strip_reg_boilerplate` (committed `d687f31`)
  — still want a corpus OFF==ON + Oracle ratify before any live default-on flip.
- Card 2/4 from the 08-25 Chris rounds (Terms legal · letterhead-name-over-abstain) — same classes recurring
  in R5 above.

## Needs the USER
- Commit + push the 7 files when reviewed.
- The `issuer_sibling_fill` DARK→on flip (or leave DARK).
- The Chris R4/R5 vet-queue cards (owner triages; do not implement without a go).

## Key facts / paths
- **Real dev app is RUNNING** (I launched it this session at the owner's `! npm start`): CDP **9222, PID
  23228**, on the live `%APPDATA%\ScanFinder\docusnap.db`. Running the current working tree (the 7 uncommitted
  changes are live; `issuer_sibling_fill` still DARK in the real DB so the sweep won't fire; Card 6 copy shows
  in Review). NOTE: `npm` is NOT on this environment's Bash PATH — `! npm …` fails; launch via PowerShell or
  the `node_modules/electron/dist/electron.exe . --remote-debugging-port=NNNN` pattern.
- **Sandbox rebuild** (`/christest` does it): fresh seed `scripts/seed-chris-sandbox.js "<sb>/userData"`;
  arm `issuer_sibling_fill`/`review_activity_strip`/`batch_audit_enabled` via
  `scratchpad/arm_switches.js`; copy corpus in; launch with `DOCUSNAP_USERDATA=<sb>/userData
  ISSUER_SIBLING_FILL=1 … electron.exe . --remote-debugging-port=9223`. Sandbox root =
  `<scratchpad>/chris-sandbox`; driver = `<scratchpad>/chris-driver` (playwright-core); capture =
  `scripts/capture-window.ps1 -OwnerPid <pid>`.
- **CHRISBOT corpus** = `Desktop\CHRISBOT` (452 PDFs: SINGLE 10 teach · IMPORT 200 cold · IMPORT2 200 warm ·
  SCANNED 10 · SCANNED_HARD 10 degraded · DOC SOL 22). Copy into the sandbox — never point Chris at the
  Desktop original.
- **Run the issuer test:** `ELECTRON_RUN_AS_NODE=1 node_modules/.bin/electron.cmd
  src/services/test_issuer_sibling_fill.js`.
- **Traps re-confirmed:** this env's Bash has NO `npm`/`git`/`grep`/`cat`/`ls` — use the PowerShell tool + the
  dedicated Grep/Read/Write tools. Chris transcripts come back EMPTY — his report is transcribed into
  `docs/CHRIS_FULL_APP_REVIEW_2026-08-25.md` (Rounds 4+5). `core.autocrlf=true` (benign LF→CRLF warnings).
- **DB:** live at `%APPDATA%\ScanFinder\docusnap.db`, migration **87**.
