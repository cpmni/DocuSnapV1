# HANDOVER 2026-09-12 — for a fresh session (Fable 5.1)

Read this top-to-bottom before doing anything. It is written to be literal — do exactly what it says; do
not infer state you can verify. Branch `feat/teach-side-overnight`. **Everything built this session is DARK
(seed OFF, byte-identical OFF, in `TEST_SWITCH_KEYS`). Nothing is flipped to a customer default. Nothing is
at risk.** All work is committed + pushed to `origin/feat/teach-side-overnight` (HEAD `7be4128`).

Also read `CLAUDE.md` (project rules) and, for the ACTIVE task, `docs/designs/RIDGEWAY_SKEW_ROOTCAUSE_2026-09-12.md`.

---

## 1. THE ACTIVE TASK (start here) — Ridgeway worksheet ref garble = SKEW

**One doc, fully root-caused, fix designed, NOT built. This is where to resume.**

Exhibit: RidgewayPlantHire worksheet_07 (live doc id **358**, template id **16**), field `reference_number`.
The page prints `WS-73673`; the app shows the wrong `VS-72672` (held for review with a "doesn't appear on
this page" note). The owner kept asking "why are the crops so bad?".

**Root cause (PROVEN, not a guess):** the scanned page is **skewed 1.7°**. The taught box is axis-aligned and
sits ~0.0117 (normalised) ABOVE the tilted text row, so the tight crop reads across the slanted line and
garbles. The shift is below the Stage-0.5 drift floor (`_DRIFT_FLOOR = 0.02`, template_mapper.py:76), so the
drift guard marks the label "stable" and the registration arbiter is skipped → the box is never relocated.
**Proof:** straightening the page by +1.7° makes the box read `WS-73673` @92 clean (see
`docs/designs/RIDGEWAY_SKEW_ROOTCAUSE_2026-09-12.md` → "PROOF" section; measurement script
`<scratchpad>/deskew_test.py`). The live `VS-72672@95` is a STALE poisoned commit from an earlier import —
the current read actually garbles.

**Why the existing fix (the review-bound deskew retry, `deskew_review_retry`, ON) didn't heal it =** its adopt
gate is WHOLE-DOC (adopt straightened only if straightened overall > raw overall). The stale `VS-72672`
commits @95, which inflates raw overall above the correct straightened `WS-73673` @92 → the gate refuses. This
is hypothesis **H2** in the design doc.

**THE FIX to build (DARK):** a FIELD-SCOPED deskew-retry adopt — when a field is flagged/held (the ref has the
ABSENT note) and straightens clean + is now on-page, adopt the straightened read for THAT field even if
whole-doc overall didn't strictly improve; keep the hold note so it never auto-files. In `process_docs.py`
`_deskew_retry_adopt` (~:70) / `_deskew_retry_apply_holds` (~:199). Plus the stale `VS-72672` needs Learning
Repair (it's poisoning the scope).

**NEXT STEP (what the owner was about to approve):** route advisor **gary** to design the field-scoped adopt
+ its test/census, with a BUILD PRECONDITION measurement (run inbox/358.pdf through the FULL process_docs
import path with `DESKEW_REVIEW_RETRY=1`, log `_max_skew`,`_oc0`,`_oc1`, adopted `reference_number` — to
confirm H2 over H1); then **Oracle** vet; then build DARK (own migration = mig 162, next free; own switch;
`TEST_SWITCH_KEYS` → 46; fail-toward-review). Ask the owner before spawning if unsure.

**Do NOT** re-pursue the "wider crop / left-clip" framing — it was WRONG (a raw-render probe manufactured a
clip that doesn't exist; it's a vertical skew offset). Arc A (mig 161) + Arc B (mig 160) below correctly
ABSTAIN on this doc; leave them DARK.

---

## 2. What shipped THIS session (all DARK, committed + pushed)

Three arcs BUILT DARK + one arc DESIGN-ONLY + a Chris vet + two backlog ideas. Each has a design doc under
`docs/designs/` and an entry in `docs/oracle_log.md` (2026-09-11). Advisor+Oracle gated. Pins green.

- **mig 159 `ref_confusable_flag`** (env `REF_CONFUSABLE_FLAG`) — flag a class-outlier letter/digit confusable
  in a REF (e.g. `SO`→`S0`) so it can't silently auto-file. Oracle SIGN OFF W/COND. `docs/designs/REF_CONFUSABLE_FLAG_2026-09-11.md`.
- **mig 160 `filing_sanity_ref_reinstate`** (env `FILING_SANITY_REF_REINSTATE`) — after Gate C flags a
  Stage-0.5 ref winner page-absent, reinstate an on-page, shape-matching, dominant-prefix keyword candidate
  over it (review-bound). Oracle SIGN OFF W/COND. `docs/designs/REF_ARBITER_REINSTATE_2026-09-11.md`. (This is
  Arc B of the Ridgeway pair — it correctly abstains on worksheet_07; the skew fix in §1 is the real answer.)
- **mig 161 `template_code_left_grow`** (env `TEMPLATE_CODE_LEFT_GROW`) — placement-certified wider re-read
  for a true horizontal left-clip of a taught code (Arc A). Oracle SIGN OFF W/COND, Phase 1 review-bound;
  Phase 2 (auto-file) HELD. `docs/designs/CODE_LEFT_GROW_2026-09-11.md`. (Also correctly abstains on
  worksheet_07 — the clip there is vertical/skew, not horizontal.)
- **Below-relocate arc — DESIGN ONLY, NOT built.** `docs/designs/BELOW_RELOCATE_ARC_2026-09-11.md` (Oracle SENT
  BACK v1 → v2 flag-first; two pre-build blockers). The parked Larkspur residual in `pendingfeatures.md` #117.
- **Chris vet** `docs/CHRIS_FULL_APP_REVIEW_2026-09-11.md` — verdict "would keep using it"; 6 finding cards,
  OWNER-VET (nothing implemented). Card 1 (the `SO/S0` confusable) → became mig 159. Cards 2-6 still to vet.
- **Two backlog ideas** in `pendingfeatures.md` (top): teach-wizard pre-offer detected values (barry-scoped);
  disambiguation-picker auto-adopt the confirmed near-miss. Both have the advisor route noted; NOT built.

`TEST_SWITCH_KEYS` is now **45** (`database/dark_switches.js`); the mig-137 reset pin
(`database/modules/test_migration137_test_switch_reset.js`) asserts 45.

---

## 3. HOW TO RUN / TEST (the app is running NOW, armed 45)

- **Dev app is currently RUNNING** with all 45 DARK switches armed (`TEST_BUILD=1 npm start`, mig 161 applied).
  Confirm: `Get-CimInstance Win32_Process -Filter "Name='electron.exe'"` filtered on `*Docusnap*`.
- **The arming dance** (needed whenever a NEW DARK key is added, or to re-arm): the marker `test_build_armed_rev`
  is sticky at `dev`, so `TEST_BUILD=1 npm start` NOOPs and won't arm a new key. To re-arm ALL keys:
  1. Close the app (kill the electron PIDs BY PID via `Stop-Process -Id` — NEVER by name/cmdline substring;
     get PIDs from the Get-CimInstance filter above).
  2. **Owner runs (it's a `!` command — the live DB is blocked to Claude's own script tools):**
     `ELECTRON_RUN_AS_NODE=1 ./node_modules/electron/dist/electron.exe -e 'const D=require("./node_modules/better-sqlite3");const p=require("path").join(process.env.APPDATA,"ScanFinder","docusnap.db");const db=new D(p);const c=db.prepare("DELETE FROM settings WHERE key=?").run("test_build_armed_rev").changes;db.close();console.log("CLEARED="+c)'`
     — want `CLEARED=1`.
  3. Relaunch `$env:TEST_BUILD='1'; npm start` (background, log to scratch). The log prints
     `test switches armed (N row(s); marker dev)`.
- To test the SKEW fix once built: the owner reprocesses the Ridgeway worksheet (doc 358) via the FULL
  Reprocess (NOT the imageless Quick path — `_deskew_retry_should_run` SKIPS `--reextract`). Expect
  `reference_number` = `WS-73673`, held with a "confirm once" note.
- Python engine changes take effect on the NEXT reprocess (Python re-spawns per reprocess). DB migration /
  JS / renderer changes need an app RELAUNCH.
- Run gate tests with **electron-as-node**, e.g.:
  `$env:ELECTRON_RUN_AS_NODE='1'; & ".\node_modules\.bin\electron.cmd" database/modules/test_migration137_test_switch_reset.js`
  Python pins: `py -3.12 python_backend/tests/test_<name>.py`.

---

## 4. GOTCHAS (verified this session — trust these)

- **The live DB `%APPDATA%\ScanFinder\docusnap.db` (a.k.a. `%APPDATA%\Roaming\ScanFinder`) is blocked to
  Claude's sandboxed script tools for WRITES** (the classifier blocks a disable-sandbox write). READS work via
  `ELECTRON_RUN_AS_NODE=1 ./node_modules/.bin/electron.cmd <script.js>` opening `new Database(path,{readonly:true})`
  (that's how the confirmed-history / template-box / landmark queries in `<scratchpad>` were done). Any WRITE
  to the live DB = the owner's `!` command.
- **The app's actual input for doc 358 is `%APPDATA%\ScanFinder\inbox\358.pdf`, which is IDENTICAL to the
  corpus `Desktop\Demo Docs\Ridgeway Plant Hire\worksheet\RidgewayPlantHire_worksheet_07.pdf`** (verified —
  same render, same 1.7° skew). So headless renders of the corpus PDF describe the app's behaviour.
- **`_DRIFT_FLOOR` = 0.02 in template_mapper.py:76 (Stage 0.5)** — DIFFERENT from anchor.py:2901 (0.03, Stage 2).
  Do not "unify" them.
- Reprocessing a doc via the app requires the app (no CDP debug port on the current launch, so Claude cannot
  drive the live UI). To reproduce a reprocess headlessly you must supply the template/format state — see the
  realdoc harness (`stress_test/realdoc_regression.js`, which is CONFIRMED-DOCS-ONLY, so it won't select a
  needs_review doc like 358).
- Scratch measurement scripts live in the session scratchpad (`deskew_test.py`, `inbox358_diag.py`,
  `arcA_diag.py`, `find_ridgeway.py`, the `ridgeway_*.js` DB probes). They are NOT committed (scratch).

---

## 5. OWNER QUEUE / NEEDS APPROVAL (nothing here is done — owner's call)

1. **Build the SKEW fix (§1)** — the active task. gary → Oracle → build DARK (mig 162). Recommended next.
2. **Learning Repair the stale `VS-72672`** on doc 358 (poisoned commit; the true read garbles).
3. **Flip census (owner-owed) before ANY customer default** for migs 154-161: the 605 + Demo corpus OFF-vs-ON
   (`RR_APP_ENV=1 OCR_RENDER_DPI=200`), M=0 + the per-arc gate; zero fires ⇒ stays DARK. None flipped yet.
4. **Chris cards 2-6** (`docs/CHRIS_FULL_APP_REVIEW_2026-09-11.md`) — vet + decide.
5. **Two backlog ideas** (`pendingfeatures.md` top) — route to advisors when wanted.
6. **Below-relocate arc** (design v2, `docs/designs/BELOW_RELOCATE_ARC_2026-09-11.md`) — two pre-build blockers.
7. **Earlier standing queue** (from `HANDOVER_2026-09-11.md`): push decision (DONE — all pushed), the 154/156/157
   flip census runner is prepped at `TESTING/_measure/flip_census_20260911/`.

---

## 6. CONVENTIONS (carry these)
- Design advisors run as subagents: `gary` (Python fix design + tests), `reggie` (regex/validation), `007`
  (spawn general-purpose + Read `.claude/agents/007.md` — placement/geometry), `oracle` (final adversarial vet,
  LAST). Brief each fully (fresh spawn = cold). Log Oracle verdicts to `docs/oracle_log.md`.
- Every extraction/anchoring change ships DARK (seed OFF in a mig + `TEST_SWITCH_KEYS` + env kill switch),
  fail-toward-review, with unit pins that FAIL if the guard is removed + a realdoc M=0 census gate before flip.
- The owner runs live-DB writes + interactive commands via `! <cmd>` (bash). Confirm before outward/destructive
  actions. Second-guess at the six junctures in CLAUDE.md (esp. #4 "am I treating a symptom?" and #6 "verify
  state at the source" — this session's skew re-diagnosis is a case study: a headless probe that TRIMMED a crop
  manufactured a clip that wasn't real; always measure the actual frame).
