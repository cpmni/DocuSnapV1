# HANDOVER — 2026-07-21 (Opus 4.8, 1M)

**Branch:** `feat/reprocess-throughput-autostraighten`
**Last PUSHED commit:** `f08f131` (from the prior late session). **This session added 7 commits, ALL UNPUSHED.**
**HEAD:** `8f41e95`. **Working tree: clean** (only `../Backup/` untracked, ignored).
**Installer:** `dist\ScanFinder Setup 2.0.0-r20260721-1010-581d626.exe` carries the tree **only up to `581d626`** — the 6 commits after it (identity self-poisoning, Search UI, box-width, letter-spacing, audit button) are **NOT in any installer. A REBUILD is required** before the owner can live-test them.
**Uncommitted batch?** No — everything is committed. Nothing to push-decide except the 7-commit stack itself (owner's call).

---

## TL;DR
An extremely long interactive session. Overnight security audit delivered (report on disk, gitignored). Then a live-testing session with the owner produced **5 shipped fixes/features + 1 UI polish**, all kill-switched, red-first-pinned, corpus-checked. A larger **diagnosed-but-not-built queue** remains (the owner asked for several, and the type-detection thread has more slices). The **live DB is being actively churned by the owner's testing**, so mid-session corpus A/B is invalid — always use a frozen snapshot.

**Shipped this session (7 commits):**
1. `581d626` label-as-value fix — a taught 'below' anchor no longer commits its own caption garble.
2. `666258a` identity self-poisoning fix — the supplier-link guard no longer judges a template's identity against the very doc being confirmed (the Copperfield-sticks root cause).
3. `27d54b7` Search window — vertical tool rail + zoom/pan preview + expandable details + arrow-key doc cycling.
4. `5760489` Search rail recycle icon → universal recycling symbol.
5. `1234814` box-width learning (migration 52; **DARK** behind `ANCHOR_MAX_CROP_WIDTH`).
6. `80d532c` letter-spacing type recovery ("PU RC HASE ORDER"→Purchase Order; **default ON** `HEADING_LETTER_SPACING`).
7. `8f41e95` audit-log "View" buttons styled (were bare default buttons).

**Security audit:** `SECURITY_AUDIT_2026-07-21.md` (repo root, **gitignored, local-only**).

---

## Committed this session — detail

### 1. `581d626` — label-as-value fix (the caption-garble commit)
- **Root cause (007 replay, all code-verified):** a correctly-taught 'below' anchor's OCR ladder preview fast-path re-crops the FULL PAGE from the UNCLAMPED box, restoring the caption band the `:525` clamp excluded; `clean_crop_segment` takes the FIRST line = the label garble ('Vetiver 10'≈"Deliver To"), committed as customer_name. Compounded by a swallowed NameError and a dead `keyword_override` merge-hold.
- **Built in Oracle's order A→C→D→B** (E crop-first DEFERRED): ladder clamp → composed reject (bare fuzzy-echo vocab AND window-overlaps-caption-band) → keyword_override one-token hold → NameError fix.
- **Kill switches:** `NAME_HOLD_ADMIT_OVERRIDE`, `LABELLOCK_INLINE_PROVENANCE`, `CAPTION_BAND_REJECT` (all default ON).
- **Verify:** pinned tests green; corpus replay byte-identical ON==baseline; the live incident no longer reproduces after the anchor was re-taught. Plan/merge-gate detail: memory `project_label_capture_plan.md`.

### 2. `666258a` — identity self-poisoning fix (`TEMPLATE_GUARD_SELF_INDEPENDENT`, default ON)
- **Root cause:** the `TEMPLATE_SUPPLIER_LINK_GUARD` confirm-time reinforcement path called `getDominantSupplier`/`establishedIdentity` over ALL confirmed docs **including the doc currently being confirmed**. So when the operator was fixing a mis-assigned identity, the wrong incumbent value voted for itself and the guard "confirmed" the stale identity — a doc re-taught to the correct supplier snapped back (the "committed 1, the other 5 still pick up Copperfield" incident).
- **Fix:** thread `excludeDocId`/`document_id` through `getDominantSupplier(... , excludeDocId=null)` (all-named binds, `(@ex IS NULL OR id != @ex)`) and `establishedIdentity`; both guard arms in `reviewService.js` + `review/handler.js` pass the doc's own id.
- **Files:** `database/modules/learning.js`, `src/services/reviewService.js`, `src/modules/review/handler.js`.
- **Verify:** guard's own pins green; corpus byte-identical. (Renderer/service change — reopen Review / restart app to load.)

### 3. `27d54b7` + `5760489` — Search window overhaul (owner feature request)
- Vertical `#search-scroll-rail` (nav group at top: up/down cycle mirroring Review; tools group: delete / recycle-bin / send-back-to-review) — **top-right buttons kept too**. Expandable right-hand details column via `#sidebar-resizer`. Zoom + pan on the preview (`#preview-img-wrap`; **pan on right-click drag**). Cursor ↑/↓ cycle docs (`cycleSelection`).
- Files: `src/windows/search/{index.html,search-preview.js,search-results.js,search-query.js,renderer.js}`. `5760489` swapped the odd recycle glyph for the universal three-arrow recycling symbol.
- **Verify:** visual/manual only. Renderer — reopen the Search window to load.

### 4. `1234814` — box-width learning (migration 52; **DARK** behind `ANCHOR_MAX_CROP_WIDTH`)
- **Purpose (owner request):** when you draw a box around a value the system learns the box WIDTH; a later, longer value in the same field is truncated by the earlier short box. Remember the **widest** box ever drawn per field and read up to (not beyond) it, capturing any shorter value.
- **Slice 1 (ships LIVE, inert):** migration 52 `field_anchors.max_w_norm REAL NOT NULL DEFAULT 0` + backfill `= w_norm`; `saveAnchor`'s 4 write paths maintain it as a high-water mark (`MAX(COALESCE(max_w_norm,0), …)`; the passive-UPDATE binds the RAW incoming `w_norm`, not the blended value).
- **Slice 2 (DARK):** `anchor._crop_and_ocr(max_w_norm=)` extends the crop RIGHTWARD to `max(w_norm, min(max_w_norm, _MAX_CROP_WIDTH_CAP=0.6))`, LEFT edge fixed. Wired at rigid / label-lock-relocate / drift-relocate; NOT cross-check/registration. OFF ⇒ byte-identical.
- **Files:** `database/index.js`, `database/modules/learning.js`, `python_backend/extraction/anchor.py`; pin `python_backend/tests/test_anchor_max_crop_width.py` (geometry + cap-trap + OCR round-trip; PASS).
- **⚠ Live DB is still at migration 51** — 52 applies on the next app start running current code. **Needs an owner live-test to flip `ANCHOR_MAX_CROP_WIDTH=1`.**

### 5. `80d532c` — letter-spacing type recovery (`HEADING_LETTER_SPACING`, **default ON**)
- **Root cause:** a type heading in a tracked/letter-spaced display font ("PURCHASE ORDER") OCRs as fragmented pseudo-words ("PU RC HASE ORDER"); the between-words-only `\s*` matcher can't match, the type never scores from its own title, and the doc mis-types to a same-logo sibling (the Larkspur PO→delivery-note garble).
- **Fix (Oracle SIGN-OFF-WITH-CONDITIONS):** `detect_document_type` recovers a letter-spaced title on the **top-band leftmost column segment** by EXACT collapsed-equality (`_despaced_heading`), forcing BOTH the 2.0 score AND the exposed `heading` signal (Seam B) so `title_trusted` is set and the whole downstream cascade fixes. **Multi-word-only** guard (so "Work Sheet"→"Worksheet" doesn't bypass the alias mechanism) + min-collapsed-len 5 + trailing-code peel. Built-in NAMES get the same treatment as custom aliases via `name_alias_lc` — the uniformity the owner asked for.
- **Files:** `python_backend/extraction/keyword.py`; pin `python_backend/tests/test_heading_letter_spacing.py` (red-first, OFF byte-identical, FP negatives, ≥2.0 score — ALL PASS).
- **Verify:** positive proof on cached-reprocess of the live garble doc. Frozen-snapshot corpus A/B: the only accuracy delta was **correcting a mis-confirmed PO** — no honest regression.

### 6. `8f41e95` — audit-log "View" buttons styled
- The per-row `.aud-view-btn` in Settings→Audit shipped as a bare default HTML button (violates the "every control ships styled" rule). Added a themed accent-pill rule in `#panel-audit` (`src/windows/settings/index.html`). Div-balance OK. **Reopen Settings to see it.**

---

## Verification state — be honest
- **All pins listed above PASS.** No full JS/py suite sweep was run this session end-to-end.
- **Corpus (`realdoc_regression.js`):** the LIVE DB was churning (docs added/confirmed by the owner mid-session, 153→155+). Any mid-session live A/B is **INVALID**. The clean checks used a **frozen snapshot** (better-sqlite3 `.backup`, or the fakeappdata APPDATA-redirect trick). Under the frozen snapshot the letter-spacing fix showed **no honest regression** (its one delta was a poisoned-GT correction: doc **#190 = LarkspurInteriors_purchase_order_08.pdf is MIS-CONFIRMED as delivery_note** in the live GT).
- **Known PRE-EXISTING test failures (NOT this session — proven pre-existing via git-stash):** `test_authoritative_anchor` (2), plus the 5 catalogued last session (`test_anchor_crop_crosscheck` ×3, `test_late_anchor_rescue` ×7, `test_template_rescue` ×1, `test_field_data_types` silent, `test_identity_fusion`). **Un-triaged.** Don't mistake these for new regressions.
- **Corrected mid-session claim:** the "processed scans not going to the settings output folder" report was **NOT a bug** — Windows `fs.copyFileSync` preserves the source scan's mtime, so filed files show the original scan date, not the copy date. The owner confirmed "sorry, they are going in." (Optional nicety: `fs.utimesSync` to stamp filing time — not built.)

---

## FIRST ACTIONS for the fresh session
1. **Rebuild the installer** from HEAD (`8f41e95`) so the owner can live-test commits `666258a`→`8f41e95` (close the dev app first — EPERM on `better_sqlite3.node`; check `dist\` for a stale artifact). Current installer only has up to `581d626`.
2. **First-run-wizard output-folder bug (NEW, real, unstarted):** the owner reports that when the output folder is set **from the first-run wizard**, files **don't copy on a different PC**. Investigate the wizard's folder-persistence vs the Settings path (does onboarding write `output_folder` the same key/format the filing handler reads? is it a path-shape/permission difference on the second machine?). This is a genuine bug, unlike the mtime non-bug above.
3. **Owner live-test to flip box-width** (`ANCHOR_MAX_CROP_WIDTH=1`) once the rebuilt installer applies migration 52 — draw a wide box on a long value, then a short-box field, confirm the long value is no longer truncated.

---

## Deferred — designed/approved, NOT built (load-bearing conditions kept)
- **Per-template field HIDING** (owner idea, approved): a per-doc-format control in the **Template Manager** to HIDE an existing field on a layout that lacks it (e.g. ITEM/SERIAL on Marlowe worksheets) — removes the "Not found"/format-check noise. **Hard rules:** hide-only, NEVER add a field that doesn't exist elsewhere (superset-locked); **structural roles (issuer/date/ref) can never be hidden**; it's a display/expectation mask, not a data delete.
- **Keyword-per-field** (BACKEND COMPLETE, UI missing): the owner approved adding per-field keywords/label-aliases in the Doc Type manager, feeding the existing `field_label_overrides` (type-scoped `merge_label_overrides`) + `addLabelOverrides` with a **collision warning** + IPCs. **Only the UI is left** — add inputs in the shared DocTypeEditor. Watch for cross-field label clashes (warn, per the design).
- **po_date ownership-flag exemption:** a taught `po_date` anchor that AGREED with the validated date still raised "taught position couldn't be confirmed" because `_anchor_corroborates` compares via `normalise_for_tokens`, which does **not** strip date separators ("22/01/2026" vs "22-01-2026" mismatch). Fix = date-aware compare (`validator.normalise_date`) in the corroboration path; kill switch + red-first pin. (Root-caused via `scratchpad/probe_podate.py`.)
- **Type-detection thread remainder** (through the design gate already, Oracle build order):
  - **Worksheet line-merge (mode 3):** DIAGNOSE **doc-156 A-vs-B first** (Oracle condition) — if the honest outcome is B, ship the **REFUSE/HOLD** variant, NOT a `best_type` flip.
  - **Built-in/custom heading-scoring consistency** sweep.
  - **Buyer-issued "Supplier:→issuer" guard TRACE:** on the Stonegate doc, `_suppress_buyer_seller_issuer` should have dropped "Supplier: Stonegate" (`_IDENTITY_CAPTION_LABELS` includes "supplier") but didn't — needs a trace to see why it didn't fire.
- **`LETTERHEAD_ISSUER` geometry slice flip** (still DARK from the prior session — real corpus 0%→67% correct; flip = owner+Oracle decision).
- **Optional:** stamp filing time on filed copies (`fs.utimesSync`) so Explorer shows the file date, not the scan mtime.

---

## Needs the USER
- Push decision on the 7-commit stack (nothing pushed since `f08f131`).
- Live-test the rebuilt installer: identity fix (Copperfield no longer sticks after re-teach), Search UI, box-width flip, letter-spacing type recovery on real dockets.
- The first-run-wizard folder bug repro details (which second-PC path, was it a mapped/UNC drive?).

---

## Key facts / paths
- **Live DB:** `%APPDATA%\ScanFinder\docusnap.db` (NOT DocuSnap). **Currently at migration 51**; box-width **migration 52** applies on next start with current code. Read-only probe: `sqlite3 …?mode=ro`.
- **DB is actively churned by owner testing** — for any corpus A/B, take a frozen snapshot first (better-sqlite3 `.backup` or redirect APPDATA to `scratchpad/fakeappdata`). The old 213-doc misfile corpus is at `%APPDATA%\ScanFinder\docusnap.backup-20260720-misfile-corpus.db`.
- **Poisoned GT:** doc #190 `LarkspurInteriors_purchase_order_08.pdf` is mis-confirmed as delivery_note — a corpus M_type=1 from this is GT, not a pipeline fault.
- **Kill switches added this session:** `NAME_HOLD_ADMIT_OVERRIDE`, `LABELLOCK_INLINE_PROVENANCE`, `CAPTION_BAND_REJECT`, `TEMPLATE_GUARD_SELF_INDEPENDENT`, `HEADING_LETTER_SPACING` (all ON) · `ANCHOR_MAX_CROP_WIDTH` (DARK/OFF).
- **New pins:** `python_backend/tests/test_heading_letter_spacing.py`, `python_backend/tests/test_anchor_max_crop_width.py`. Run script-style: `cd python_backend && PYTHONUTF8=1 py -3.12 tests/<name>.py`. JS gate tests need Electron-as-Node.
- **Security report:** `SECURITY_AUDIT_2026-07-21.md` (gitignored). Headlines: DB is **not encrypted** (recommend BitLocker/OS-level + optional SQLCipher later, keeping backup/restore workable); licensing self-grant is main-process-only and not offline-fixable beyond current design; PHP-backend hardening (SEC items) stays **inert until IONOS deploy**; OCR text is stored in plaintext in `extractions`/`documents` (same disk-encryption recommendation).
- **Running processes:** the owner's dev app (`npm start`, ~4 electron/python procs) may still be up — **restart it** to pick up migration 52 + the renderer changes (Review/Search/Settings all need a reopen).
- **Scratch probes:** `scratchpad/probe_podate.py` (po_date corroboration root-cause), snapshot helpers under the session scratchpad.
