# HANDOVER 2026-09-11 (morning) — six DARK arcs + the customer_name placement ROOT-CAUSE fix + night flip-gates

Branch `feat/teach-side-overnight`. **Everything below is LOCAL/unpushed** (push is your call). Cold-start for
the next session. Read `docs/oracle_log.md` (7 new 2026-09-10 verdicts) + the per-arc design docs.

## What shipped this session (all DARK, byte-identical OFF, in `TEST_SWITCH_KEYS` → now 42)
Owner live-exhibit loop + a night run. Every arc advisor + **Oracle-gated**, seed OFF, kill switch, pinned.
1. **mig 154 `trust_ref_role_shape`** (`1383bef`) — a REF role verified by learned SHAPE, never 'constant'
   set-membership. Fixes the 20-held-Thornbury-invoices auto-file blocker (a duplicate confirm made
   invoice_number classify 'constant' → every new ref refused). `docs/designs/REF_ROLE_SHAPE_2026-09-10.md`.
2. **mig 155 `anchor_axis_lock`** (`1b4a956`) — additive width-invariant label-column read for below/right
   free-text. **Censused → 0 fires → STAYS DARK** (`a5de4ab`); dormant insurance vs the box-drift class.
3. **mig 156 `name_role_nonname_flag`** (`02dcd93`) — flag+hold a name field whose whole value is a bare
   postcode/email/VAT/IBAN (the `CH1 2HU`-as-customer_name silent misfile). Non-soft note (mig-142 seam).
4. **mig 157 `template_drift_override_guard`** (`55a6271`) — ⭐ the customer_name postcode-drift ROOT CAUSE.
   The Stage-0.5 drift guard fuzzy-matched the address word "Chester" (0.667) as "Customer" → phantom drift →
   relocated customer_name onto the postcode line; the correct name read @95 was discarded. Fix: a
   drift-relocate may only discard the absolute read on a credible label match (taught, or match_score ≥ 0.8).
   Oracle C1: weak match keeps `anchor_stable=False` so the registration arbiter still fires. LIVE-CONFIRMED
   #243 `CH1 2HU`→`Larch & Hollow Cafe Co`. `docs/designs/DRIFT_OVERRIDE_GUARD_2026-09-10.md`.
5. **mig 158 `note_topic_dedup`** (night; fork) — de-dup stacked ref-confusable notes into one (owner: "very
   wordy message"). A topic-aware `composeNote` keeps the higher-rank lane-hold, drops the redundant advisory.
   gary+Oracle SIGN-OFF-W/COND. `docs/designs/NOTE_TOPIC_DEDUP_2026-09-10.md`. **[VERIFY the fork's pin results.]**
6. Note reword (`7bbf48f`) — the filing-sanity soften note is now concise (names the value once).
(Earlier in the session: mig 151 edge-clip-heal, 152 role-disagree-at100, 153 taught-corrob-adopt — see the
prior 09-10 PM handover; those predate this branch head.)

## Night flip-gate MEASUREMENTS (2026-09-10; `TESTING/_measure/night_20260910/GATE_RESULTS.md`)
Reprocessed a COPY of your live DB OFF-vs-ON per arc (read-only; no live-DB writes). **All three live arcs M=0**
(no new silent auto-file on your real docs):
- **drift-override:** 19 Vellum sales orders → ONLY #243 `CH1 2HU`→`Larch & Hollow Cafe Co` (the correction);
  18 byte-identical; 0 new auto-files. The 0.8 floor flipped only the phantom match.
- **non-name:** flags ONLY #243's postcode, spares all 18 real company names (0 false positives; only removes
  an auto-file).
- **ref-role:** 21 Thornbury invoices → 0 changes, 0 regressions.
(The 605 corpus isn't in the reset live DB, so these prove NO-REGRESSION on YOUR docs; the full 605 M=0 + each
arc's census are still owed before a customer flip.)

## ⚑ NEEDS YOUR APPROVAL (morning)
1. **Push** — 7+ local commits (`1383bef`,`1b4a956`,`a5de4ab`,`02dcd93`,`55a6271`,`7bbf48f`, + the mig-158
   note-dedup commit). `! git push` when ready.
2. **The flips** (each DARK, gate data above). Recommended order once you're happy: **drift-override (157)** is
   the highest-value + cleanest (M=0, pure correction) — it makes the non-name guard (156) a rare backstop.
   Each still owes its 605 M=0 + census (below) before a *customer* default.
3. **Per-arc flip census (owner-owed):** 157 = the match_score census (the mig-157 census now logs it) to
   place the 0.8 floor + the 605 M=0; 156 = the accepted_names batch-stall check + the 605 fire census; 154 =
   the live re-judge of any still-held Thornbury + 605 M=0 + new-would-file-ref==GT.
4. **DEV arming is stuck:** the dev arm-marker is `dev`, so `TEST_BUILD=1 npm start` NOOPs and won't arm the
   newest guards (156/157/158). To see them live: run (app CLOSED) `! node -e "const D=require('./node_modules/
   better-sqlite3');const db=new D(require('path').join(process.env.APPDATA,'ScanFinder','docusnap.db'));
   console.log('cleared',db.prepare(\"DELETE FROM settings WHERE key='test_build_armed_rev'\").run().changes);
   db.close()"` then relaunch `TEST_BUILD=1 npm start` (re-arms all 42). The live-DB path is blocked to Claude's
   tools, so this is YOUR command.
5. **Chris NOT run tonight** (session length) — queue it: `/christest` on today's arcs (drift fix, the guards,
   the note). Plus the standing Chris card 7 (backup restore took a FABRICATED device fingerprint — verify
   backupService device-binding).
6. **The parked placement residual** (`pendingfeatures.md`, Larkspur below-relocate): a WORD-LIKE wrong line
   (e.g. "Site Office, Foundry Lane") is NOT caught by the drift-override or non-name guards — still needs the
   Stage-2 below-relocate arc. Don't let it fall off the backlog.

## Gotchas (carried)
- The live DB `%APPDATA%\ScanFinder\docusnap.db` is BLOCKED to Claude's script tools (both read and write fail
  "unable to open database file") even with the app closed — copy `docusnap.db`+`-wal`+`-shm` together to a
  scratch path to inspect; live writes are the owner's `!` commands.
- Dev arm marker `dev` is sticky: once armed, new DARK keys don't arm until the marker is cleared (see #4).
- `_label_score` (direct) ≠ `_locate_anchor`'s composite `match_score` — the drift-override floor is placed
  against the composite (0.667/0.824), not the raw helper.
- `--files-file` for process_docs must be JSON (`["doc1.pdf"]`), not a newline list.
