# HANDOVER — 2026-08-04 (C6 scorer + taught/jitter arms; 2b/2c flipped; the born-digital finding)

**Branch** `feat/reprocess-throughput-autostraighten` · **HEAD `81a3a83`, PUSHED, tree clean.**
**Installer:** unchanged (no build). **Uncommitted batch?** No. **Running processes:** none.
**Context:** the morning after the 08-03 NIGHT arc (read `HANDOVER_2026-08-03_NIGHT.md` for that).
Owner drove live testing; the day ended with the Oracle-C6 scorer built + its jitter experiment
DEFINING the next arc.

---

## TL;DR
- **SFDEV "broken" = two non-bugs**: Ctrl+Shift+I never existed in this app; the chord's inField
  guard ate keys while focus sat in a field. FIXED (`d642117`): Ctrl+Shift is never text entry.
- **Teach-time word-snap SHIPPED ON** (`698ddec`, setting `teach_box_word_snap`, default ON — the
  read-back display is the gate): the wizard stores word-union boxes; owner sees the snap.
- **NAME_UNCLIP_RECONCILE built DARK** (`c9f253f`): the 'Kingfisher Print Stuc' class (cutting
  Stage-0.5 box silently beats fuller correct reads). Oracle SIGN-OFF-W/COND both built: the
  CUT-GLYPH rule ('Studio'.startswith('Stuc') is FALSE — one edge-glyph substitution at the cut)
  + the 4.5-wordness starvation pin (Python-side note at engine.py ~:5991 fires first on
  lexicon-rich scopes). 23 pins; realdoc no-harm GREEN. **Flip HELD** (see jitter finding).
- **Customer-corpus GT scorer SHIPPED** (`68765cb` + `46a00b9` + `5b2895a` + `81a3a83`):
  `stress_test/customer_corpus_score.js` (+`teach_from_gt.py`) — cold/TEACH/TEACH_JITTER arms over
  the 11,000-doc Desktop corpus, 9 GT lanes, env-switch arms, per-fire census capture.
- **Slice-2 2b + 2c FLIPPED ON** (`universal_verify_numeric`/`universal_verify_flag` = true in the
  live DB): three corpus arms + realdoc all byte-identical — ZERO fires, ZERO flag noise measured.
  Honest caveat: neither gate could FAIL yet (no number-TYPE corpus fields; no cold text-fire
  exhibits) — evidence = pins + quadruple no-harm + measured-zero noise.
- **THE HEADLINE — the jitter crater**: taught arm proves teach-once (cold→taught ref 45→70 ·
  date 68→92 · issuer 0→75 · type 83→100 over 288 docs). But an 18% right-cut on the taught boxes
  CRATERS everything (ref 70→22, date 92→21, issuer 75→0) and the WHOLE shipped heal stack rescues
  ~nothing. Provisional-seeding parity (owner-ordered, built `81a3a83`) proved consent was NOT the
  constraint (crater unchanged; only job_ref 50→64). **Born-digital word-geometry gap = suspect #1**:
  digital 12.5% vs scanned 32.1% under damage — the text-layer path produces NO word boxes, so
  locate/inline/snap/cluster all starve where extraction should be easiest.
- **S2 leak fixed** (`5f9f0d5`): `getFieldFormats` excludes provisional rows by default (they were
  flowing into trust.js's auto-file gates); only the training-file build opts in.
- **C2a decline instrumentation** (`698ddec`): `clip_decline` diag reasons in the SFDEV trace.

## Verification state — honest
- All gate/arm reports READ in-session: realdoc name-unclip arm (0 changed, M 10==10); corpus arms
  b300/uvnum/uvflag (byte-identical trio); t300/t300nu (taught, 0 fires); j120/j120nu/j120s
  (jitter — crater; seeding no rescue). Artifacts in `stress_test/out/customer_score_*.{md,jsonl}`.
- Corrected mid-session claims: (1) "diagnostic logging is OFF" was WRONG — dev builds write
  `repo/Debug/diagnostic_<UTC>.jsonl`, not userData; (2) "drift reconcile is dark" was a stale
  comment (fixed in code 08-03); (3) "consent starvation is the crater's cause" — DISPROVEN by the
  seeded arm.
- NOT verified: live end-to-end provisional seeding on a real teach (believed automatic — the
  wizard commit ends in a confirm — but untapped); docket_10's `clip_decline` reason (owner never
  captured the SFDEV trace); the teach-snap live feel (owner hasn't taught since it shipped).
- "0 heal fires" in scorer censuses is PARTLY an instrumentation gap: mapper-side heals log
  nothing (diag markers only) — job_ref +2 under seeding proves silent fires occur.

## FIRST ACTIONS for the fresh session
1. **Born-digital word-box synthesis** (pendingfeatures 2026-08-04 entry, item 1 — PROMOTED):
   synthesize word boxes from the PDF text layer (pypdfium2 has char positions) so born-digital
   docs feed the word-geometry machinery. Advisors (oscar/007 + gary) → Oracle → build dark →
   gate with the SCORER's jitter arm (j120s baseline = the measuring stick: digital must close on
   scanned, then both climb).
2. Mapper-heal census instrumentation (log lines on A/A2/C2a/B fires) — makes every future arm's
   census real.
3. Owner live checks (below) if the owner is present.

## Deferred (designed, not built) — load-bearing conditions attached
- `pendingfeatures.md` 2026-08-04 entry = the dependency-ordered arc list (born-digital #1,
  census #2, customer-name GT for the corpus #4, vat_no teach-locator + custom-field alias
  seeding #5, digital-ref anomaly, C2b copy, ref_field_key threading, rehearsal read, annealing).
- NAME_UNCLIP flip: HELD. Its corpus lane is structurally absent (no non-supplier name GT);
  flip evidence when resumed = live census + the substrate fixes landing first.

## Needs the USER
- Teach one field live → confirm the box snaps at read-back (`teach_box_word_snap` default ON).
- Reprocess docket_10 with the SFDEV console open → paste the `delivery_number` mapping row's
  `clip_decline` reason.
- Vet the C2b disagreement-note copy (Oracle-approved direction: name both reads).

## Key facts / paths
- **Live DB** `%APPDATA%\ScanFinder\docusnap.db`. Settings flipped true: `universal_verify_restore/
  numeric/flag`, `template_code_edge_clean`, `template_target_word_snap`, `template_code_frag_clean`,
  `template_clip_commit`, `crosscheck_outlier_reconcile` (+ `teach_box_word_snap` default-ON by
  absence). Dark: `name_unclip_reconcile`.
- **Scorer**: `ELECTRON_RUN_AS_NODE=1 SAMPLE=… SET=both TAG=… [TEACH=1] [TEACH_JITTER=0.18]
  [switch envs] "node_modules/electron/dist/electron.exe" stress_test/customer_corpus_score.js`.
  Deterministic sample (SEED). Corpus + `ground_truth.json` on the Desktop.
- **Gotchas**: electron.exe never electron.cmd · never edit mapper py mid-arm · `git add -A` from
  repo ROOT stages `Backup/` (stage explicitly; stale index.lock after a killed add) · dev diag
  logs = `repo/Debug/` · advisor files carry prior-art rule + track records (keep accruing).
