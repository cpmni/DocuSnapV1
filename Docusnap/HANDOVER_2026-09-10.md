# HANDOVER — 2026-09-10 (morning brief; the 2026-09-09 evening session + NIGHT RUN)

Branch `feat/teach-side-overnight`. HEAD **`4bb2efd`**. Code is **PUSHED through `4bb2efd`**. The night's DOC
commits are **LOCAL, UNPUSHED** (push is approval-class in the night protocol — your call in the morning).

**Installer ready to try:** `dist/ScanFinder Setup 2.0.0-r20260909-2024-4bb2efd-TEST.exe` (signed, gates green,
`-TEST` so it arms the DARK arcs). Install it in the morning.

---

## 1. What shipped in the evening session (all PUSHED)
- `7a63555` **Quick-check "Send back to Review"** button (per-row, reuses the Learning-Repair door).
- `d31e0df` **`filing_sanity_confusable_soften` → customer default** (mig 148 @DEFAULT_FLIP) — the phantom O/0
  "doesn't appear as written" message is now a calm, truthful soft note. Census PASS (1 case, 0 wouldFile change).
- `bdc6ffd` **Arc A** — confusable-prefix AUTO-FILE via confirmed prefix history (mig 149, **DARK**). Oracle
  SIGN-OFF-WITH-CONDITIONS C1–C7 folded in (the C2 counter==0 mirror guard, C3 fail-safe). **See §3 — the night
  census says DON'T FLIP it.**
- `19e4b94` **Arc B** — in-view countdown VIEW-SETTLE RECHECK (mig 150, **DARK**). Fixes the WS-95132 "no
  countdown" gap (the sweep's offer was dropped by the presence race; now re-offered on view-settle).
- `4bb2efd` **Quick Reprocess issuer fix** (default-ON, kill `QUICK_IMAGELESS_IDENTITY_PRESERVE=0`). Stops the
  imageless run downgrading a logo-recognized issuer to a text hint @66 (the Ironbridge mass-hold). Transparent.

All pins green; `test_migration137` = 34 TEST_SWITCH_KEYS; release gate OK.

## 1b. IMPLEMENTED TONIGHT (gated) — Slice 0: the tesseract crash fix = the import-after-teach unblocker
The owner asked to **streamline import-after-teach** + authorised gated implementation. barry + gary + Oracle
converged on a surprising root cause: **the propagation the owner wants ALREADY EXISTS and is ON** — the quiet
re-read lane re-reads a taught sender's queued siblings and (at `auto_file_threshold=90`, verified on the install)
auto-files the clean ones. It just **gets WEDGED by the tesseract crash** (no per-call timeout → a child aborting
in libstdc++ hangs on the WER modal → the shard never returns → siblings never merge → the watchdog orphans the
child). So the fix is the crash fix. Oracle: **SIGN OFF WITH CONDITIONS**, GO tonight — fail-toward-review +
byte-identical. Built:
- **`SetErrorMode(0x8003)`** at both worker entries (`process_docs.py` `_suppress_windows_error_dialogs`,
  `ocr/region.py` main) — kills the WER modal so an aborting tesseract child fails FAST, not hangs. Bonus
  (Oracle): pytesseract kills the child before raising, so this **also largely closes the ORPHAN class** —
  oscar's Job Object #5 becomes defense-in-depth, not primary.
- **Per-call `timeout=OCR_CALL_TIMEOUT` (generous 120 s, env-tunable, NEVER tighten — Oracle S0-2)** on every
  crash-path pytesseract call (`ocr/tesseract.py` full-page + ocr_image, `ocr/region_core.py` field-crop ladder +
  multiline, `ocr_region.py` zone, `ocr/segmentation.py`), each guarded so a timeout/abort → **empty read →
  per-field MISS (held), never a silent wrong value**.
- Pin **`test_ocr_timeout_backstop.py` 12/12** (monkeypatch: a raise → empty at every site, never propagates;
  source-shape: no untimed crash-path call can be reintroduced; SetErrorMode present).
- **Validation:** byte-identical 123-doc sweep re-run (S0 vs pre-S0 — see `consensus_s0.jsonl`); Hard Set cold
  0/0 (pre-S0). **⚠ S0-4 (owner-gated):** the pins + byte-identical prove NO-REGRESSION, NOT "crash fixed" — the
  serial harness cannot reproduce a CONCURRENT-spawn abort; prove the crash fix with your `-TEST`-vs-release
  concurrent repro (HANDOVER_2026-09-09_EVENING §1).
- **NOT done (logged for morning):** `timeout=` on the 3 PERIPHERAL OCR sites (`heading_reread`/`landmarks`/
  `orientation`) — not the crash path; SetErrorMode + their existing guards cover them; `image_to_osd` has a
  `timeout=` TypeError risk. A tidy-up to complete source-shape coverage, not urgent.

### Slice 1 (the one-click "re-read the rest now") — DESIGNED, LOGGED, NOT built
gary + Oracle: reuse the existing `reprocess-batch` machinery, server-scoped population from
`quietLane._candidates`, **shard=1 holding `_reprocessThreadCap` (byte-identical, verified)**, wired to the teach
follow-up card, DARK flag `teach_reread_now_enabled`. **THE SEAM (Oracle S1-3, why I did NOT rush it):** the
reprocess merge hardcodes `via:'manual'` (release-if-reliable), which DROPS the unconditional first-fill hold the
layout lane relies on (`quietLane.js:456-458`) — a single clean layout-population read could be released +
offered for filing. Must carry per-doc `via` into the holds (S1-3) + file only via the consent bar, never
`scheduleScopeAutoAccept` (S1-4). Build in the morning with S1-1..S1-4; the full design is in the Oracle log.
Oracle's DO-LESS note: Slice 0 + surfacing the lane's existing (unshown) `job_start`/`doc_done` events as a
"N docs re-reading" signal is most of the win; Slice 1 only additionally helps *continuous active use*.

## 2. 🔴 NIGHT-RUN TOP FINDING — a real silent wrong-date auto-file (M)
The 123-doc anomaly sweep found **two Copperfield sales_order docs (#77/#78) that would AUTO-FILE a WRONG date,
high confidence, no flag** — the cardinal sin. 007 root-caused it (`docs/designs/DATE_LEFT_CLIP_M2_2026-09-09.md`):
- **#78** is real at product 200 DPI (`11/10`→`01/10`); **#77** is a 300-DPI-only artifact (correct at 200). So
  **M=1 on a new install (ocr_dpi=200), M=2 on an older/upgraded install (300 default).**
- Root cause = **`teach_angle_compose_scan`** (a shipped default) shifts a narrow date box right ~11–15px on a
  sibling more tilted than its teach sample → the left edge **clips the leading day digit** → `01`/`09`, valid-
  shaped + confident + wrong. Every existing flag/gate misses it, and the one gate that WOULD catch it
  (`trust_role_disagreement_refuse`) is **bypassed at overall_confidence==100**.
- **Fix (approval-class, NOT built):** smallest = run the role-disagreement refusal at overall==100 (decouple it
  from `strict_100_autofile`). Deeper = fix why overall emits 100 when a required field is 94; and wire mig 143
  `template_pad_date_adopt` to a keyword-corroborated pad disagreement so the CORRECT date is filed. Full design
  + test plan in the design doc. **Blast radius is general** (any narrow date box on a tilted sibling) — worth
  fixing before a wide rollout.
- **FIX DE-RISKED (A/B measured tonight):** I ran the existing `strict_100_autofile` (a BROADER proxy for 007's
  narrower decoupling) over the live sweep. Result: **#78 now HELD** (`reason=disagreeing-read:order_date`),
  **exactly 2 docs newly held (#77+#78, both wrong), ZERO correct docs newly held, M=0.** So even the broad
  version catches the bug with no collateral on this corpus; 007's targeted fix is at least this safe. Validate
  on the full 605 before flipping, but the direction is proven. (`consensus_strict.jsonl` vs `consensus.jsonl`.)

## 3. Night-run — the other results (all read-only; nothing built)
- **Arc A flip census (Oracle C6/C7) — VERDICT: DO NOT FLIP.** 6 real ≥5-confirmed graduated scopes (non-
  vacuous), M=0, the adversarial mirror correctly refuses — BUT its ONE confusable target (#45) carries a
  keyword page-family disagree, so `trust_role_disagreement_refuse` holds it regardless of the note → net
  arc-live auto-files = **0**. A is correct but vacuous; the confusable case is correctly held by the disagree
  gate + calmed by the mig-147/148 softener (already shipped). Keep A DARK, don't flip.
  (`TESTING/_measure/night_20260909/A_CENSUS_RESULT.md`.)
- **Born-digital `bd_used:false` (gary) — DEAD END for a code fix.** `bd:true` means the feature was ARMED, not
  "detected"; `bd_used:false` = no usable text layer → OCR fallback (correct). The gate is right; genuinely-
  digital PDFs already read exact text (no O/0). **Do NOT loosen the gate.** The one real gap = the born-digital
  USE path is UNTESTED on real PDFs (all corpus/harness docs are image-only). Worthwhile: add an Oakhaven-PO
  corpus test-arm asserting `bd_used:true` + M=0 (test-only, not code).
- **Second sweep (147-doc corpus subset) — 4 M were POISONED GT, not bugs.** #42/#25/#27/#40 (Ironbridge
  invoice) flagged "wrong date", but every FRESH read MATCHES THE PAGE exactly (`26/06`, `19/10`, `28/01`,
  `15/09`) via anchor_crop/inline @98 — HEAD reads them CORRECTLY; the **confirmed GT is the clipped/wrong value**
  (`06-06`, `09-10`, `03-01`, `09-09`) baked in by past confirms (the exact poison flagged in the 09-06/09-07
  handovers). → **Learning Repair these 4** (data hygiene; they poison date learning). Also 1 sales_order
  `customer_name` miss (20/21) — non-filing field, low harm. **No new REAL M beyond #77/#78.**
- **Field accuracy: 100%** across every field/type + issuer fill on the live sweep (only the 2 sales_order dates
  wrong, both the same template-mapping compose clip).
- **CALIBRATION — ~10% of held docs read CORRECTLY** (filing-critical fields all high-conf-correct) but are held
  for review-volume reasons (`TESTING/_measure/night_20260909/CALIBRATION_FINDING.md`). Top sub-class = a
  **verified auto-file/UI ASYMMETRY**: ~6 docs (overall 100, every field correct) held by the stale *"couldn't
  match this document to the supplier's saved layout"* note — which the **renderer suppresses on display**
  (`_STALE_TYPE_NOTE`, renderer.js:2503) once the type is confirmed-valid, but the **auto-file gate does NOT
  strip**. Copperfield has 21 confirmed worksheets + 21 POs → the type is valid → the note is stale → the UI
  says "fine" while the gate holds it. Any confirm-but-never-teach-a-template supplier hits this. A softer
  sub-class: ~5 sales_orders at overall 82–83 with every role @94–98 correct, dragged below floor by a date
  soft-note (mig-142 / overall-calibration territory).

## 4. NEEDS YOUR APPROVAL (morning)
1. **PUSH** the night's doc commits (this handover + the design docs + oracle_log + NIGHT_RUN ledger). Push is
   approval-class; they're local until you say go.
2. **Build the M=2 date-clip fix** (§2) — the smallest (role-disagreement at overall==100) via the
   reggie/gary + Oracle gate. Highest priority: it's a silent wrong-file class, general blast radius.
3. **Flip decisions:** Arc A = DON'T flip (census says vacuous). Arc B (`sweep_inview_recheck`) = your call —
   it's armed in the `-TEST` build, try the on-view countdown cadence first. `filing_sanity_confusable_soften`
   already a default.
4. **oscar tesseract timeout fix** (HANDOVER_2026-09-09_EVENING §1) — the per-call `timeout=` + `SetErrorMode`.
   A timed-out doc is a detection failure (the 2 Ironbridge dockets errored this way). Build #1 first; approval-
   class. B was queued behind it.
5. **Born-digital test-arm** (§3) — additive test, no code. Low-risk; do whenever.
4b. **Fix the type-note auto-file/UI asymmetry** (calibration §3) — run the `_STALE_TYPE_NOTE` suppression in the
   auto-file gate too (or self-heal the note when the supplier has confirmed docs of that type, not only a taught
   template), so docs that read perfectly and whose type the app treats as valid can auto-file. Safe direction;
   approval-class. The single biggest review-volume reducer the night found.
6. **Recover the 17 Ironbridge docs** — a normal (full) Reprocess re-matches the logo + restores the ready issuer
   (the fix stops future degradation but doesn't un-degrade the current 17).
6b. **Learning Repair the 4 poisoned-GT dates** — Ironbridge invoice #42/#25/#27/#40 have CLIPPED dates baked
   into their confirmed values (`06-06` should be `26/06`, etc.); send them back to Review + re-confirm the
   correct date so they stop poisoning date learning. (These are on the 147-census corpus, not your live app.)
7. **Chris** — deferred to a clean morning `/christest` (a second OCR-heavy app instance would have contended
   with the sweeps + the tesseract crash class).
8. Carried from before: security vet (backup restore accepted a fabricated fingerprint), client/cert-tool E44
   finish, the 605+Hard-Set full sweep (needs a corpus DB build — tonight ran the 123-doc live corpus + a 147
   census subset).

## 4c. Regression + validation checks (read-only, tonight)
- **Hard Set cold, BOTH arms (400 adversarial docs): wrong+would-file = 0 across all 10 classes; 0 silent-wrong;
  0 `[filed]` misfiles.** No regression on the adversarial digital OR scan corpus — the "no regression" guarantee
  holds on HEAD. (`hardset_digital_cold.log`, `hardset_scan_cold.log`.)
- **M=2 fix refined by A/B:** the BROAD `strict_100_autofile` catches #78 with 0 cost on the live 123-doc sweep,
  but on the 147 corpus it over-blocks **8 correct docs** — so **use 007's NARROW fix** (decouple only the
  `_pageFamilyDisagrees` check at overall==100, NOT the full docTrustGate). The narrow fix avoids those 8 (they're
  held by other docTrustGate conditions, not disagreements). Implement the narrow version + its own A/B before flip.

## 5. Measurement artifacts
`TESTING/_measure/night_20260909/` — `harness.log` + `consensus.jsonl` (123-doc sweep), `A_CENSUS_RESULT.md`,
`harness147.log`/`consensus147.jsonl` (147-doc corpus subset). Design docs: `DATE_LEFT_CLIP_M2_2026-09-09.md`,
`CONFUSABLE_PREFIX_AUTOFILE_2026-09-09.md`, `NIGHT_RUN_2026-09-09.md`. Oracle log updated (2 entries).

## 6. 2026-09-10 — edge-clip heal + M=2 belt BUILT DARK + `-TEST` build + Chris vet
Branch `feat/teach-side-overnight`, pushed `4bb2efd..bfbac84`. Built + pinned (all DARK, byte-identical OFF,
flip owner-gated): **mig 151 `template_edge_clip_heal`** (007 root cause + Oracle SIGN-OFF-W/COND C1-C7,
`docs/designs/EDGE_CLIP_HEAL_2026-09-10.md`) — placement-certified taught-box edge-clip CODE recovery, adopts
capped 87, C5 keyword-witness floor relax in trust.js; NAME half deferred. **mig 152 `role_disagree_refuse_at100`**
(the `DATE_LEFT_CLIP_M2` recommended fix, Oracle C6/C7) — runs the `_pageFamilyDisagrees` role leg at
overall==100 via a new docTrustGate `roleDisagreeOnly` mode; catches the silent wrong-date auto-file. Also the
SFDEV trace-console → foreground-doc-only fix (`f89fb63`). Pins: `test_edge_clip_heal.py`,
`test_role_disagree_refuse_at100.js` (both green; scope_trust + sibling pins green; release gate 36 DARK keys).
Click-through build: `dist/ScanFinder Setup 2.0.0-r20260910-1001-bfbac84-TEST.exe` (`-TEST`, all switches arm at
first launch; parents `template_pad_window_code`/`_labelled`/`teach_angle_compose_scan`/`trust_role_disagreement_refuse`
are PROVEN_ON so the full chain is live on a fresh DB).

**FLIP GATES owed before any customer default (both arcs):** realdoc M=0 + `wouldAutoFile(ON) ⊆ wouldAutoFile(OFF)`;
edge-clip also needs the fire census (every adopt == page truth) + the constructed adversarial neighbour set →
Oracle. The `-TEST` corpus for edge-clip must be SKEWED SCANNED dockets (born-digital Demo Docs never clip).

**Chris vet 2026-09-10** (`docs/CHRIS_FULL_APP_REVIEW_2026-09-10.md`; sandbox 9223/PID 18708, both arcs armed):
verdict **YES**. The M=2 belt WORKED in-experience — 2 overall-100 docs held that would have filed silently,
correct values shown. Every destructive warning told the truth. Vet queue (owner decides; nothing implemented):
1. **[our new surface] Finding 1** — the graduated-scope HELD-doc SUMMARY says "a value was flagged by a formatting
   check" for what is actually a straightening RE-READ (or a name flag); wording only, the per-field note is
   correct. Highest-signal fix, lands on the new safety surface.
2. Finding 2 — the name-wordness check flags "Stonegate Property Mgmt" ("Mgmt" not name-like) → false-alarm; add
   common business tokens to the name-like set (synthetic-data caveat).
3. Finding 3 — auto-filed docs show "100% confidence" while fields read 80-82% (learned-layout trust vs field read;
   recurring 09-09 F3/F5).
4. Finding 4 — a solo owner can't send for approval + the message says "grant stamping" not "add a colleague".
5. Finding 5 — Review activity chips show four non-reconciling counts (recurring).
Coverage gap: Chris never hit a CODE recovered at LOWER confidence (born-digital demo has no skew clip), so the
edge-clip heal's own presentation is UNTESTED in-experience — needs skewed scanned dockets. 09-09 F1 (phantom
Format check) + F6 (separator copy) verified FIXED.
