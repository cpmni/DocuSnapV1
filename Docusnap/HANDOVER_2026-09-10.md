# HANDOVER — 2026-09-10 (morning brief; the 2026-09-09 evening session + NIGHT RUN)

Branch `feat/teach-side-overnight`. HEAD **`4bb2efd`**. Code is **PUSHED through `4bb2efd`**. The night's DOC
commits are **LOCAL, UNPUSHED** (push is approval-class in the night protocol — your call in the morning).

**Installer ready to try:** `dist/ScanFinder Setup 2.0.0-r20260909-2024-4bb2efd-TEST.exe` (signed, gates green,
`-TEST` so it arms the DARK arcs). Install it in the morning.

---

## 1. What shipped in the evening session (all PUSHED)
- `7a63555` **Quick-check "Send back to Review"** button (per-row, reuses the Learning-Repair door).
- `d31e0df` **`filing_sanity_confusable_soften` → customer default** (mig 148 @DEFAULT_FLIP) — the phantom O/0
  "doesn't appear as written" message is now a calm, truthful soft note. Census PASS (1 case, 0 wouldFile change).
- `bdc6ffd` **Arc A** — confusable-prefix AUTO-FILE via confirmed prefix history (mig 149, **DARK**). Oracle
  SIGN-OFF-WITH-CONDITIONS C1–C7 folded in (the C2 counter==0 mirror guard, C3 fail-safe). **See §3 — the night
  census says DON'T FLIP it.**
- `19e4b94` **Arc B** — in-view countdown VIEW-SETTLE RECHECK (mig 150, **DARK**). Fixes the WS-95132 "no
  countdown" gap (the sweep's offer was dropped by the presence race; now re-offered on view-settle).
- `4bb2efd` **Quick Reprocess issuer fix** (default-ON, kill `QUICK_IMAGELESS_IDENTITY_PRESERVE=0`). Stops the
  imageless run downgrading a logo-recognized issuer to a text hint @66 (the Ironbridge mass-hold). Transparent.

All pins green; `test_migration137` = 34 TEST_SWITCH_KEYS; release gate OK.

## 2. 🔴 NIGHT-RUN TOP FINDING — a real silent wrong-date auto-file (M)
The 123-doc anomaly sweep found **two Copperfield sales_order docs (#77/#78) that would AUTO-FILE a WRONG date,
high confidence, no flag** — the cardinal sin. 007 root-caused it (`docs/designs/DATE_LEFT_CLIP_M2_2026-09-09.md`):
- **#78** is real at product 200 DPI (`11/10`→`01/10`); **#77** is a 300-DPI-only artifact (correct at 200). So
  **M=1 on a new install (ocr_dpi=200), M=2 on an older/upgraded install (300 default).**
- Root cause = **`teach_angle_compose_scan`** (a shipped default) shifts a narrow date box right ~11–15px on a
  sibling more tilted than its teach sample → the left edge **clips the leading day digit** → `01`/`09`, valid-
  shaped + confident + wrong. Every existing flag/gate misses it, and the one gate that WOULD catch it
  (`trust_role_disagreement_refuse`) is **bypassed at overall_confidence==100**.
- **Fix (approval-class, NOT built):** smallest = run the role-disagreement refusal at overall==100 (decouple it
  from `strict_100_autofile`). Deeper = fix why overall emits 100 when a required field is 94; and wire mig 143
  `template_pad_date_adopt` to a keyword-corroborated pad disagreement so the CORRECT date is filed. Full design
  + test plan in the design doc. **Blast radius is general** (any narrow date box on a tilted sibling) — worth
  fixing before a wide rollout.

## 3. Night-run — the other results (all read-only; nothing built)
- **Arc A flip census (Oracle C6/C7) — VERDICT: DO NOT FLIP.** 6 real ≥5-confirmed graduated scopes (non-
  vacuous), M=0, the adversarial mirror correctly refuses — BUT its ONE confusable target (#45) carries a
  keyword page-family disagree, so `trust_role_disagreement_refuse` holds it regardless of the note → net
  arc-live auto-files = **0**. A is correct but vacuous; the confusable case is correctly held by the disagree
  gate + calmed by the mig-147/148 softener (already shipped). Keep A DARK, don't flip.
  (`TESTING/_measure/night_20260909/A_CENSUS_RESULT.md`.)
- **Born-digital `bd_used:false` (gary) — DEAD END for a code fix.** `bd:true` means the feature was ARMED, not
  "detected"; `bd_used:false` = no usable text layer → OCR fallback (correct). The gate is right; genuinely-
  digital PDFs already read exact text (no O/0). **Do NOT loosen the gate.** The one real gap = the born-digital
  USE path is UNTESTED on real PDFs (all corpus/harness docs are image-only). Worthwhile: add an Oakhaven-PO
  corpus test-arm asserting `bd_used:true` + M=0 (test-only, not code).
- **Second sweep (147-doc corpus subset) — 4 M were POISONED GT, not bugs.** #42/#25/#27/#40 (Ironbridge
  invoice) flagged "wrong date", but every FRESH read MATCHES THE PAGE exactly (`26/06`, `19/10`, `28/01`,
  `15/09`) via anchor_crop/inline @98 — HEAD reads them CORRECTLY; the **confirmed GT is the clipped/wrong value**
  (`06-06`, `09-10`, `03-01`, `09-09`) baked in by past confirms (the exact poison flagged in the 09-06/09-07
  handovers). → **Learning Repair these 4** (data hygiene; they poison date learning). Also 1 sales_order
  `customer_name` miss (20/21) — non-filing field, low harm. **No new REAL M beyond #77/#78.**
- **Field accuracy: 100%** across every field/type + issuer fill on the live sweep (only the 2 sales_order dates
  wrong, both the same template-mapping compose clip).

## 4. NEEDS YOUR APPROVAL (morning)
1. **PUSH** the night's doc commits (this handover + the design docs + oracle_log + NIGHT_RUN ledger). Push is
   approval-class; they're local until you say go.
2. **Build the M=2 date-clip fix** (§2) — the smallest (role-disagreement at overall==100) via the
   reggie/gary + Oracle gate. Highest priority: it's a silent wrong-file class, general blast radius.
3. **Flip decisions:** Arc A = DON'T flip (census says vacuous). Arc B (`sweep_inview_recheck`) = your call —
   it's armed in the `-TEST` build, try the on-view countdown cadence first. `filing_sanity_confusable_soften`
   already a default.
4. **oscar tesseract timeout fix** (HANDOVER_2026-09-09_EVENING §1) — the per-call `timeout=` + `SetErrorMode`.
   A timed-out doc is a detection failure (the 2 Ironbridge dockets errored this way). Build #1 first; approval-
   class. B was queued behind it.
5. **Born-digital test-arm** (§3) — additive test, no code. Low-risk; do whenever.
6. **Recover the 17 Ironbridge docs** — a normal (full) Reprocess re-matches the logo + restores the ready issuer
   (the fix stops future degradation but doesn't un-degrade the current 17).
6b. **Learning Repair the 4 poisoned-GT dates** — Ironbridge invoice #42/#25/#27/#40 have CLIPPED dates baked
   into their confirmed values (`06-06` should be `26/06`, etc.); send them back to Review + re-confirm the
   correct date so they stop poisoning date learning. (These are on the 147-census corpus, not your live app.)
7. **Chris** — deferred to a clean morning `/christest` (a second OCR-heavy app instance would have contended
   with the sweeps + the tesseract crash class).
8. Carried from before: security vet (backup restore accepted a fabricated fingerprint), client/cert-tool E44
   finish, the 605+Hard-Set full sweep (needs a corpus DB build — tonight ran the 123-doc live corpus + a 147
   census subset).

## 5. Measurement artifacts
`TESTING/_measure/night_20260909/` — `harness.log` + `consensus.jsonl` (123-doc sweep), `A_CENSUS_RESULT.md`,
`harness147.log`/`consensus147.jsonl` (147-doc corpus subset). Design docs: `DATE_LEFT_CLIP_M2_2026-09-09.md`,
`CONFUSABLE_PREFIX_AUTOFILE_2026-09-09.md`, `NIGHT_RUN_2026-09-09.md`. Oracle log updated (2 entries).
