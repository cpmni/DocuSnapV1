# HANDOVER 2026-07-27 (Opus 4.8 · long live-testing + fix day, owner-directed)

Branch `feat/reprocess-throughput-autostraighten` · **ALL PUSHED through `c0f9434`** (origin `0 0`) ·
working tree CLEAN (only the pre-existing untracked `Backup/` + `Docusnap - Copy*` at repo root) · no
running background processes (the owner's dev `npm start` closed cleanly mid-session) · installer
`r20260726-1018` predates EVERY commit below — rebuild to carry any of it into the packaged app.

## TL;DR
A long owner-driven live-testing day. Nine commits, each kill-switched + advisor→Oracle gated + gated on
a live-DB snapshot (realdoc is BLIND to the JS/renderer paths, so several used live-DB replays instead).
Shipped: the **Northgate PO→Invoice type-flip** + the **decompile-hardening scaffold** (both from the
overnight, pushed at the day's start), then **4 new live-found fixes** — SO-template phash-collision refuse,
field-hiding UNION across duplicate templates, po_number "Order No." reader, and the **template-dedup fix
(Lever 1 prevent + Lever 2 group_id/backfill)** — plus a new CLAUDE.md rule (juncture 6). Two residuals
are flagged honestly (669 ref misread; the deferred template Python tie-break).

## Committed this session (all PUSHED, origin `0 0`)

### `e0b5c04` — Northgate PO→Invoice type-flip (task #5) — Lever 1 + Lever 3, DEFAULT ON
Herald-designed, Oracle SIGN-OFF-W/COND (C1-C6). Lever 1 `HEADING_FUZZY_VOCAB` (fuzzy-to-closed-vocab title
arm beside the exact `_despaced_heading`; difflib ≥0.82 + 0.08 margin) + Lever 3 `KW_TYPE_NONDISTINCTIVE_HOLD`
(subset-fingerprint HOLD, no exact tie). Corpus byte-identical OFF; value accuracy byte-identical, M_type 0,
−6 false-holds. See `project_northgate_type_flip_20260726` + `docs/HERALD_TYPE_DETECTION_REFERENCE.md`.

### `c617230` — decompile-hardening scaffold
Rung C (asar `test_*.js` exclusion) DEFAULT ON + Rung A fuses hook (`scripts/afterPack-fuses.js`, kill
`HARDEN_FUSES`) DEFAULT OFF. `@electron/fuses` (MIT) devDep. Dry-pack verified; check-licenses OK (79
components commercially-free). Deferred rungs B/D/F/E need a live smoke. `docs/BUILD_HARDENING_PLAN_2026-07-26.md`.

### `522749a` — CLAUDE.md juncture 6 (owner rule)
Never assert system STATE (a template/field/setting/column exists or is configured) from an indirect/UI
signal — VERIFY at the source (DB query / grep). Added after I asserted "no Northgate sales_order template"
from a "No template match" flag; a one-line `templates` query showed it existed. Mirrored to memory.

### `93b766c` — SO template match: two-factor the logo-refuse supplier guard (kill LOGO_REFUSE_SUPPLIER_CORROB, ON)
Phillip-measured: correctly-typed sales orders refused "no template match" because the 64-bit logo phash
can't separate suppliers (cross-supplier Hamming 0-6) → nearest logo = a FOREIGN wrong-type template →
trusted-title refuse captures the foreign supplier → the af346d8 C1 guard blocks the correct same-supplier
rescue (id32, kw 1.000). 10/20 roulette. Fix = capture the foreign supplier as a blocker ONLY when its
branding is on-page + a winner-side distinctive-presence gate (all-generic foreign n==0 → HOLDS). Gate:
SO trace 10 refuse→0, realdoc value byte-identical, M_type 0, +9 correct auto-files. Unit test_template_matcher +3.

### `8be9ac5` — field-hiding UNION across duplicate templates (FIELD_VIS_LIVE_RESOLVE)
A worksheet matched an EMPTY duplicate template (id34) while the owner's hidden-field config sat on id33 →
item/serial showed despite being hidden. New `templates.getHiddenFieldsForSupplierType` UNIONs the config
across all (name, type) sibling templates, applied EVEN when a template matched (old code resolved by
name+type only on a no-template MISS). Display-only, fail-safe. Verified live: (Northgate, worksheet)
resolves {item, serial_no} where matched id34 alone gave []. Unit test_field_visibility_resolve +6.

### `9164c28` — po_number "Order No." reader + guard; 007-A full-res re-read DARK
007 pixel-proven (owner rule "look at the SLICES"): doc 669 committed `PO-78309` for a page reading
`PO-78399` — the inline-harvest reads the VALUE from the ~120-DPI DOWNSCALED label-locate pass
(`_prep_for_lines` `_MAX=1100`) → a printed 9 blurs to 0. reggie-A/B (`PO_ORDER_NO_LABELS`, ON): inject
"Order No."/"Order Number" po_number labels IN CODE (config byte-identical) + `_qualified_order_caption`
guard ("our" excluded) + closed a LATENT sales_order_number-grabs-PO leak. **007-A** (`ANCHOR_INLINE_FULLRES_REREAD`,
full-res re-read of the located box) BUILT but shipped **DARK** — its corpus A/B REGRESSED (ref 97.6→86.8%,
silentAutoFile 3→27): re-cropping full-res at the coarse LOW-RES-derived box + re-OCR corrupts. Gate
(reggie-only): accuracy byte-identical, M unchanged, one fewer silent. Unit test_po_order_no_labels.
`project_po_order_no_reader_20260727`.

### `1f0a021` + `c0f9434` — template dedup (Lever 1 prevent + Lever 2 group_id/backfill) — kill TEMPLATE_REUSE_BY_NAME (ON)
Phillip-measured: duplicate same-supplier same-type templates (Northgate id33/34, id30/31, Copperfield
27/28/29) form because ⊕TEACH `_upsertTemplate`'s logo arm grabs the nearest same-slug FOREIGN template
(phash can't separate) + branding misses (0.778<0.80, OCR-garble title tokens pollute the fp) + the CONFIRMED
ISSUER is never consulted for reuse. Oracle SIGN-OFF-W/COND (C1-C6). **Lever 1**: `templates.reuseByEstablishedName`
(exact `establishedIdentity`+slug, NEVER containment, richest canonical, plausibility-gated) wired FIRST in
BOTH birth paths (`_upsertTemplate` + `graduationTemplate.decide`) → prevents new dups. **Lever 2**:
group_id activation — `getHiddenFieldsForSupplierType` resolves group_id ∪ name (additive/inert till
grouped) + owner-run reversible backfill `scripts/template-group-backfill.js` (dry-run default, `--apply`).
Keyed on establishedIdentity NOT cosmetic name (id31 is named "Stonegate Property Mgmt" but is confirmed
Northgate). Gate: `stress_test/reuse_by_name_replay.js` live — 28 identities, **0 cross-identity**, richest
canonical, containment rejected; backfill dry-run reports exactly the 3 clusters. Deferred (Oracle C5):
Python Stage-0 tie-break + JS IDF hardening. `project_template_dedup_root_20260727`.

## Verification state — honest
- All corpus/replay gates were RUN and their report files READ (no "the gate passed" hand-waves).
- Value/type gates on the frozen live-DB snapshot `scratchpad/typegate_so.db` (667 confirmed docs).
- **007-A is DARK because its A/B REGRESSED** (measured, not assumed) — the classic "helped one doc, failed
  the corpus" trap; needs a full-res re-LOCATE (not a coarse re-crop) before it can re-enable.
- **Mid-session corrections I made (own errors):** (a) I told the owner the flagged PO docs were "safe to
  Confirm & File" — WRONG for 669 (its committed ref is a real digit misread); corrected. (b) I asserted
  "no Northgate sales_order template" from a UI flag — WRONG (it existed); this became juncture 6.
- NOT verified: no installer rebuilt; the backfill was NOT `--apply`'d (owner-run by design); nothing that
  needs the packaged app or a live smoke.

## FIRST ACTIONS for the fresh session
1. **Owner action first** (all need the app): restart `npm start` (main-process changes — field-hiding +
   template-reuse — need a full restart, not a Review reopen); reprocess to see the fixes live; optionally
   run the backfill `--apply` to collapse the existing duplicate templates.
2. **669 ref residual** — the taught anchor's low-res misread still WINS over reggie's correct keyword read
   → held with the wrong value shown. The refinement = re-LOCATE the value box at full res (not re-crop the
   coarse box) so 007-A can re-enable; OR a vetted anchor-vs-page-present-keyword precedence change. reggie
   gives the corroborating read already; 007 + Oracle before build.
3. **Deferred template tie-break** (Oracle C5) — Python Stage-0 matcher tie-break-to-group-canonical, so the
   matching roulette between grouped siblings resolves deterministically (config harm already neutralised).

## Deferred (designed/measured, NOT built — with the load-bearing conditions)
- **007-A re-enable** — ONLY after a full-res re-LOCATE (the coarse re-crop regressed the corpus). Never a
  blanket "higher-res"; the ladder/locate must produce a precise value box at full res.
- **Template Python tie-break + IDF hardening** (Oracle C5 deferred) — the config-union already covers the
  config harm; ⊕-teach anchors are (supplier,type)-keyed. IDF/short-token fuzzing = a precision minefield.
- **"Purchase Order No" leading-dot** — the explicit config label keeps a `. ` prefix (pre-existing, minor).
- Unchanged: hardening rungs B/D/F/E (live smoke); caption-strip flip; workflow slices 5+6; H2 pairing.

## Needs the USER
- Run the template backfill `--apply` (reversible) to group the existing duplicates.
- Restart the app + reprocess to see field-hiding + template-reuse live.
- Rebuild the installer to carry today's work into the packaged app + smoke the hardening (arm `HARDEN_FUSES=1`).
- Decide whether to pursue the 669 re-locate refinement / the deferred template tie-break.

## Key facts / paths
- Live DB `%APPDATA%\ScanFinder\docusnap.db`, migration **54**. Frozen gate snapshot with the day's docs:
  `…/scratchpad/typegate_so.db` (open `?mode=ro`).
- Corpus: `ELECTRON_RUN_AS_NODE=1 ./node_modules/electron/dist/electron.exe stress_test/realdoc_regression.js`
  (env kill-switch flags per cell; `RR_DB=<snapshot>` `RR_TYPE_ENUM=<file>`; run WITHOUT `GATE=1` — the
  pre-existing #583 M forces exit 1). Clear `python_backend/**/__pycache__` after a Python edit.
- Live-DB read-only replays (realdoc-blind JS paths): `stress_test/reuse_by_name_replay.js` (template dedup),
  `stress_test/northgate_type_trace.js` (type + po_number, `RR_DB=<db> <ids…>`), `stress_test/type_outcome_report.js`.
- Owner-run backfill: `scripts/template-group-backfill.js` [`--apply`].
- Kill switches added: `HEADING_FUZZY_VOCAB`, `KW_TYPE_NONDISTINCTIVE_HOLD`, `LOGO_REFUSE_SUPPLIER_CORROB`,
  `PO_ORDER_NO_LABELS`, `ANCHOR_INLINE_FULLRES_REREAD` (default OFF), `TEMPLATE_REUSE_BY_NAME`, `HARDEN_FUSES`
  (default OFF). All others OFF ⇒ byte-identical.
- Unit: `py -3.12 python_backend/tests/<t>.py`; JS gates via Electron-as-Node.
- Agents used: Herald, iris (registered); Phillip/007/oscar/reggie/eric/oracle (personas). Memory files
  written for each fix; `[[project_po_order_no_reader_20260727]]` + `[[project_template_dedup_root_20260727]]`
  + `[[project_so_template_phash_collision_20260727]]` are the day's headliners.
