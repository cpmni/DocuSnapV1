# Chris The Customer — full app review — 2026-09-23 (the teach-a-document night)

> **Round header (main session).** Sandbox per `/christest`: fresh install (DB at mig 212, 0 users), ALL SIX
> 2026-09-23 switches armed ON via `arm_switches.py` (`glyph_fallback_enabled` 207 · `glyph_confusable_resolve` 210 ·
> `glyph_confusable_release` 211 · `glyph_slice_integrity` 212 · `corrob_date_fold_wide` 208 ·
> `recon_singlechar_misread_flag` 209), branch `feat/teach-side-overnight` at `625ccc0` (+docs `fd0d82c`),
> corpus = a copy of `Desktop\Demo Docs` (1,475 PDFs), Output `<sandbox>\Output`, CDP 9223, app PID 30400.
> Mission (owner, going to bed): FULL teach-a-doc test — teach, confirm twice in Review, import the rest, count
> auto-files vs holds with reasons, verify no wrong value filed, compare with previous rounds. Chris ran ~67 min,
> 171 tool uses, 80 documents across 4 senders. Report below VERBATIM; screenshots `<sandbox>\shots\s00–s72` +
> `eyes_*.png`. NOTHING implemented from this round — every card queues for the owner's vet. The main session's
> source verification of the load-bearing cards follows the report.

# Chris The Customer — the teach-a-document night — 2026-09-23

*One made-up, non-technical office manager, hands-on in the sandbox you set up (port 9223, app PID 30400, fresh install). Everything below happened inside that sandbox only — nothing outside it was touched, and I changed no code. Screenshots are in `chris-sandbox-20260923\shots\` (`s00`–`s72`, plus `eyes_*.png` which are my own page crops for checking values). All findings are for your morning vet; I implemented nothing.*

## TL;DR (3 lines)
1. **On the two order types it was exactly what you asked for:** teach one, confirm two, and 25 of the next 30 filed by themselves — every one right against the page, every hold with a real reason, and it *caught* four wrong dates and one S-for-5 misread instead of filing them.
2. **On invoices it was the opposite:** the Total I taught (as the tour told me to) is never shown on screen, silently blocks every invoice ("a value was flagged by a formatting check" — no field named, "Overall 100%"), and is dropped from every filed record. 0 of 30 invoices filed by themselves until I switched Total off for the sender; then 25 filed, all correct.
3. **The worst thing that slipped through:** on the Copperfield sales orders, the *Customer* name I taught filed wrong on 6 of 11 that filed themselves — twice it is literally the word "Customer" — and every one of those is labelled "Checked by you" in Search. Numbers, dates and folders were right on all 67 filed documents; the wrong values were only in that taught extra field.

---

## Walkthrough (what I did, with screenshot references)
- **First contact** (s00–s07): created the admin, saved the recovery code (greyed Continue until ticked), accepted the Terms, 7-step wizard (pointed the Output at the sandbox `Output`, "Looks good ✓"), 6-card tour, opened the practice run to its first teach screen. No jargon, nothing wrong. Home now shows "Choose where filed documents are saved" ticked and struck through (the stale tick from 08-23 is gone).
- **Supplier 1 — Vellum & Crane Stationers, invoices** (s08–s30): imported 5 → all "Confirm to file" with the honest reason "The letterhead reads 'Vellum & Crane Stationers' — filled in for you, but please confirm it's the sender, not the customer" (s09). The Invoice type had **no Total** field, so in the Teach wizard I used "Edit this type…" and added "Total" (it guessed Currency on its own, s10). Drew four boxes — company, date, number, total — every read-back exactly as printed, each naming the label it found ("Label: Total Due (left of the value)", s11). Saved in 2 s; the done card said "Confirm 2 more … and the rest … will file themselves — 4 are waiting" (s12). Confirmed two by hand (s15–s16) → group header flipped to "✓ files by itself" — and nothing filed. Imported the other 15 → all 15 held, "Overall 100%", "a value was flagged by a formatting check", no field named, no Total row anywhere (s19–s20). Reprocess one: unchanged (s23–s24). File All Ready: "Nothing is ready to file yet" (s25). Confirmed two more by hand (5 clean confirmations, the Settings number) — still nothing. Switched Total off for this sender ("Change what's read from Vellum & Crane Stationers's documents", s28) and reprocessed one → "Nothing was flagged — Ready to file". Then "Reprocess 15 from Vellum" → Full re-read → **14 filed themselves + 1 labelled "You filed 1"** (s29–s30). All 15 checked against my page crops: right.
- **Supplier 2 — Thornbury Fasteners, invoices** (s31–s42): 5 imported, three of them "Sender not identified" although the letterhead is plain (s32). Taught invoice_03 (four boxes, all right, s33–s35); the teach fixed the identity of the three siblings. Needed **three** hand confirms, not two, before "files by itself". 15 more imported → 13 held by the same nameless "formatting check", 3 held with excellent reasons (see "What worked"). Total off + Full re-read → **11 filed themselves + 1 "You filed 1"**, 12/12 right. Four stayed, all with good reasons — including one **wrong read the app caught** (invoice_06: it read 14-07-2026, the page prints 04/07/2026 — held with "This date couldn't be confirmed anywhere else on the page").
- **Supplier 3 — Saltmarsh Seafoods, purchase orders** (s43–s53; this type has no Total, I added nothing): 5 imported; purchase_order_05 came in as **PO-S0471** — the page prints **PO-50471** (the S/5 look-alike, s44). Taught _01 (three boxes, right). The taught box re-read _05 as **PO-50471** — corrected. Two hand confirms → "1 filed themselves" (that corrected one, filed right). 15 more imported → **"12 documents auto-filed — no review needed"** at import, 3 held with the best notes of the night ("Kept the read value '15/11/2026' — the taught date box read '14/11/9096', which is far in the future"). 16/16 on disk right.
- **Supplier 4 — Copperfield Electrical, sales orders** (s54–s62; type has an optional Customer field, I taught it too): taught _01 (four boxes; read-back correctly said "Label: Customer (above the value)"). Two hand confirms → 1 filed itself; 15 more → **10 auto-filed, 5 held** (two of the five show a wrong date the app kept out of the folder: SO-71797 "01-10-2026" vs page 11/10; SO-93226 "18-01-2026" vs page 08/01). Then I read the sidecar records of the 11 that filed themselves: **6 wrong Customer names**.
- **Scary buttons** (s64–s72): File All Ready (twice), Defer and bring back, Delete one, Delete All Review (read + cancelled), Reprocess one, Re-read all from a sender (twice), recycle bin → Restore all. Table below.
- **Home at the end** (s67): "67 filed today · 4 senders file by themselves · 51 filed by themselves in the last 7 days · 11 waiting". 67 PDFs on disk. Numbers agree.

## Numbers (verified against my own page crops, not the app's word)
| Supplier / type | Imported | Taught (filed) | Confirmed by hand | Filed by itself | Held at the end (reason) | Wrong value filed |
|---|---|---|---|---|---|---|
| Vellum & Crane · Invoice (+Total) | 20 | 1 | 4 (2 asked + 2 extra to test the "5" setting) | **0 of 15 with Total on** → **15 of 15 after Total off + Full re-read** (14 "filed themselves" + 1 mislabelled "You filed 1") | 0 | **0** (number/date on 20/20 right); Total missing from every filed record except the taught one |
| Thornbury Fasteners · Invoice | 20 | 1 | 3 (third was required) | **0 of 15 with Total on** → **12 of 16 after Total off** (11 + 1 "You filed 1") | 4: number box drifted (70%) ×2 · date-box misread kept out ×2 · **wrong date read 14-07 vs page 04/07 — held** | **0** (12/12 right) |
| Saltmarsh Seafoods · Purchase Order | 20 | 1 | 2 | **13** (1 after 2nd confirm + **12 at import**) | 4: "on Saltmarsh's letterhead but names 'Pemberton Joinery' as the supplier — confirm which company" · date-box misread kept out ×2 · straighten re-read note | **0** (16/16 right, incl. PO-S0471 → PO-50471 corrected) |
| Copperfield Electrical · Sales Order (+Customer) | 20 | 1 | 2 (+1 via File All Ready, right) | **11** (1 after 2nd confirm + **10 at import**) | 5: customer "Sandpiper Hotels :" looks like a label (35%) · date 01-10 vs page 11/10 (held) · date 18-01 vs page 08/01 (held — but for the customer, not the date) · date-box misread kept out ×2 | number/date **0**; **Customer wrong on 6 of 11** self-filed |
| **Total** | **80** | 4 | 11 (+1) | **51** | **13** | **0 in company/date/number on 67 filed · 6 wrong Customer names** |

## Previously-reported items — verify
- 08-23 A1 (a document filed itself with the wrong date) — **BETTER.** Four wrong date reads tonight (14-07, 01-10, 18-01 and the boxed "9096"-style ones) and **none filed**; three were held with the date named, one (SO-93226) was held for a different reason — see card 4.
- 08-23 A3 (put back doesn't stick) — **NOT RE-TESTED** (I didn't open a "filed themselves" chip to Put back).
- 08-23 A4 (type-split dead end in Review) — NOT RE-TESTED.
- 08-23 A6 ("✓ files by itself" over a pile that waits) — **STILL THERE** (card 5).
- 08-23 A7 (Home contradicts the strip) — **FIXED.** Home 67 filed = 67 on disk; "51 filed by themselves" matches the chips.
- 08-23 stale "Choose where filed documents are saved" tick — **FIXED.**
- 09-20 F2 (banner pointing at nothing) — **BETTER for one banner, NEW-PROBLEM for another.** "Needs a quick check — 1 field was read below the level needed to file on its own" now names the field; but "a value was flagged by a formatting check" names nothing and sat on 30 invoices (card 2).
- 09-20 F1 / 09-22 F6 ("Checked by you" vs %) — **NEW-PROBLEM the other way**: "Checked by you" now appears on documents I never checked (card 3).
- 09-22 F3 (Delete All count ≠ tab) — **consistent tonight** ("Delete ALL 11" with Review 11; no "Not recognised" documents existed).
- 09-22 F1 (teach accepted an address as company name), F2 ("Not recognised" copy) — NOT RE-TESTED (I drew correctly; no document landed in Not recognised).

## NEW finding cards (ranked by harm)

### 1. A taught Customer name filed wrong on 6 of 11 self-filed orders — twice as the word "Customer" — each marked "Checked by you"
- **Citation (verbatim):** filed records for Copperfield Electrical: `Sales-Order.11-07-2026.SO-34294.pdf` → Customer **"Customer"**; `…SO-75446.pdf` → **"Customer"**; `…SO-29543.pdf` → "Ashcombe Care Homec"; `…SO-39092.pdf` → "Larch & Hollow Cafe C"; `…SO-17901.pdf` → "Larch & Hollaw cat. -."; `…SO-73695.pdf` → "Ashcombe Care Homes" (page: "Ashcombe Care Homes Ltd"). Search preview for SO-34294: "Customer Name Customer · Confirmed · Checked by you" (s70). Import screen: "10 documents auto-filed — no review needed."
- **User-moment:** I taught the wizard where the customer's name sits (it read "Kingfisher Print Studio" perfectly on the example) and let the sender file by itself, as the app said it would.
- **Observed confusion:** The app *can* spot a bad customer value — it held sales_order_16 because "Sandpiper Hotels :" "looks like a label, not a field value" — yet it filed the bare label word "Customer" twice and three garbled names without a word. A search for Larch & Hollow's orders would miss two of them; the accountant would see "Checked by you".
- **Harm + severity:** wrong value filed silently — the thing I fear most (high). Folders and file names are fine, so nothing is lost; the *record* is wrong.
- **Class:** CONFUSION.
- **Proposed alternative:** when a taught field reads the label word itself, or fails the same "doesn't read like a name" check the app already uses on other rows, hold once and say "Customer read as 'Customer' — that's the label, not a name". And show the taught extra fields in the "filed themselves" list so I can eyeball ten names in five seconds.
- **What I may be missing:** Customer isn't a filing field and is optional; maybe optional fields are allowed to be rough. From where I sit, a field I *taught* should get the same care as the reference.

### 2. The taught Total is invisible, blocks every invoice with a reason that names nothing, and is dropped from the filed record
- **Citation (verbatim):** Review, 30 invoices: "Nothing looks wrong — a value was flagged by a formatting check. Overall 100% · waiting for your check. Confirm it and it files. This isn't the confidence setting — changing that won't file this one." (s20). The panel shows Document Issuer / Invoice Date / Invoice Number only; "Change what's read from …" says "Total — shown" (s21); the import log says "FOUND total: '10,239.38'" on every one. Filed record for INV-71515 (which I confirmed): no Total. Group header the whole time: "✓ files by itself".
- **User-moment:** The tour told me to teach "the company name, the reference, the date, the total". I did. Then I waited for the promised "rest will file themselves".
- **Observed confusion:** Nothing on screen tells me the Total is the problem — the banner names no field, the field isn't drawn, Reprocess doesn't change it, File All Ready says nothing is ready. I only found it by switching Total off for the sender and re-reading: "Nothing was flagged — Ready to file" immediately, then 25 of 30 invoices filed themselves, all correct. And confirming by hand doesn't save the Total either — the record I confirmed has no Total in it.
- **Harm + severity:** blocked (the headline feature never fires on invoices) + trust-eroded (the promise vs the pile) — high.
- **Class:** CONFUSION.
- **Proposed alternative:** name the field in the banner ("Total was flagged by a formatting check — it read 'GBP 10,239.38'"), draw the Total row like any other field so I can confirm or fix it, and either don't let an optional extra field block a sender that "files by itself", or say plainly "the Total box is holding these — switch it off for this sender or re-teach it".
- **What I may be missing:** there may be a good reason the Total reads are being doubted (they were right on every page I checked). Whatever it is, it needs to say so on the screen.

### 3. "Checked by you" on documents I never checked — and "You filed 1" for documents that filed themselves
- **Citation (verbatim):** Search preview, "Confirmed · Checked by you" on PO-81066, SO-73695 and SO-34294 — all three "Filed (auto)" on the import screen. Activity strip after each sender re-read: "✓ You filed 1 · Just now" beside "✓ 14 filed themselves" (Vellum) and "✓ 11 filed themselves" (Thornbury) — I pressed nothing on those. INV-72651: "Confirmed · Checked by you" (s69), a document I only ever reprocessed.
- **User-moment:** Looking up a filed order for the accountant and wondering who checked it.
- **Observed confusion:** "Checked by you" was the fix for last round's doubt-number, and it reads well on the ones I *did* check. On the automatic ones it puts my name on values I never saw — including the wrong customers in card 1.
- **Harm + severity:** trust / blame direction (medium-high). **Class:** CONFUSION.
- **Proposed alternative:** three states: "Checked by you" (I pressed Confirm), "Filed itself after your confirmations", "Filed automatically on import". The strip line should read "1 more filed itself", not "You filed 1".
- **What I may be missing:** maybe "you" means "your rules" — but that's not how it reads.

### 4. A wrong date was held only because something *else* was flagged (a near miss)
- **Citation (verbatim):** sales_order_09: "ORDER DATE High · 96%" showing **18-01-2026** with no note; page prints **08/01/2026**. The only note on the card was on the customer ("doesn't read like a name — please verify"), banner "Nothing looks wrong — a value was flagged by a formatting check. Overall 100%".
- **User-moment:** Reading the held card to see what to fix — the card points me at the customer name, which was right, and says nothing about the date, which was wrong.
- **Observed confusion:** On three other documents the app *did* catch a wrong date ("This date couldn't be confirmed anywhere else on the page", "Read differently after straightening — was '11-10-2026'"). Here the same kind of slip got "High · 96%" and would have filed if the customer name had passed.
- **Harm + severity:** near miss (medium). **Class:** QUESTION — why did the date check that fired on invoice_06 and SO-71797 not fire here?
- **Proposed alternative:** none from me — but this one document is worth your eyes.
- **What I may be missing:** one document; I can't see the mechanism, only that its twin (SO-71797, 01-10 vs 11/10) was caught.

### 5. "✓ files by itself" on the group header while every document underneath waits (repeat of 08-23 A6)
- **Citation (verbatim):** "Vellum & Crane Stationers · 17 documents · 17 need a look · ✓ files by itself" for over ten minutes (s19); "Thornbury Fasteners · 16 documents · 16 need a look · ✓ files by itself" (s40).
- **User-moment:** Deciding whether I can leave this sender alone.
- **Observed confusion:** The badge says it files by itself; nothing files; the reason is card 2. The badge is the one line I most want to believe.
- **Harm + severity:** trust-eroded (medium). **Class:** QUESTION.
- **Proposed alternative:** "Would file by itself — 17 are held by a flagged value (Total)" when the count of held documents equals the pile.

### 6. Three different numbers for "how many confirms until it files by itself"
- **Citation (verbatim):** Teach done card (Vellum): "Confirm 2 more … and the rest … will file themselves"; (Thornbury): "Confirm 3 more … 2 from Thornbury Fasteners are waiting in Review. Their details still need checking, so I can't promise the exact moment."; Settings → Processing: "Clean confirmations before a sender is trusted … 5". Vellum's group after two confirms: "✓ files by itself" — then nothing filed after 2, 3 or 4 more.
- **User-moment:** Planning my morning: how many do I have to do by hand?
- **Harm + severity:** slowed / trust (low-medium). **Class:** QUESTION.
- **Proposed alternative:** one number everywhere, and the done card should name what can still hold a document afterwards ("…unless a value is flagged").
- **What I may be missing:** the teach may count as confirmations and the "5" may be a different thing — but the screens don't say that.

### 7. Engineering words leak into field notes and the import log
- **Citation (verbatim):** field note "The taught box has drifted off the value — '' was read beside its label and has independent support on this page. Please re-teach the box for this template, then confirm this one." (invoice_14); "Read differently after straightening — was 'PO-75562', now '' — confirm once." (the "now" is blank); import log lines "✓ → undefined", "Stage 0.5 heal: invoice_number inline_disagree_flag (template_mapping_shapewarn)", "[Learning] 0 hints, 0 anchors, 0 logos, 0 templates".
- **Harm + severity:** cosmetic / slowed (low). **Class:** PREFERENCE.
- **Proposed alternative:** "The box I learned for the number missed on this page — please draw it again on this document, then confirm." / "After straightening the page I read 'PO-75568' instead of 'PO-75562' — confirm once." The detailed log can stay technical, but "✓ → undefined" is a bug in the log line.

### 8. "Sender not identified" on three invoices whose letterhead says the name — while the group is already named
- **Citation (verbatim):** Review rows "ThornburyFasteners_invoice_04.pdf · Sender not identified · check sender" ×3 under the group header "Thornbury Fasteners · 5 documents · 5 need a look" (s32). The import log had "Letterhead issuer SUGGESTED: 'Thornbury Fasteners' (not assigned)". After the teach all three read "Thornbury Fasteners".
- **Harm + severity:** slowed (low). **Class:** QUESTION.
- **Proposed alternative:** if the group can be named from the letterhead, prefill the row the same way (as it did for the other two) and say "from the letterhead — please confirm".

## Warnings truth-table
| Action | What it said (verbatim / condensed) | True? |
|---|---|---|
| Confirm & File (×11) | toast "Filed as Invoice.24-04-2026.INV-75758.pdf in Vellum-&-Crane-Stationers / 2026 / April." | ✅ each landed exactly there, sidecar alongside |
| File All Ready, nothing ready | chip "✓ Nothing filed — 17 kept back" + "Nothing is ready to file yet — every document in the queue is waiting on a check or a missing detail." | ✅ (no dialog, nothing filed) |
| File All Ready, one ready | "File 1 ready document (of 14 in the Review queue)? Not included — they stay in the queue: • 13 flagged — waiting for you to check a value. Each one is filed exactly as if you confirmed it yourself…" | ✅ exactly 1 filed (SO-55760, all values right) |
| Reprocess (one document) | no warning; "Reprocessing…" ~15 s | ✅ filed nothing, values unchanged |
| Re-read 15/16 from sender → Full re-read | "Quick reuses the text already read… Full re-reads every document the usual way… Because this sender already files by itself, anything that re-reads clean will file straight away — you'll see it in the activity strip with a Put back. A value that reads differently from before is kept for you to check." | ✅ filed 14 / 11 clean ones; ⚠️ one each labelled "You filed 1" (card 3); Put back not opened |
| Change what's read (Total off) | "Applies to all of this sender's documents, including the ones waiting. Values already read aren't changed… use Reprocess on a document to read it again." | ✅ exactly so |
| Defer | title "Set this one aside for later — it moves to the Deferred tab. Nothing is filed or deleted." | ✅; way back = the row's "Review" button ("Move back to review queue") |
| Delete one | "Delete "ThornburyFasteners_invoice_17.pdf"? It goes to the app's recycle bin — you can restore it from Search." | ✅ in the bin, restored |
| Restore all (bin) | "Restore all 1 document from the recycle bin? They go back to where they were deleted from (the review queue, or their filed folder)." | ✅ back in Review |
| Delete All Review | "Delete ALL 11 document(s) in the Review queue? They go to the app's recycle bin… Files on disk are kept. Confirmed and deferred documents are NOT affected." (cancelled) | ✅ count matched the tab |
| Teach → Save | "Nothing is saved until the very end — you can go back at any point." / "Save teaching & file" | ✅ saved in 2 s, one document filed, done card honest |

## The second reader — what I actually saw
- Import log, after a layout was taught: **43 lines** of the form "Second reader checked the reference 'INV-34177' — agrees." (15 Vellum, 14 Thornbury, 14 Saltmarsh). None on the first five of any sender (before teaching) — which matches "a box you taught".
- **Zero** "Second reader disagrees … holding" lines and **zero** "Two … readers" holds on screen all night. Every reference it said "agrees" on, I checked against the page: all right.
- The one look-alike misread I saw (PO-S0471 for PO-50471) happened on the *first* read, before the teach; the taught box then read it correctly and it filed right. So I never got to judge the disagreement wording — only that the "agrees" line is plain and reassuring, and that nothing wrong went through on a reference all night.
- The date fix and the money check: I saw no "same day in words and numbers" hold and no "balances only by one wrong digit" hold — nothing in these documents seems to trigger them. Judged as not seen.

## What genuinely worked (the standout)
**Saltmarsh and Copperfield did the whole thing you asked for.** Teach one → confirm two → the next 15 arrive and "12 documents auto-filed — no review needed" / "10 documents auto-filed" — every filed number and date right against the page — and the ones it held, it held for reasons I could read and act on: "Kept the read value '15/11/2026' — the taught date box read '14/11/9096', which is far in the future (likely a mis-scanned year). Please check." That note shows the wrong read it *kept out*, keeps the right value in the box, and asks for one look. Same for "This date couldn't be confirmed anywhere else on the page" on a genuinely wrong 14-07. And the S-for-5 reference was corrected by the taught box rather than filed. Honourable mentions: the teach read-backs (every one of 15 boxes exactly as printed, with "Label: … (left of / above the value)"), the "★ Scan Finder has learned Vellum & Crane Stationers — their clean Invoice documents will now file automatically" banner on the Import screen, and the "Nothing is ready to file yet" honesty on File All Ready.

## Top friction point
**Card 2** — a field the tour told me to teach silently blocks every invoice for that sender, shows up nowhere, and the banner names nothing. It turned the headline feature off for two of my four suppliers until I guessed the cause.

## Comparison with previous rounds (fair)
- **Same:** every destructive warning told the truth (as on 09-20 and 09-22); the teach read-back is as good as ever; speed the same (5 docs/20 s, 15 docs/35 s).
- **Better:** wrong dates. On 08-23 one wrong date filed itself at "High · 94%, Nothing was flagged" (invoice_16). Tonight four wrong date reads and none filed; three were held with the date named. The Home screen now agrees with the strip (08-23 A7). Holds like "read at 83%, below the 90% you've set" and the "kept the read value … the taught box read …" notes are the clearest I've seen. Purchase orders and sales orders filed themselves *at import* (12/15 and 10/15) — on 09-22 I had to press File All Ready for that.
- **Worse:** invoices. On 09-22 one confirm got "17 of 19" through File All Ready; tonight, with the Total taught, **0 of 30** invoices moved on their own and File All Ready found nothing ready, until I switched the Total off. And a *new* class of wrong value filed silently — the taught Customer name (6 of 11) — which no earlier round tested. The "✓ files by itself" badge over a waiting pile is unchanged from 08-23.
- **Net:** the machine that files reference/date/company is more trustworthy than in August (0 wrong on 67); the extra fields I teach are not yet.

## Two-week verdict
**Yes — with two things I'd switch off on day one.** I'd keep using it because on the order types it delivered exactly the promise (teach one, confirm two, most of the rest file themselves, every one right, every hold explained) and because every wrong number or date tonight was *held*, not filed. But I'd turn the Customer field off until it stops filing the word "Customer" as a name, and I'd leave the Total off for invoices until it either appears on screen or stops holding everything. And I'd want "Checked by you" to mean me.

## Humility block
One simulated non-technical customer, not a user test; I speak only for myself and about screens actually driven tonight. Judged copy and behaviour, not code, and I don't claim to know *why* anything happened. How I "saw": I drove the real buttons and drew real mouse boxes, but I aimed those boxes using the page's own word positions as my eyes, and I checked values against page crops I rendered myself (`eyes_*.png`), not by opening each filed PDF in a viewer. Native confirm() dialogs were captured and accepted/dismissed by the driver; one modal I initially missed and re-read from the screenshot. Coverage gaps: didn't finish the practice run; didn't test Put back, the "Not recognised" tab, type changes, the LAN client, or Quick File; only 4 senders, 80 documents. Three of my own early readings were wrong and corrected at the source before writing this (a "stuck import banner" that was hidden, a "blank sender name in a note" that my own dumper had stripped, and the modal I missed). Sandbox housekeeping: output set to the sandbox `Output`; added a Total field to the Invoice type; **left Total switched OFF for Vellum & Crane and Thornbury Fasteners** (via "Change what's read"); deleted and restored one document; deferred and brought back one; 13 documents left in Review, none deferred. Nothing outside the sandbox was touched; nothing was implemented — all cards await your vet.

---

## Main-session source verification of the cards (2026-09-23 night, read-only on a COPY of the sandbox DB + the code; nothing implemented)

**Chris's numbers check out against the DB:** 80 docs · 67 `confirmed` (4 taught, 11 hand confirms, 52 machine: `confirmed_via` = `scope_sweep` "files by itself" / `auto_threshold` "Auto-filed (100%)" at import / `auto_reprocess`) · 13 `needs_review`. All six 2026-09-23 switches ON; `auto_file_threshold` 90; `graduation_window` 5.

| Card | Verdict | Mechanism (verified at the source) |
|---|---|---|
| **2 — the taught Total blocks every invoice, names nothing, vanishes from the record** | **REAL — three seams, all reproduced** | (a) **Engine:** `template_format_fail_yield` (ON since mig 70) runs `_stage05_format_fails`; its LEGACY currency leg `^[£$€¥]?\s*[-–]?\s*[£$€¥]?\s*\d` rejects an ISO currency CODE prefix, so a taught Total box on a page that prints `GBP 5,926.48` inline FAILS on every document → the bare keyword read (taught label "Total Due") wins + the note *"Kept the read value '10,239.38' — a taught mapping read 'SBP 10,239.38', which doesn't match this field's expected format. Please check."* Reproduced by re-running docs 6/7/25 on a copy with Total un-hidden: mapping `GBP 11,066.95` / `SBP 10,239.38` / `SBP 21,864.86` (the number agrees every time; `SBP` = a G→S scan misread of the code). The strict-money sub-flag (`money_strict_shape`, which strips codes) is the Oracle "never flip" one. (b) **Gate + copy:** `optional_soft_flag_autofile` is OFF → a note on an OPTIONAL field counts as `flagged`; trust.js:1311 returns the bare reason `'flagged'` with no `:field_key` → Review's banner has no field to name. (c) **Stale field list:** the Teach wizard's "Edit this type…" adds the field through the `add-field` IPC (`doctype-editor.js:523`), and `add-field`/`update-field`/`delete-field` do NOT broadcast `doc-types-changed` (only type create/rename do) → an already-open Review window keeps its old `allDocTypes` → `fieldDefs` lacks `total` → the row is never drawn → Confirm's DOM scrape omits it → `buildXml(allValues)` writes a sidecar with no Total (the DB row exists: INV-71515 `total` = `14,398.88` @85 `keyword_override`). Reopening Review would have shown the row. **Candidate fixes (owner → reggie/gary → Oracle, NOT built):** fold `number_format.strip_currency` (or accept `[A-Z]{3}\s`) in the legacy currency leg — "money = numbers only" is already the validator's rule; broadcast on field add/update/delete; append the field key to the `flagged` reason. |
| **1 — a taught Customer filed as "Customer" ×2 + 3 garbles, "Checked by you"** | **REAL** | `customer_name` is optional + non-role. The taught box read the LABEL line ('Customer') or a garble; the page-text (keyword) family read the right name on 4 of the 6 (`Sandpiper Hotels`, `Ashcombe Care Homes Ltd`, `Larch & Hollow Cafe Co` ×2) and the disagreement is RECORDED in `corroboration` — but `_pageFamilyDisagrees` guards ROLES only, the label-like guard needs a trailing colon, the mig-156 non-name guard is postcode/email/VAT/IBAN only, and nothing compares a value to its own field label. `isAutoFileEligible` on docs 69/76/67/78/65 as filed → `ok` (basis threshold). **Direction (not built):** a taught name-like optional field whose value equals its label/anchor text → flag; and/or extend the page-family disagreement refusal to TAUGHT optional name fields (Chris's own principle: a field you taught earns the role's care) — census first, blast radius = every optional field with a keyword competitor. |
| **3 — "Checked by you" on auto-filed docs; "You filed 1"** | **REAL, cheap** | `searchActions.js:38` prints "Checked by you" for EVERY `confirmed` doc regardless of `confirmed_via` (the DB knows: `scope_sweep` / `auto_threshold` / `auto_reprocess`, `confirmed_by_username` = "Auto-filed (…)"). `processing/handler.js:5798`: the reprocess-all completion auto-file records the ledger event as `kind:'approved', approved:true` → the strip says "You filed N". Copy + ledger-kind fix. |
| **4 — a wrong date held only because something else was flagged (near miss)** | **REFUTED as a near miss; display gap real** | On a DB copy with the customer note stripped, `isAutoFileEligible(sales_order_09)` → `disagreeing-read:order_date` — the mig-152 M=2 belt (`role_disagree_refuse_at100`, ON here) holds a page-family date disagreement at overall 100. It would NOT have filed. What is missing is the SIGNAL: the date row shows "High · 96%" with no note while the page read (`08/01/2026`) contradicts the box (`18-01-2026`). The badge answer exists DARK for refs (mig 185 `ref_badge_verify_state`); the date role is its deferred sibling (Oracle C4). |
| **5 — "✓ files by itself" over a waiting pile** | as 08-23 A6 | The badge is the SCOPE's trust state (graduated/corroborated), not the pile's. Copy fix queued since 08-23. |
| **6 — three "confirms until it files" numbers** | by design, poorly explained | Teach follow-up: `needed = FORMAT_SOLID_MIN (3) − the format group's confirmed_count` (hence 2 after a teach, 3 when a role group had none); Settings `graduation_window` 5 = the 95-floor GRADUATION. Two mechanisms, one promise on screen — and card 2's note then broke the promise. The done card should name what can still hold a document. |
| **7 — engineering words / `now ''` / `✓ → undefined`** | cosmetic, queue | the deskew-retry note renders with an empty re-read value; the import log's `✓ → undefined` line is a bug in the log line. |
| **8 — "Sender not identified" ×3 under a named group** | design + DARK | `issuer_undetected_blank` (mig 195) is DARK; "Letterhead issuer SUGGESTED (not assigned)" is the mig-89 fill-on-confirm design. The teach then named the siblings (as designed). |
| **Second reader** | consistent with the C10 census | 43 "agrees", 0 holds on clean demo scans; `PO-S0471` was the pre-teach first read (no taught box → the reader doesn't fire); the taught box then read `PO-50471`. The date fold (208) and arithmetic witness (209) had nothing to fire on here — "not seen", as Chris says. |

**Vet queue for the owner (nothing built):** (1) card 2a the ISO-code currency prefix in the legacy format-fail leg — reggie+gary → Oracle (touches an ON switch's yield leg; census on the 727 + Hard Set money class); (2) card 2c `add-field`/`update-field`/`delete-field` broadcast `doc-types-changed` — eric, low risk; (3) card 2b name the field in the `flagged` reason — low risk; (4) card 3 "Checked by you" → three states + the reprocess ledger kind — copy; (5) card 1 the taught-optional-name disagreement / value-equals-label flag — design + census; (6) card 4 a date-role badge/note when a page family disagrees — the mig-185 sibling; (7) cards 5/6/7 copy.
