# HANDOVER — 2026-07-14

**Branch:** `feat/review-straighten-followups` (off `main`) — **PUSHED to origin** (unmerged; open a PR when ready).
Working tree CLEAN. This session's commits (newest first): `c0ac414`, `f06f349`, `8ab5bd2`, `1249c17`,
`c023e29`, `2c01d8c`. Every code change is kill-switched + additive; each has unit tests, most have a
corpus A/B. Live corpus is now **~182 confirmed docs** (reset/reimported — NOT the historical 2495).

---

## START HERE NEXT SESSION → the logo discriminator, Slice B + C
Read `../../` memory `project_logo_collision_20260714.md` first. The **inert, proven foundation is
committed** (`c0ac414`); the behaviour-changing wiring is NOT built:

- **Slice B** — schema migration: store a **detail-hash SET** per supplier on `logo_fingerprints` +
  each `template_logo_hashes` row (go-forward, NULL-inert, mirroring the migration-26 `logo_phashes`
  set). Enrolment: `learning.saveLogoFingerprint` (JS) computes + stores `logo_detail.detail_hash` on
  confirm (needs a Python call at the confirm seam, or compute at import and thread through).
- **Slice C** — the **ABSTAIN-ONLY disambiguator** via ONE shared helper landed at BOTH
  `template_matcher.identify_template` (Python Stage-0, the real uncovered site — `try_logo_supplier_match`
  already abstains on a Hamming near-tie) AND `learning.findLogoMatch` (JS confirm/anchor). Rule: only
  inside an already-ambiguous **≥2-supplier** logo cluster, if the query's detail hash is FAR
  (min-over-set) from a candidate supplier's stored set → drop that candidate / abstain → falls to
  keyword-hint + the branding net + review. **Never picks a supplier, never raises confidence, never
  bypasses the branding note.** Threshold from the probe's measured drift (see below). Runtime-inert on
  `detail_hash`→None (isolate-fail / low-detail) and on NULL stored rows. Kill switch. Verify on
  `stress_test/corpus` + the NT/CW repro + a **B&W-simulated** scan, reporting **false-accepts** (NOT
  `test_harness`, which randomises logos per doc → can't measure logo matching).

**GATE-0 numbers (already measured, `stress_test/logo_detail_probe.js`):** on 24 Northgate / 120 Cascade
docs the isolated-mark 256-bit hash SEPARATES the pair on colour AND B&W — inter-min **114** vs typical
intra **~30** (rare isolation-fail outliers push intra-max to 92–104, absorbed by the min-over-set). So a
Slice-C threshold in the ~60–80 band (between typical intra and inter-min), applied min-over-set, is the
starting point — calibrate against same-supplier drift with headroom (Oracle's condition).

Advisor consensus (Phillip fingerprinting + oscar OCR) + Oracle SIGN-OFF-WITH-CONDITIONS on the
architecture are in the memory file. **Colour was RULED OUT** (NT/CW are both dark navy ~6° apart, and
B&W scans strip colour entirely — owner's constraint); the STRUCTURAL isolation is the fix.

---

## What shipped this session

### `c0ac414` — logo isolated-mark detail hash (INERT foundation)
`python_backend/logo_detail.py`: crop the letterhead region → **pure-NumPy 4-connectivity connected
components** (no scipy/OpenCV) → pick the most logo-like blob (compact, square-ish, dense; thin text
strips / vertical rules rejected) → **256-bit phash** of the isolated mark. Colour-free → identical on
B&W. `detail_hash()`/`detail_distance()` return None on any failure (fail-safe). Nothing wired → zero
behaviour change. `tests/test_logo_detail.py` (green); `stress_test/logo_detail_probe.js` +
`_logo_detail_hash.py` (the GATE-0 gate, PASS colour + B&W).

### `f06f349` — branding cross-check names the real supplier from a garbled letterhead (Slice 1, suggest-only)
The logo-collision wrong-supplier case (a Northgate doc blind-matched "Cascade"): `_flag_branding_conflict`
already flagged it, but its alt-supplier hunt used EXACT matching so garbled "rthgate textiles" never
resolved "Northgate". Fix: FUZZY alt-naming (`template_matcher._keyword_hit_ratio_fuzzy`,
`difflib.SequenceMatcher.ratio()≥0.85`, tokens ≥6, ±2 window — a SEPARATE helper; shared
`_keyword_hit_ratio` untouched), **ISSUER-BAND scoped** (`chrome_band.issuer_chrome` lifted to a
stdlib-only module so it runs without rapidfuzz; a mid-page recipient like "Primrose Childcare" — even a
known supplier — is NEVER named as issuer, positional not name-based), `own_ratio` stays EXACT+whole-page
(fuzzing it = fail-open). Additive `suggested_supplier` (fuzzy-path-only) plumbed to the emit for a future
renderer button. Kill switch `BRANDING_ALT_FUZZY`. Oracle **SEND-BACK the engine value pre-fill** →
**Slice 2 (NOT built) = a renderer "Use '<name>'" button** (value-change belongs at confirm-time; the
engine value/`_supplier_name` never change → no split-brain, no rubber-stamp). realdoc_regression A/B (182
docs, off vs on) **byte-identical** on every scored metric. `test_branding_conflict.py` extended, green.

### `8ab5bd2` — skew type-flip robustness (Fix A + B1)
Same-letterhead suppliers (one logo, several doc types → identical fingerprints) coin-flip the TYPE under
MFD skew → wrong-type auto-file. **Fix A** (`TYPE_AMBIGUITY_GUARD`): flag an ambiguous same-letterhead type
pick → persisted note + needs_review (HOLD-only). Covers BOTH the logo-cluster path (`_type_ambiguity`,
band-13) AND the keyword-fingerprint fallback (`_kw_type_ambiguity`, ≥2 distinct slugs tie at top score,
single-supplier cohesion C1). **Fix B1** (`REF_PREFIX_RETYPE`): resolve the correct sibling from the doc's
own ref-PREFIX (reused poison-barred `prefix_index`), pre-select + seed + pin the sibling template, but
KEEP the hold (Oracle rejected auto-file: PO↔SO cross-ref hole). C2: `pinned_template_id ⇒ _type_ambiguous`
unconditionally (split-brain guard). `skew_type_probe.js` b1Corrected=2, all safety counters 0;
realdoc A/B accuracy UP, M unchanged. Tests: `test_kw_type_ambiguity.py`, `test_ref_prefix_retype.py`,
`test_type_ambiguity_flag.py`, `test_template_type_ambiguity.py`.

### `1249c17` — DESKEW×CACHE fast path · `c023e29` — docs sync · `2c01d8c` — type-scoped matching + graduation auto-template + never-freeze recipient names
(Earlier in the session — see the commit messages; graduation auto-template = a template is auto-created at
graduation so a graduated supplier's docs auto-file without a manual "Save as template"; DESKEW×CACHE =
reprocess-with-straighten only re-OCRs actually-tilted docs; recipient-name freeze fix stops a template
freezing a per-doc customer name.)

---

## Diagnoses closed to root (no code — for the record)
- **Customer-drop / hint bleed** (a customer field showed a stale hint over the correct keyword read): NOT
  a hint bug — the hint apply is strictly fill-empty (verified in code + live trace). It's a two-condition
  SYMPTOM of the wrong-supplier resolution: (1) doc mis-resolves → Cascade; (2) **Cascade's sales_order
  customer scope is poisoned to 1 distinct value ("Primrose Childcare" usage 20)** → the variability guard
  treats it as stable-recurring → hint armed. When supplier resolves correctly (Northgate), no bleed
  (verified on all 4 Northgate sales orders). **Self-corrects** as Northgate's text signal strengthens
  (`hint_text_match`@85 beats the colliding logo). The logo fix kills the class at source.
- **Logo center-crop direction**: colour ruled out (measured, + B&W constraint); structural mark-isolation
  validated (GATE-0). See above.

---

## Open follow-ups (not started)
- **Logo Slice B + C** — the pickup point above (the big one).
- **Branding Slice 2** — renderer "Use '<name>'" button fed by `suggested_supplier` (Oracle-preferred over
  the engine pre-fill).
- **Anchor label-reject guard** — a relocated/harvested anchor read that fuzzy-matches the anchor's OWN
  label is the LABEL, not the value (the "vetiver tO" = garbled "Deliver To" case). Deterministic,
  upstream of the credibility checks. Designed-not-built.
- **CROP_GEOMETRY snap-to-glyph** — stop the crop landing between two lines / on the caption (the owner's
  "drift toward the label, stop at the label" heuristic).
- **Pre-existing #23/#24** — two purchase_order docs would auto-file a WRONG ref (a ref misread clearing the
  floor); identical with guards off → orthogonal, pre-existing (like the Cloudpeak class).
- **Variability-guard poison hardening** — trusting a SINGLE confirmed value as "stable" is fragile when a
  scope is poisoned by mis-attribution; could require a min confirmation count. Deeper design; the logo fix
  reduces the pressure.

## Kill switches (all default ON)
`BRANDING_ALT_FUZZY` · `BRANDING_CONFLICT_GUARD` · `TYPE_AMBIGUITY_GUARD` · `REF_PREFIX_RETYPE` ·
`DESKEW_CACHE_FAST` · `PREFIX_OUTLIER_GUARD` · `HEADING_GARBLE_GUARD` (+ the older guard switches).

## Verification harnesses
`stress_test/skew_type_probe.js` (Fix A/B1 gate) · `stress_test/logo_detail_probe.js` (logo GATE-0) ·
`stress_test/realdoc_regression.js` (M=0 + per-field accuracy on the live confirmed corpus; read the report
`stress_test/out/realdoc_regression.md`, NOT the exit code). `test_harness` CANNOT measure logo matching
(randomises logos per doc).
