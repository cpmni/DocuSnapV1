# Handover — 2026-07-07

Branch **feat/tray-stage1**. All work committed; tree otherwise as before (sandbox/, output/,
NIGHT_*.md, screenshots untracked). Run JS harnesses with Electron-as-Node
(`ELECTRON_RUN_AS_NODE=1 ./node_modules/electron/dist/electron.exe <script>`) — better-sqlite3 ABI.

## Commits this session (newest first)
- `307a81e` build(ocr): unbundle RapidOCR — drop prebuild guard + provisioning (Slice 1)
- `27920e2` feat(extraction): wire identity_fusion in SHADOW mode (inert by default)
- `d3fb3b2` fix(startup+processing): core-aware concurrency default + reveal-resilient first launch (friend)
- `b32c2b0` (prev) fingerprinting Phase-0: shared logo_hash.py, dormant identity_fusion.py, config type buckets

---

## Thread 1 — identity_fusion (text-led supplier identity): SHADOW wired, promotion pending
**What:** `python_backend/extraction/identity_fusion.py` — precision-first supplier ID from the page
CHROME vs the known-supplier gazetteer (dual-gate, rapidfuzz). Wired into `engine.py` as
`ExtractionEngine._shadow_identity()` (mirrors `_shadow_reconcile_components`): `extract(identity_shadow=True)`
records `resolved` vs `text_led` (agree/conflict) and **changes nothing**. Off by default → byte-identical
(guaranteed: `False` default + `if identity_shadow:` guard + `file_done` omits the key when absent).
- Turn on: `process_docs --identity-shadow` → emits `file_done.identity_shadow`.
- Guarded import (needs **rapidfuzz**; absent → shadow disabled). Dev `py -3.12` already has rapidfuzz.
- Gazetteer derived in-engine from the logo/hint/anchor scopes already loaded (no new plumbing).

**Measured (real engine, `rich_field_runner --identity-shadow`, GT cross-check tally; earlier sandbox 100%/0 over 1600 docs):**
- REALISTIC scanned (recurring suppliers, OCR conf 84, 200 docs): text-led **100% precision (RIGHT 200 / WRONG 0)**
  and **recovers all 40 logo-misses** (pipeline supplier 80% → ~100%); 0 conflicts, 0 false-alarms.
- ADVERSARIAL harness (randomised per-doc logos, OCR conf 26, multi-company chrome, mixed customer/supplier scope,
  200 docs): **precision DROPS to 67% (RIGHT 65 / WRONG 32 silent-wrong)** — but still **CATCHES 9** genuine
  logo-misfires (the pipeline's `logo` was only ~54% precise there) vs 3 false-alarms.
- Takeaway: the sandbox's 100% does NOT survive noisy OCR + multiple company names in the flat first-6/last-3
  chrome — the failure is text-led matching a NON-issuer name (customer/printer/footer) that happens to be in the
  gazetteer. Fork note: flat-vs-geometry chrome was NOT the deciding variable in the sandbox (both 100% there);
  the real-engine gap is chrome CONTENT (issuer band) + gazetteer SCOPE, not plumbing.

**STATUS — issuer-band chrome DONE (commit 0cd056e); promotion still gated on real-doc evidence:**
1. **DONE — issuer-band chrome** (`identity_fusion.issuer_chrome`, reggie-reviewed marker set, `test_identity_fusion.py`
   9/9): truncates the chrome at the first recipient marker + drops the footer. Realistic scanned HELD 100% / 0
   silent-wrong + the 40-doc recovery (no regression); closes the clean `Bill To: <known supplier>` recipient leak.
2. **Harness residual is OUT OF SCOPE — do NOT chase it, do NOT build an OCR-quality gate.** silent-wrong stayed
   32→31 because the harness makes the issuer a randomised logo IMAGE with its name ABSENT from the OCR text while a
   legible WRONG name is present — a text feature can't win, and an OCR-quality gate can't fix it (wrong names match
   at conf 100 = legible; `overall_conf` overlaps 31-95 vs 0-63). Real issuers are text letterheads (why realistic
   docs are clean).
3. **DONE — LIVE-DB validated** (`stress_test/live_identity_shadow.js`, READ-ONLY, 166 confirmed docs, real gazetteer;
   `stress_test/out/live_identity_shadow.md`): fresh pipeline supplier **99.4%**; text-led ACCEPTED 161/166, **RIGHT 160 /
   silent-wrong 1 = 99.4% precision**, **CONFLICT 0 / false-alarm 0**. The 1 miss (#45) AGREES with the pipeline
   (letterhead 'Contoso Asia' ≠ filed 'Anconia Corp') → invisible in flag-only. CATCH/RECOVER 0 because this pipeline is
   already 99.4% (learned docs mask the upside). **VERDICT: GREEN for flag-only promotion.**
4. **DONE — flag-only promotion SHIPPED** (commit bba41e5; OPT-IN, default OFF via `identity_conflict_flag`;
   enable at Settings → Processing → Name checks → "Flag likely supplier misreads"): `engine.set_identity_conflict`
   + `_compute_identity_verdict` (shared shadow+active); a CONFLICT raises needs_review + a "Letterhead may read X
   — detected Y" note on the identity field (caps conf at 70) — never override/fill or flag on abstain/agree;
   `--identity-conflict` flag; handler reads the setting. `rapidfuzz` added to BUILD.txt (MIT → the license gate
   AUTO-allows it; NO `check-licenses.js` edit needed). Firing proof: harness 8/8 + 7/7 conflict docs flagged with
   the note; `test_identity_fusion.py` 10/10.
5. **REMAINING — build machine only:** `pip install rapidfuzz` into `vendor\python` (already in the BUILD.txt dep
   lists) + regen `THIRD-PARTY-LICENSES.txt` (`node scripts/gen-third-party-notices.js`). Feature is inert-safe
   until then (guarded import → no-op if rapidfuzz absent).
- `rich_field_runner.js` (untracked) shadow tally has a GT cross-check (`textRight`/`textWrong`=silent-wrong,
  `catch`/`falseConflict`); reports in `stress_test/out/rich_*.md`.
- `stress_test/rich_field_runner.js` (untracked) now passes `--identity-shadow` + prints an
  "identity_fusion SHADOW" report section.

---

## Thread 2 — RapidOCR removal: Slice 1 DONE, Slice 2 pending
**Why:** opt-in beta, never default (bake-off never run), slow, +80–180 MB installer, mandatory build
provisioning + a prebuild guard that could block releases, and it pinned the bundled Python to 3.12.
Licence clean (Apache/MIT/BSD) → pure simplification. Runtime **auto-fallback to Tesseract** makes removal
behaviour-neutral.

**Slice 1 (`307a81e`):** `package.json` build no longer runs `check-rapidocr-bundled.js`; `BUILD.txt` drops
the REQUIRED provisioning step + the now-false guard note + corrects the 3.12 rationale.

> ⚠ **BUILD-MACHINE ACTION** to realize the size drop: the repo change stops *future* provisioning, but an
> existing `vendor\python` still contains rapidocr. On the build machine rebuild `vendor\python` fresh OR:
> `vendor\python\python.exe -m pip uninstall -y rapidocr-onnxruntime onnxruntime opencv-python shapely pyclipper`

**Slice 2 (pending, ~a dozen files):**
- `python_backend/ocr/engine.py` — remove the RapidOCR engine class; **KEEP** the engine abstraction +
  `TesseractEngine` (`read_page`/`reconstruct_page_text` is the SHIPPED full-page path).
- Settings → OCR engine toggle: `src/windows/settings/{index.html,renderer.js}` + `src/modules/settings/handler.js`;
  hard-wire tesseract or drop the `--ocr-engine` / `ocr_engine` plumbing in `process_docs.py` +
  `src/modules/processing/handler.js` + `src/modules/templates/handler.js`.
- Delete: `python_backend/requirements-ocr.txt`, `scripts/check-rapidocr-bundled.js`,
  `python_backend/tools/ocr_bake_off.py`, `python_backend/OCR_RUNTIME.md`, `package.json` `check:rapidocr` script.
- Prune third-party/compliance: `THIRD-PARTY-LICENSES.txt` + `scripts/check-licenses.js` allowlist entries
  (rapidocr-onnxruntime, onnxruntime, opencv-python, shapely, pyclipper, PyYAML-if-only-rapidocr-pulled-it);
  update `COMPLIANCE.md`, the `BUILD.txt` dev OPTIONAL note (§1), `python_backend/tests/test_ocr_engine.py`,
  CLAUDE.md, `MSIX_SETUP.md`.
- Grep anchor (26 files): `rapidocr|RapidOCR|ocr_engine|onnxruntime`.

---

## Thread 3 — friend-fixes (`d3fb3b2`, DONE)
Core-aware `defaultConcurrency()` (a 6-core PC no longer defaults to 2) across onboarding/runtime/settings;
first-launch reveal resilience (window backstop 2s→12s + keep the login/onboarding cover until the main
shell paints). Slow-first-launch follow-ups still open (eric's plan): dashboard "Loading…" skeleton, defer
`sweepInboxOrphans`+`audit_archive.runMaintenance` to `setImmediate`, code-sign the installer.

## Gotchas
- Never put `python_backend/extraction` on `sys.path[0]` — its `keyword.py` shadows stdlib `keyword`. The app
  uses `from extraction import ...` with `python_backend` (parent) on the path.
- Live DB: `%APPDATA%\Roaming\ScanFinder\docusnap.db` (read-only via Python sqlite3 for diagnostics).
