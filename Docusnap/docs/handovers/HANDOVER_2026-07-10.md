# Morning report — overnight session 2026-07-09 → 10

**TL;DR: all three root causes of yesterday's "keeps re-typing as Sales Order / customer name
in the Document Issuer" are fixed, Oracle-vetted, test-pinned, and harness-verified (type
identification 99.9% on your 1760 real docs, would-auto-file-wrong = 0). A fresh installer is
in `dist\`. Everything is revertable in one command each (code + DB). Two 2-minute human steps
remain for you (below).**

---

## What was wrong (three separate defects, all proven)

1. **Stale build** (fixed last evening): the installed `…-1326-e5d8c85.exe` predated the
   doctype-precedence fixes — root cause of the original mis-typing. Fixed by rebuild.
2. **Migration 43 never ran on real installs**: the `title_aliases` column-add sat inside the
   long-stamped migration-2 block, so it only ever landed on FRESH databases (its unit test
   passed while every live DB stayed at v42). → proper stamped migration 43 + a regression
   test simulating a stamped v42 install. Commit `d0ef5b7`.
3. **Reprocess pinned the machine's own wrong type forever**: `--known-doc-slug` replayed the
   pipeline's wrong guess as if a human chose it, and skipped the new title-precedence logic
   entirely (`if not _ks`). So no engine fix could ever reach an already-mis-typed doc via
   "Reprocess with Learned Data". → the **type-authority override** (Oracle-signed design):
   a NEVER-confirmed doc's type may be re-typed by its own TRUSTED standalone title heading;
   a human-confirmed type is never touched; clipped scans keep the pin; the wrong-type linked
   template is not resurrected; the flip plants a review note (blocks auto-file — a flip can
   never silently file) and drops the old type's stale extraction rows. Also fixed a
   pre-existing split-brain (fresh title-trust shipped alongside the assigned slug).
   Commits `041f2c4` (Python) + `f684c17` (handler/persistence).
4. **"Customer:" reads landed in Document Issuer** (your goodnight directive): on a type whose
   identity field is `customer_name` (sales-order style), the shipped keyword label bank is
   ENTIRELY recipient captions ("Bill To/Customer/Client…"), so the BUYER's name filled the
   issuer at an unflagged 78% and the doc filed under the buyer. → the **recipient-caption
   issuer guard** (Oracle-signed, flag-only): such a read is capped to 69 + carries
   "Document Issuer was read from a customer/recipient caption — this may be the recipient,
   not the issuer. Please confirm." → always lands in Review, never auto-files, value never
   rewritten. Exempt: all learned/taught methods (template_fixed*, keyword_override, anchor*,
   hint), your accept buttons, agreement with the resolved supplier, and types carrying both
   company keys. First doc from an unknown sender = a human confirms the issuer; subsequent
   docs = learned methods (your directive, in fail-toward-review form). Commit `c17b66f`.

Plus the **Ashford data fix**: template 9 (sales_order) now carries a LOCKED fixed
Document Issuer = "Ashford Wholesale" (the exact write the "Always use the same value" UI
makes). Verified end-to-end on the real AW_sal_10: `type=Sales Order, issuer=Ashford
Wholesale @95 via template_fixed_locked, no flag` → files under `Ashford-Wholesale\`.

## Verification (everything ran tonight, in order)

| Gate | Result |
|---|---|
| 21 Python unit checks (type authority + manifest plumbing) | ✅ all pass |
| 18 JS unit checks (flip persistence + LOAD-BEARING note proof: the flipped 100% doc auto-files if the note is stripped) | ✅ all pass |
| 22 Python unit checks (issuer guard, incl. 69-vs-70 pin + learned-exemption pin) | ✅ all pass |
| Real-PDF E2E — AW_wor_15: pin (no flag) = Sales Order unchanged / machine authority = re-typed **Worksheet**, wrong template refused / human = pinned | ✅ |
| Real-PDF E2E — AW_sal_10: issuer flagged @69 with note pre-data-fix; issuer = Ashford Wholesale @95 locked, no flag post-data-fix | ✅ |
| Harness A/B leg 1→2 (reprocess fix): **byte-identical** on 1760 confirmed real docs | ✅ |
| Harness leg 3 (issuer guard): **byte-identical to baseline too** (flag-only; live effect proven by the E2E) | ✅ |
| Oracle FINAL full-diff review: **SHIP WITH CONDITIONS** — every design condition verified in code; caught a pre-existing suite my change broke (`test_reprocess_manifest.py`, 4-tuple unpacks → fixed) + demanded wiring/polarity pins (added). All conditions executed, all suites re-run green. Commit `63b0cdb` | ✅ |
| Existing suites re-run: detect_type_aliases, template_matcher, scope_trust, doc-type aliases/structural/presets | ✅ all pass |

Baseline accuracy on your real corpus (1760 docs): **type 99.9% · supplier 99.8% · ref 99.7%
· date 99.8% · would-auto-file-wrong 0**. (The handful of listed "regressions" are the known
stale-GT docs — e.g. #1778's poisoned Sales-Order confirm — not pipeline errors.)

## Revert map (if anything is broken)

- **Code**: `git revert c17b66f` (issuer guard) / `f684c17` + `041f2c4` (reprocess fix) /
  `d0ef5b7` (migration fix). Baseline before tonight = `a873c4e`.
- **DB**: backups in `%APPDATA%\ScanFinder\db_backups\` — `…pre-overnight.bak` (23:08, before
  everything) and `…pre-template-fix.bak` (23:40, before the Ashford fixed value). The
  Ashford fixed value alone: `node …\set_ashford_fixed_issuer.js revert` (script in the
  session scratchpad) or untick it in Settings → Templates.
- **Installer**: previous good build `…-2128-a873c4e.exe` still in `dist\`.

## Your two 2-minute steps

1. **Install `dist\ScanFinder Setup 2.0.0-r20260709-2256-63b0cdb.exe`** (built 23:58 from
   tonight's final HEAD — the older `…-2128-a873c4e.exe` and `…-1326-e5d8c85.exe` in the
   same folder do NOT contain tonight's fixes; safe to delete once the new one is in).
2. **Doc 1778 (AW_pur_08)** is still confirmed as a Sales Order (filed as
   `Unknown-Company\…\Sales-Order.PO8843.pdf`): Settings → Learning Repair → *Send back to
   Review* → **switch the type to Purchase Order BEFORE hitting Confirm** (confirming it as
   Sales Order would recreate the learning poison we cleaned). I'll verify the scope after.

## Decisions that need YOU (Oracle flagged, deliberately not done overnight)

- **Recipient-identity businesses** (e.g. a preset "Sales Invoice" filed BY CUSTOMER): the new
  issuer guard means that doc class always goes to Review (no auto-file) until a carve-out
  ships. Safe direction, but it's a UX cost you must ratify or we design the carve-out
  (variability-evidence has a pollution seam the Oracle wants you to rule on).
- **"First doc always manual" for ALL types** (your goodnight message, taken literally): would
  also cover supplier_name letterhead reads — big bulk-import UX change; needs a product
  decision, not a midnight edit. The guard as shipped covers the customer-caption class.
- Polluted-scope nag: if a scope ever had a buyer name mis-confirmed as issuer, the template
  fixed value never self-forms and that supplier's sales orders keep flagging — cure is
  Review → Advanced → learning history → purge the bad value (or a locked fixed value, as
  done for Ashford).

## Also noted
- The dev app was closed at ~23:40 to unlock the DB/build (it was idle; any unsaved in-Review
  edits from before bed would have been lost — none observed).
- CLAUDE.md has two stale claims the Oracle flagged earlier tonight (saveAnchor sweep is now
  supplier-scoped; the "daytime cause fix not done" note is done on this branch) — worth a
  docs pass.
- Garbled anchor-label admission at SAVE time (the id=45 class) is still open (e793dbd fixed
  only the readout) — small follow-up.
