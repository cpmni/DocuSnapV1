# NIGHT RUN — 2026-09-17 (owner "going to bed"; the dark-switch flip-census batch)

Branch `feat/teach-side-overnight`. Standing protocol: auto + safe; agents free (advisors + Oracle, parallel
when independent); Chris ALWAYS sandboxed; approval-class LOGGED under "NEEDS YOUR APPROVAL (morning)" and
skipped (a flip is approval-class — never flip autonomously); dangerous → agents → stop that item. All night
commits LOCAL/unpushed (push is the owner's call).

Source of truth for the plan: `HANDOVER_2026-09-17_C12_AND_SWITCHES.md` PART B (the per-switch gate map).
This prompt is PART B item B3.2 — the overnight Recipe-1 (R1) flip-census batch over the reading switches.

---

## What this night does
Run ONE R1 flip-census arm per reading switch on the 700-corpus fixture: baseline OFF vs that switch ON, over
the 400 GT docs, and report per switch **M (new would-auto-file of a WRONG value — must be 0), FIRE count
(0 fires = vacuous → stays dark), the would-file SET delta (removals=holds OK; any addition must equal GT),
and every value change**. Present the passers in the morning for a batch flip like migs 172-174. **Do not flip
anything** — that is the owner's call.

## Setup (reuse the 2026-09-16 runner — do NOT rebuild from scratch)
1. COPY `Desktop\Flip Corpus 700\warm_700.db` to scratch and migrate the COPY to HEAD (the Desktop fixture is
   NEVER touched). Template: `TESTING/_measure/flip_corpus_20260912/rerun_20260916/mig.js` +
   `census_run.sh` (copy both into this night's scratch dir, repoint `SP`/`DB`/`IDS`, change the `run_arm`
   list to tonight's switches). IDS = `TESTING/_measure/flip_corpus_20260912/rr_ids_700.txt`.
2. Baseline = `RR_APP_ENV=1` (the app's real flag env; `=0` is the vacuous-arm trap). The switch lever is the
   SHELL ENV the arm spawns with (env var = the switch key UPPERCASED, e.g. `trust_ref_role_shape` →
   `TRUST_REF_ROLE_SHAPE`), NOT a DB write — the flags are read outside the mirrored `_appSpawnEnv`.
3. Each arm ~10 min. Run arms DETACHED via PowerShell `Start-Process bash …` (the Bash tool caps background
   jobs at 10 min). NEVER run the pin suite while arms run. Kill a stuck arm by PID (bash first, then its
   electron children). Never edit a mapper/engine `.py` while an arm is running.
4. Compare with `rerun_20260916/compare.js` (baseline vs each arm). READ every comparator report before
   claiming a pass — never claim a pass you did not read.

## The switches, in order (highest value per hour first)
**FIRST — the two near-flip-ready (already safety-passed once + live-confirmed 09-10). One clean R1 pass at
HEAD each → present for flip first:**
- `trust_ref_role_shape` (154) — ref role by learned SHAPE not set-membership (20-held-Thornbury fix).
- `template_drift_override_guard` (157) — the CH1 2HU postcode-drift root cause.

**THEN — the R1-gateable reading batch (one arm each; expect M=0 trivially for the review-bound ones — the
real question is FIRES + no new filer):**
- Reference-flag family (migs 107-118, all REVIEW-BOUND): `format_variance_relax`, `format_variance_relax_ref`,
  `format_variance_relax_ref_inline`, `filing_sanity_ref_corrob_soften`, `filing_sanity_ref_history_soften`,
  `resolve_ref_near_miss`, `resolve_ref_positional`.
- 09-06 log-review arcs: `anchor_bare_label_fuzzy`, `anchor_labelless_currency_refuse`.
- 09-07 placement arcs: `reread_hold_corrob_release`, `template_date_left_clip_grow`,
  `template_pad_date_containment_flag`, `template_clip_commit_left_slack` (read
  `TESTING/_measure/clip_left_slack_20260907/diff_off_on.txt` FIRST).
- `inline_disagree_corrob_soften` (135/136).
- `type_split_teach_scope_suppress` (144) — read-only; the gate is `wouldAsk(ON) ⊆ wouldAsk(OFF)` + pins.
- `role_disagree_refuse_at100` (152) — it only ever REMOVES a filer: assert `wouldFile(ON) ⊆ wouldFile(OFF)`.
- `note_topic_dedup` (158) — cosmetic (note text only): the auto-file SET must be identical ON == OFF.
- `template_date_invalid_yield_lowconf` (166) — M=0 + set-equal would-file + zero date-accuracy drop.
- `date_forms_wide` (167) — run with mig 166 in its OFF state (measure the two date fixes apart); plus the
  `VAL_CENSUS_DIR` crop/keyword acceptance census OFF vs ON, every NEW acceptance eyeballed against the page.

**NOTE the load-bearing 09-12 finding:** the synthetic corpus proves SAFETY (M=0) but CANNOT show EFFICACY for
GEOMETRY fixes (clip/skew/drift fire 0 there — the shipped keyword read + pad-window already recover the easy
geometry). For the placement/clip arcs, R1 = the safety leg only; efficacy stays a live-copy re-judge (logged,
not run tonight). If an arm shows 0 fires, record "vacuous on this corpus → efficacy owed on real exhibits".

## LOG for the morning (do NOT run blind tonight — named precondition / non-census gate)
- `format_class_join` (120) — HOLD, needs a LOOK not a re-run (09-12: M=0 but a blast-radius finding, re-arms
  total-format checks on unrelated invoices → 7 new review-bound notes). Have an advisor look at WHY.
- `confusion_precedence` (119), `buyer_issued_convention_one_confirm` (125) — R1 is VACUOUS (need a correction
  history / a convention record). Build the fixture, then R1 for safety. Log as owed.
- `template_edge_clip_heal` (151) — CODE precondition first (`_corrobLicensedKeyword` scoped to `_edgeclipheal`).
- `template_code_read_widen` (141) — geometry: R1 safety only; efficacy = a real wider-docket exhibit.
- Name-grow belts (140): `template_name_grow_band_pick`, `template_name_cut_defer_cap`,
  `keyword_superstring_name_note` — gate ALSO wants `teach_run_ab`/`score_teach_run` ≥ today (teach-side harness).
- `type_uninstalled_heading_fold` — R1 for safety; efficacy = the 09-06 text census (already MET 20/20).
- `teach_angle_compose_null_abstain` (129) — NOT a flip candidate alone (see its dark_switches note).
- `template_fragment_containment_yield` (100), `template_locate_role_qualifier` (99) — Recipe 2 (realdoc-605),
  not R1; queued separately in `NIGHT_RUN.md`.

## Morning deliverables
- A per-switch table: switch · M · fires · would-file delta · value changes · verdict (flip-ready / vacuous /
  efficacy-owed / precondition-owed). Artifacts under `TESTING/_measure/flip_census_20260917/`.
- The passers presented under "NEEDS YOUR APPROVAL (morning)" for a batch `@DEFAULT_FLIP` flip (recipe:
  `HANDOVER_2026-09-17.md` §7 — labelled UPSERT migration + delist + pin + ledger).
- Update `docs/DARK_SWITCH_LEDGER.md` (strike/annotate each entry tested) and this file's DONE ledger.
- Chris sandboxed round only if CPU frees after the arms (optional).

## DONE ledger — see the tail of `NIGHT_RUN.md` + the wrap handover.
