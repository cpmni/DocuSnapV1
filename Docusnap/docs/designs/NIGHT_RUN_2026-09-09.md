# NIGHT RUN — 2026-09-09 · DETECTION ANOMALY HUNT + owed gates

Owner order (2026-09-09): "a night of testing and looking for detection anomalies." Auto + safe; agents free;
Chris ALWAYS sandboxed; approval-class → log under "NEEDS YOUR APPROVAL (morning)" and skip; dangerous →
agents → Oracle, else STOP the item. Nothing touches the live DB / app / Desktop originals. HEAD `4bb2efd`.

## Tier 1 — the anomaly hunt (safe, read-only)
1. **Full detection sweep** — `realdoc_regression.js` on a COPY of the live DB (the owner's real
   Copperfield/Ironbridge/worksheet set — the exact suppliers that hit anomalies this session) + the 147-doc
   census corpus DBs. Capture per-doc read/method/confidence/wouldFile/flags (RR_CONSENSUS). Rank:
   - **M — silent wrong auto-files** (must be 0),
   - high-confidence-but-WRONG (dangerous) + low-confidence-but-CORRECT (calibration holds — the WS-95132 class),
   - confusable clusters (O/0, I/1 on refs), issuer/identity degradations (Ironbridge/logo), type mis-detection.
   Deliver a ranked anomaly list → morning.
2. **Arc A flip census (Oracle C6/C7)** — construct a ≥5-confirmed graduated scope so the prefix-autofile arc
   is exercised; arc-inert vs arc-live decomposition + the adversarial mirror set + M=0. (May need a built
   sandbox scope — if setup is non-trivial, LOG the exact steps + partial result for morning.)

## Tier 2 — detection improvements (investigate + DESIGN; build is approval-class → morning)
3. **Born-digital `bd_used:false`** — WHY is born-digital detected but the text layer unused? Would using it
   read exact text (no OCR O/0 at all)? Oracle called this higher-value than either confusable arc. Analysis +
   a fix design. NO build tonight.
4. **Tesseract timeout/crash (oscar #1)** — the per-call `timeout=` + `SetErrorMode` design (from
   HANDOVER_2026-09-09_EVENING §1). A timed-out doc is a detection failure. LOG the finished design + the
   decisive attribution test for morning approval. NO build tonight (approval-class).

## Tier 3 — customer lens + specialists
5. **Chris The Customer** — the `christest` skill (sandboxed COPY; cards logged, never implemented). Detection +
   UX anomalies from the non-technical lens. Hand Chris the explicit sandbox path; `%APPDATA%\ScanFinder` = the
   owner's LIVE app, OFF LIMITS (2026-09-09 queue item).
6. **Advisor consults on the Tier-1 clusters** — herald (type flips), iris/Phillip (logo/identity), oscar/007
   (OCR reads), reggie (ref patterns). Spawn AFTER the sweep names the clusters.

## Guardrails
Anomaly → design, never a blind fix; every code change goes through the advisor + Oracle gate. Approval-class
builds are LOGGED, not shipped. Deliverables: a ranked anomaly report + this session's HANDOVER + the NEEDS
YOUR APPROVAL queue + the DONE ledger update in NIGHT_RUN.md.
