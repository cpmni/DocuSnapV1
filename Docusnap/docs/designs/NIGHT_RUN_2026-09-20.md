# NIGHT RUN — 2026-09-20

Branch `feat/teach-side-overnight` @ HEAD `4d0dc09` (pushed). Today shipped: graphical split + blank removal,
clip fix (`anchor_code_left_grow` mig 192, DARK), LAN-connect fixes, thumbnail retry; hardened customer pair
built. Autonomy protocol as standing: auto-run the safe items, agents free, **Chris always sandboxed**,
**approval-class → log for morning + skip**, dangerous → agents → stop that item. Nothing pushed/deployed
(owner's call). If the peer video session is still on this machine, stagger heavy jobs.

## Queue (priority order)

### 1. Clip-fix efficacy — the Oracle-REQUIRED synthetic clip injection  ·  AUTO (test only)
The safety census PASSED but was VACUOUS (0 fires on the synthetic corpus). Oracle made a synthetic left-clip
injection a sign-off condition: prove `anchor_code_left_grow` actually FIRES on the class AND correctly refuses
to converge on the adversarial "really-is-V" case. Build a real fixture (PIL-rendered ref where the taught box
clips the leading glyph, so the rigid crop reads e.g. "VS-…" while the wider re-read + full page read the true
"WS-…"), drive it through the real recovery (NOT stubbed), assert: OFF → flag; ON → converges → clean, no flag;
adversarial (true value really is the minority form, on-page) → no convergence → flag STANDS. If the raster proves
too flaky for Tesseract to reproduce the clip deterministically, fall back to the born-digital text path and
DOCUMENT the residual. Output: a committed efficacy test + a note in the census RESULT.md → flip-ready or findings.
Safe: writes a test, no live changes, DARK switch unchanged.

### 2. DESIGN — opt-in auto-split on import + the page-1 separator-sheet trigger  ·  gary + Oracle → LOG for morning
Shape agreed (pendingfeatures.md 2026-09-20): default auto-detect split OFF; a multipage scan lands whole with a
calm "Split" affordance; triggered by (a) a global opt-in setting or (b) a separator sheet placed as PAGE 1
(override, checked before the opt-in gate); sheet-separation stays; ONE setting for import + watch; page-1-only
extraction (confirm the engine doesn't already OCR every page — sizes the win). gary designs the pipeline +
page-1-sheet override + the extraction-cost measurement; Oracle vets. Output: a build-ready plan. **Do NOT build
(approval-class) — log for morning.**

### 3. DESIGN — 'Undetected' issuer bucket  ·  barry + reggie/gary + Oracle → LOG for morning
Shape agreed (pendingfeatures.md): when the best issuer candidate FAILS name-quality (`value_quality.is_name_like_field`/
`name_quality`) AND there's no logo/template/teaching, mark the issuer Undetected (blank) instead of showing a
disclaimer sentence; group those docs in a calm Review 'Undetected' section. barry (bucket UX) + reggie/gary (the
declare-undetected gate — exactly when it fires, byte-identical when a real name is present) → Oracle. Guard against
hiding a genuine-but-faint real name. Output: a build-ready plan. **Log for morning.**

### 4. DESIGN — Castellan Slice 2 reframe  ·  gary + Oracle → LOG for morning
Oracle SENT BACK the caption-suppression approach: the cold-start hold on the FIRST doc of a newly-taught supplier
is `docTrustGate`'s no-history refusal (`unverifiable-value`), not caption disagreement. Reframe: is "first doc from
a new supplier files with zero confirms" a thing we should do at all (the safety is deliberate), and if so the lever
is the no-history/graduation gate (approval-class), not the caption rule. Output: a reframed approach or a documented
DO-NOTHING. **Log for morning.**

### 5. Chris `/christest` on the clean hardened build  ·  AUTO (sandboxed)
Enforcement re-run + a fresh full-app vet on the shipped hardened build (2026-09-20 pair), fully sandboxed (fresh DB,
isolated userData, copied Demo Docs). Standing safety rules: destructive freedom INSIDE the sandbox only, real
screenshots, findings queued — never implemented without the owner's vet. Output: `docs/CHRIS_FULL_APP_REVIEW_2026-09-20.md`.

### 6. Full pin-suite regression at HEAD  ·  AUTO
`node scripts/run-pins.js` across all locations + the Python suites touched today (splitter, blank-scan, anchor
crosscheck, anchor_code_left_grow, migration pins). Confirm the whole tree is green at `4d0dc09` after the day's
changes. Output: a pass/fail line; investigate any red.

## Explicitly NOT tonight (owner / approval-class)
- Flipping `anchor_code_left_grow` (needs live-Larkspur efficacy OR item 1 green + owner go).
- Building items 2/3/4 (design only tonight).
- Push (already pushed) · deploy the 3 licensing-server changes · DB-encryption 2a/2b decision · VM live-verify.

## DONE ledger
- [x] **1 clip-fix efficacy injection** — DONE, committed `8787693`. `test_anchor_code_left_grow_efficacy.py`:
  real render + real Tesseract, recovery FIRES (WS-62315 recovered @90 through every guard), adversarial minority
  form ABSTAINS. Flip gate now fully met (M=0 safety + efficacy fires + adversarial abstains). RESULT.md updated.
- [x] **2 opt-in-split design** — gary → Oracle **SIGN OFF WITH CONDITIONS**. Build-ready plan
  `docs/designs/OPTIN_SPLIT_2026-09-20.md`. Ship-blocker Oracle caught + folded in: a mandatory multipage-auto-file
  HOLD (else a graduated supplier silently auto-files a merged bundle). Awaiting owner go to build.
- [x] **3 undetected-issuer design** — barry UX shape (a "Not recognised" tab; membership never keyed on
  confidence; part (A) don't-commit-a-non-name is the L1 first build). pendingfeatures.md updated. Next stage =
  reggie/gary value-quality gate → Oracle, then build.
- [x] **4 Castellan Slice 2 reframe** — gary → **DO-NOTHING** (Oracle-aligned): the first-doc hold is
  `docTrustGate`'s no-history anti-poisoning safety (`unverifiable-value`, trust.js:1133), NOT caption disagreement.
  Zero-confirm-first-doc trades away the only value-correctness checkpoint a taught scope gets; Slice 1 already gives
  "one confirm clears the batch." Keep the safety. Also ruled: `trust_ref_role_shape × role_dominant` — HOLDING is
  correct (don't defer ref-shape to role-dominant on an outlier-collapsed scope; the outlier means poisoned history,
  fix via Learning Repair). If the owner still wants zero-confirm, the lever is the graduation gate (approval-class,
  own Oracle pass) and it likely won't even release Castellan (the non-strict issuer still holds) — weak premise.
- [ ] 5 Chris /christest — RUNNING (sandbox port 9223, PID 8536).
- [x] **6 full pin regression** — **401/401 green, 0 red** at HEAD (`8787693`). `TESTING/_measure/nightrun_pins_20260920.log`.
