# NIGHT RUN — 2026-09-21

> Owner going to bed. Execute this. Standing autonomy protocol applies (`memory/feedback_night_run_autonomy_protocol.md`):
> **auto + safe → do it; agents free to spawn; Chris ALWAYS sandboxed; approval-class → log for the morning + skip;
> dangerous → ask the agents, and if still risky STOP that item (not the whole run).** Commit each change LOCALLY;
> **do NOT push** (owner reviews + pushes in the morning). Update the DONE ledger in repo `NIGHT_RUN.md` as you go.
> Cold-start state is in `HANDOVER_2026-09-21_NIGHT.md` — read it first.

Two workstreams. **Do TASK 0 first, then run A and B; B (Chris) is the long pole — kick Chris off early so his
vet runs while you do the census in A.**

---

## TASK 0 — housekeeping (5 min, do first)
1. **Commit the pending UI tweak.** `src/windows/settings/renderer.js` has the owner-requested graphical folder
   TABS for the Document-Types Scanned/Quick-File views (uncommitted). Commit it alone so the tree is clean:
   `polish(settings): graphical folder tabs for the Document Types lane views`. (Owner asked for it; they'll eyeball
   it in the morning — isolated commit = easy to tweak/revert.)
2. **Confirm green baseline:** `node scripts/run-pins.js` (expect **409/409**; the `test_ref_class_fix` timing flake is
   the only known-flaky one). Also run the two `src/lib` pins directly (the runner doesn't scan `src/lib`):
   `src/lib/test_ooxml_grid.js`. Do NOT run pins while a census arm is running (they fight for the machine).

---

## TASK A — batch dark-switch census + flip the clean passers
**Goal:** most OFF switches are built-correct and waiting on ONE test. Run each still-OFF candidate against the
700-doc set; flip the clean passers to customer-default. **Owner authorised flipping clean passers this run.**

### A1. Get the TRUE current OFF list (the ledger is behind)
- `docs/DARK_SWITCH_LEDGER.md` is the plain-English list BUT is stale (it doesn't record the 09-19 flips migs
  187-190 = trust_ref_role_shape/template_drift_override_guard/note_topic_dedup/reread_hold_corrob_release).
- **Read `database/dark_switches.js` for the ACTUAL current state** — the switches seeded OFF + still in
  `TEST_SWITCH_KEYS`. Cross-check against the ledger's WAITING group. Census ONLY switches that are still OFF today.
- **Refresh the ledger** as part of this task (mark the 09-19 flips DONE).

### A2. The census recipe (do it EXACTLY this way — traps below are load-bearing)
- Method + the reusable runner: `TESTING/_measure/flip_corpus_20260912/CENSUS.md` (+ its `rerun_20260916/` runner +
  comparator). Memory: `project_flip_corpus_pipeline_20260912.md`.
- Corpus: `Desktop\Flip Corpus 700\warm_700.db` (warm/confirmed). **COPY it + run migrations to HEAD first** so the
  schema matches (mig 198). Run `stress_test/realdoc_regression.js` with `RR_DB` = the migrated copy.
- **Baseline MUST be `RR_APP_ENV=1`** (the faithful operating point; `=0` is the vacuous-arm trap that made ref look
  47%). Sound baseline on this corpus: ref ~97% / date ~99% / ~179-of-400 auto-file / **0 wrong filing-field auto-files**.
- **The switch lever is a SHELL ENV var, NOT a DB write** (the switches are read outside the mirrored `_appSpawnEnv`
  functions). Set the switch's env for the ON arm; leave unset for the OFF arm.
- **Per switch:** OFF arm vs ON arm on the migrated copy. Compare: **M** (misfiles / any doc that got worse), whether
  it **fires** at all, and the **direction** of every change.
- **PASS bar (reading/date/reference group):** `M = 0` **AND** ≥1 real fire **AND** every change is only
  auto-file→hold or a correct heal (NEVER hold→wrong-file, NEVER a new wrong auto-file). Pass → flip.
- **Auto-file-friction group** (the ones that turn held→filed): SAME test **plus** eyeball every NEW auto-file — bar
  is **0 wrong auto-files**. Higher risk; if any new auto-file is wrong, DON'T flip, log it.
- **Do NOT census (log for the morning instead):** `segment_pair_hold` (needs the C12 Rejoin button built first),
  `format_class_join` (found a wider blast radius — needs a code look), `deskew_corrob_autofile` (owner's call).

### A3. Flip a clean passer (the graduation ritual — see CLAUDE.md "release-gate rule")
For each PASS: add a migration that UPSERTs the setting to `'true'` with `// @DEFAULT_FLIP <N>` directly above its
`if (!applied.has(N))`; **delist the key from `TEST_SWITCH_KEYS`**; add a pin (`database/test_default_flip_<N>.js` or
extend an existing one). Re-run `node scripts/run-pins.js` green after the batch. Commit each flip (or a batched flip
commit). Record M/fires/direction per switch in `TESTING/_measure/flip_census_20260921/RESULT.md`.

### A4. Traps (from the pipeline memory — non-negotiable)
- The Bash tool has a **10-min background cap** → launch a long census **DETACHED via PowerShell**
  `Start-Process bash` (or `run_in_background` + a Monitor), not the Bash background.
- `TaskStop` leaves the bash running its NEXT arm → kill by PID (bash first) if you abort.
- Never run `run-pins` while an arm runs. The soak/census sandbox app may still be up on a DevTools port — harmless.

---

## TASK B — Chris: full Quick File daycare vet → recommendations → plan → Oracle → implement
**Owner's words:** "Chris runs a full test of the Quick File features, using the daycare example as a guide. He
pretends he works in daycare and will try to file relevant docs. After he finishes, take his recommendations, plan
the fixes and run it by the Oracle — if there is agreement, implement the changes."

### B1. Set up the sandbox WITH the features ON
- Use the **`/christest`** skill (fully sandboxed: fresh DB, isolated userData, copied Demo Docs — never the live DB).
- **Before Chris starts, flip the feature flags ON IN THE SANDBOX DB** (else he sees nothing): `direct_intake_enabled`,
  `lookup_lists_enabled`, `quickfile_multidoc_enabled` = `'true'`. (Pattern: `scratchpad/flip_flags.js` — but point it
  at the SANDBOX db path, NOT `%APPDATA%\ScanFinder`. The christest skill prints the sandbox userData path.)
- Seed a realistic daycare shape so he has something to file: a Quick File document type ("Child Record") with custom
  fields (Child name = master, DOB [date], Address, Medical notes [longtext], Guardian), a small Records list
  imported from a CSV of a few children, bound to the type. If the skill's Demo Docs don't include daycare-ish files,
  generate a handful (a couple of PDFs/images named like consent/medical/registration forms) in the sandbox inbox.

### B2. Chris's brief (customer-experience-review lens; he FINDS, doesn't implement)
He is a daycare admin with a stack of handwritten/printed docs about children. Have him actually DO:
- Set a type to Quick File / add custom fields (the Settings lane tabs + "How is this type filed?" + Add field).
- Create/import a Records list; add a child on the fly.
- Quick File a single doc: pick the child via 3-char typeahead → check the fields fill, are editable, sensible.
- Quick File MULTIPLE docs at once (the multi-doc pane): different children per doc — check the per-doc form +
  preview + "apply to all" + that each files under its own child.
- File a handwritten doc (no OCR) and confirm it's searchable after.
Apply the standard battery (cold narration, decision count, copy read-aloud, fear probe, jargon tripwire). Findings in
the owner-vet card format. Save to `docs/CHRIS_QUICKFILE_VET_2026-09-21.md`.

### B3. Take his recommendations → plan → Oracle → implement (owner-authorised)
For each finding worth acting on:
1. Plan the fix with the right advisor (barry product / eric electron-UI / gary data / reggie patterns).
2. **Run it by the Oracle.** Only if the Oracle **agrees** (SIGN OFF or SIGN-OFF-WITH-CONDITIONS, conditions honoured)
   → **implement** it: smallest correct change, DARK behind a flag where a flag is warranted, pinned, `run-pins` green.
3. Oracle SEND BACK / DO NOTHING / WRONG LAYER → **log it for the morning, do NOT implement.**
- Log every verdict in `docs/oracle_log.md`. Commit each implemented fix locally (do not push).
- Anything destructive, irreversible, or beyond a Quick File UX/logic fix → **log + skip** (approval-class).

---

## Morning report (leave at the top of a fresh `handover.md` or a HANDOVER_*.md)
- Task A: the census RESULT table (switch → M / fires / direction / flipped?); which flipped (migs), which logged.
- Task B: Chris's card doc, the Oracle verdicts, what got implemented (commits) vs logged.
- `run-pins` final count; anything left red; the DONE ledger updated in `NIGHT_RUN.md`.
- Reminder: NOTHING pushed — owner pushes after review.
