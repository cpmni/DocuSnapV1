# NIGHT RUN — 2026-09-10 (owner "going to bed"; planned + executed by the session)

Branch `feat/teach-side-overnight`. Standing protocol: auto+safe; agents free; Chris ALWAYS sandboxed;
approval-class LOGGED for morning + skipped; dangerous → agents → stop that item. All night commits LOCAL/
unpushed (push is the owner's call).

## Context — what shipped THIS session (all DARK, committed local, NOT pushed)
Six migrations built + Oracle-gated today, byte-identical OFF, in `TEST_SWITCH_KEYS` (now **41**):
- **mig 154 `trust_ref_role_shape`** (`1383bef`) — ref role verified by learned SHAPE not 'constant' set-membership. Fixes the 20-held-Thornbury-invoices blocker.
- **mig 155 `anchor_axis_lock`** (`1b4a956`) — additive width-invariant label-column read. **Censused → 0 fires → STAYS DARK** (`a5de4ab`).
- **mig 156 `name_role_nonname_flag`** (`02dcd93`) — flag+hold a name field reading a bare postcode/email/VAT/IBAN.
- **mig 157 `template_drift_override_guard`** (`55a6271`) — a drift-relocate may only discard the absolute read on a credible label match. The customer_name postcode-drift ROOT-CAUSE fix (CH1 2HU → the name). LIVE-confirmed.
- Note reword (`7bbf48f`) — concise filing-sanity soften note (owner "very wordy").

## THE PLAN (this night)
1. **Flip-gate measurements** for the 3 live DARK arcs (154/156/157) — reprocess a copy of the owner's real DB
   OFF-vs-ON per arc, dump every field + `wouldAutoFile`, report the delta. Gate: **wouldAutoFile(ON) ⊆
   wouldAutoFile(OFF)** (no NEW silent auto-file) AND every value change is a correction, not right→wrong.
   Harness: scratchpad `gate/gate.js` (reprocesses stored_path PDFs, computes trust.isAutoFileEligible; the
   605 corpus isn't in the reset live DB, so this proves no-regression on the owner's OWN docs — the
   class-relevant Vellum/Thornbury docs ARE present). Live-DB writes are NOT done (read + reprocess only).
2. **Note-composition cleanup** (owner "very wordy") — gary designs the de-dup of stacked ref notes; build DARK
   if the seam is clean, else LOG for the owner.
3. **Chris** — sandboxed vet (`/christest`) on the drift fix + today's arcs, once the gates free the CPU.
4. **Log** the flip decisions + push + any approval-class item under "NEEDS YOUR APPROVAL (morning)".

## Scope IDs
- drift-override (157): the 19 Vellum & Crane sales_order docs (231-251) — the class.
- ref-role (154): the 21 Thornbury invoices (168-188).
- non-name (156): the delivery_note + worksheet + sales_order name-block class.

## DONE ledger — see the tail of `NIGHT_RUN.md` + `HANDOVER_2026-09-11.md` (written at wrap).
