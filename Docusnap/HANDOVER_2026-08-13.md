# HANDOVER — 2026-08-13 (corroboration slices 2+3 + machine-feed slice 1, all dark)

**Branch `feat/teach-side-overnight` · HEAD `e752b95`, PUSHED, tree clean (3 commits this
session).** Autonomous continuation of the NIGHT2 arc; owner not present. Every slice
gary→Oracle-gated (gary ×3, Oracle ×3 — all SIGN-OFF-W/COND, zero SEND BACKs, every blocking
condition applied in-build). No flags flipped, no live-DB writes, no migrations (still 63).
**One background harness may still be RUNNING at wrap — see Verification.**

## TL;DR
The NIGHT2 first-action (demote live verify) was ALREADY DONE in the live DB — verified 5/5
correct — and its formally-owed gates are now CLOSED. Three new dark slices shipped on top:
**slice 2** (adjusted-total money note demote, `df3f668`), **slice 3** (name-guard note demote,
`f6fea09`), **machine-feed slice 1** (machine confirms leave the learning substrate, `e752b95`).
All DEFAULT OFF with toggles. Slice-3 and machine-feed flips are BLOCKED on named gates below.

## 1. SLICE-1 (dates) OWED GATES — CLOSED ✅
- **Live verify:** 5 Nordwind quotes carry `anchor_crop_crosscheck+corrob_clear` in the live DB
  (docs 1217/1218/1223/1227/1232), notes gone, conf 94, witness_family=`mapping` — proof the
  `775ad2c` located-bit fix works. Zero noted-xcheck rows remain on live docs. All 5 demoted
  dates checked against corpus GT: **5/5 correct.**
- **Realdoc M=0:** full-902 base==armed **md5-identical** (@300 DPI, vacuous for the class) PLUS
  a **targeted 200-DPI Nordwind arm where the demote FIRED (6 demotes)** and the report was
  still byte-identical. Census 6/6 correct vs GT (quotes 0016/0017/0025/0054 dates).
  **Flip bar met: demoted-and-wrong = 0 (harness 6/6 + live 5/5).**
- **THE TRAP THAT MADE THE FIRST ARM VACUOUS (recurs — remember it):** `realdoc_regression.js`
  spawns python at DEFAULT 300 DPI; the live app runs `ocr_dpi=200` via `OCR_RENDER_DPI`
  (handler.js:91-98). The crosscheck disagreement class only forms at 200 DPI. Set
  `OCR_RENDER_DPI=200` on class-exercised arms. Also: realdoc replays CONFIRMED docs only.

## 2. SLICE 2 — adjusted-total note demote (`df3f668`, DEFAULT OFF)
Flag `RECON_TOTAL_NOTE_DEMOTE` / toggle `recon_total_note_demote` (Settings→Processing).
gary design → Oracle SIGN-OFF-W/COND C1-C5, all applied:
- `RECON_TOTAL_ADJUSTED_NOTE` hoisted single-source (engine.py ~:64); both PASS-1/PASS-2 total
  write sites reference it; eligibility EXACT equality (no method gate possible — the pick
  commits the candidate's method). PASS-2 **subtotal note stays a literal + survives a total
  demote** (pair elected each other — pinned; the doc stays held).
- Witness: crop-side ledger, un-noted, ≥80, mapping located-carve-out, **penny-exact
  (round(x*100), NO tolerance) + SIGN-agreeing (`validator._is_negative_value` both sides —
  Oracle C1: parse_amount is sign-blind, 4th member of the 08-07 family)** AND
  `total_reconciles(...) is True` re-verify (None/False keep the note).
- **NO confidence minting** (below slice-1's E2 posture; money has no shape rail; the pick
  commits max(inc,cand) so the swapped value can inherit the displaced incumbent's conf —
  census records committed_conf/witness_conf for visibility). Conf-78-stays-78 pinned.
- Dissent: `self._recon_displaced` per-doc stash (reset beside `_field_candidates`) →
  `note_demoted.rejected_read` + census `recon_demote_census.jsonl` (records DECLINED too).
- Pins `test_recon_note_demote.py` **34/34**; slice-1 suite 26/26 untouched.
- **SAY AT FLIP:** the note does NOT re-form on harness replay (census 0 rows over 73 Nordwind
  docs incl. needs_review — the anchor_inline wrong-row misread is import-batch-specific), so
  the class-exercised acceptance = the owner's live reprocess after flip (08-07 precedent);
  release may not un-park on its own (no minting); the classic drift exhibit (no crop backing)
  is deliberately NOT demoted.

## 3. SLICE 3 — name-guard note demote (`f6fea09`, DEFAULT OFF) — **FLIP BLOCKED ON B2**
Flag `NAME_CORROB_NOTE_DEMOTE` / toggle `name_corrob_note_demote`.
gary design → Oracle SIGN-OFF-W/COND B1-B3 (B1+B3 applied in-build; **B2 = the flip gate, OWED**):
- `anchor.NAME_GUARD_DISAGREE_NOTE` hoisted (byte-identical to the test_name_guard_keyword_clear
  pin); the five OTHER relocate-guard texts stay composed/ineligible.
- Demote requires ALL: **W1** crop-side ledger witness (≥80 un-noted, mapping carve-out, MINUS
  `template_fixed` — the F8 memory-masquerade hole: `template*` buckets as mapping — and minus
  `'+corrected'`/`'+confirmed_adopt'`) · **W2** keyword-family read (≥70 un-noted — breaks the
  flush-clip crop↔crop common mode; committed value is crop-side so full-page agreement IS
  pixel-independent, the INVERSE of slice-1 Gate-C) · **D1** a guard-rejection RECORDED for the
  field · **D2** ledger unanimity (no un-noted ≥60 disagreeing read of any family). Equality =
  `_values_normalise_equal(...,False)` NO fuzzy — clipped 'Lt' ≠ 'Ltd' pinned both directions.
  **supplier_name NEVER demotes (pinned).** NO confidence minting.
- **B1 (production behaviour change, ships regardless of flag):** the anchor rejection recorder
  is now ALWAYS ON — `self._rejected_reads` (reset per doc), both Stage-2 + late-rescue sites;
  trace event still --trace-only. The Stage-2b parallel predicate **no longer keys on
  `on_reject is None`** (would have silently force-serialised every run = a READ-DETERMINISM
  change) — takes `force_serial=self._trace` via a new `extract_with_anchors` kwarg. Pinned.
- **B3:** NAME_GUARD_KEYWORD_CLEAR door comments name the interaction (both-ON idempotent; the
  #259 precondition governs both doors). Pinned.
- Pins `test_name_corrob_demote.py` **39/39**; keyword-clear + name-lock suites green.
- **B2 — OWED BEFORE FLIP (Oracle ruled #259 NOT discharged by design):** an armed #259-class
  replay (a doc whose ONLY hold is this note while a SIBLING field is silently wrong → after
  demote the doc must still be held or the census must catch it) + demoted-and-wrong measured
  at **DOC level** on newly-unparked auto-files; 101-doc `auto_reprocess` cohort = SUSPECT GT.
  Also census must ANSWER A2 (does release alone un-park under the far_lowconf two-tier?).

## 4. MACHINE-FEED SLICE 1 (`e752b95`, DEFAULT OFF) — **FLIP BLOCKED, gates listed**
Flag/setting `learning_exclude_machine_confirms` (read INSIDE learning.js; env
`LEARNING_EXCLUDE_MACHINE_CONFIRMS` wins both ways — the shadow-row-skip C5 pattern).
gary design → Oracle SIGN-OFF-W/COND C1-C6:
- **NEW `database/modules/machine_vias.js`** — the ONE sentinel set (FIVE: scope_sweep,
  auto_corroborated, auto_reprocess, auto_graduated, auto_threshold). **Oracle C1 catch:
  templates.js had DRIFTED (filtered 2 of 5) while `_autoFileDoc` calls `learnTemplateOnCommit`
  — machine files were driving template maturity (frozen-string counts → young-identity guard).**
  trust.js:595 + learning.js + templates.js now share the module (pinned).
- getFieldFormats armed filter: machine rows leave sample_values/value_counts/confirmed_count;
  **C2 carve-out (RETAIN ruled): a row with a human corrections.corrected_value stays counted.**
  Excluded evidence → additive `machine_value_counts` (armed-only, consumed by NOTHING — repo
  scan pin; slice-2 refusal union reads it later). Pre-mig-57 no-via-column = legacy, no throw.
- templates.js C1 leg: armed flag blocks ALL FIVE vias from template learning (legacy 2-value
  filter stays unconditional). C6: CONFADOPT docstring "≥5 human confirms" corrected (was a
  FALSE safety claim — counts ALL rows until this flag is armed); stale learning.js:1298 comment
  superseded in place.
- Battery `database/modules/test_machine_confirm_learning.js` **16/16**; scope-trust + CONFADOPT
  + wiring suites green.
- **Census numbers (live, post-remediation):** 76% of substrate machine-fed (683/902 docs);
  groupsDie **0** (nothing starves); domMachineMaj 20; confadoptDrop 0; strongFlips 0.
  **C3 DISCHARGED = 0:** only census hits are customer_name and the 2.5d snap SKIPS name fields
  (engine.py:7569) + the snap writes a note (not silent). **C4 DISCHARGED:** the one shape flip
  = Bramblewood|purchase_order|customer_name freetext→constant('Quillstone Print & Packaging'),
  direction FAVOURABLE. **Quillstone founding-fixture gate PASSES:** pre-remediation backup
  (docusnap_pre_namerepair_20260812.db) all-rows doc_freq **0.888** (RED, the recorded poison)
  → human-only **0.900 ≥ the 0.9 STRONG bar** (residual 0.900-not-1.000 = the 4 wrong-party
  human Bramblewood rows awaiting owner ruling).
- **STILL OWED BEFORE FLIP:** the armed realdoc M=0 arm (RUNNING at wrap — below) · C5
  gate-unify round-trip pin (armed exclusion + unify-ON stamp; exclusion is BLIND when
  `autofile_gate_unify` is OFF — via stamps NULL) · the C1 small census (post-stamp docs that
  drove template learning). Slice 2 (refusal-side unions: CONFADOPT B4 second-key, charset,
  snap known-union) = own Oracle pass, must land before any further CONFADOPT/snap widening.

## Verification state — honest
- All suites green at HEAD: demote 26/26 + 34/34 + 39/39 · machine battery 16/16 ·
  settings-wiring · scope-trust · CONFADOPT · keyword-clear · name-lock. `test_anchor_crop_
  crosscheck.py` 3 fails PRE-EXISTING (re-verified identical post-edit). Python tests need
  `PYTHONIOENCODING=utf-8` on this console.
- **Full-902 OFF arm (both new slices dark): md5-IDENTICAL to the pre-edit base** — incl. the
  always-on recorder + force_serial B1 change.
- **ARMED machine-exclusion realdoc arm COMPLETED before wrap-end (owner waited): M=0 BOTH
  COUNTS.** Values identical (same 484 pre-existing replay regressions); auto-file identical
  (14 would-file, same 3 pre-existing wrong dates; zero new wrong auto-files). **Counted cost:
  54 wrong reads lost their review FLAG (231→285 silent) — one class, the Pelican invoice-ref
  separator replays; the field's shape was machine-fed so exclusion kills the shape warn. All
  54 stay review-bound (none crossed into auto-file). SAY THIS AT FLIP.** Reports:
  `%TEMP%\rr_machine_armed_report.md` / `_diff.txt` (vs `rr_base_demote_report.md`).
- Neither slice 2 nor slice 3 fires on harness replay (censuses 0 rows) — the triggering
  misreads are import-batch-specific. Unit suites carry the exhibit shapes with real values;
  live acceptance at owner flip.
- Baseline reports this session: `%TEMP%\rr_base_demote_report.md` (=`rr_off_full_report.md`,
  same md5 `829a87d4...`); Nordwind mini-arm baselines `%TEMP%\rr_nord_*.md`.

## FIRST ACTIONS (fresh session)
1. ~~Read the armed arm diff~~ **DONE at wrap: machine-feed M=0 gate CLOSED** (values + auto-file
   identical; 54 flag-losses counted, one Pelican shape class, all review-bound — see
   Verification). Remaining machine-feed flip blockers: C5 round-trip pin + the C1 small census.
2. **Build the slice-3 B2 replay gate** (the flip blocker): #259-class armed replay + doc-level
   demoted-and-wrong census. #259 = ThornburyFasteners_delivery_docket_02 (ref DN-28472 misread
   DN-38472 while the name note was the sole hold).
3. **C5 gate-unify round-trip pin** for machine-feed (armed exclusion + unify-ON stamp).
4. Then the queue leftovers: Pelican #1067 (honest two-defect doc) · slice-1 F8 backport
   consideration (template_fixed buckets as mapping witness for DATE demotes too — filed).

## Needs the USER
- **Flip decisions** (all ship OFF): `recon_total_note_demote` (evidence complete — say the
  flip caveats in §2) · `name_corrob_note_demote` (BLOCKED on B2) ·
  `learning_exclude_machine_confirms` (BLOCKED on the armed arm + C5; requires
  `autofile_gate_unify` ON — it is).
- The 4 `Bramblewood Joinery Ltd` wrong-party customer rows on confirmed Quillstone POs —
  Learning Repair or leave (also the last 0.100 of the Quillstone lexicon gate).
- UI smoke of the three new toggles' copy (Settings→Processing).
- Nordwind 1217/1218 sit parked-eligible; 1217's remaining holds are exactly the slice-2 +
  slice-3 notes (the double exhibit) — flipping both + reprocess should release it end-to-end.

## Deferred (designed, not built)
- **Machine-feed slice 2** — refusal-side unions from `machine_value_counts` (CONFADOPT B4
  machine-observed second-key; charset unanimity; the snap known-union). Own Oracle pass.
- **Dead structural pin repair** (`test_xcheck_corrob_demote.py:158-160` — vacuous, Oracle-
  proven; pendingfeatures entry). Repair-or-delete, separate commit.
- Slice-1 F8 backport (above).

## Key facts / paths
Live DB `%APPDATA%\ScanFinder\docusnap.db` (mig 63, ocr_dpi=200) · backups beside it incl.
`docusnap_pre_namerepair_20260812.db` (the Quillstone gate fixture). New instruments:
`stress_test/…` unchanged; scratchpad (session 4223d9fa) holds `machine_learning_census.js`,
`machine_c3_c4_census.js`, `realdoc_nordwind.js`/`_nr.js` (supplier-filtered realdoc copies —
patch `require` to absolute paths, the outside-repo trap). Census env:
`XCHECK_DEMOTE_CENSUS_DIR` now yields three files (xcheck/recon/name_demote_census.jsonl).
New toggles (Settings→Processing, ALL OFF): `recon_total_note_demote`,
`name_corrob_note_demote`, `learning_exclude_machine_confirms`. GOTCHAS: realdoc DPI trap (§1);
realdoc = confirmed docs only; the slice-1 window pin's 900-char budget (call-site comments stay
tiny); `ELECTRON_RUN_AS_NODE=1` for JS suites; plain `node` wrong ABI for better-sqlite3.
