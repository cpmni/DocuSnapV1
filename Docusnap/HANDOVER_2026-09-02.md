# HANDOVER — 2026-09-02 (overnight, autonomous)

Branch `feat/teach-side-overnight`. **7 commits ahead of origin, NOTHING PUSHED** (owner-review gate).
Tree otherwise clean. Nothing flipped, nothing shipped to customers. The night began as a wifi-crash
recovery of unfinished Plan A/B work, then ran the owner's armed night: Quick-Reprocess integration gate +
a Chris sandboxed round + a full pre-release SECURITY AUDIT.

## Commit ledger (this session, oldest → newest; all LOCAL)
- `1463f60` Plan A — Chris 2026-09-01 cards 1+2 (issuer-ripple teardown + supplier_name-only teach guard). *(recovered from the crashed session; verified)*
- `8ec97fd` Plan B foundation — Quick Reprocess OCR-recipe stamp + cache predicate (mig 104, DARK). *(crashed-session commit)*
- `7a8b797` Plan B slices 4-7 — batch partition + imageless merge guards (C1/C4/C6/C7) + Quick/Full dialog; pin `test_quick_reprocess_merge.js` (68). *(recovered + completed this session: wrote the missing merge pin, verified non-imageless path byte-identical)*
- `a6ff457` **Security hardening** — trace-slice cleanup on console-off (R3), client TLS self-signed gated to dev-only (C-4), client nav lockdown (C-3), CLAUDE.md electron-builder version (C-8); pin `src/test_security_audit_20260901.js` (17).
- `2c25a6c` **Quick dialog discloses the pixel-heal skip** (Oracle §3 C4) — dialog + Settings copy.
- `2469c97` **Quick Reprocess emit fixture (8/8)** + real-DB gate spec (`docs/QUICK_REPROCESS_GATE_2026-09-01.md`, `stress_test/reextract_qr_emit_probe.js`).

## 1) SECURITY AUDIT — `docs/SECURITY_REVIEW_2026-09-01.md` (the owner's headline ask)
Advisors: eric (Electron), a dependency researcher (CVE sweep), gary (raw-OCR-at-rest), main-Claude
(licensing), **Oracle** (adjudication). Bottom line: **the code security is genuinely strong**; the real
gaps are artifact-trust + a disclosure decision, not code holes.

### ✅ Fixed tonight (safe, Oracle-signed, pinned 17/17 — DARK/dev-only behaviour change)
- **R3** — the in-Review trace console clears its cropped document-image slices the moment it's toggled
  off, not only at quit.
- **C-4** — the detached client's `ALLOW_SELF_SIGNED` TLS-bypass env is now ignored in a packaged build
  (was honoured → could disable CA verification on customer traffic; MITM close-out).
- **C-3** — the detached client gained an app-level navigation lockdown (it had none; the core always had).

### 🔴 NEEDS YOUR APPROVAL (morning) — release-gate items (NOT done autonomously)
1. **Code-sign the installer/binary (eric C-1).** *The single most important thing.* Unsigned = no
   supply-chain integrity + SmartScreen + it makes the asar-integrity fuse and the unpacked-`.node`
   protection meaningless. **The one blocker with no documentation workaround on a direct/NSIS channel.**
   Oracle's exception: **if MS Store is the SOLE shipping channel, the Store signs it and this is
   satisfied** — then the primary blocker becomes item 2's disclosure. → Tell me the channel.
2. **The plaintext DB decision (gary R1 → Oracle §2).** The DB holds the full OCR text of every document
   in plaintext (encryption arc is built but DARK). Oracle's clear recommendation: **ship opt-in
   encryption + a LOUD, honest BitLocker posture — do NOT default-on.** Default-on undrilled
   code-as-passphrase trades a bounded, disclosable exfiltration risk for an UNBOUNDED, silent
   data-loss risk (a lost recovery code = a lost DB). **The blocker is the silent form — add a visible
   Security panel disclosing plaintext + BitLocker guidance; the cipher stays opt-in.** (Caveat: if your
   real base is shared-PC / roaming-profile / networked-%APPDATA%, encryption matters more — but still
   drill the recovery path before offering, never default-on-undrilled.)
3. **Honest binned-doc retention copy (gary R2 → Oracle promoted to fix-before-release).** A soft-deleted
   ("binned") document keeps its plaintext original in `%APPDATA%\ScanFinder\inbox` AND its `ocr_text` row
   indefinitely (only *confirmed* copies are culled). A security-conscious user who "deletes" a sensitive
   statement is not told it persists until Empty Bin. **At minimum fix the copy** ("original kept until you
   Empty Bin"); optionally an auto-purge age. Cheap; do before release.
4. **`node-forge` 1.3.1 → `^1.4.0` (D-1).** The only `npm audit` finding (7 advisories). Oracle
   **de-escalated it from "must-fix blocker" to free hygiene** — NOT reachable today (licence + TLS both
   use Node native crypto; forge only handles the app's own local CA). One-line, non-breaking; do it, but
   it does not gate release. `cert-tool` already resolves 1.4.0.
5. **Add a CVE prebuild gate (D-3).** `npm run build` gates licenses only. Add `npm audit --omit=dev
   --audit-level=high` + `pip-audit`/`osv-scanner` (on `vendor/python`, run on the build machine).
6. **Verify the fuses in the packaged artifact (C-2).** Owner-machine: launch the packaged exe + `npx
   @electron/fuses read --app dist/win-unpacked/ScanFinder.exe` — config-present ≠ fuse-flipped, and
   `onlyLoadAppFromAsar` bricks launch if the header hash wasn't injected.

### Acceptable-with-documentation (schedule, don't block)
Native PDF/image decoders are the real contained RCE surface (subprocess-isolated — keep `vendor/python`
patched); the pre-existing TOFU-enrollment MITM on the detached client (needs a pairing-integrity check
before the CLIENT ships — separate from the core); filed-output PDFs plaintext by design (document the
BitLocker/output-folder posture); WAL/SHM encryption to verify once R1 ships; tamper-evident-not-proof
licensing vs a local admin (the accepted reality of every offline desktop licence). Details + file:line in
the review doc §A-E.

## 2) QUICK REPROCESS integration gate — `docs/QUICK_REPROCESS_GATE_2026-09-01.md`
Still DARK. **Ran tonight (8/8 green):** the confirm-before-build emit fixture on real Python proved a
FRESH run emits `ocr_recipe` (mig-104 self-heal), `--reextract` emits `imageless:true`+no recipe, and
`_logo_phash` is null on reextract (validating the **C6** preserve-logo guard). Existing pins green (merge
68, Python recipe 20, ocr-cache 23, autocommit 28). **Oracle SIGN OFF WITH CONDITIONS** — the load-bearing
catch: the M=0 danger class (a stored IMAGE-family read that Full would pixel-heal → Full holds → Quick
refiles the stale image value) is **invisible on a clean corpus and C1 can't catch it** (text-only; the
C1×C4 seam keeps it eligible). It is **disclosed-by-design → the dialog copy fix `2c25a6c` is now the
safeguard.** The full 5-arm flip gate is written + runnable **for your real Castellan DB** (the only place
that class is measurable). **The flip stays OFF until Arm B runs there.**

## 3) CHRIS sandboxed round — `docs/CHRIS_FULL_APP_REVIEW_2026-09-01_NIGHT.md`
Sandbox on port **9223** (PID 20992, LEFT RUNNING), `quick_reprocess_enabled` enabled in the sandbox DB
only. Full report `docs/CHRIS_FULL_APP_REVIEW_2026-09-01_NIGHT.md`. **Verdict: YES** (would keep using after
two weeks); every warning truthful. Cards logged, NOT implemented.

**Two headline positives (validating tonight's work):**
- The **Quick/Full dialog disclosure copy** (`2c25a6c`) LANDED — Chris explicitly understood Quick reuses
  the first read and won't re-check a tilted / faint / just-taught page, and that Full is for a fresh look.
  Nits: "scan settings" is a touch vague; four paragraphs is a lot; he didn't catch the summary toast.
- The **DB-encryption ceremony** reads clear + careful, NOT terrifying (honest scope, masked code, typed "I
  HAVE SAVED IT" gate). He didn't reach the Unlock/Recover screen this round.

**Card triage (owner vet — implement nothing without your go):**
- **#1 (top harm) — teach-box clip → misspelled issuer → green tick + "Apply to 19".** I DIAGNOSED this at
  source: Plan A's guard IS in place at the ripple head (`offerIssuerRipple` renderer.js:7855-7857 →
  `checkIssuerRead` → `shouldOfferIssuerRipple`) and on the write side (`apply-issuer-ripple` refuses). It
  works AS SCOPED — it catches OBVIOUS garbles (the prior "NOCUMENT" case). **The gap is real and
  out-of-scope for Plan A:** `checkIssuerRead` → `issuerReadLooksImplausible` is a "does this look like a
  company?" NAME-QUALITY gate, so a PLAUSIBLE-WORD clip ("lorthgate Textiles" — a one-letter clip that still
  reads as a company) PASSES it and the offer stands with a green tick. Chris's implied fix is a DIFFERENT,
  stronger signal — **letterhead-MATCH**: caution when the taught issuer value doesn't match the company
  name the app can already read on that page's branding, before the green tick + "Apply to N". New card →
  needs design (reggie/eric → Oracle); NOT a Plan A bug. **This is the one I'd prioritise** — it's a
  mis-file-at-scale path a nervous user can hit.
- **#2 stale "needs a quick check" banner** (doesn't refresh after a teach raises confidence 69%→90%;
  Reprocess-Quick cleared it) · **#3 check-summary double-counts** two issues on one field · **#4 "ready"
  overloaded** (activity "19 ready" vs group "2 to file by itself") · **#6 binned-doc detail hides ref/date**
  — all copy/refresh cards, owner vet.
- **#5 diagnostic-log toggle looked ON vs "Off by default" — RESOLVED, dev-harness artifact, NOT a shipping
  bug.** Verified at source: `dev-start.js:11` forces `DOCUSNAP_DIAGNOSTIC_LOG=on` for every dev session
  (owner request 2026-08-22) and the sandbox runs via dev-start; the PACKAGED product reads only the
  `diagnostic_logging` SETTING, which defaults OFF (`database/index.js:196`, `diaglog.js:8`). A real customer
  install shows it OFF. No action.
- Verifies: the "ready language war" is BETTER-BUT (File-All no longer says "Nothing is ready"; "ready"
  still overloaded); heading-guess issuers NOT cleanly tested (no sender-less doc in the corpus).
- Caveat: Chris's automated confirm-accept silently cancelled native dialogs and nearly filed two false
  "it's broken" findings (File All Ready + Delete) before he caught it and re-tested — both work correctly.

## Verification state — honest
- All pins green under E44: security-audit 17, quick-merge 68, ocr-cache 23, recipe.py 20, autocommit 28;
  the emit fixture 8/8 on real Python; non-imageless reprocess path byte-identical (type-flip /
  annotated-empty / reextract-merge green). Edited JS files pass `node --check`.
- NOT verified (needs you / the owner machine): the Quick-Reprocess real-DB flip gate (your Castellan DB);
  the packaged-artifact fuse read (C-2); any of the approval-class security items above.

## Next actions (morning)
1. Read `docs/SECURITY_REVIEW_2026-09-01.md` + this handover's **NEEDS YOUR APPROVAL** block; decide the
   shipping channel (settles C-1) and the plaintext-DB disclosure (settles R1).
2. Read the Chris review doc; pick which cards to action.
3. Review the 7 local commits; push when satisfied.
4. When you want the Quick-Reprocess flip: run the 5-arm gate on a `db.backup()` of your real DB per
   `docs/QUICK_REPROCESS_GATE_2026-09-01.md`.
