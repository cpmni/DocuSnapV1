# requirements-ocr.txt — OPT-IN RapidOCR full-page OCR engine runtime (Stage 3).
#
# NOT installed by default and NOT bundled by the standard build. The shipped OCR
# default stays Tesseract; RapidOCR is chosen per-install via Settings -> OCR engine
# and falls back to Tesseract AUTOMATICALLY if this runtime is absent or fails to init
# (see python_backend/ocr/engine.py). Provisioning + offline verification:
# python_backend/OCR_RUNTIME.md.
#
# Commercial-use licences (all safe; no GPL/AGPL anywhere in this stack):
#   rapidocr-onnxruntime  Apache-2.0   (the wheel BUNDLES the PP-OCR det/rec/cls ONNX
#                                       models, themselves Apache-2.0 — offline, no download)
#   onnxruntime           MIT          (CPU build — do NOT use onnxruntime-gpu)
#   transitive (pulled automatically by rapidocr-onnxruntime):
#     opencv-python        Apache-2.0
#     pyclipper            BSD-3-Clause
#     shapely              BSD-3-Clause
#     PyYAML               MIT
#   already present in the backend: numpy (BSD-3-Clause), Pillow (HPND/MIT-like)
#
# Pinned to the tested series. During provisioning, capture `pip freeze` of the
# resolved versions into a full lock for reproducible packaged builds.
#
# onnxruntime: floor raised to >=1.19 for NumPy 2 compatibility. onnxruntime
# <=1.18 was compiled against NumPy 1.x and its native module REFUSES to load
# under NumPy 2 (the backend ships numpy 2.x), failing RapidOCR init with an
# opaque ImportError. >=1.19 added NumPy 2 support; validated at 1.27.0.
rapidocr-onnxruntime~=1.3.0
onnxruntime>=1.19,<2
