# HANDOVER — 2026-08-23 (overnight: the Chris round-17 fix loop, rounds 18+)

> Owner's standing instruction (asleep): "take chris's recommendations to the agents, plan and fix. then get
> him to rerun again. after that plan and fix, then rerun again and keep a log of what he thinks."
> Rules kept all night: Oracle-gated; plain bugs ship, behaviour widenings DARK behind their own switch;
> zero-misfile rule; the owner's live app + filesystem untouched; **nothing pushed**.

Branch `feat/teach-side-overnight`. Start HEAD `db0cc1a` (round 17 verbatim). Commits tonight, one per card:

| Commit | What |
|---|---|
| `99b90f1` | card 1 — the refused-confirm return carries every hold payload (`nearMatch`, `typeSplit`) — the inline holds NEVER rendered before |
| `2cf5f26` | card 7 — `markUndone`: an undone event stops offering Put back; second press "Already back in Review." |
| `11ca0ba` | card 2 — **THE IDENTITY UNFREEZE CLASS**: company keys judged per template by dominance, loud guard, re-freeze script |
| `875a433` | card 2(c) — a scope only "has a template" when the carried template's established identity IS the scope |
| `72a75bd` | card 2(a)+(b) — the WIDE debris leg (DARK `template_fixed_debris_wide`) + the JUNK kind of the letterhead suggestion |
| `9ae6c2b` + `f098480` | card 3 — the near-match SUB-RUN arm (a one-line box over a stacked name asks with the full name first) |
| `320433f` | card 5b + card-1 rider — a blank issuer is never 'ready'; one mistyped confirm no longer silences the type-split ask |
| `e9106aa` | cards 4 / 5a / 6 / 8 (eric batch) — tile yields under the strip; File All = one bulk chip; banner self-heals; Reprocess copy; "Sent back from Search" |
| `740a243` | **OWNER CARD 1 — the READY arm** (DARK `quiet_reread_on_ready_templated`) |
| `473dcfd` | docs: triage table, Oracle log, plan STATUS |

## THE FINDINGS (read these even if nothing else)

### 1. The identity UNFREEZE class (`11ca0ba`) — the "Gay" / "DOCUMENT" folders
`_fieldsWithMultipleConfirmedValues` (`src/modules/review/handler.js`) decided whether `supplier_name` is
"variable" by counting DISTINCT confirmed suppliers across the whole TYPE. Any type with ≥2 senders (every real
install) → every template of that type had its frozen identity written NULL at its next template write → a
junk read on a sibling could name the folder. Verified on two live backups. Now: per template by DOMINANCE
(`templates.getDominantSupplier(db, templateId, excludeDocId)`: variable iff total ≥ 2 AND the leader does not
hold a strict majority); `_identityOverwriteGuard` warns + audits `template_identity_unfrozen` on any
NULL-over-frozen write. Non-company keys keep the type-wide rule.
**OWNER STEP (not done for you — it touches the live DB):**
```
node scripts/refreeze-template-identity-20260823.js                       # dry run (default)
node scripts/refreeze-template-identity-20260823.js --apply --templates-dir "C:\GIT Projects\Docusnap\templates"
```
Refuses unless `template_identity_on_page='true'`. Rule: ≥3 confirms, ≥90 % share, plausible name, not locked.
Dry run on the live backup copy: 5 to re-freeze (Silverbeck, Veltrix, Castellan, Harrowgate, Nordwind).
**GATE:** realdoc on the base copy vs the re-frozen copy → byte-identical reports (389/416 would auto-file,
M=0, M_type=0, per-field tables equal).

### 2. The holds never rendered (`99b90f1`)
`confirmCurrentDoc` returned `{error, code, prefixOutlier}` on a refused confirm — the near-match and
type-split hold payloads were dropped before the renderer could show them. Everything round 16/17 said about
"the ask didn't appear" was this one line.

### 3. The READY arm (`740a243`, DARK) — owner card 1
Once Q2 makes the teach-time re-read bind siblings BEFORE any confirm (overall 91–93 under floor 100), the
'ready' crossing re-read only template-LESS docs, so "✓ files by itself" sat over a pile waiting for File All.
The arm widens the 'ready' job to the scope's OWNED-template held docs with `overall_confidence <
trust.scopeTrust().floor`, under the layout arm's guards (on-page ON, judgeable name, no prior S3-C5 note)
and the C3.3 first-fill hold ("Read after learning — confirm once."). A doc AT the floor is left to the sweep.
**It cannot help DOCUMENT SOLUTIONS** (all-generic name → unjudgeable → skipped, audited `ready_arm`). The
honest fallback there is badge copy — owner's call.

## Switches added tonight (all DARK, dev-gated Settings toggles)
`template_fixed_debris_wide` · `quiet_reread_on_ready_templated` (rides `quiet_reread_on_ready`).
Env bridges: `TEMPLATE_FIXED_DEBRIS_WIDE`, `QUIET_REREAD_ON_READY_TEMPLATED`, `IDENTITY_SUGGEST_CANONICAL_JUNK`
(default ON inside the suggestion arm).

## Tests
New/updated: `test_confirm_hold_payloads.js`, `test_review_events.js` (bulk merge, same-key merge, markUndone),
`test_review_events_doors.js`, `test_reviewservice.js` (rider, bulk receipt + control), `test_activity_strip.js`
(eric-batch contracts), `test_build_template_fields.js` §6, `test_quiet_lane.js` §B4b, `test_near_match_prefix.js`,
`test_quiet_lane_ready_templated.js` (16), `test_review_readiness.js`, `test_review_queue_issuer_suggested.js`,
Python `test_fixed_seed_debris.py`, `test_identity_variant.py`.
Whole suite (run tonight after `740a243`): **544 files, 532 green, 12 red — every red a documented pre-existing one, ZERO new**; `test_xcheck_corrob_demote.py` went green. **Re-run after the round-18 fixes (`371ef2d`): 548 files, 535 green, 13 red = the same 12 + `test_page_ocr_cache.py`, which is green 3/3 run alone (a load-timing flake under the parallel runner; file untouched tonight).** `TESTING/suite_results.json`. Pre-existing reds
unchanged: `test_authoritative_anchor`, `test_v1_contract`, `test_doctype_surface_parity`, `test_teach_multipage`;
Python `test_identity_fusion` + 6 script-style.

## Chris
- Round 17 verbatim + the triage/fix table: `docs/CHRIS_FULL_APP_REVIEW_2026-08-22.md`.
- **Round 18 verbatim: `docs/CHRIS_FULL_APP_REVIEW_2026-08-23.md`** (fresh sandbox PID 31972, every switch ON incl.
  the two new). Grades on the r17 fixes: 2/4/5/6/8 FIXED, 3/7 BETTER-BUT, 1 NOT FIXED in Review (A4). New cards:
  **A1 a wrong date self-filed** (Copperfield invoice_16: blank at import, first-filled 13-11-2026 by the teach-time
  re-read, single-family `mapping`, swept at the ready crossing; page 03/11/2026) · **A3 Put back does not stick**
  · A4 Review type-split dead end · A2 one click past the fragment warning · A5 type change wipes values (PREFERENCE)
  · A6 DS badge over a waiting pile + "Customer Name couldn't be checked" on a worksheet · A7 Home "nothing filed
  by itself" · A8 untaught garble groups (Ol Meadowvale).
- **Round-18 fixes so far:** `7b8c8e1` A4 (the `confirm-review` IPC whitelist dropped `typeSplit` — one link past
  the r17 fix) · `19e91b0` A3 part 1 **PUT BACK MUST STICK** (mig 86 `documents.put_back_at`; the ONE predicate
  refuses `'put-back'` before the floor; claim clears; classifier 'flagged'; class-fix excludes; Review copy) ·
  `8b5ae1a` A2 (Use "FULL NAME" is the primary under the warning; Save re-asks; `acknowledgeIssuerNearMatch`),
  A6 copy (foreign-field hold named honestly), card 7 (chip says put back), card 5 (bulk lists senders),
  "null characters" (the service now forwards `nearMatch.kind`), the lane notice names ready/layout/typesplit ·
  `615263c` A7 (the Home 7-day tally counts every machine via).
- **A1 design (gary → Oracle, verdict pending at the time of writing):** a per-lane-JOB FIELD-RELIABILITY hold —
  K=2 S3-C5 disagreements (+ valued→empty losses) on the same role field in one job → that field's uncorroborated
  first-fills in the job are held "confirm once" (retro at the crossing), DARK `quiet_reread_first_fill_reliability_hold`.
  Rejected: re-enabling C3.3 on the teach arm (kills the DS hand-off, r16 card 2) and a single-family refusal in
  trust.js (no "was empty at import" fact at sweep time). A3 part 2 (a correction should pause the sweep): NO pause
  (readiness counts corrected confirms by design; `recent-correction` is unreachable below W) — a later own-slice
  correction-ripple HOLD instead.
- **Owner vet items from round 18 (not built):** A5 carry date/ref across a type change; A8 the Use/Keep hold for
  untaught fragment groups; the S3-C5 display stance (the box shows the NEW wrong value, the note names the old);
  **the sweep's licence ≠ the badge's readiness** (DS: 2 human confirms licensed a 7-doc sweep at 23:59:51 while the
  group said "1 more to file by itself"; the badge needs `FORMAT_SOLID_MIN=3`, the gate passed — verify which rule
  licensed it; all 7 were correct); the sweep stamps the triggering user's name on `scope_sweep` rows (Chris: "the
  records say I confirmed them"); the empty ghost folder after a re-file; Home FINISH SETTING UP stale tick; the
  wizard's "No — go back and change the type" lands on the boxes page; "Learned N layouts" counts templates (re-teach
  of a sender reuses its template — honest but surprising).
- Sandbox: `<scratchpad>\chris-sandbox\` — `userData_r17` / `Output_r17` / `r17_shots` archived beside the
  fresh `userData` / `Output`.

## Traps this session
- `core.autocrlf=true`: renderer.js etc. are CRLF on disk — tests slicing on `\n}\n` must normalise; Python patch
  scripts must `open(p, newline="")` and replace `\n` with the file's newline, or they flip the whole file to CRLF.
- bash heredocs strip one backslash level — write test/patch files with the Write tool.
- Hand-rolled test schemas lack mig columns — sniff with `pragma_table_info`.
- `chris-the-customer` is NOT a registered subagent type this session — spawn `general-purpose` and have it
  Read `.claude/agents/chris-the-customer.md` first.
- The ledger's list order is INSERTION order: a merged event does not move to the front (both show "Just now").

## Resume
1. Read the round-18 section below; triage → agents → fix → round 19 (the loop).
2. Owner: apply the re-freeze script live (above); decide the DS badge copy; vet the DARK switches
   (`template_fixed_debris_wide`, `quiet_reread_on_ready_templated`) with their Settings toggles.
3. Push when the owner says so.
