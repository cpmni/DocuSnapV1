# HANDOVER 2026-07-30 (Opus 4.8)

**Branch** `feat/reprocess-throughput-autostraighten` · **HEAD `fbea19d`** · origin at `d35f42c`
(**`fbea19d` is the ONE unpushed commit**) · tree clean bar pre-existing untracked (`../Backup/`,
`../Docusnap - Copy*`, `HANDOVER_2026-07-28*.md`, `docs/SECURITY_HARDENING_REPORT_2026-07-28.md`,
`scripts/remove-superstore-invnum-anchor.js`). **Dev `npm start` was left RUNNING.** Installer built
MID-SESSION at `2b8bdb2` → `dist/ScanFinder Setup 2.0.0-r20260730-1231-2b8bdb2.exe` (does NOT include the
later teach fixes / learn-on-commit flip / reextract — a rebuild ships those). Live DB
`%APPDATA%\ScanFinder\docusnap.db` — **backfill --apply WAS RUN on it today** (backed up + reversible, below).

## CONTINUATION (2026-07-30, later) — reextract JS delivery + BOTH gates DONE
**HEAD now `f79980b`, pushed (origin in sync).** The OPEN BUILD below is COMPLETE + gated (still DARK, kill switch
`reextract_fast_enabled` / env `REEXTRACT_TEXT_ONLY=1`, OFF):
- **B-1 `312674b`** — IPC `reextract-fields-fast` (in **processing/handler.js**, where the spawn plumbing lives — NOT
  review/handler.js) + fill-only `mergeReextractRows` (the INVERSE of mergeReprocessRows) + preload `reextractFieldsFast`.
  Zero-byte PLACEHOLDER folder (reextract never reads the file bytes, process_docs.py:492 — no source copy, works when
  working_path nulled). NO DB write → returns suggestions. Pin `test_reextract_merge.js` 13/13 + a CLI smoke green.
- **B-2 `faaf584`** — review/renderer.js `_selectDoc` → `_scheduleReextractFast` (450ms debounce, doc-guarded,
  edit-guarded `!hasPendingReviewEdits`, only-when-gain precheck) + `_applyReextractSuggestions` (`.value` DIRECTLY —
  no input event, no `corrections[]` = Oracle C4; ⟳ pill + accent border; re-runs validateConfirm). node --check clean.
- **C2 `39c35d3`** (`stress_test/reextract_c2_gate.js`) — dual-run imageless-vs-full-reprocess, production-faithful
  merge + a forced-empty POWER metric. Live corpus 80 docs → 40 candidates, **100% AGREE**, 0 type flips → **PASS**.
- **C5 `f79980b`** (`stress_test/reextract_c5_profile.js`) — vendor/python cold spawn→file_done **~284ms median**,
  no sys.path trap, non-blocking → **acceptable for on-open**. Slice C warm worker NOT required (nice-to-have).
- **KEY FINDING:** fire-rate **0** on a stable corpus — a field empty in stored stays empty in fast (same keyword stage
  on the same OCR); real fills come only from learning added SINCE import (the "newly-found template" case). No-op on
  stable docs (good).
- **LEFT (owner):** live B-2 smoke — engineer the dynamic case (teach a template, open a sibling doc with that field
  empty; a normal open shows NO pills = correct, not a bug). Then flip `reextract_fast_enabled` ON. Slice C deferred.
- Also this session: CLAUDE.md trimmed 989→870 (`9ebd91d`) + session-state pointer refreshes. Memory
  `project_reextract_fast_20260730` rewritten to this state.

## CONTINUATION 2 (2026-07-30, later) — WORKSHEET type-presence fix FLIPPED ON
**HEAD `f3a650a`, pushed.** Ridgeway worksheets mis-typed PO/SO (neither printed — only "WORKSHEET") — FIXED + all 4
kill-switches flipped ON.
- **Root cause (herald instrumented trace):** (1) `_type_keyword_pattern` bounded only SINGLE-word phrases → the PO
  keyword "order to" = `order\s*to` prefix-matched "Order Total" (14 docs); (2) the existing TYPE_PRESENCE_VETO only
  guards the STRONG logo-lock accept (conf≥60 ⇔ phash dist≤6), but phash hashes LAYOUT so a worksheet locks CLOSER to
  Copperfield's templates (dist 8) than its own Ridgeway SO template → typed by the UNGUARDED keyword-fingerprint arm
  (6 docs). The PO-vs-SO split across the 20 is pure phash noise.
- **Slices (all committed + FLIPPED ON; kill each with its env =0):** 1a `f2d18ae` TYPE_KEYWORD_BOUND · 1b `2300afb`
  TYPE_PRESENCE_GATE + Python `_type_heading_tokens` twin (pin test_type_heading_tokens.py 13/13) · 2 `21519f9`
  TYPE_PRESENCE_VETO_KW (template_matcher.py:719, the kw arm) · nudge `85c7f0b` TYPE_HEADING_NUDGE
  (`keyword._harvest_top_band_heading`, pin test_harvest_heading.py 9/9) · flip `f3a650a`.
- **Gate:** realdoc BYTE-IDENTICAL to baseline **5×** (type 100%, M_type 0, supplier 100%, ref 96%, date 99%, auto-file
  82, zero false-untyped). All 20 worksheets untype at import + emit "Worksheet"; genuine POs 61-80 / SOs 82-100 stay
  typed. herald→gary→Oracle SIGN-OFF-W/COND, all met (Oracle killed "zero accuracy drop by construction" → a MEASURED gate).
- **OWNER cleanup:** the fix is IMPORT-time; the 20 existing rows keep their stored type until Add-a-"Worksheet"-type +
  reprocess (once installed the WORKSHEET heading is title_trusted → machine-override re-types). De-confirm #120 (worksheet
  confirmed as SO) via Learning Repair. **Follow-on (deferred):** gary's pooled per-(supplier,type) count-arm flag
  (poison-immune) — Slice 2 uses the picked-template ratio (fine for single-template suppliers). Memory
  `project_worksheet_type_presence_20260730`. Harness `stress_test/type_reprocess_probe.js`.

## TL;DR
Huge multi-thread day. SHIPPED + FLIPPED ON: the **warm cross-contamination fix** (supplier-scoped ref
shape-withhold) and **Slice 1 learn-on-commit** (templates keep converging on every confirm) — the latter's
backfill was `--apply`'d on the live DB and PROVEN (Copperfield POs now resolve to their PO template).
SHIPPED: **security F1 (offline-forge: baked licence keys) + F2 (seat-race)** with an independent Sammy
Security re-audit; the **import "couldn't be read" banner** (details+dismiss); three **teach-wizard** fixes
(only-current-box overlay, Straighten text button, label-detect frame-math fix, all-captured header cleanup).
IN PROGRESS (dark, foundation only): **fast on-open re-extract from cached OCR** — the engine/Python half is
built + tested; the JS delivery + gate remain. Reran the scans + born-digital harnesses. Answered a
parallelism question (6×1 beats 3×2 — Tesseract is thread-invariant).

---

## THE OPEN BUILD — fast on-open re-extract (Slice B), the next session's main task
**Design: gary + eric → Oracle SIGN-OFF-WITH-CONDITIONS.** Full design in the 2026-07-30 chat + task #10 +
`pendingfeatures.md`. Idea: reuse pass-1 cached `ocr_text` to refresh fields on doc-open WITHOUT a full
reprocess (skip full-page OCR — already cached — AND per-field crop OCR). Kill-switched, DARK.

**DONE + committed `fbea19d` (unpushed), tested GREEN:**
- `process_docs.py` **`--reextract`** flag: with cached OCR supplied, render NO images (`ocr_text=cached,
  page_images=[]`), skip the born-digital text-layer block (Oracle **C3**: born-digital excluded). Absent =
  byte-identical.
- `engine.py` **Oracle C1** (the catch — gary's original B2 was WRONG): guard ONLY the Stage-0 live
  `identify_template` CALL on `_id_img is not None`, **never the `if templates:` block** (which also holds the
  known-id honour path `:2848` + `extract_with_template` `:2900` the feature needs). Imageless → live identify
  skipped (no unguarded text-arm match with the logo guards off) → the honour path applies the caller-supplied
  `known_template_id` text-only. Byte-identical when an image is present.
- Pin `python_backend/tests/test_reextract_text_only.py` (imageless extract applies the known template
  text-only + reads keyword fields from cached OCR, no crash) — **GREEN**.

**LEFT TO BUILD (the JS delivery + gate) — all Oracle-conditioned:**
1. **IPC `reextract-fields-fast(docId)`** (review/handler.js, beside `check-template-match-for-document`
   `:856`, admin/edit gated). Reuse the reprocess spawn plumbing (`processing/handler.js:1318-1612`,
   `buildTrainingArgs`) with `--reextract` + `--cached-ocr-file` (the doc's stored `ocr_text`) +
   `--known-template-id` (the `checkTemplateMatch`-found id). Read the emitted extractions; **return them,
   NO DB write.**
2. **Fill-only merge** (new `mergeReextractRows` — do NOT reuse `mergeReprocessRows:221`, which clobbers on
   new-value). Rules: adopt a fast value ONLY when the stored field is (a) EMPTY, (b) has NO learned anchor in
   `(supplier,type)` scope, (c) passes Stage-4 validation; **keep-existing on differ**; a stored empty-string
   WITH a `validation_note` is NOT "empty" (preserve the flag). Never delete/reinsert, never change type/status,
   **never a 2nd `_maybeAutoFile` site** (SEAM-1 pin, engine.py:2840).
3. **Renderer trigger + display** (review/renderer.js `_selectDoc` ~`:1304`, mirror the `checkTemplateMatch`
   idiom `:1316`): fire-and-forget after first paint, gated on `!hasPendingReviewEdits()`, doc-guarded,
   debounced, **only when there's something to gain** (an empty non-anchored field, or a newly-found template).
   Show fills as a **pill-marked suggestion**; **set `.value` WITHOUT firing the input event** (Oracle C4 — else
   it plants a phantom "human correction" that feeds strong learning). Persists on the operator's confirm
   (verified: renderer `allValues` → `reviewService.confirm` files + learns).
4. **Preload** `reextractFast` beside `checkTemplateMatch` (`preload.js:222`).

**FLIP prerequisites (Oracle C2/C5 — not commit blockers; the switch stays OFF):**
- **C2 — the REAL gate:** run `--reextract` (imageless) over the corpus and diff vs the full-image run —
  every fill EQUALS the full-pipeline value, the fast path NEVER emits a value the full run suppresses/leaves
  empty, zero supplier-scope disagreements, zero type/status change. gary's "replay-merge-over-stored" is
  necessary but CANNOT see these bugs (it never runs the imageless engine) — must actually invoke `--reextract`.
- **C5 — profile the cold spawn** (~0.3–1.0s ASSUMPTION, unprofiled) against **`vendor/python`** (embeddable
  sys.path trap), not `py`. If too slow for on-open-auto, gate harder / land the warm worker first.
- **C6 pins:** anchor-abstain (empty anchored field skipped), no-2nd-auto-file-site, no type/status mutation.

**DEFERRED — Slice C (eric): the warm text-extract worker** — a persistent Python child (clone
`processing/regionWorker.js` + `ocr/region_worker.py`) that imports the engine ONCE and answers text-only
requests over stdio (kills the per-doc cold-start → instant on-open). Cache-coherence: a `learningVersion`
stamp (worker re-pulls the training bundle on change) OR respawn-on-learning-write. Also finally wire the
pending `regionWorker.warmUp()` on Review-open (memory C2). Not required for slice-B correctness.

---

## COMMITTED this session (newest first)
- **`fbea19d`** reextract foundation (DARK, UNPUSHED) — above.
- **`d35f42c`** teach: all-captured header cleanup (owner) — header shows "Teaching complete — Ready to
  review" + Review pointer, not the last field's step. Needs app-reopen smoke.
- **`1ef3e50`** teach: **label auto-detect frame fix** — root cause was a frame-math bug (the label-band `ds`
  recompute at renderer.js:787/803 ignored `TEACH_NATIVE_CROP` → `cY`/box coords scaled ~0.42× vs native crop
  px → `nearestRowTo` missed → "No label found"). Both `ds` now honour `TEACH_NATIVE_CROP`. **Owner smoke-
  tested: labels detect now.** Also cleared the stale Set-A cross-contamination backlog entry (now fixed).
- **`8c487e4`** flip learn-on-commit default ON (`template_learn_on_confirm`; `=0`/setting=false disables).
  See Slice 1 below.
- **`23a8f2e`** cross-contamination Stage-2 residual → **DO-NOTHING** (Oracle traced it: `anchor_crop` is
  handled at anchor.py:582 + the existing 1102/1175 recovery; the true residual is a narrow text-fallback
  sliver, corpus-inert, and a fix would DEMOTE a keyword auto-file to a flagged wrong-value review — not worth
  it). Corrected framing in `pendingfeatures.md`.
- **`2b8bdb2`** flip cross-contamination fix default ON (`SHAPE_WITHHOLD_SUPPLIER_SCOPED`; `=0` disables).
- **`d9ec7d5`** **cross-contamination FIX** (DARK→flipped). iris PROVED (isolation) it was the learned-shape
  `('')` doc-type aggregate = one supplier's ref convention hard-nulling stranger refs at Stage 4.5 (NOT
  phash/anchor — all falsified). gary design, Oracle W/COND. A `('')`-only verdict now FLAGS-not-NULLS a
  stranger ref; supplier-scoped withhold byte-unchanged. Gate: score_demo A warm ref **55.3→88.9%**, realdoc
  **M=0** byte-identical. Memory `project_shape_withhold_supplier_scoped_20260730`.
- **`c1d128f`** teach: only-current-box overlay + Straighten text button (owner).
- **`4b40284`** import "couldn't be read" banner: Details toggle + dismiss (owner).
- **`0b63794`** **security F1 + F2** (+ Sammy re-audit). F1 [HIGH]: `config/license.json` public keys ship as
  a loose editable file (offline-forgeable) → baked into `src/lib/license/pinnedKeys.js`, `loadConfig` uses
  those (kill `LICENSE_PINNED_KEYS=0`); pin `test_license_pinned_keys.js`. F2 [MED]: `activate.php` seat-race →
  `SELECT … FOR UPDATE` (+ explicit `ENGINE=InnoDB`). Sammy found H-1 (backup restore bypassed the settings
  guards) → FIXED via `protectedSettings` (client_api_*/terms_accepted/first_run_completed excluded) + M-1
  (empty device_fp bypass) + L-5 (XML control chars). Details in gitignored `SECURITY_AUDIT_2026-07-29_LOCAL.md`.
- **`912a066`** Slice 1 learn-on-commit BUILT (dark) — enrichIdentity + C-A appendLogoOnly + the 3 commit
  routes + backfill.

## SLICE 1 LEARN-ON-COMMIT — flipped + backfill APPLIED on the live DB
- Flip `8c487e4` (default ON). Owner closed the app; ran `scripts/template-enrich-backfill.js --apply` on the
  live DB → **backup `docusnap.backup-2026-07-30T13-18-45-930Z.db` + snapshot
  `template-enrich-snapshot-2026-07-30T13-18-45-930Z.json`** beside the DB (fully reversible: `--revert
  <snapshot>` or copy the backup back). Healed template 3 (dropped PURCHASE/Supplier/Sandpiper/Hotels).
  **PROVEN:** `stress_test/probe_held_pos.py` — confirmed Copperfield POs resolve to template 3 (purchase_order),
  not the invoice magnet; `template_gate_probe` 0 wrong-matches. The 3 held POs were already File-All'd (0
  needs_review). Memory `project_shape_withhold_supplier_scoped_20260730` + `project_overnight_hardening_20260729`.

## VERIFICATION STATE (honest)
- **GREEN, ran:** all reextract/enrich/learn-on-commit/pinned-key pins; full licence suite (fixed a
  pre-existing stale phase2 regex); cross-contamination unit + score_demo (55→89) + realdoc (M=0); the teach
  fixes parse + owner-smoked the label fix; backfill C-A/C-D + the held-PO probe.
- **NOT verified:** reextract JS delivery (not built); the C2 corpus shadow gate + C5 spawn profile (flip
  prerequisites, not run); F2 seat-race is PHP (no local MySQL — verify on the WAMP backend); the teach
  header-cleanup + only-current-box need an app-reopen visual smoke.
- **Corrected mid-session claims:** the cross-contamination cause was NOT phash/anchor (iris falsified);
  gary's reextract B2 guard was WRONG (Oracle caught it — guard the call, not the block); the Stage-2
  cross-contamination residual is much narrower than the backlog claimed (Oracle — DO-NOTHING).

## FIRST ACTIONS (fresh session)
1. **Push `fbea19d`** (the lone unpushed commit).
2. **Continue the reextract JS delivery** (Slice B, §above): the IPC + fill-only merge + renderer trigger,
   DARK. Then C2 gate + C5 profile before any flip. Task #10 has the condition list.
3. **Rebuild the installer** (owner will likely want it) so the teach fixes + learn-on-commit flip +
   cross-contamination flip reach the other PC — the shipped `-1231-2b8bdb2.exe` predates them.
4. App-reopen **smoke** the teach header-cleanup + only-current-box overlay (renderer changes).

## NEEDS THE USER
- Push decision on `fbea19d`; rebuild-installer decision (#3).
- F2 seat-race PHP verify on the WAMP backend.
- The deferred security items (F3 trial-rollback, F4 sandbox:true, M-2 pairing, L-1/2/4/6) in the local audit doc.

## KEY FACTS / PATHS
- Live DB `%APPDATA%\ScanFinder\docusnap.db` (read-only replay `?mode=ro`). Backfill backup+snapshot beside it.
- Run JS gate tests: `ELECTRON_RUN_AS_NODE=1 ./node_modules/.bin/electron <path>`. Python: `py -3.12 <path>`;
  clear `python_backend/**/__pycache__` if a change doesn't take. Backfill script needs ELECTRON-AS-NODE
  (better-sqlite3 ABI).
- Harnesses: `stress_test/realdoc_regression.js` (scans, live DB read-only) · `gen_demo_digital.py` +
  `score_demo_digital.js A|all [warm]` (born-digital → Desktop) · `template_gate_probe.py` (TEMPLATE_PROBE_DB) ·
  `probe_held_pos.py`.
- Backlog: `pendingfeatures.md` (repo root) — outstanding fixes/features + rulings.
- Agents used today: iris, gary×3, eric, Sammy Security (general-purpose), Oracle×4 — all registered/persona.
- Kill switches flipped ON this session: `SHAPE_WITHHOLD_SUPPLIER_SCOPED`, `template_learn_on_confirm`,
  `LICENSE_PINNED_KEYS` (baked keys). Reextract stays behind its (caller-side) switch, DARK.
