# HANDOVER — 2026-07-19

**Branch:** `feat/reprocess-throughput-autostraighten` · **Last commit:** `e85f646` · **UNPUSHED: 15 commits**
(ask before pushing — house rule). **Installer: STALE** (`r20260718-0818-bea1028`; not rebuilt this session).
**Uncommitted batch:** none of this session's work — all 4 workstreams are COMMITTED. Only pre-existing
non-mine changes remain in the tree (`.gitignore`, `CLAUDE.md`, `stress_test/out/accuracy_baseline.md`) +
untracked prior handovers + `stress_test/_*.js` scratch.
**Context:** an Opus-4.8 (1M) session. Print-flow UX fix + Workflow Suite Slices 2 & 3 + a routing-settings
slice that reframes the whole workflow feature (Barry-led). Multi-step (old Slice 4) shelved.

---

## TL;DR
- **Print flow fixed** (`75206fb`) — but **UNVERIFIED live** (needs the owner at the Windows machine + Ricoh).
- **Workflow Slice 2** (decision snapshot) and **Slice 3** (amount routing) BUILT + committed, dark.
- **Routing-settings slice** (`e85f646`) — **closes SEAM A**: routing now fires at the FILING seam for EVERY
  filed doc (auto-filed + reviewed + bulk), driven by admin rules in a new (hidden) Workflow Settings tab.
- **Multi-step routing DEFERRED** (Barry: single-hop covers the home/small-office segment).
- Everything workflow stays DARK behind `WORKFLOW_FEATURE_ENABLED=false` (a const) — byte-identical for users.

## Committed this session (per commit)
1. **`75206fb` fix(print)** — owner reported the Windows driver dialog dropping behind the app. Root cause
   (eric): `parent` on a `show:false` window can't keep an owned dialog on top. Fix: rename "Full printer
   dialog…" → **"Advanced printing"** which now CLOSES the custom modal (owner's idea) + `alwaysOnTop`+
   `skipTaskbar` on the offscreen print window forces the OS dialog topmost + an `isEnabled`-poll reaps the
   ghost window when the dialog closes (fixes a cancel-path window/Promise/audit leak; the deliberately-omitted
   "never opened" wall-clock cap could close a live dialog). Files: `src/modules/print/handler.js`,
   `src/windows/review/index.html`, `src/windows/review/renderer.js`. `test_print.js` green.
   **⚠ NOT LIVE-VERIFIED** — z-order + Ricoh spool need a real Windows session (see "Needs the user").
2. **`a6e8153` Slice 2 — decision snapshot** — append-only `route_decisions` written at `resolve` (the £ + who/
   what the approver saw, immutable). No-FK soft-ref (survives doc-delete cascade), append-only triggers, env
   `WORKFLOW_DECISION_SNAPSHOT`. gary+Oracle C1–C6. `test_route_decisions.js` + `test_workflow_snapshot.js` green.
3. **`92851ab` Slice 3 — amount-threshold routing** — `workflow_route_rules` + `startDefaultRoute` at confirm,
   trust-gated total (dropped-decimal guard), env `WORKFLOW_AMOUNT_ROUTING`. gary+Oracle C1–C6. **KNOWN LIMIT
   at the time = SEAM A** (confirm-path only) — CLOSED by the next slice.
4. **`e85f646` routing settings slice — closes SEAM A** (the big one). Reframe: Review=data-completeness,
   routing=a separate step at the filing seam. Stages: engine (null-total shadowing fix C1, type-only rules
   skip the amount gate, route-to-self allowed / role→confirmer still SoD C3, immutable `matched_rule_summary`
   C6, pure `dryRunRules` C5) · DB (`assignSystem` NULL-'Auto-filed' sender via shared `_validateAssignTarget`,
   `assign` byte-identical C4; `summarizeRule`; rule CRUD; migration) · wiring (`_autoFileDoc` hook +
   `_autoFileRouteDeps` with entitlement-parity C2; `reviewService.confirm` on BOTH bulk+non-bulk = D2; one-
   route-max) · UI (hidden entitlement-gated **Workflow** Settings tab: sentence rule builder APPROVAL-ONLY per
   D1, rules list toggle/edit/delete, read-only dry-run). Gate: Barry→gary+eric→Oracle SIGN OFF WITH CONDITIONS.
   Record: `docs/designs/WORKFLOW_ROUTING_SLICE_2026-07-19.md`. New SEAM-A test `src/modules/processing/test_autofile_route.js`.

## Verification state — honest
- **Green (test-verified this session):** `test_amount_routing`, `test_autofile_route` (SEAM-A closure),
  `test_workflow` (+ assignSystem pins), `test_workflow_snapshot`, `test_reviewservice`, `test_v1_workflow`,
  `test_workflow_ipc`, `test_route_decisions`, `test_workflow_route_rules`, `test_access_service`, `test_print`.
  Settings `index.html` div-balanced (425/425); all touched JS `node -c` clean.
- **Corpus M=0 asserted BY CONSTRUCTION, not run** — the workflow slices change NO extraction/filing/trust-
  DECISION code (`trust.currencyConsistentForField` is additive/inert; routing runs strictly after a completed
  file), and `realdoc_regression.js` never calls `resolve`/`_autoFileDoc`. If you want the number, run it;
  it should not move.
- **NOT verified:** (a) the **print fix live** (z-order on real Windows + does the Ricoh spool). (b) The
  **settings UI live** — the whole feature is dark (`WORKFLOW_FEATURE_ENABLED=false`), so the tab is hidden and
  the create-a-route path can't be clicked through; the routing engine is proven via the integration test with
  entitlement injected, not via a live click. Like Slice 3, it wants a real click-through once enabled.
- **Schema gotcha fixed mid-session:** adding `matched_rule_summary` to `insertRoute` crashed 4 test suites
  whose inline `:memory:` `document_routes` lacked the column — all four schemas were updated (test_workflow,
  test_workflow_snapshot, test_v1_workflow, test_workflow_ipc). Any NEW inline-schema workflow test must include it.

## FIRST ACTIONS for the fresh session
1. **Build the FYI non-locking slice** (the recommended next — Barry's "more-loved half", Oracle D1 deferral):
   make `acknowledge` routes NOT edit-lock the doc while still granting visibility + dedupe. The lock signal is
   `workflow.hasActiveRoute` (drives `editGuard` edit-lock + reprocess-lock + `isOpenRouteParty` visibility) —
   SPLIT it: a new `hasActiveApprovalRoute` (pending/claimed **approve** only) for the EDIT/REPROCESS lock;
   keep `hasActiveRoute` (any open route) for visibility + the routing dedupe. Then flip the settings rule
   builder to offer "just see it" (acknowledge). Run it through Barry (per the new memory rule) → gary/eric → Oracle.
2. **E1 (pre-launch, before flipping `WORKFLOW_FEATURE_ENABLED`):** add an admin **cancel-route** path — a
   null-sender auto-file route isn't user-recallable, and `editGuard` admin-override frees the DOC not the ROUTE
   (Oracle corrected gary here). Without it, a system route to a later-deactivated recipient permanently locks a doc.
3. **Owner: live-test the print fix** (see below) — it's the one thing shipped-but-unproven.

## Deferred (designed/known, NOT built) — with the load-bearing conditions
- **Workflow multi-step routes** (old Slice 4) — DEFERRED per Barry (single-hop covers the segment). If ever
  revived: it needs a STAMPED rebuild of `document_routes` (Oracle SIGN OFF WITH CONDITIONS C1–C8 already done:
  toggle FK OFF outside the tx + `foreign_key_check`, orphan-sanitize in the SELECT, fail-open-don't-brick-boot,
  shared `ROUTES_COLS` split-brain guard, `VACUUM INTO` scoped to `COUNT>0`, `sqlite_sequence` preserve,
  Model-1 aggregation with a `superseded` state, no-stuck-groups). Oracle noted the table is EMPTY on all real
  installs so the rebuild is near-zero-risk — but ADD-COLUMN-only would deliver 100% of multi-step function
  without the rebuild (owner call if revived).
- **FYI non-locking** (action #1 above) · **suggested-rule starter kit** · **route-to-an-area** (shared tray via
  the existing `claimed_by` claim model — cheap) · **supplier condition** on rules · **"why it routed" surfaced
  on the doc** (the `matched_rule_summary` is stored; just needs a read in the Review/Search doc view).

## Needs the USER
- **Print fix live test** (Windows + Ricoh): (a) click "Advanced printing" → the preview closes, Windows' dialog
  appears; (b) click the app while the dialog is open → it should STAY in front now; (c) cancel → nothing stuck.
  Known acceptable side effect of keeping `parent`: minimising Review while the dialog is open minimises the
  dialog too — if that bugs you, eric's one-line fallback is to drop `parent` (the cancel-leak fix covers it).
- Whether to **push** the 15 unpushed commits (house rule = ask first).

## Key facts / paths
- **Live DB:** `%APPDATA%\Roaming\ScanFinder\docusnap.db` (NOT DocuSnap). Highest STAMPED migration = 50; the
  workflow tables (`document_routes`, `route_decisions`, `workflow_route_rules`) + the `matched_rule_summary`
  column live in the UNSTAMPED idempotent block at the end of `runJsMigrations` (`database/index.js` ~1015+).
- **Run tests:** `ELECTRON_RUN_AS_NODE=1 ./node_modules/.bin/electron <path>` (NOT plain node — native ABI).
- **Kill switches:** `WORKFLOW_FEATURE_ENABLED` (const, `entitlementService.js:37`, master dark) · env
  `WORKFLOW_DECISION_SNAPSHOT` · env `WORKFLOW_AMOUNT_ROUTING` (gates BOTH the confirm + auto-file routing).
- **New process rule (memory `feedback_barry_new_features`):** include **Barry** the brainstormer EARLY on any
  new feature, before the design/specialist/Oracle gate. Advisors: bob/oscar/eric/reggie/gary/oracle registered;
  barry registered; 007 = spawn general-purpose + persona.
- **Routing engine map:** `src/services/amountRouting.js` (pure matcher + gates + dryRunRules) · `workflowService`
  (assign/assignSystem/resolve) · `workflow.js` (rules + summarizeRule + CRUD) · `_autoFileDoc`
  (`processing/handler.js` ~2527) + `reviewService.confirm` = the two filing seams · settings `renderer.js`
  `initWorkflowPanel`/`applyWorkflowTabVisibility`.
