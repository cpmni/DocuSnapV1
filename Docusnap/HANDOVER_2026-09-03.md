# HANDOVER — 2026-09-03

Branch `feat/teach-side-overnight`. **HEAD = `13fa3a9`** (local; NOT pushed this session — check `git push`
status). Working tree clean of arc files (the diagnostic probes were removed via `git checkout HEAD --
template_mapper.py`). The dev app (`npm start`, background task `b294whrbf`) is still running with the arc
force-ON.

## TL;DR
Owner reported: Print Tracker `reference_number` flags "**manually mapped value differs from the usual
format for this field — please verify**" even for serial values that already exist confirmed in the DB
(a re-import of the same batch). Root-caused, designed (gary → Oracle **SEND BACK then C1a**), and built
**DARK** a new arc `FORMAT_VARIANCE_RELAX_REF` that suppresses the mapper shape-warn ONLY when the read is
an **exact confirmed in-scope literal**. Committed (`dd4c855`) + force-ON for the test build (`13fa3a9`,
mig 108). **The arc is real and runs correctly at runtime** (proven with a live probe: `_gate_value` runs,
`FLAG=True`, `value_counts` present). BUT the owner's specific exhibit docs still show the note — for
reasons that are NOT an arc bug (see **Runtime diagnosis**). This is the open thread.

## What was built — `FORMAT_VARIANCE_RELAX_REF` (DARK, Oracle C1a)
The Stage-4.5 shape check compares **SHAPE, not the literal value**, and the accepted `shapes` set is
**count-gated** (`format_anomaly_checker.py:84-86`: MIN=3 AND RATIO=0.10 → at N=80 the bar is ~8 same-shaped
docs; ABS-escape 8). Print Tracker `reference_number` is high-variance (80 confirmed / 28 distinct printer
serials), so most values' shapes never clear the bar and re-flag on every import — even ones confirmed
before. "Value confirmed once" ≠ "shape trusted."

- **`format_anomaly_checker.value_is_confirmed_literal(value, entry)`** — pure; normalised exact-match
  (`text_normalise.normalise_for_tokens`) of `value` against the scope's `value_counts` keys. Absent
  value_counts → False (fail-toward-flagging).
- **`template_mapper._gate_value`** shape leg (`:1219` region): when `shape_mode=='flag'` and
  `_format_rejects` is True, suppress `shape_warn` **only if** `value_is_confirmed_literal` — behind DARK
  flag `_FORMAT_VARIANCE_RELAX_REF` (`== '1'` idiom). Byte-identical OFF. Only the derived flag-rungs are
  touched; `ignore`/`drop` rungs + SELF_VALIDATING types unchanged.
- mig 107 seeds `format_variance_relax_ref` OFF; `handler.js` (`_reconcileEnv`, ~`:532`) bridges the setting
  → env. mig 108 **force-ON** for the test build (TEST-ONLY / REVERSIBLE; census still owed).
- Pin `python_backend/tests/test_template_mapper_variance_ref.py` (14 checks, RED-first, built via the real
  `build_format_class_index`).

### Oracle gate (important — the SEND BACK caught a real ship-blocker)
First design set `shape_warn=False` on any high-variance value. Oracle **SEND BACK**: clearing `shape_warn`
drops **both** the 70 cap AND the note (`_mapping_result`), so the value commits at native 90-96 and can
**silently auto-file the filename-bearing ref**; `docTrustGate`'s coarse `_codeish` (trust.js) does NOT
re-catch a code-class ref bleed. **C1a** = suppress ONLY the exact-confirmed-literal (provably not a bleed;
a never-confirmed value stays flagged + capped + review-bound = fail-toward-review). reggie stalled mid-run
(watchdog); gary + Oracle carried the gate.

## Commits (local, on `feat/teach-side-overnight`)
- `dd4c855` feat(extraction): suppress ref shape-warn on exact confirmed literals (DARK, FORMAT_VARIANCE_RELAX_REF)
- `13fa3a9` chore(settings): TEST-BUILD force-ON format_variance_relax_ref (mig 108)

## Runtime diagnosis — why the exhibit STILL shows the note (NOT an arc bug)
Verified live with a temporary probe in `_gate_value` (now removed). During a reprocess of the 7 held
Print Tracker docs, with the app running the arc:
- `_gate_value` **runs**, `FLAG=True` (env bridge works on the import/reprocess spawn), and `value_counts`
  reach the entry (`has_vc=True`). The fix executes.
- **`reference_number` is read almost entirely via `shape_mode='ignore'`** (the ABSOLUTE drawn-box rung),
  which skips the shape check entirely and can never produce the shape-warn note. My suppression lives in
  the `shape_mode=='flag'` (DERIVED-rung) branch, so it does not touch those reads — and it doesn't need to
  (an absolute-box read that succeeds is already clean).
- The one derived-rung (`flag`) ref read this run logged: `val='H571X05783' … has_vc=True **is_lit=False**
  shape_warn=True`. The value was **not in the confirmed `value_counts` at that moment** → correctly stays
  flagged (fail-toward-review). Same for `IG25802868`, `W2S7828006` — never confirmed → correctly held.
- `1984800049` (the owner's exhibit; confirmed 3×, IS a literal) was read via `shape_mode='ignore'` this
  run → the flag branch never evaluated it. Yet doc121's stored row is still `template_mapping_shapewarn`
  @70 with the note. **So its note is STALE / merge-kept, not re-derived this run.**

### The real open question for the next session — RESOLVED 2026-09-04 (WRONG-CHOKE-POINT, sibling arc built)
**FINAL: doc121's flag was minted at a SECOND choke point the parent arc never touched.** A real-engine run
on live `121.pdf` (electron-as-node, `buildTrainingArgs`+`process_docs`, flag ON) STILL flagged it → probe
proved the flag is NOT from `_gate_value` but from `template_mapper._pick_fuller_code`'s `inline_disagree_flag`
(box-drift: taught drawn box reads GARBAGE `10RARNNNAD`@44, label-anchored inline recovers `1984800049`@96,
flags the disagreement, reuses the same `_SHAPE_WARN_NOTE`). Built the SIBLING arc `FORMAT_VARIANCE_RELAX_REF_
INLINE` (`238e13a` mig 109 DARK; `cd1121f` mig 110 TEST force-ON) — same C1a predicate at `_pick_fuller_code`
+ Oracle **R2 rigid-credibility guard** (never overturn a CREDIBLE rigid `<70`). Pin `test_pick_fuller_code_
literal.py` 16/16; PROVEN end-to-end on live doc121 → clean `template_mapping`@95 no note; doc138 (`RFHO738865`,
letter-O misread) correctly stays flagged. App restarted (migs 109/110 applied). FLIP GATE = WARM-DB census
(each clean-commit == that DOC's OWN prior-confirmed value; realdoc M=0) + revert mig 108/110 before customer.
The paragraph below (my earlier "stale pre-code row" reading) was ITSELF WRONG and is kept only as a caution:
a fresh reprocess does NOT heal without the sibling arc.

<details><summary>Earlier (WRONG) "stale row" reading — superseded by the sibling arc above</summary>
**The arc is correct; doc121/138 are STALE pre-final-code rows.** Proven end-to-end on the LIVE
data: dumped the REAL `getFieldFormats` (electron-as-node, readonly) → the `reference_number` entry carries
`value_counts` WITH `1984800049`+`RFH0738865`; real `build_format_class_index` keeps it (class
`upper_alphanum`, NOT freetext-dropped); real `tm._gate_value(shape_mode='flag')` with the flag ON →
`1984800049`/`RFH0738865` shape_warn=**False** (suppressed), machine-only (`H571X05783`/`IG25802868`/
`W2S7828006`) stay True. **Lead #1 (mergeReprocessRows keeps the noted row) is DISPROVEN** — the merge falls
to `used_new` and adopts a clean fresh row. The true cause is TIMING (lead #2): the exhibit docs 105-138 were
re-imported 17:21-17:23, but the finished arc commit `dd4c855` is timestamped 18:14 — the import ran ~1h
before the code was in the tree. **doc121/138 heal on a fresh in-app reprocess.** OWNER DECISION 2026-09-04:
KEEP CONSERVATIVE — do NOT widen `value_is_confirmed_literal` to machine-confirmed serials (would reopen the
C1a filename-bleed). Machine-only serials (`learning_exclude_machine_confirms=true` hides them; 9 machine-only
vs 19 human literals in this scope) correctly stay held until a human confirms one. Loose ends: 2 commits
still unpushed; mig 108 force-ON is TEST-ONLY (revert + census before a customer build).
</details>

<details><summary>Original leads (superseded)</summary>
Why does doc121 `1984800049`'s `template_mapping_shapewarn` note persist when the current reprocess reads
it via the absolute box (clean, no note)? Two leads:
1. **`mergeReprocessRows`** (`handler.js:1367`) may be keeping the old noted row instead of adopting the new
   clean absolute-box read. Check whether reprocess actually UPDATED doc121's reference row (compare
   `processed_at`, method before/after) — the note may simply never be getting overwritten.
2. **Rung selection differs import-vs-reprocess.** The ORIGINAL fresh import likely read `1984800049` via a
   DERIVED rung (registration/relocation, `flag`) — and at import time, before that batch's values are
   confirmed, `value_is_confirmed_literal` can be False for a value whose confirmed instances are few /
   were themselves just re-imported into needs_review. The arc only helps once the value is genuinely in
   the confirmed `value_counts`.
</details>

### Design tension to weigh (barry/gary/Oracle) — DECIDED 2026-09-04: keep conservative (no widen)
The owner's mental model is the Learning-History "**Times seen**" count (which includes non-confirmed
occurrences), but the gate's literal check reads **confirmed-only `value_counts`**. On a repeated re-import
of the same batch the values may not yet be confirmed, so the arc withholds by design (fail-toward-review).
If the owner wants re-imports of already-seen serials to heal immediately, the next design question is
whether "confirmed literal" should widen to "a literal in the learning history" — but that reopens the
bleed-safety Oracle closed with C1a, so it needs its own advisor→Oracle pass. Do NOT widen it silently.

## Verified vs open
- **Verified:** unit pin 14/14 (RED-first). Sibling format/mapper pins green. JS syntax + Python import
  smoke green. Live efficacy modelled (inspect5): 12 of the pure shape-warn holds are exact confirmed
  literals. Runtime: `_gate_value` runs with the flag + value_counts (probe).
- **Open:** the reprocess/merge staleness on doc121 (lead #1 above). The census (Oracle flip gate) is NOT
  run. mig 108 force-ON is TEST-ONLY.

## Cleanup / state
- The two temporary `_gate_value` diagnostic probes were **removed** (`git checkout HEAD -- template_mapper.py`);
  the committed clean fix is intact (grep `value_is_confirmed_literal` → 1244).
- Diagnostic log files left on disk (harmless, deletable): `C:/temp/ref_gate_debug.log`,
  `%TEMP%/ref_gate_debug.log`. Scratch scripts in the session scratchpad (`inspect4-7.py`).
- The dev app (`npm start`) is still running with the arc force-ON. Two apps share one DB
  (`%APPDATA%\ScanFinder\docusnap.db`); an installed `%LOCALAPPDATA%\Programs\ScanFinder\ScanFinder.exe`
  exists but was NOT running this session (only `electron.exe` = the dev instance).

## Key facts / paths
- **Flip gate (owner, before any customer build):** revert mig 108 (or gate it), run the census OFF-vs-ON on
  the real ref corpus — value diffs 0, ZERO held→auto-file on a NON-confirmed shape-mismatch (an
  exact-confirmed-literal held→auto-file is the allowed win).
- **The shapewarn note text lives ONCE:** `template_mapper._SHAPE_WARN_NOTE` (`:114`), set only in
  `_mapping_result` when `shape_warn=True`, which comes only from `_gate_value`. The engine's own
  Stage-4.5 flag uses different wording ("format differs from the usual — please verify").
- **Live DB read** (avoid the WAL file-copy trap — it fails with "no such table"): use the sqlite backup
  API — `src=sqlite3.connect(db); dst=sqlite3.connect(':memory:'); src.backup(dst)`.
- Run the pin: `PYTHONIOENCODING=utf-8 py -3.12 python_backend/tests/test_template_mapper_variance_ref.py`.
- `git root` is `C:/GIT Projects` (app under `Docusnap/`); pathspecs from there need the `Docusnap/` prefix.
- Memory: `memory/project_ref_variance_relax_20260903.md` + MEMORY.md index line.
