# HANDOVER — 2026-09-20 (hardened build + the 20 new-user tutorial videos)

> Read this, then `AUTO_SESSION_2026-09-20.md` (full detail of this session's video/build work) and
> `handover.md` (the 2026-09-19 session — its morning approval queue is still the main DEV backlog).

## ⚠️ LIVE COORDINATION — READ BEFORE ANY VIDEO WORK
A **separate peer Claude session** — `Handover continuation` (background, ~20h) — is **actively recording
the tutorial-video series on THIS machine** (single mouse, sandbox `C:\ScanFinderDemo`, DevTools port 9222,
publishing to `Desktop\Tutorials\`). We collided earlier (two recorders, one mouse → crashed runs + corrupted
the sandbox DB). It accepted a handoff: **it owns recording + dedup + the `Desktop\Tutorials\README.md` index.**
- **A new session must NOT record videos, must NOT rename/add files under `Desktop\Tutorials`, and must NOT
  kill sandbox/record.py/electron processes, WHILE that peer is still running.** First run `ListAgents`: if
  `Handover continuation` is gone, the coast is clear; if present/busy, leave video recording to it.
- The peer may `--reset` the demo profile between runs (expected, not a bug).

## Git / build state
- Branch `feat/teach-side-overnight`; **HEAD `16432a2`**; **2 commits ahead of origin, UNPUSHED** (from the
  09-19 session — `70c94b8`, `48559c0`; push is the owner's call). Migration **191**.
- Uncommitted = VIDEO TOOLING ONLY: `tools/video_tutorials/scanfinder_video_runner.py` (my hardening edit),
  `voice.py`, and 6 tracked `scripts/*.json` (owner's + peer's edits) + many untracked new `scripts/*.json`
  (this session) + `AUTO_SESSION_2026-09-20.md`. No app/DB/engine code was touched this session.
- **Hardened installer built + verified this session:** `dist\ScanFinder Setup 2.0.0-r20260920-1220-16432a2.exe`
  (`npm run build:release`, exit 0: boot smoke, 14 windows/0 failed, bytecode-only, 9 fuses as declared, signed).
  Log `TESTING/_measure/build_20260920_hardened.log`. The departments release-gate waiver (`16432a2`) is why
  `build:release` now passes.

## What this session did (videos + build)
1. **Hardened build produced + verified** (above). This was the "pick up the hardened-build work" ask — done.
2. **Planned + scripted the 20 most useful NEW-USER tutorial videos** (barry brainstorm + fear-first customer
   lens). Ranked plan + per-video status in `AUTO_SESSION_2026-09-20.md`. ALL 20 scripts now exist in
   `tools/video_tutorials/scripts/`, schema-valid, selectors grep-verified. New ones authored this session:
   confidence-explained, correct-a-field, document-types, output-structure, auto-file, learning-repair,
   watch-folder, backup-restore, practice-run, quick-file, split-and-rejoin, where-docs-go, export-data,
   lan-search-client. (Pre-existing/owner-tone: first-run-a/b, import-a-folder, review-and-confirm,
   search-for-a-document, teach-a-document, the-template-manager, welcome-to-scanfinder.)
3. **2 real script bugs fixed** (hidden checkbox behind a styled `.toggle-slider` → runner refuses it as
   "not visible"): `auto-file.json` `#auto-file-toggle` → `#auto-file-toggle + .toggle-slider`;
   `watch-folder.json` `#watch-folder-toggle`/`#close-to-tray-toggle` → `+ .toggle-slider`.
4. **Hardened the runner** `scanfinder_video_runner.py`: target-resolve window 8→20s, CDP poll 2→4s — a brief
   app stall under load is ridden out instead of aborting. Compiles.
5. **Killed 5 runaway 4-day-old `grep` zombie processes** (~1900 CPU-sec each) that were burning ~70% CPU and
   starving the app (better-sqlite3 stalls) — the real cause of the first recording failures.
6. **Recording:** 6 videos finished + voiced in `Desktop\Tutorials\` at last check (2 by this session:
   `10-document-types`, `14-output-structure`; 4 by the peer: welcome, import, review, finding-any-document).
   The peer is completing the remaining ~14 from the shared scripts. `lan-search-client` = SCRIPT-ONLY (needs a
   2nd machine + the client app — owner records manually).
   - To record any video alone on an idle machine (only if the peer session is GONE): `python record.py
     scripts\<slug>.json --publish "%USERPROFILE%\Desktop\Tutorials\<slug>"` then `python voice.py
     out\<slug>.narration.json --voice en-GB-<Ryan|Sonia>Neural` (voice: odd rank = Ryan, even = Sonia).

## NEXT (owner approval / new session)
Video track: let the peer finish; then review `Desktop\Tutorials\`, confirm all 20 present + voiced + deduped
(peer owns dedup). Commit the video-tooling changes if you want them kept (owner's call — peer may still be
editing scripts, so coordinate/commit after it's done).

Main DEV backlog is UNCHANGED from the 09-19 morning queue (`handover.md`) — none of it was done this session:
1. **Push** the 2 unpushed commits (`70c94b8`, `48559c0`).
2. **Verify the TEST installers on the VM** (Castellan hold now files; client thumbnails without full-screen).
3. **Deploy the 3 licensing-server changes** (`BEFORE_RELEASE.md`, manual IONOS: CF real-IP fix ordered upload,
   New-account admin feature, API-activity page) + the privacy-notice IP-logging disclosure.
4. **DB-at-rest encryption decision (2a whole-DB vs 2b TOTP-only)** — approval-class, still owed. The hardened
   installer does NOT encrypt by default (inert until a key exists — correct/unchanged).
5. **Castellan Slice 2 reframe** (Oracle SEND BACK — the cold-start hold is `docTrustGate`'s no-history refusal,
   not caption disagreement) + the `trust_ref_role_shape × role_field_dominant_class` design Q.
6. **/christest** (enforcement re-run on the clean hardened build).

Memory: `AUTO_SESSION_2026-09-20.md` is the durable record of the video/build work + the peer-collision resolution.
