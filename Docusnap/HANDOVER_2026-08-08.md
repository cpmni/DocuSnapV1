# HANDOVER — 2026-08-08 (SFDEV debug-table BUILT + owner-driven totals fix: net-misread FLAG + missing gross labels)

**Branch** `feat/reprocess-throughput-autostraighten` · continues `HANDOVER_2026-08-07.md`. Autonomous overnight
session (owner asleep, explicit "auto mode, work with the agents, fix where there is agreement, no regressions,
reversible to current state"). **All new behaviour is behind DEFAULT-OFF switches → byte-identical, reversible.**

---

## TL;DR
1. **Built the SFDEV bulk debug-table** (owner's 08-07 NEXT item) — queue-wide field grid in Review; click a cell
   to mark it WRONG (no typing), Submit → `Debug/debug_table/debug_values.json` for the dev session. Owner used it
   live and confirmed it works; refined it per owner feedback (movable/resizable panel, flag-only, filename→select).
2. The owner submitted 39 flags (28 customer, 10 total, 1 account). Root-caused the **totals** (owner's stated
   concern: "a lot are net totals — read the wrong line") with gary + reggie + Oracle and shipped **two
   complementary DEFAULT-OFF fixes**, both corpus-gated M=0.
3. **The debug-table → advisor → measured-fix loop worked** — turned "my totals look wrong" into a gated,
   reversible, quantified fix. (Chris's worthwhile-exercise verdict: see below.)

## Committed this session (feature branch)
Debug-table feature + two extraction fixes + pins. All flags OFF by default.

### A. SFDEV bulk debug-table (dev-only, password-gated)
- `src/modules/processing/handler.js`: `_buildDebugTable(db)` (queue-wide grid: review queue rows × union of the
  present types' fields, cells = value+conf+method) · `_saveDebugTable(ctx,payload)` (writes
  `Debug/debug_table/debug_values.json`; copies winning-crop slices ONLY from paths inside `devSliceDir` —
  path-traversal-defended) · IPCs `dev-debug-table-data` / `dev-debug-table-save` (admin/edit-gated like the
  sibling dev IPCs).
- `src/preload.js`: `devDebugTableData` / `devDebugTableSave`.
- `src/windows/review/index.html` + `renderer.js`: "▦ Table" button in the SFDEV trace-console header → a
  **movable + resizable floating panel** (doc preview stays visible behind it). Click a cell = toggle WRONG
  (owner rule: flag only, no typing 77 values — the dev session derives the truth). Click a filename = select
  that doc. Submit → toast.
- Pin: `src/modules/processing/test_debug_table.js` (16, incl. the slice path-traversal defence). Reopen the
  Review window to load renderer changes; a NEW main-process IPC needs a full app restart (stale-main gotcha —
  cost the owner a "failed to load" until the relaunch).

### B. Totals fix #1 — `NET_MISREAD_TOTAL_FLAG` (default OFF) — SAFETY
`python_backend/extraction/engine.py`: `_flag_net_misread_total` + pure `_net_misread_verdict`, run AFTER
`_reconciliation_pick_total` and BEFORE Stage-4. When the committed total ≈ the subtotal (the NET) AND a distinct
nearest-above VAT-plausible (1.01–1.30×) confident (≥70) larger total was also read → cap conf to 50 + review note
"this looks like the net/subtotal; a larger total ('£…') was also found — please check". **Never swaps the value**
(fail-toward-review; arithmetic/role rail → the authoritative-anchor invariant is preserved). Pin
`test_net_misread_total_flag.py` (14). Oracle SEND-BACK the original `total_reconciles`-gated design (inert on the
vat-missing target) → SIGN-OFF-W/COND on this corrected verdict.

### C. Totals fix #2 — `TOTAL_GROSS_LABELS` (default OFF) — the bigger lever
`python_backend/extraction/keyword.py`: the shipped `total_amount` label bank misses common gross captions, so on
those layouts keyword reads NO gross (tanks the total lane AND starves fix B — no gross candidate to corroborate).
Adds reggie-cleared payable captions in CODE (config unchanged; mirrors `PO_ORDER_NO_LABELS`): "Total to Pay",
"Net to Pay", "Balance to Pay", "Amount Now Due", "Total Incl VAT/.", "Total (inc/incl/inc. VAT)",
"Total Charge(s)" (residual, last). Pin `test_total_gross_labels.py`.

## Gate (customer_corpus_score.js, SAMPLE=300 SEED=7 both) — HONEST
- **Fix C cold:** total **40.6% → 50.0%** (+9.4; scanned +16.3). Every OTHER lane byte-identical (M=0).
- **Fix B+C TEACH arm:** total **30.7% → 34.4%** (+3.8). Every other lane IDENTICAL (M=0). The net-misread flag
  fired **4×, all real catches** (Meadowvale net-misreads, some previously SILENT), **ZERO false-flags**
  (Oracle's load-bearing gate).
- The harness CANNOT bit-reproduce the owner's exact 10 Castellan net-misreads (Castellan prints "TOTAL" — a
  keyword candidate — so the flag WILL fire there); the deterministic decision is gate-able, the end-to-end
  taught heal is OWNER-WATCHED. Baselines: `stress_test/out/customer_score_{base_teach,base_cold,cold_gross,teach_both}.md`.

## NEEDS THE OWNER
- **Flip the two switches after review** (Oracle: leave OFF tonight; the false-flag rate is the load-bearing
  unknown — the corpus shows 0, but flip on your own data with eyes open). Both bridge via env; wire a Settings
  toggle + `handler.js _reconcileEnv` bridge when you're happy (like the heading flags). To trial in the sandbox:
  `NET_MISREAD_TOTAL_FLAG=1 TOTAL_GROSS_LABELS=1 … npm start`.
- Reprocess the Castellan sandbox queue with both flags ON → confirm the net-misreads are now held for review
  (not silently filed) and more grosses read correctly.

## STILL OPEN (from the owner's 39 flags — NOT fixed tonight)
- **customer field (28 flags)** — teaching one credit note DEGRADES the recipient field (cold keyword 79.6% vs
  taught 41.7%): the taught fixed box lands on label captions ("BILL TO"/"SITE ADDRESS") across the corpus's
  variant layouts. ALL of these already get a name-quality review flag (safety holds — they don't silently file).
  Needs its own advisor pass (likely: for a taught free-text/name field that fails its quality gate, fall back to
  / corroborate with the keyword label-hunt read). Deferred — no agreed safe fix reached tonight.
- The 12 garbled totals (crop clips) mostly fail the currency shape → already capped 50 → review.

## DEFERRED (own switch + own gate)
- Robust shadow `vat_tax` read (inline-rate "VAT @ 20%") → lets the EXISTING `_reconciliation_pick_total`
  AUTO-CORRECT net→gross instead of only flagging. Bigger blast radius.
- reggie's residual gross labels "Balance Outstanding"/"Outstanding Balance"/"Balance Owing" (statement
  running-balance collision) — vet on the full corpus before adding.

## GOTCHAS reaffirmed
- New MAIN-process IPC ⇒ full app restart (renderer-only change ⇒ just reopen the window). "failed to load" on the
  debug-table was stale main.
- Windows cp1252 stdout can't encode "→"/emoji in Python `print` — use ASCII in test prints.
- The corpus scorer's TEACH arm reproduces the taught Stage-0.5 total (net-vs-gross line selection) but NOT the
  app's exact OCR crop garble — gates prove role/arithmetic + no-regression; heals are owner-watched.

## Chris the Customer — worthwhile-exercise verdict + UX cards (from artifacts, not a live drive)
**Verdict: WORTHWHILE, would keep using it.** His reasoning (as the owner-operator): totals were right only
40.6% — "worse than a coin toss, on the number I care about most"; the exercise didn't just nudge it to 50% but
PAIRED a measured no-regression gain with a net that HOLDS the ambiguous ones instead of silently misfiling —
"50% still isn't good enough to trust blindly, but with the safety flag catching the misses, 'still imperfect'
becomes 'safe enough to keep improving'." He praised the flag's discipline ("fired on 4, zero false alarms — the
opposite of warning fatigue"). His cards:
- **APPLIED tonight (his top friction — customer-facing):** the review-note copy. Dropped the "net/subtotal"
  jargon and now name BOTH the filed value AND the larger one: *"we filed £-551.82, but it looks like a
  part-total — a larger total (£-662.18) was also found; please check which is the real total"* (engine.py; pin
  updated).
- **For the OWNER to decide (NOT applied — you were explicit about flag-only / no typing):** (Card 3) offer an
  OPTIONAL, skippable "should say…" box on a flagged cell for the odd garbled case (the backend already supports a
  `correct` value; only the renderer input was removed per your rule). (Card 4) let the grid sort/filter by
  lowest confidence so suspects float up. (Card 1) the mid-number minus "£-662.18" reads like a typo — consider an
  app-wide "£662.18 credit" / "-£662.18" convention (bigger than this note).

## Advisors / log
gary (engine seam + gate) · reggie (total precedence + £-neg currency + the gross-label set) · Oracle (gated the
net-misread flag: SEND-BACK original, SIGN-OFF-W/COND corrected) · Chris (worthwhile verdict + copy fix). Oracle
log: `docs/oracle_log.md` (2026-08-06 entry).
