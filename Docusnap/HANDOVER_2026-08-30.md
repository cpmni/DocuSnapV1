# HANDOVER 2026-08-30

**Branch:** `feat/teach-side-overnight` · **HEAD `0ff8b42`** · **NOT pushed** (owner reviews then pushes).
**Installer:** unchanged this session (E44 installer from 08-29 PR #12 still current). **Uncommitted:** only
`CLAUDE.md` (updated by this handover). **Dev app: RUNNING** — I relaunched it (`Start-Process cmd /c npm start`,
real `%APPDATA%\ScanFinder` DB, dev CDP). Context: deskew feature rebuilt + shipped, dev-switch exposure,
and a total-corroboration root cause diagnosed with a design doc for the next arc.

## TL;DR
- **Deskew re-read PIVOTED + SHIPPED** (`4607cc6`, DARK): the field-scoped slice arm was FALSIFIED by a
  200-DPI census (fired 0×) and REMOVED; replaced by a **review-bound whole-page straighten retry**. Gate:
  **5/20 Nordwind supplier identities healed, 0 regressions.**
- **Settings toggle** for it added + exposed (`8333023`); `deskew_review_retry_enabled` set **true on the
  live DB** for testing.
- **Live-DB switch audit:** all **148** mig-93 "should be on" switches already ON; `dev_switches_unlocked`
  already true (dev switches already visible in Processing). Nothing to flip.
- **Nordwind 0023 "no corroboration" root-caused + reproduced** → design doc `0ff8b42` for the next arc
  (fix #1 discount format-invalid witness; fix #2 the owner's capped corroboration-gated re-slice sweep).
- **ref/date "empty" claim CORRECTED:** that was a SANDBOX artifact (the snapshot DB lacked the owner's
  Nordwind learning); on the live app ref/date fill fine. NOT a real defect.

## Committed this session (all NOT pushed)
| commit | what |
|---|---|
| `4607cc6` | **deskew: review-bound whole-page straighten retry** (replaces field-scoped arm) |
| `8333023` | **settings: expose the deskew toggle** (standard visible Processing row) |
| `0ff8b42` | **design doc** for the corroboration-gated re-slice sweep |
| (earlier today) `f7cc2ed` search populate+refresh · `698fd1a`/`1885045`/`ea8a2a6` the deskew S0/S1 that `4607cc6` then SUPERSEDED |

### 1. Deskew review-bound retry (`4607cc6`, DARK — `DESKEW_REVIEW_RETRY` / setting `deskew_review_retry_enabled`)
- **Root of the pivot:** a product-faithful **200 DPI** census (`OCR_RENDER_DPI=200`) on the Nordwind
  corpus showed the field-scoped slice arm **fired 0×** — the garbled supplier names read at 88–96%
  confidence, so they are never *withheld*, and the slice arm was a sibling of `_maybe_gate_reread` (runs
  only on a withheld value). Wrong layer. The **whole-page** straighten (existing `--deskew-pages`) healed them.
- **Shipped:** in `process_docs.py`, after the normal `engine.extract`, a doc that WOULD land in review
  (`_needs_review`) AND whose page is skewed ≥ floor (default **0.3°**, `DESKEW_REVIEW_MIN_ANGLE` /
  `deskew_review_min_angle`) is re-OCR'd STRAIGHTENED + re-extracted; the straightened read is adopted
  WHOLE only if `_overall_confidence` is **strictly higher**, and the adopted doc is forced `needs_review`.
- **Safety:** only ever runs on a doc ALREADY review-bound (can't demote a clean auto-file); never silently
  auto-files a straightened read (Oracle's 2026-07 SEND-BACK was about auto-file, not review); skips
  upright/born-digital pages, `--reextract`, and an already-deskewed run. Off ⇒ byte-identical.
- **Removed:** `engine.py _maybe_deskew_reread` + its DARK consts + wiring; `ocr/deskew_reread.py`;
  `tests/test_deskew_reread.py`. `engine.py` back to single-pass reader.
- **Gate (Nordwind, 200 DPI, on vs off):** **5/20 identities healed** (`Jordwind`/`…Lt`/`…Lte` →
  `Nordwind Refrigeration Ltd`), **0 regressions**. Fired 11×; kept raw on the 6 where straightening did
  not score higher (incl. one where deskew read WORSE, 93<96 — correctly kept). Adopt-if-STRICTLY-higher
  yields 1 fewer heal than unconditional `--deskew-pages` (6) but guarantees no regression.
- **Pins:** `python_backend/tests/test_deskew_review_retry.py` (13/13) pins the two pure helpers
  `_deskew_retry_should_run` (review-bound gate) + `_deskew_retry_adopt` (strictly-higher). Withhold/
  gate-reread path unchanged (`test_gate_fail_reread`, `test_gate_reread_clean`,
  `test_shape_withhold_supplier_scoped` all green).
- **OWED before any live flip beyond testing:** Oracle ratify the revised mechanism + a broader-corpus census.

### 2. Settings toggle (`8333023`) + switch exposure
- New STANDARD visible Processing row "Straighten a tilted page and read it again when a document needs
  checking" → `deskew_review_retry_enabled` (near the "faint small print" row). NOT in `DEV_SWITCH_IDS`
  (owner: expose now, gate later). **RESTART to load** (renderer + `handler._reconcileEnv` Python bridge).
- **Live-DB audit** (`tmp/_switch_audit.py` vs `ALL_ON_DEFAULTS_93`, 148 keys): **all 148 already ON**;
  `deskew_on_import` correctly false; `dev_switches_unlocked` already true; `trust_company_key_own_scope`
  already true (the "do not flip live" note is historical — owner already has it on). Nothing to flip.
- `deskew_review_retry_enabled` set **true on the live DB** (direct UPSERT, `tmp/_set_deskew.py`) — active
  on the next reprocess (handler reads the setting live at spawn; no restart needed for the behaviour, only
  for the toggle UI).

## Verification state — honest
- The 5/20 heal / 0 regress number is REAL (read `scratchpad/nw_retry3.txt`). Harness trap found + noted:
  the `!= '0'` switch idiom treats an EMPTY string as ON, so an A/B baseline arm set to `''` silently ran
  the retry (masked the heal as 0/0). Production is safe (handler sets `'1'` or leaves UNSET). In a switch
  A/B, set the OFF arm to explicit `'0'`.
- **CORRECTED mid-session claim:** "every Nordwind ref/date is empty (template gap)" was a SANDBOX artifact
  — the snapshot DB `scratchpad/live_e44gate.db` lacked the owner's Nordwind template/learning. On the live
  app they fill. Do NOT chase a ref/date "gap".
- 0023 total misread REPRODUCED (`tmp/_ocr_slice.py` on `tmp/slices_0023/slice_9_target.png`): a clean
  134×38px 200-DPI `£2,363.76` crop OCRs to `£9 32632.76` at every PSM.
- NOT verified: the deskew retry on the BROADER corpus (only Nordwind); fix #1/#2 unbuilt.

## FIRST ACTIONS for the fresh session (the NEXT ARC — owner APPROVED both)
Read `docs/designs/CORROB_RESLICE_SWEEP_2026-08-30.md` FIRST. Then:
1. **Fix #1 (small):** discount a **format-INVALID corroboration witness** — it must not count as a
   `disagree` and must not WIN over a valid keyword read that reconciles. Clears 0023's flag (keyword
   already read `2,363.76`, which reconciles). Locus: `engine._build_corroboration_emit` /
   `_corrob_record_bucket` / the mapping-vs-keyword merge. DARK + pin + census.
2. **Fix #2 (the arc):** the owner's **capped corroboration-gated re-slice sweep** for total/ref/date —
   higher-DPI zone RE-RENDER (not raster upscale) + PSM/binarise/£-strip/straighten ladder, STOP on
   corroboration (format-valid AND matches an independent family OR reconciles), adopt only then, ≤~4–6
   tries, review-bound. oscar/reggie/007 → Oracle → DARK switch → OFF/ON census (0023 must heal, M=0).
3. Housekeeping: `MEMORY.md` is ~20.2KB (limit 24.4KB) — compact it.

## Deferred / conditions
- Deskew retry: broader-corpus census + Oracle before defaulting ON anywhere beyond the owner's testing.
- The sweep's refs/dates leg has NO math reconcile — its stop is "matches another family" (or two
  varied-param passes agreeing, weaker — gate carefully). See design doc §3.
- Search perf: `verifyAuditChain` re-verifies the whole audit log on each Search open (grows with usage) —
  noted 08-30, not fixed.

## Needs the USER
- Review + push the branch when happy (standing rule: owner pushes).
- **RESTART the app** to see the new deskew toggle (behaviour already active via the live-DB flip).
- Test 0023 / Nordwind reprocess with deskew on.

## Key facts / paths
- Live DB: `%APPDATA%\ScanFinder\docusnap.db` (= `C:\Users\cmccu\AppData\Roaming\ScanFinder\docusnap.db`).
- Sandbox snapshot: `scratchpad/live_e44gate.db` (LACKS recent live learning — do not trust its ref/date/
  template behaviour as the owner's).
- Nordwind test docs: `C:\Users\cmccu\Desktop\Demo Docs\Other\IMPORT\Nordwind-*`.
- Census/trace scratch this session: `C:\Users\cmccu\.claude\jobs\<job>\tmp\` (`_switch_audit.py`,
  `_set_deskew.py`, `_ocr_slice.py`, `_trace_0023.js`→`trace0023.txt`, `slices_0023/`) +
  `scratchpad/_nordwind_retry_ab.js`, `_retry_probe.js`, `nw_retry3.txt`.
- Product OCR DPI is **200** (`ocr_dpi` → handler `_ocrDpiEnv`); the harness must set `OCR_RENDER_DPI=200`
  to be product-faithful (it defaults 300).
- Restart mechanic: two launchers seen — the owner's `Desktop\Docusnap.bat` AND `npm start`; kill dev-start
  node + electron PIDs INDIVIDUALLY, relaunch via `Start-Process cmd -ArgumentList '/c','npm start'
  -WorkingDirectory 'C:\GIT Projects\Docusnap' -RedirectStandardOutput <log>`.
- Run a Python pin: `cd python_backend && py -3.12 tests/<t>.py`. `git commit -F <file>` ONLY.
