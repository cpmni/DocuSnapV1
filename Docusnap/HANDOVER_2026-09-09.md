# HANDOVER — 2026-09-09 (morning summary of the 2026-09-08 NIGHT run)

The night run (`docs/designs/NIGHT_RUN_2026-09-08_NIGHT.md`) executed all 7 items on auto under the night
protocol. No pushes, no switch flips, no live-DB writes. Branch `feat/teach-side-overnight`.

## What ran (1–7) — all done
| # | Item | Result | Commit |
|---|---|---|---|
| 1 | Triage the 17 pre-existing red JS pins | **325/325 green.** All 17 were STALE (0 real regressions) | `5db333e` |
| 2 | `--smoke-windows` + one rebuild | Built + pinned (33/33); **packaged build's window smoke 14/14** | `82843a0` |
| 3 | Oracle before-flip/census prerequisites | Real-page name-band pin + 2 tautological pins fixed + `RR_ALLOW_ARMED`/`_operating_point`/`RR_APP_ENV` default-on; gate arm byte-identical | `790c7d1` |
| 4 | Heading-fold OCR re-detect arm | Hard Set digital+scan: **0 type / 0 filing changes** (safety); efficacy = 09-06 text census | (measure note) |
| 5 | Chris vet-queue diagnoses | 2 real display/wiring bugs + 1 owner-gated design; nothing implemented | `9097b31` |
| 6 | Chris round 2 (sandboxed) | **Verdict YES.** 7 new cards; all destructive warnings verified true | `ef12f72` |
| 7 | client/cert-tool E44 install + smoke; final sweep; build | E44 graph (0 vulns), code smoke clean; sweep 325/325; release build OK | — |

**THE new click-through target = `dist/ScanFinder Setup 2.0.0-r20260908-2238-ef12f72.exe`** (release build,
`testBuild:false`, manifest `dist/release-manifest-20260908-2238-ef12f72.json`, `problems:[]`, boot smoke
exit 0 + **window smoke 14/14**, identity via file). Supersedes `…-1843-9251551.exe`.

## Item 1 — the 17 red pins (detail in `5db333e`)
All STALE. 6 = fixture-schema staleness (`ocr_recipe` mig 104 ×5, `charset_flag_meta` mig 102, a missing
`document_routes` table on the /v1 access-gate read path). 11 = assertion drift from intentional refactors
(supplier-scoped anchor sweep vs the removed cross-supplier one; CRLF-defeated comment stripper; grown
functions; the 2026-08-27 activity-panel redesign; a 3rd speaker arg; hide→disable edit button; a `quick:`
reprocess field; the `_deferOrRefresh` wrapper; orphaned stamp-panel ids). The failure-row producer, the
supplier-scoped sweep, and the /v1 access gate were each verified SOUND. Every fix keeps a live guard.
**Doc nit:** CLAUDE.md's field_anchors "sweeps ALL suppliers" wording is stale (that moved to read-time
priority in 2026-07-09; the DB sweep is supplier-scoped — see `test_saveanchor_scope.js`).

## Item 2 — `--smoke-windows`
`src/main.js` opens every renderer window HIDDEN after the DB open (no login/licence — same no-state boot as
`--smoke-boot`), awaits load, and flags a window that fails to load / crashes / throws at load / is missing a
load-bearing page-script global (the dropped-`listCaption.js`/`reviewReadiness.js` P0 class). Serial, 20s/win
+ 5min cap, exit 0/5. Verifier spawns it after `--smoke-boot` and refuses on exit 5 or a 0-exit-no-report.
Proven: dev 14/14 clean; negative control (inject a bogus required global) → exit 5 naming it; **packaged
build 14/14**. eric designed the seam; nothing skipped (every window loaded headless).

## Item 3 — census prerequisites (test/harness only)
- Real-page `_name_band_read` pin (real PIL page + monkeypatched `_read_lines_full`; the body was hook-bypassed).
- Positive controls on the two "scan reports 0 hits" pins (scan must CATCH a planted violation).
- Realdoc harness: `_operating_point` prints `test_build_armed_rev` + ON `TEST_SWITCH_KEYS` count and REFUSES
  an armed DB unless `RR_ALLOW_ARMED=1`; `RR_APP_ENV` now DEFAULT-ON (opt out with `RR_APP_ENV=0`). Gate arm
  on `arm137.db` byte-identical (147 rows) to today's `consensus_137.jsonl` — measurement unchanged.

## Item 4 — heading-fold OCR re-detect (evidence: `TESTING/_measure/heading_fold_ocr_redetect_20260908_NIGHT.md`)
`score_hard_set.js` OFF vs ON, config `arm137.db`, DPI 200: digital 200 docs → 0 type / 0 filing changes;
scan 200 docs → 0 / 0. **SAFETY: the switch is inert on installed-type docs, even under scan garble.** The
Hard Set has no uninstalled-heading (Statement) class (its 10 classes are invoice-family stress variants;
arm137.db has no `statement` type), so it measures safety, not efficacy. **Efficacy already MET** by the
09-06 text census (20/20 Ironclad→Statement, 521 unchanged). A full 605-corpus OCR-efficacy arm is optional
(carried by the text census) — see the measure note.

## Item 5 — Chris vet-queue diagnoses (`docs/CHRIS_VET_DIAGNOSES_2026-09-08_NIGHT.md`, nothing implemented)
- **Mailbox card "Type —/Unknown/Document —"**: REAL display bug (not role-gated, not extraction). The
  mailbox row passes a bare `{id}` to `selectDoc` dropping the route's type/supplier, and
  `previewService.getDocumentDetail` never resolves `type_name`. Fix + pin designed.
- **Import row stuck on "Confirm to file →" after a MANUAL confirm** (open since 09-03): REAL wiring gap —
  `markRowFiled` only listens for `doc-auto-filed`; the manual confirm emits only a bare `review-count-changed`.
  Fix (emit `doc-confirmed {docId}` + a renderer listener) + pin designed.
- **Bundle issuer read from a later page**: works-as-architected (whole-bundle `ocr_text` election, no
  per-read page provenance). Extraction-layer → OWNER-GATED. Provenance-note + split-nudge design; seam named
  (`watch_separate_enabled` + page-0 election).

## Item 6 — Chris round 2 (`docs/CHRIS_FULL_APP_REVIEW_2026-09-09.md`) — verdict YES
7 new cards (owner vets): 1 (top) phantom "Format check · 1" flag points at no visible field · 2 Export
"Filed folder" shows the scans folder not the filed copy · 3 self-taught+confirmed doc exports "Confidence
41%" (+ jargon) · 4 Processing switches say "Off by default" but show ON · 5 post-confirm "can file these"
numbers disagree · 6 "separator sheet" never says what it's for · 7 (QUESTION/security) backup restore
accepted a fabricated device fingerprint on an un-licensed machine. All destructive warnings verified TRUE
(Split "permanently removed", delete→recycle-bin, File-All math, Confirm&File, Defer/Restore).
**⚠ Boundary note:** Chris's helper misidentified `%APPDATA%\ScanFinder` (the owner's live app, PID 23828) as
the sandbox and opened it **READ-ONLY** (no writes — owner DB unharmed; WAL concurrent read is safe), then
restored that backup into the sandbox. The safety classifier blocked his retry; he stopped. The SANDBOX is
config-contaminated → **cards 3/4/5 may reflect the merged owner config, not fresh-install truth**; cards
1/2/6/7 are from the clean flow. I killed the contaminated sandbox after his run (next `/christest` rebuilds
clean). **Owner: harden the `christest` skill brief** to hand Chris the explicit sandbox DB path and mark
`%APPDATA%\ScanFinder` OFF-LIMITS.

## Item 7 — housekeeping
- `client/` + `cert-tool/` `npm install` → Electron 44 in the package graph, 0 vulns; **code smoke clean
  under core Electron 44** (no 31→44 API breakage; only the benign "Network service restarting" GPU noise).
  BUT the Electron **binary download is gated by the allow-scripts policy** → `dist/electron` absent, so a
  real from-own-dir launch/build can't run yet. Reverted `cert-tool/package-lock.json` (the upgrade is
  incomplete/owner-gated). **Owner: approve the electron postinstall (`npm approve-scripts`) + reinstall +
  launch-smoke + commit both locks as one deliberate upgrade.**
- Final `npm run test:pins` = 325/325; name-grow-belt Python pin green.
- Release build OK (see the target above).

## NEEDS YOUR APPROVAL (morning) — approval-class, logged not run
1. Scripted click-through + uninstall "remove all data" drill on `…-2238-ef12f72.exe` → then the
   `build`/`build:store` default flip (plan 2.7b, Oracle C5) + retire the "revert list" wording (C10).
2. **Push** (this session added 6 commits; total local unpushed is now ~48 — `git log --oneline eeb8d56..HEAD`).
3. Chris round-2 cards 1–7 (see item 6; 3/4/5 need a fresh-sandbox re-vet) + the item-5 fix builds
   (mailbox card, import-row-stale — "build with the go").
4. **Security:** the backup device-binding gate accepted a fabricated fingerprint (Chris card 7) — verify
   `backupService` anti-trial-stacking gate. Harden the `christest` brief (boundary above).
5. Flips (each with its gate): mig-140 name-grow belts · `type_uninstalled_heading_fold` (safety arm clean;
   efficacy = 09-06 census) · `deskew_corrob_autofile` · `watch_separate_enabled` (soak).
6. client/cert-tool E44 finish (approve allow-scripts + reinstall + commit locks).
7. Learning Repair #42/#25/#27/#40, Ridgeway 8/19/21, doc 99 `ws-55718`; incorporation→signing; DB-at-rest;
   backend DocumentRoot + 2FA; `pip uninstall` 9 dead packages + `--require-hashes`; the DPI/budget decision.

## State
HEAD after wrap commit; 6 night commits (`5db333e`,`82843a0`,`790c7d1`,`9097b31`,`ef12f72`, + this wrap).
Tree clean except this handover + the NIGHT_RUN/CLAUDE wrap edits. Owner's dev app PID 23828 untouched.
Contaminated Chris sandbox was killed. Reference DBs `TESTING/_measure/reset_arm_20260908/arm13{6,7}.db`.
