# HANDOVER 2026-09-24 — the Chris teach-round verdict, the fix set built, gates in flight

**Branch:** `feat/teach-side-overnight` · **HEAD `3de96dd`** · **origin is at `14396ab` (09-23 morning) — 38 commits
UNPUSHED** (the owner pushes) · installer: none built this span · **working tree CLEAN** (only regenerable census
artefacts + the stray `../Docusnap - 20.09.26.zip` untracked) · migrations **213** · `TEST_SWITCH_KEYS` **17** ·
`/v1` contract **1.9.0** · Python pins run from the repo root; JS pins Electron-as-Node.
**Read first:** this file → `docs/designs/CHRIS_ROUND_FIXES_2026-09-24.md` (designs + Oracle + build status) →
`docs/CHRIS_FULL_APP_REVIEW_2026-09-23.md` (the round + the source verification + the control test) → then
`HANDOVER_2026-09-23_NIGHT.md` for the second-reader arc (mig 207-212) this sits on.

## TL;DR
1. **Chris's 09-23 teach-a-doc round** (all six 09-23 switches ON): orders filed exactly as promised (Saltmarsh 13 +
   Copperfield 11 by themselves, 0 wrong ref/date/company on 67 filed, 4 wrong dates + 1 S/5 ref HELD); invoices 0/30
   until he switched a taught Total off; a taught Customer filed as the label word "Customer" ×2; "Checked by you" on
   machine-filed docs. Verdict "Yes, with two things I'd switch off".
2. **Control test (owner: "what made him worse?"):** on all 67 of his confirmed docs, `14396ab` OFF vs today OFF =
   **md5-identical**; today ON changes ONE doc (the S/5 PO released by mig 211). The Total hold reproduces on the OLD
   tree → the 2026-08-09 legacy currency leg meeting a taught box that captures the inline `GBP` code. Not the second
   reader. No earlier round taught an optional field. His slices are clean (63/66 agree).
3. **reggie + gary + eric → Oracle** designed the fix set (`docs/designs/CHRIS_ROUND_FIXES_2026-09-24.md`); the
   Oracle caught reggie's R1 widening a clipped-total hole (currency has no page-family filing consumer) → the
   exemption moved to the call site and gated on cents+sign agreement.
4. **Owner "do the recommended" → ALL SEVEN SLICES BUILT + PINNED + COMMITTED** in the Oracle's order (table below).
   R1 is LIVE inside the mig-70 switch (env kill only); G1 is DARK (mig 213); G2 was a census (bar met ONLY with a
   ≥2-token page-witness guard → design + Oracle next).
5. **Gates in flight / owed:** the R1 gate (727 P/K/N + Hard Set, then G1 OFF/ON) is RUNNING detached from the job
   scratch dir — **NOT READ YET**; the whole pin suite was KILLED by the harness for low memory at 369/415 (2 stale
   pins fixed; 49 unreached — listed; the 6 most relevant ran green by hand).

## Committed this span (all on the branch, none pushed)
| commit | slice | what | pins |
|---|---|---|---|
| `b48848f` `1a74e36` `2ce6fc6` | docs | Chris round verbatim + source verification; control test; advisor designs + Oracle vet | — |
| `5afb359` | **E1a** | `settings/handler.js` add/update/delete-field → `notifyAllWindows('doc-types-changed')` on success only | `test_field_broadcast.js` 10 (fake ipcMain + auth/doctypes stubs) |
| `265ea6c` | **G3** | `trust.js` `flagged:<key>` (first blocking row, same filter that blocked, id order) + `isFlaggedReason(r)`; 13 test files converted from `=== 'flagged'` | `test_caption_empty_flagged.js` (+3); 13 gate tests green |
| `a5badb7` | **E3** | `reprocess-autocommit-accept` takes `{consented:boolean}` (renderer: click true / countdown expiry false); expiry + single doc → ledger `self_filed`, `_recordAutoFiled(false)`, toast "filed itself"; audit row + `auto_reprocess` unchanged | autocommit 31, countdown ALL PASS |
| `0c58d87` | **E2** | `previewService.deriveFiledBy` → `filed_by` ∈ person / approved_batch / self_after_confirms / auto_import / auto_reprocess + `filed_by_username` (human only) on the detail DTO; `searchActions.filedByLabel` 5 copies + "Checked" fallback; `SearchState.username`; **contract 1.8.0 → 1.9.0 lockstep**; client copies regenerated (`sync-client-search.js`) | `test_filed_by.js` 24, v1 contract, preview reads, client popout 509 |
| `a983185` | **E1b** | Review `_refreshFieldDefsForCurrentDoc` on `doc-types-changed`: diff BY KEY (append new / remove gone rows), never `renderFields`; no-op while `_confirmInFlight` (confirmCurrentDoc wrapped by REASSIGNMENT — the declaration is byte-identical for the payload pins) or `bulkFiling`, re-run after | `test_field_refresh_on_type_change.js` 15; `test_confirm_hold_payloads` + `test_chris_r5_ui_cards` still green |
| `7ddf9fa` | **R1** | `number_format._ISO_CODES` + `has_currency_code_prefix`; `engine._format_fail_code_agree` at the merge CALL SITE (Oracle C1): a code-led mapping read is not a format failure only when `has_currency_code_prefix ∧ money_strict_shape ∧ money_cents(code-stripped) == money_cents(challenger)` (sign too); env kill `TEMPLATE_FORMAT_FAIL_CODE_AGREE='0'`; traced `format_fail_code_agree`; the pure helper untouched | `test_number_format` +15, `test_stage05_format_yield` +15 |
| `a3371dc` | **G1** | `value_quality.value_is_own_label` (exact equality to label / mapping `anchor` / generic caption, or a ≥4-char clipped prefix of label/anchor; never the prefix leg on the generic list) → engine block after mig 156: keep value, cap 69, note "This reads as the label ‘X’, not a name — please check the value.", `+nonname_flag` sentinel (trust.js untouched), trace `name_value_label_flag`; **mig 213 `name_value_label_flag` DARK**; `_reconcileEnv` mirror; count pins 16→17 | `test_name_value_label_flag.py` 39, `test_migration213_*.js` 15 |
| `6913cbf` | **G2** | census `TESTING/_measure/taught_name_disagree_census_20260924/{census.py,RESULT.md}` (read-only SQL on the 727 copy + Chris's sandbox copy) | — |
| `d13e58f` `6defb98` `3de96dd` | docs/tests | build status; two stale source pins updated (`test_optional_soft_flag.js` → rows shape; `test_client_search_popout.js` → 1.9.0); handover notes | — |

## Verification state — honest
- **Per-slice pins:** every pin named above ran green in isolation. The 13 rewritten gate tests, the two renderer
  slicing tests, the mig-212/213 + three count pins, `test_name_role_nonname_flag.py`, `test_money_strict_shape.py`
  all green.
- **Whole pin suite (`node scripts/run-pins.js`):** ran 369 of 415 files, then the harness KILLED it (system memory
  0.7 GB free — the gate's Python shards + four claude processes + chrome). The 2 reds it found were both my stale
  source pins → fixed in `6defb98`. The 49 unreached files are in `%USERPROFILE%\.claude\jobs\68b38f39\tmp\pins_unrun.txt`
  (the review / search / settings / shared window tail); six of them ran green by hand (`test_search_window_functional`
  99, `test_search_detail_depathed`, `test_core_transport` 34, `test_settings_wiring`, `test_doctype_surface_parity`,
  `test_review_untyped_reason`). **UPDATE (afternoon): all 49 unreached pins ran green one at a time (`tmp\pins_unrun_results.txt`) → the suite is effectively green across all 415 files; a single clean `run-pins.js` pass is still nice-to-have, not owed.**
- **R1 quick gate on Chris's 36 invoices (DB copy `tmp\sbdb2`, R1 worktree):** OFF = 36 held by the Total note, 0
  would-file (his experience exactly); ON = **22 would-file**, 14 held each with a real reason — cents disagree
  (`GBP 9,229.12` vs `9,229.14`, `GBP 11,309-59` vs `11,386.55`), misread code (`SBP` ×2, `BP`), dropped digit
  (`GBP 7,214.7`), `-` for `.` (`22,334-54`), the reconcile note, and **5 by the pre-existing `template_mapping_edgecut`
  guard** ("the taught box's edge cuts through the printed value") on CORRECT totals — the taught box drifts a row on
  those pages; a Part-A slice-integrity follow-up for TOTAL boxes, not R1's.
- **VENDORED (owner "vendor onnxruntime and the model, then build"): onnxruntime 1.27.0 installed into `vendor/python` (--no-deps; 44 MB on disk), REQUIRED in `check-vendor-python.js`, lock regenerated, licence gate MIT/ALLOWED, notice regenerated; probe under the vendored interpreter: `available()` True, `INV-34177` read at 0.95 in 25 ms. **BUILD 1 = `dist\ScanFinder Setup 2.0.0-r20260924-1802-87024ba.exe` (exit 0, 371 MB vs 353 MB before = +18 MB for onnxruntime). PACKAGED SMOKE PASSED on `dist\win-unpacked`: `resources\python_backend\ocr\modelsec.onnx` present + SHA-256 == the pin, the sourceless `.pyc` backend imports, `resourcesendor\python\python.exe` loads onnxruntime 1.27.0 (CPU provider), `glyph_reader.available()` True, a synthetic `INV-34177` crop reads back at 0.95 in 22 ms → the second reader is PROVEN in the shipped layout. The third-party notice lacked the PP-OCR MODEL credit (not a pip package, the generator cannot list it) → added by hand (`fa249d1`) → BUILD 2 = `dist\ScanFinder Setup 2.0.0-r20260924-1805-fa249d1.exe` (exit 0, 371 MB) — the shipped notice carries the PP-OCR model credit; packaged smoke re-run on its tree: `available()` True, `INV-34177` @0.95 in 20 ms. THIS is the installer to hand out.**
- **FLIP (owner "flip 207, 210, 212, 213 and 214 and push"): mig 215 @DEFAULT_FLIP batch UPSERTs the five 'true' + DELISTS them (`TEST_SWITCH_KEYS` 18 → 13; per-mig pins for 212/213/214 moved to the graduated form; `test_default_flip_215.js` 33 green; release gate OK "13 DARK keys guarded"). 211 stays DARK. ⚠ onnxruntime + the rec model are NOT in `vendor/python` (the vendor gate lists onnxruntime as REMOVED): the second reader abstains on a packaged build until the owner approves vendoring — the flip is effective on the dev install now. Pushed to origin.**
- **G2 gate (b) — READ: extraction identical OFF/ON on both DBs; 727 lost 15 filers = 14 wrong boxes + the pinned `Fernbank`; sandbox lost exactly the 6 wrong customer names; no flood → mig 214 flip-ready (`TESTING/_measure/g2_taught_name_gate_20260924/RESULT.md`). The four gate worktrees are REMOVED.**
- **G1 gate (Oracle C8) — READ: OFF md5-identical; ON = 2 new holds on the 727, both the label word `Customer` (docs 75/82), 0 other changes, 0 real names held → mig 213 flip-ready (`TESTING/_measure/g1_label_flag_gate_20260924/RESULT.md`). G2 gate (b) arms running (727 off/on + sandbox off/on).**
- **R1 full gate (Oracle C3) — 727 READ (`TESTING/_measure/r1_code_agree_gate_20260924/RESULT.md`): K md5 == P, N == K (0 removals, 0 events, 517/517 would-file) → the exemption is INERT on the owner's data; efficacy = the Chris-36 quick gate. Hard Set P: wrong+would-file 0; PRE-EXISTING digital-arm supplier-EMPTY observation logged. K/N Hard Set + G1 + G2 arms still in flight.** (Original note follows.) `tmp\gate\run_gate.sh` (detached, `nohup`): 727-doc arms P (pre-R1
  tree `a983185` via `RR_PY_ROOT` worktree `tmp\gate\wt_pre`), K (R1 tree, `TEMPLATE_FORMAT_FAIL_CODE_AGREE=0`), N (R1
  tree, ON) → then Hard Set digital+scan cold ×3 → then `run_gate2.sh` G1 OFF/ON on the 727 (worktree `tmp\gate\wt_g1`).
  Status file `tmp\gate\status.txt`; consensus `tmp\gate\rr_<arm>.jsonl`; traces `rr_<arm>.trace.jsonl`; Hard Set
  scores moved to `tmp\gate\hs_<arm>\`. At the wrap: arm P at 70% (511/727, ~2 h in — it sped up once the sandbox app +
  pin suite were gone). **Bar:** K md5 == P; N vs K: fire-set diff REMOVALS only, every removal a cents-agree pair
  (`format_fail_code_agree` trace events), `wouldFile(N)−wouldFile(K)` all `total == GT`, M=0, zero per-field drop;
  Hard Set wrong+would-file stays 0; G1on vs G1off: OFF identical to N, ON lists every new hold (each must be a
  caption / anchor / clip — a REAL name held = SEND BACK). Compare with `tmp\arms\compare_arms.py <a> <b> A B`.
  **If N shows anything other than removals, put `TEMPLATE_FORMAT_FAIL_CODE_AGREE=0` in the app spawn env** (R1 is live).
- **Corrected mid-session claims:** (i) my brief to gary said "the wordness gate skips authoritative reads" — WRONG
  (`_authoritative` there is PATTERN authority; the gate ran on "Customer" and passed it); (ii) reggie's "fire-set ⊆"
  was FALSE as designed (the helper runs on the challenger too) — the Oracle's C1 made it true by construction;
  (iii) eric corrected my "reprocess-completion records approved" — it is the ACCEPT door reached by the countdown
  EXPIRY; (iv) Chris's card-4 "near miss" is REFUTED by the predicate (`disagreeing-read:order_date`, mig 152 ON).

## FIRST ACTIONS for the fresh session
1. `tail tmp\gate\status.txt` (job dir `C:\Users\cmccu\.claude\jobs\68b38f39\tmp\gate\`). When `GATE_DONE.flag` +
   `GATE2_DONE.flag` exist: run the comparisons (K vs P identity, N vs K diff, G1on vs G1off), count the
   `format_fail_code_agree` events in `rr_N.trace.jsonl`, read `hs_*\score_*_cold.md`; write
   `TESTING/_measure/r1_code_agree_gate_20260924/RESULT.md`; record in the design doc + `docs/oracle_log.md`. If the
   job dir is gone (the job was deleted), re-run the gate from a fresh worktree pair — the scripts are in this file's
   history (`run_gate.sh` / `run_gate2.sh` shapes are trivial: RR_DB = `stress_test/out/c0_live_copy/docusnap.db`,
   `RR_PY_ROOT` = the worktree's `python_backend`, env kill/flag per arm).
2. Run the WHOLE pin suite on a quiet machine: `node scripts/run-pins.js` (from the repo root; ~415 files). Expect
   green; any red → fix or revert the slice, never leave it.
3. Remove the three gate worktrees when done: `git worktree remove --force <path>` ×3 (`tmp\gate\wt_pre`, `wt_r1`,
   `wt_g1`) + `git worktree prune`.
4. Owner decisions queued: push the 38 commits · flips 207 + 212 (+210) per the C10 result · G2 design (gary's
   predicate + the ≥2-token witness guard) → Oracle → build DARK · the Part-A slice-integrity idea for TOTAL boxes
   (the 5 `template_mapping_edgecut` holds on correct values) · `docs/detached-client.md` contract note 1.9.0.
5. Delete `stress_test/out/c0_live_copy/` (a copy of the owner's live DB, gitignored) when the gates are done.

## Deferred (designed, not built) — load-bearing conditions
- **G2 `taught_name_disagree_refuse` — NOW BUILT DARK as mig 214** (the afternoon; gary final design → Oracle SIGN OFF W/COND C1-C7; JS-twin census 24 holds on the 727 = 23 catches + the pinned `Fembank`, 6/6 sandbox, lone-word 0, no flood). OWED before a flip: gate (b) — realdoc 727 + Hard Set + sandbox OFF/ON via `TAUGHT_NAME_DISAGREE_REFUSE` (JS-only, so extraction md5 must be identical), `wouldFile(ON) ⊆ wouldFile(OFF)`, every lost filer listed. The original design text follows.** (trust.js both sites :1052-1056 + :1073-1075): keys where `isNameLikeField`,
  `required===0`, type ∈ {text,''}, record `winner_family==='mapping'`, page witness `nameQuality≥0.6` **AND ≥2
  tokens** (the census's decisive guard: a lone `Make`/`Studio` witness scores 1.0), refuse `disagreeing-read:<key>`;
  HARD deps `trust_role_disagreement_refuse` + `role_disagree_refuse_at100`; NO containment exemption (every
  containment in the census was a clipped box = a catch); pinned trade-off = `Fernbank` vs `Fembank` (a wrong page
  witness holds a correct box, 1/727). Oracle vet needed before build.
- **Part A for TOTAL boxes** (new): the taught Total's absolute rect drifts a row on Chris's invoices; the mapper
  relocates off "Total Due" but `template_mapping_edgecut` then holds 5 correct values. The S1 word-snap idea
  (`slice_integrity.py`) applied to money boxes — design + census first.
- From the 09-23 night handover, unchanged: S-B1 / S2 / S3 / Part C of the Paddle corroboration arc; mig 211 stays
  DARK; flips 207/212 pending the two byte-identity runs on the 727 (the OFF identity is now proven on the 67).

## Needs the USER
- Push. Flip decisions. The G2 go. A Chris re-run on the built tree (teach a Total again: expect ~22/36 invoices to
  file by themselves and the held ones to NAME the Total in the banner; teach a Customer with `name_value_label_flag`
  ON: expect "reads as the label ‘Customer’" holds instead of silent files).
- The sandbox app was CLOSED (memory); `/christest` rebuilds it. Its DB copies live in `tmp\sbdb*` until the job dir
  is deleted.

## Key facts / paths
- Live DB `%APPDATA%\ScanFinder\docusnap.db` (BLOCKED to script tools — copy `.db`+`-wal`+`-shm`); the 727 read-only
  copy `stress_test/out/c0_live_copy/docusnap.db` (gitignored, DELETE when done).
- Harness: `ELECTRON_RUN_AS_NODE=1 ./node_modules/electron/dist/electron.exe stress_test/realdoc_regression.js` with
  `RR_DB` `RR_IDS` `RR_APP_ENV=1` `RR_CONSENSUS` `RR_TRACE_OUT` `RR_SLICE_DIR` `RR_PY_ROOT` `RR_ALLOW_ARMED=1` (an
  armed DB is refused otherwise) `OCR_RENDER_DPI=200`; it selects CONFIRMED docs only. Hard Set:
  `stress_test/score_hard_set.js <digital|scan> cold` with `RR_DB` + `RR_PY_ROOT`, writes into `Desktop\Hard Set`.
- Never edit `python_backend` while an arm runs from the working tree — run arms from a `git worktree`.
- Bash-tool trap: a heredoc containing certain quoting fails to parse ("unexpected EOF") — write edit scripts with the
  Write tool and run them; Python prints of `→`/`‘’` crash on the cp1252 console — keep test labels ASCII.
- Advisors this span: reggie (R1), gary (G1/G2/G3 + the brief corrections), eric (E1/E2/E3), Oracle (vet, log entry
  2026-09-24). Chris's report + my verification: `docs/CHRIS_FULL_APP_REVIEW_2026-09-23.md`.
