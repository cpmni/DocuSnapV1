# HANDOVER — 2026-08-29 (themes + licensing admin redesign shipped; overnight security audit + Chris + Electron-41 plan)

**Branch** `feat/teach-side-overnight`. **HEAD `59923ed`, PUSHED — in sync with origin** (git auth now works
via the Windows Credential Manager / GCM; the owner authenticated mid-session). **Uncommitted:** `CLAUDE.md`
(session-state block) + **three report docs left uncommitted for owner review** (below). Tree otherwise clean
of code changes.

## ⏭ NEXT SESSION PRIMARY JOB — execute the Electron 31.7.7 → 41 upgrade
**Read `docs/ELECTRON_41_UPGRADE_PLAN.md` FIRST.** It is **Oracle-gated: SIGN OFF WITH CONDITIONS** (the
verdict + conditions are the "⚖ Oracle gate" section at the top of the doc). Do NOT start the bump without
honouring the conditions — most importantly:
- **New BLOCKING gate 5b — in-place DPAPI continuity.** The plan's other gates STRUCTURALLY can't catch it
  (unit tests inject a fake `safeStorage`; a clean-VM smoke writes new E41 blobs). Take a `db.backup()` copy of
  a real **E31-written** `%APPDATA%\ScanFinder` (live `.audit-key`, + `ca.key`/`server.key` if the LAN add-on
  is in scope), launch the **E41** build against it via `--user-data-dir=<copy>` (NOT `SCANFINDER_USERDATA` —
  ignored by a real Electron run), and assert `verifyAuditChain`→ok · `canStamp` still true · no
  `tamper_detected` rows · LAN boots + pinned-CA handshake. Rationale: a DPAPI decrypt trip would fail-close
  `canStamp` for everyone + spam `tamper_detected` after a routine update, and the LAN TLS decrypt throws
  through a bare un-try/caught auto-start (`api/handler.js:1232` via `main.js:1404`).
- **Never run gates 4/5/5b against the LIVE profile** (both E31+E41 resolve the same `%APPDATA%\ScanFinder`;
  `dev-start.js` doesn't redirect) — always a backup copy via `--user-data-dir`; stop the live dev app or use
  distinct ports (CDP 9222 / API 8765 collide).
- **Gate 3:** diff known-red failure SIGNATURES, not names. **Gate 4:** hold `ocr_dpi` env identical across
  arms. **§6 fuses:** unwire `scripts/afterPack-fuses.js` if adopting eb-native `electronFuses`.
- **Version:** Oracle recommends defaulting to the **newest stable** argon2+better-sqlite3 support (44.0.0 is
  newest; 41 is 3 majors back = less runway) — owner's call; the plan documents 41 as requested.
- Do it on a **git worktree** (`git worktree add ../Docusnap-e41 -b chore/electron-41`) so `main`/this branch
  stay on E31. Concrete pins: electron **41.10.7**, electron-builder **26.15.3**, argon2 **0.45.1**,
  better-sqlite3 try **12.10.0** then **13.0.3**. Most-likely-to-fail = argon2 source build (gate 1, cheap
  abort). Third app to bump/retire: **`cert-tool/`** (`scanfinder-cert-tool`, also E31).

## TL;DR — what shipped this session
1. **Themes (pushed):** **Light Festive** + **Spooky (Halloween)** added to core + detached client (`c4d1033`);
   **seasonal revisions** — Summer de-aciced + denser half-drop artwork for all 5 seasonal tiles (`589b93e`).
   Design-critique agent + WCAG pass folded in. Registration in `theme.css`/`theme.js`/settings picker +
   `client/renderer`; new SVG tiles `patterns/festive-light.svg` + `spooky.svg`.
2. **PHP licensing admin console — full redesign (pushed; owner DEPLOYED to IONOS):** header-nav → **left
   sidebar** in the ScanFinder "Warm Archive" website style, self-hosted IBM Plex fonts (no CDN), logo. One
   shared chrome (`lib/admin_auth.php admin_page_open`) drives all pages; active item from `PHP_SELF`.
   - `2c54c11` sidebar chrome + `admin_nav()` neutered to a no-op.
   - `dc178be` **2FA now OPTIONAL by default** (`admin_2fa_required()` reads opt-in `LICENSING_ADMIN_REQUIRE_2FA`)
     — owner's request, to fix a login lockout after deploying the newer `admin_auth.php` (which had made 2FA
     mandatory). ⚠ **This is a security downgrade the owner chose knowingly** (see security report Finding 2).
   - `40bc2b6` **dashboard** redesign (`index.php`) — status tiles + quick-action cards + activity feed +
     health chip, bound to the existing vars.
   - `c094c4d` fix: `--accent-ink` was repurposed to cream button text, making `temp.php`'s "+ Create a
     temporary license" toggle invisible (white-on-paper) → restored `--accent-ink` dark, added `--on-accent`.
   - `59923ed` **section-page pattern** across all pages — `admin_page_head()` + `admin_chips()` helpers in
     `admin_view.php` + chrome CSS; icon headers, context chips, toolbars. Forms/tables/logic untouched, every
     page `php -l` clean.
3. **Overnight SECURITY AUDIT** (5 specialist agents + Oracle vetting) → **`docs/SECURITY_REVIEW_2026-08-28.md`
   (UNCOMMITTED, sensitive).** See below.
4. **Chris product vet** (core + detached client, all switches on, CHRISBOT copy) → **`docs/CHRIS_FULL_APP_
   REVIEW_2026-08-29.md` (UNCOMMITTED).** Verdict **YES, would keep after two weeks**; 8 cards.
5. **Electron 41 upgrade plan** (eric + Oracle) → `docs/ELECTRON_41_UPGRADE_PLAN.md` — the next session's job.

## Security audit — headline (full detail in the report)
- **Top item = document-level access control (HIGH; CRITICAL for the multi-user/LAN-client deployment).** No
  identity/owner/doctype scoping — any authenticated account reads every confirmed doc, cleanest over `/v1`; DB
  has **no owner column**. **Single-user desktop is SAFE** (app runs as the logged-in user — no boundary
  crossed). Remedy scaffolded (`doctype_grants` stub in `accessService.js`) but not built; Oracle: it's ONE fix
  across read + search/queue **enumeration** + **write** on both transports, with a routing-override policy
  decision + a pinned test (`test_access_service.js:157`) to change. **Hold the LAN add-on back from
  mixed-sensitivity multi-user offices until built.**
- **Verified safe:** local-DB licence forgery **impossible** (device-bound, pinned, non-disablable);
  website login **sound** (PDO/CSRF/bcrypt/fail-closed throttle); **no LLM → prompt-injection N/A.**
- **HIGH:** Electron 31 EOL (→ the upgrade plan). **A1 (Chris card 2, code-confirmed):** the `/v1` client login
  does NOT enforce the forced temp-password reset the core does — a "one-time" temp password persists on the
  client. 23 findings + Oracle's do-first list in the report.

## Chris vet — triage (nothing implemented; owner vet queue)
Verdict YES. Cards: **1 (top friction)** first real batch = 9/10 held ("Sender not identified" on obvious
letterheads; he flags CHRISBOT is all same-buyer → likely worse than a real mixed inbox) — onboarding-copy +
tick-your-doc-kinds. **2** = security A1 (above). **3/4/6** stale/contradictory copy (recognise-vs-not-seen,
Send-history "Nothing yet", issuer-nudge-after-fill) — refresh bugs. **5** delete-copy vagueness on filed docs.
**7** presence flicker. **8** Terms still say "NOT YET IN FORCE / [SOLICITOR:]" (legal, owner's call). Scary
buttons + approval workflow + client↔core all told the truth end-to-end.

## Verification state — be honest
- Themes: design-critique agent + WCAG pass; render-checked in a preview + the owner eyeballed/approved.
- Licensing admin: every page `php -l` clean; the chrome + helpers render-checked via a standalone PHP harness
  (WAMP php 8.0). Owner deployed to IONOS and confirmed login + pages work.
- Security audit: 5 agents traced code; Oracle vetted (calibrated the CRITICAL → HIGH deployment-conditional,
  merged the two access-control findings, confirmed the 3 "safe" negatives). A1 confirmed in code by me.
- Electron plan: eric produced it from the real config; Oracle SIGN-OFF-W/COND. **Nothing upgraded — plan only.**
- **NOT run:** any Electron upgrade; the full-corpus M=0 head-gate for the prior session's 2 Pelican fixes
  (still open, `scratchpad/rr_gate_full.cmd`).

## FIRST ACTIONS (fresh session)
1. **Electron 41 upgrade** per `docs/ELECTRON_41_UPGRADE_PLAN.md` (Oracle conditions above) — the primary job.
2. Decide + tell the owner: commit the 3 report docs? (Security report is sensitive — owner's call whether it
   goes in the repo.) The branch is already pushed through 59923ed; a docs commit would be local until pushed.
3. If the owner wants any Chris card or security finding actioned, take it through the advisor→Oracle gate
   (esp. the access-control Finding 1 — a design arc, do NOT stub-fill).

## Needs the USER
- Whether to commit/push the 3 report docs.
- The security remediation decisions (access-control arc scope; provision admin 2FA; harden fuses).
- Kill the Chris sandbox when done poking it (next `/christest` rebuilds it).

## Key facts / paths / TRAPS
- **Chris sandbox LEFT RUNNING:** core CDP **9223** (2 filed + 8 review docs, user "Sam" with stamp permission,
  one order approved+stamped), detached client on **9224** signed in as Sam. Sandbox root under this session's
  scratchpad `chris-sandbox/` (session-mortal). Kill via the PIDs / next `/christest`.
- **git push now WORKS** (GCM cached the owner's credential this session) — earlier failures were the sandbox
  shell being non-interactive. The owner set `credential.helper manager`.
- **`git commit -F <file>` ONLY** (here-strings + heredocs break in this PowerShell). Electron-as-Node prints
  nothing through the PowerShell tool unless wrapped (`Start-Process cmd /c ... -RedirectStandardOutput`).
- **PHP lint:** `C:\wamp64\bin\php\php8.0.30\php.exe -l <file>`. The licensing backend is a SEPARATE deploy
  (WAMP local / IONOS prod) — the owner uploads the changed files; deploy ONLY the code, NEVER `keys/` (the
  repo's `keys/` lacks the live secrets).
- **Live core DB:** `%APPDATA%\ScanFinder\docusnap.db`. **DPAPI-at-rest:** audit-HMAC key + LAN TLS keys (NOT
  the licence token — that's a JWS). This is the seam the Electron upgrade's gate 5b must prove survives.
- Pre-existing test reds (do not chase): `test_authoritative_anchor`, `test_v1_contract`,
  `test_doctype_surface_parity`, `test_teach_multipage`; Python `test_identity_fusion` + ~6 script-style.
