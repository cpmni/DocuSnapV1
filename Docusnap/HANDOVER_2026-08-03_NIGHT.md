# HANDOVER — 2026-08-03 NIGHT (autonomous perfect-catch arc — 6 flips live, all Oracle-gated)

**Branch** `feat/reprocess-throughput-autostraighten` · **HEAD** = the wrap commit after `df80601`,
PUSHED. **Owner mandate executed:** "hash it out between yous, have the oracle vet it and
implement when there is agreement" — goal: teach once → perfect catch on CLEAN siblings for all
anchored built-in AND custom values, silently (no new customer UI).

---

## TL;DR — what is LIVE in your app right now
Six switches flipped across the day/night, every one advisor-designed → Oracle-signed → corpus-
gated (all revertable in Settings → Processing):
1. `universal_verify_restore` (2a ref/date restore — you flipped it) — Slice-2 universal verify.
2. `template_code_edge_clean` — "Tidy stray marks": pure-punctuation label-tail debris
   ('. DN-60902') heals when the inline witness verbatim-agrees.
3. `template_target_word_snap` — "Snap taught boxes to the printed text": the seated box grows to
   cover the FULL value word on the drift/registration rungs; never absorbs neighbours/labels.
   Own gate: +1 ref heal, +1 date heal (a '2096' century garble), 5 false-flag drops, M identical.
4. `template_code_frag_clean` — "Remove label fragments": the ALNUM fragment class
   ('o. DN-67428' — the 'o' of "No.") heals under the consent LADDER (confirmed shape verdict
   FINAL → provisional taught skeleton → 1-letter floor) + label-suffix binding + inline verbatim.
5. `template_clip_commit` — "Complete a cut-short read": right-clipped rigid ('DN-6742') commits
   the inline 'DN-67428' CLEAN under 3 corroboration legs — killing the **factually false**
   "manually mapped value differs" note (the disagreement branch stamped shape_warn on a value
   that was never shape-checked and PASSES the learned shape — rb_531, traced).
6. `crosscheck_outlier_reconcile` (yesterday, still ON).
Plus: **provisional consent channel** (a freshly-taught doc's value skeleton corroborates sibling
#1 — S2-isolated from every veto path), role-aware `ocr_type` seeding (the teach-wizard 'text'
default bug) + both edit-surface selects, advisor prior-art/track-record memory in
`.claude/agents/*.md`.

**FIRST ACTION: RESTART the app** (main-JS changed after your last start — the stale-main gotcha),
then reprocess the Northgate dockets. Expected: every one reads clean `DN-#####`, no flags, files.

---

## The night's engineering story (read docs/oracle_log.md NIGHT entry for verdict detail)
- Your 21:39 screenshot's residual = TWO classes gary separated by tracing rb_531:
  (1) the crop grabbed the ALNUM 'o' of "No." (edge-strip can't touch letters), and
  (2) the box ALSO right-clipped the value → the reconcile's disagreement branch committed the
  clean inline value but stamped the false shapewarn note @70 → held. WORSE silent twin: on
  un-drifted docs the dirty fragment+full-core read committed clean @90 (α-variant).
- Oracle forks ruled: composite fragment predicate (gary's label-suffix binding + reggie's
  strictness as a consent ladder); B flip signed after the +6/+6 was decomposed honestly
  (+1/+1 real heals + 5 new-corpus docs).
- Oracle seams caught: S1 (C2a's locate-token leg could compare the locate text WITH ITSELF —
  `_read_inline_box` now returns a `from_ladder` provenance bit); S2 (provisional index must be a
  separate channel — engine strips provisional rows before EVERY established builder); S4
  (decapitation: taught 'NO-#####' refuses stripped '#####'); S5 (`_pick_fuller_code` branch
  order is load-bearing — source-pinned).
- Gates: B's own window GREEN with heals; composed (all-ON) window BYTE-IDENTICAL (M 10==10,
  zero drop) → flips within your standing instruction.
- Pins: `test_template_frag_clip.py` (29) + `test_template_target_word_snap.py` (18) +
  `test_template_code_edge_clean.py` (24) + all prior suites green.

## Chris rating (you asked)
**KEEP — clearly useful, measurable impact.** His "a hold I can act on is help; a hold with
nothing to fix is a tax" + the 7→2 trust-ledger on warning-believability set the night's PRIORITY
(kill false holds first) and both gary and Oracle explicitly engaged his framing (gary: "the flag
describes a state that NEVER existed"; Oracle: reflex-confirm as the C2a justification). He made
no citation errors, stayed advisory, and was the cheapest agent of the night. His impact was on
priority + framing, not predicates — those came from code traces, which is the right division.
His phase-1 finding (19 holds, no progress signal) is what the provisional channel now fixes
architecturally. His ideal-world ask ("show me the two reads") survives as the C2b copy item.

## OWNER-MORNING list (also in pendingfeatures.md NIGHT entry)
1. **Restart + live re-test** the Northgate dockets (above).
2. **C2b copy** — the SURVIVING disagreement note still says "manually mapped value differs…";
   Oracle-approved direction names both reads. Your vet (user-facing voice).
3. **Teach-time box word-snap** (store snapped boxes at teach; you SEE the snapped box before
   commit). gary-designed, UI-visible → your call before build.
4. **`_seed_field_patterns` ref_field_key threading** — unconventional-key ref roles are still
   free-text-gated (the real production hole behind the ocr_type bug). Gated follow-up.
5. Rehearsal read + template annealing (barry #3/#6) — the "teach once" durability pair, design
   docs on request.
6. Slice-2 2b/2c (numeric/text universal verify) still dark pending the Customer-corpus GT
   scorer (Oracle C6, unchanged from the afternoon).

## Gotchas (new tonight)
- `git add -A` from the repo ROOT stages the untracked `Backup/` tree (repo root is
  `c:/GIT Projects`, not the Docusnap dir) — stage files explicitly; a timed-out add leaves a
  stale `index.lock`.
- Never edit `template_mapper.py` while a realdoc arm runs (workers import per shard).
- Diagnostic logs in DEV builds live in `repo/Debug/diagnostic_<UTC>.jsonl` — NOT userData.
