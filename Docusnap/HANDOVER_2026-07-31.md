# HANDOVER 2026-07-31 (Opus 4.8)

**Branch** `feat/reprocess-throughput-autostraighten` · **HEAD `c70bae7`, PUSHED** (origin in sync).
Tree clean bar pre-existing untracked (`../Backup/`, `../Docusnap - Copy*`, `HANDOVER_2026-07-28*.md`,
`docs/SECURITY_HARDENING_REPORT_2026-07-28.md`, `scripts/remove-superstore-invnum-anchor.js`).
**Dev `npm start` was restarted this session** (it had been stale). Installer still at `2b8bdb2` — a rebuild
ships everything since.

## TL;DR
Overnight autonomous run on owner request. Fixed the **Teaching Wizard / Template Manager clipping taught CODE
fields** (the headline: a taught delivery docket read `DN-93159` → `N-93159`, `DN-39550` → `39550`, garbage).
Root cause: the Stage-0.5 mapper trusts the **absolute drawn box**, which drifts off the value's prefix under
per-scan offset/scale; the alphanumeric gate can't see a clip; the clipped taught read (conf 90) then OVERRODE
the correct keyword read — so teaching REGRESSED the field. Fixed with a label-anchored **inline reconcile**,
FLIPPED ON, fully gated. Also fixed the teach **cursor lock on the "Teaching complete" step** (owner-reported).

## The fix — inline-code reconcile (FLIPPED ON, kill `TEMPLATE_INLINE_CODE_RECONCILE=0`)
`python_backend/extraction/template_mapper.py` `_inline_code_reconcile` — after the drift guard + registration
arbiter, for a single-token CODE field taught INLINE with its label, cross-checks the absolute box read against
a **full-res, label-anchored inline read**:
- sourced from a **page-wide** locate (Oracle **Seam A**: the pre-pass LOCAL locate can clip a wide value too);
- committed as a **re-read of `inline_box` through the OCR ladder** (Oracle **Seam B**: the ~120-DPI PSM-6 locate
  text under-reads dashed codes — never trusted as the value, only a fallback);
- decision on alnum-folded values: box read is a **SUFFIX** of inline → left-clip → un-clip CLEAN; inline is a
  suffix of box → box fuller, keep; **genuine disagreement** (floated box, or an inline OVER-read INV-121⊂INV-12110)
  → prefer higher OCR-confidence read, inline wins → **FAIL TOWARD REVIEW** (capped 70 + `_shapewarn` note).
- scoped `{alphanumeric, reference_code}` (job_reference EXCLUDED — internal spaces); free-text box-first seam
  (template_mapper.py:742-750) untouched. NO `_located_too_wide` guard in the reconcile (it blocks a merged
  title/label/value OCR line whose value column is still correct — the real-doc miss; pinned by unit #10).

Commits: **`f2e5ee3`** fix (built DARK) · **`c70bae7`** flip default ON + parity harness + fast-path test isolation.

## INDEPENDENCE (owner's hard requirement) — PROVEN
Owner: teach/template must be a SEPARATE validator, NOT reuse Review's detection pipe (else it's a false
qualifier — same pipe = same answer = no validation). Holds: Teach reads operator-taught geometry
(`template_field_mappings`) via `template_mapper.py`; Review reads corpus-mined `field_anchors` via `anchor.py`.
The fix does NOT call anchor.py. `stress_test/teach_vs_review_parity.js` proves it end-to-end on the 10 dockets:
**10/10 DIFFERENT methods** (Review `keyword_override` vs Teach `template_mapping`), **10/10 agreeing values**,
teach ≥ review.

## Gates (all clean)
- `stress_test/delivery_reconcile_probe.js` — 5/10 clipped (OFF) → **10/10 full `DN-#####`** (ON). Run:
  `RECONCILE=1 ELECTRON_RUN_AS_NODE=1 ./node_modules/.bin/electron stress_test/delivery_reconcile_probe.js`.
- `stress_test/realdoc_regression.js` — **OFF == ON**: same **13 pre-existing** regressions, **0 new**,
  type/supplier 100%, ref 91.5% / date 99.3% (both runs). ⚠ The 13 are the corpus's known **GT-poison**
  (see memory `project_corpus_gt_poisoning` — true M is ~2); `GATE=1` exits 1 at BASELINE, so the signal is the
  **0 delta OFF↔ON**, NOT the exit code. Diff was 13 shared / 0 off-only / 0 on-only.
- `python_backend/tests/test_template_mapper.py` — **87 checks pass** (11 new: suffix-only un-clip, over-read
  never clean-commits, merged-title not blocked, free-text + job_reference excluded, disagree→conf/flag, OFF
  byte-identical). Run: `PYTHONIOENCODING=utf-8 py -3.12 python_backend/tests/test_template_mapper.py`
  (the em-dash in the summary crashes cp1252 on redirect without PYTHONIOENCODING — pre-existing, cosmetic).
- Design: 007 (placement) + gary (Python) → **Oracle SIGN OFF WITH CONDITIONS**, all 5 met + the suffix/confidence
  hardening Oracle's Q3 asked for (`docs/oracle_log.md` not updated — Oracle ran as a subagent this session).

## Perf note
The reconcile does a **page-wide locate per clean CODE read**. It's `line_cache`-shared with the registration
landmark fit, so **registration-enabled docs pay ~0 extra OCR** (cache hit on the (0,0,1,1) crop). realdoc ON
completed in normal time. `test_absolute_target_first` now runs with the reconcile forced OFF (it guards the
pre-reconcile absolute-first fast path).

## NEEDS THE OWNER (in-app / decisions)
1. **Heal the existing dockets — NOT retroactive.** The ~20 needs_review dockets keep their stored clipped value
   until **reprocessed** (full `reprocess-document` in Review — NOT `reextract_fast`, which is fill-only). Open a
   docket in Review and reprocess; the corrected `DN-#####` reads via the reconcile. (Also the worksheet cleanup
   from the prior handover is still pending: Add a "Worksheet" type + reprocess + de-confirm #120.)
2. **Rebuild the installer** to ship this + the teach fixes + the 07-30 flips to the other PC (`-1231-2b8bdb2.exe`
   predates all of it). Close the dev app first (EPERM).
3. **Visual smoke** the teach cursor-lock: open Teach, capture all fields → at "Teaching complete" the cursor is a
   pointer and you can't draw a box; re-selecting a field restores the crosshair.

## LEFT TO BUILD (deferred)
- **Slice 2** — the DRIFTED-sibling clip: `_geometric()` (template_mapper.py:573) has the same narrow-box clip
  risk. None of the 10 dockets drift, so Slice 1 is complete for the observed bug; add `_inline_code_reconcile`
  in `_geometric()` when a drifted taught code field shows it. Logged in `pendingfeatures.md`.
- The prior handover's open items (07-30): reextract ⟳-pill visual confirm on restart; worksheet 20-row cleanup;
  F2 seat-race PHP verify; deferred security F3/F4/M-2/L-1/2/4/6.

## KEY FILES / PATHS
- Fix: `python_backend/extraction/template_mapper.py` (`_inline_code_reconcile` + `_code_norm` +
  `_target_inline_with_anchor`, call site in `_extract_one` before the abs commit; flag `_INLINE_CODE_RECONCILE_ON`).
- Tests: `python_backend/tests/test_template_mapper.py::test_inline_code_reconcile`.
- Harnesses (read-only, live DB): `stress_test/delivery_reconcile_probe.js` (RECONCILE=0/1),
  `stress_test/teach_vs_review_parity.js`, `stress_test/realdoc_regression.js` (GATE=1). Diagnostic scratch:
  `…/scratchpad/diag_reconcile.py` (renders + traces one docket) — reusable to inspect a single doc.
- Live DB `%APPDATA%\ScanFinder\docusnap.db` (read-only `?mode=ro`). Delivery dockets = docs 141-151
  (141 taught/confirmed; 142-151 needs_review). Ridgeway delivery template = id 8.
- Memory `project_teach_inline_code_reconcile_20260731`.
- Agents used: 007 (general-purpose+persona), gary, Oracle — all advisory, all sign-off met.
