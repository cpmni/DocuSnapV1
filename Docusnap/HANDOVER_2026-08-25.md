# HANDOVER — 2026-08-25 (resumed after an accidental close: finished the mid-task work + a full Chris fix loop)

**Branch** `feat/teach-side-overnight` · **origin is at `034a3ad`** · **13 commits AHEAD, LOCAL / NOT pushed**
(owner reviews then pushes — standing rule). The session was closed by accident mid-commit; I read
`HANDOVER_2026-08-24.md`, picked up the uncommitted work, then ran the owner's loop: Chris vet → fix → Chris
re-verify → (for the one recommendation) fix → one more Chris. Every code fix went through advisor/Oracle;
nothing tuned to one doc; sandbox-only for Chris.

## What was on disk when I resumed (uncommitted, mid-build — now committed)
Three complete-but-uncommitted arcs, all DARK / OFF-inert by construction:
1. **`c1d5c08` the "Quick check" grid FRONT-END** — the UI half of `eef96bd` (backend+orchestrator+IPCs
   already landed). Cards + Table views, page pan/zoom, edit+re-file. Armed only by `batch_audit_enabled`;
   the IPCs also refuse when off.
2. **`d687f31` two DARK detection arcs** (both env-gated, OFF byte-identical):
   - **`name_dominant_snap`** — a solid single-value scope's one-glyph legal-suffix slip ("…Lid"→"…Ltd")
     silently adopts the confirmed spelling + auto-files (was a "Suggested … [Resolve]" hold).
     `name_match.name_snap_adopt` (identity-preserving: only a trailing legal-suffix ≤1-edit slip, exact
     core, ≥5-confirm scope, core len ≥6). Tests `test_name_snap.py` (19) + `test_name_snap_gate.js`.
   - **`branding_strip_reg_boilerplate`** — strip {vat, reg, registered, company} from `_distinctive_tokens`
     so a WRONG logo-collision supplier can't score free own_ratio hits on a "VAT Reg GB…" line (doc-732
     Oakhaven→Castellan @94). Test `test_branding_reg_boilerplate.py` (15).
   - **These two are OWNER-VET-QUEUE, not flipped** — DARK, need a corpus OFF==ON + Oracle ratify before any live flip.

## The Chris fix loop (sandbox; login `chris` / `plumber2026`)
Full report: `docs/CHRIS_FULL_APP_REVIEW_2026-08-25.md` (3 rounds, verbatim + triage + vet queue).
- **Round 1 (full vet, Quick-check grid armed):** strong "yes, would keep + pay." 7 cards.
- **Round 2 (re-verify):** the 4 fixed cards (1/3/5/7) all FIXED live, zero regressions; flagged Card A.
- **Round 3 (Card A verify):** Card A FIXED. Sanity pass.

### FIXED this session (commits `200e68d` + `97d3527`)
- **Card 1 (HIGH) — invalid-date silent misfile.** A present-but-unparseable date-ROLE value passed the
  empty-required Confirm gate and filed SILENTLY to `Company/Unknown Year/Unknown Month` (commitDocument
  substitutes Unknown + returns success). **gary + Oracle both said WRONG LAYER** — the renderer-only +
  `validation_patterns` idea was a fig leaf (that predicate is LOOSER than `filing.parseDate`: passes
  "15/12/202", "15/12/25", slash-ISO, full-month; and false-blocks OCR-spaced dates). Fix gates on
  **`filing.normaliseDate`** (the EXACT parser the folder builder uses) at every door:
  `reviewService.confirm` (refuse pre-claim → interactive Confirm error toast, File-All skip, /v1),
  `_autoFileDoc` (hold with a note → closes the conf-100/`strict_100_autofile`-off auto-file hole),
  `batchAuditService.validateEdit` (aligned — was the same fig-leaf), renderer `validateConfirm`
  (early-feedback pre-block via `_parseDrawnDate`). Pins: `test_normalise_date_predicate.js` (16, both
  sides of the contract incl. the literal "1/ 2026" + OCR-spaced controls), `test_reviewservice.js`
  (refusal + not-filed, replaces the old "left as typed" pin), `test_batch_audit_correct.js` (clipped-year
  refused, OCR-spaced still files).
- **Card 3 (MOD-HIGH) — Quick-check re-file renamed `Invoice.…`→`Document.…`.** `getWithExtractions`
  (→`getById`, SELECT *) carries no `type_slug`, so `_resolveDtInfo` got a null slug → filename builder
  fell back to literal "Document". Resolve the slug from `document_type_id` via `doctypes.getAll`. Pins added.
- **Card 5 (LOW-MOD) — Quick-check Cards leaked dev codes** (anchor_inline/keyword). `_baFieldMeta` now
  shows confidence + any human-readable note only.
- **Card 7 (LOW) — Empty bin orphaned the `.metadata/*.xml`.** `_purgeOne` removes the sidecar too
  (app-owned filed tree only, best-effort).
- **Card A (LOW/cosmetic, round-2 finding) — OCR-spaced date mixed signal.** The on-blur `fieldValidationError`
  used the loose no-preclean pattern while the button + filing preclean, so "15 / 12 / 2025" showed a
  spurious "Not a valid date" while filing fine. Date note now accepts whatever `_parseDrawnDate` accepts
  → agrees with the button/builder. Only relaxes → no new false warning. Pin in
  `test_validation_pattern_surfaces.js`.

## Verification state — honest
- **Every touched suite green:** `test_normalise_date_predicate.js` (16), `test_reviewservice.js`,
  `test_reviewservice_refile.js`, `test_batch_audit_correct.js` (29), `test_batch_audit_learning.js`,
  `test_name_snap_gate.js`, `test_name_snap.py` (19), `test_branding_reg_boilerplate.py` (15),
  `test_dominant_snap.py`, `test_drawn_date_parse.js`, `test_filing_edge_cases.js`, `test_filename_pattern.js`,
  `test_validation_pattern_surfaces.js`, `test_autofile_route.js`, `test_import_autofile_gate.js` (33) — plus
  the corroboration-emit (26), name_match, template_matcher (OFF byte-identical), format_anomaly, crosscheck
  regression suites unchanged.
- **Card 1 corpus gate:** NOT run. The Card-1 fixes are confined to CONFIRM/FILE-time
  (`reviewService.confirm`, `_autoFileDoc` post-eligibility, `batchAuditService`, renderer) — none touch the
  extraction / `isAutoFileEligible` READ path `realdoc_regression` measures, so that corpus is byte-identical
  by construction. The predicate itself is pinned 16/16.
- **DEFERRED pin (owner):** a synthetic conf-100 `_autoFileDoc` unit pin (the function isn't exported;
  standing up its full dep graph for one pin was disproportionate overnight). Covered by the predicate pins +
  the Chris sandbox end-to-end + the obvious pre-claim guard.
- **NOT full-suite:** ran the touched + related suites, not `run_all_suites.py`. Pre-existing reds unchanged
  (`test_authoritative_anchor`, `test_v1_contract`, `test_doctype_surface_parity`, `test_teach_multipage`;
  Python `test_identity_fusion` + 6 script-style).

## FIRST ACTIONS for the owner
1. Review the 13 local commits + `git push` when happy (origin is behind at `034a3ad`).
2. **Owner vet queue (diagnosed, NOT built):**
   - **The two DARK detection arcs** (`name_dominant_snap`, `branding_strip_reg_boilerplate`) — want a
     corpus OFF==ON + Oracle ratify before any live default-on flip.
   - **Card 2** — Terms say "WORKING DRAFT / NOT YET IN FORCE" + `[SOLICITOR:]` notes (legal, your domain;
     the real fix is finalising the solicitor text before release — NOT auto-edited).
   - **Card 4** — half a new supplier's invoices → "Sender not identified" (badge matched another company,
     text disagrees). The letterhead-name-over-abstain arc — needs a census + Oracle (too risky overnight;
     one confirm teaches the sender). Note: this is the SAME class `branding_strip_reg_boilerplate` targets.
   - **Card 6** — the queue row still says "Needs: Invoice Date" after it's filled (stale-refresh; low).
   - **The grid↔activity-strip coupling** — the Quick-check button lives IN the activity strip, so
     `batch_audit_enabled` is useless unless `review_activity_strip` is also on. Flip them together (or make
     the strip render whenever the grid is on).
   - **The green-pill/red-note pairing** — on a broken date a green "High · 98%" read-confidence pill sits
     beside the red "not valid" note (Chris confirmed it did NOT mislead; cosmetic only).

## Key facts / paths
- **Chris sandbox:** `<scratchpad>\chris-sandbox\` (userData + Output + Demo Docs 1330 files), **CDP 9223,
  PID 19928** — LEFT RUNNING on the newest code (`97d3527`). `batch_audit_enabled=true` + `review_activity_strip`
  on (Chris enabled it). Login `chris`/`plumber2026`. The next `/christest` kills + rebuilds it. Driver:
  `<scratchpad>\chris-driver` (playwright-core). Capture: `scripts\capture-window.ps1 -OwnerPid 19928`.
- **Live app DB** = `%APPDATA%\ScanFinder\docusnap.db` (untouched this session — sandbox-only).
- Card-1 fix: `src/services/reviewService.js` (~184 date loop), `src/modules/processing/handler.js`
  (`_autoFileDoc` ~5486 pre-claim), `src/services/batchAuditService.js` (`validateEdit` + `_resolveDtInfo`),
  `src/windows/review/renderer.js` (`validateConfirm` + `fieldValidationError` + `_baFieldMeta`),
  `src/modules/review/handler.js` (`_purgeOne` + the `normaliseDate` wiring). Predicate pins
  `src/modules/filing/test_normalise_date_predicate.js`.

## Traps this session (all previously-known, re-confirmed)
- **CHRIS TRANSCRIPTS COME BACK EMPTY** — write the report out of the completion notification immediately.
  (Done — all 3 rounds transcribed into the review doc.)
- **`_autoFileDoc` is NOT exported** — a direct unit pin needs exporting internals + its dep graph.
- **The batch-audit test stub set `type_slug` directly on the doc**, which is why the Card-3 bug (real
  `getWithExtractions` has no `type_slug`) slipped through — a stub that's more complete than reality.
- **`fieldValidationError` / `_parseDrawnDate` / `filing.parseDate` are THREE date validators** — keep them
  aligned; the loose `validation_patterns.date` is the fig leaf (substring, 2-4-digit year, no preclean).
- **Relaunching the sandbox:** kill 9223 via `Stop-Process`, relaunch with the SAME `DOCUSNAP_USERDATA` via
  the Bash `run_in_background` pattern (a PowerShell `Start-Job` did NOT persist across tool calls).
- `core.autocrlf=true` (benign LF→CRLF warnings on commit); repo root is `c:\GIT Projects` (paths `Docusnap/…`).
